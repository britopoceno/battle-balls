# Story e3.7: Revisão dos números de D-09 com telemetria; randomização de ordem de builds se D-06 virar meta (não bloqueia o portão)

## Status

Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run check", "npm run sim:check (idêntico ou re-baseline conforme o caso, documentado)", "revisão manual: números de D-09 revisados só com evidência de telemetria, não por raciocínio"]
```

## Story

**Como** desenvolvedor executando o passo 7 do plano de construção da Fase 3 (`docs/architecture-e3.md`
§12) — o único passo que explicitamente **não bloqueia o portão** da fase,
**eu quero** revisar os números provisórios de D-09 (preços, renda das rodadas 6/7, magnitudes de item) com
os dados reais coletados pela telemetria de `e3.5`, e implementar a randomização da ordem de exibição das
opções de build **somente se** a telemetria mostrar que a default determinística de D-06 (`buildPadrao`)
virou meta de jogo,
**para que** os provisórios marcados ao longo da fase (`ECONOMIA_PROVISORIA` de `e3.2`, preços do catálogo
de `e3.1`) sejam fechados com **medição**, não com raciocínio — completando o ciclo que a arquitetura da
Fase 3 deixou deliberadamente aberto.

## Depende de

`e3.6` (ajuste de D-05, re-baseline registrado). A revisão de D-09 usa dados de telemetria coletados **depois**
que o jogo já está no alvo de duração (D-05 fechado) — revisar preços/renda antes do ajuste de duração
mediria um jogo que ainda vai mudar de ritmo, invalidando a leitura. Esta story **não bloqueia o portão**
(P3.1-P3.4 já foram avaliados com os dados de `e3.5`/`e3.6`); ela fecha provisórios remanescentes.

## Acceptance Criteria

1. `npm run check` verde.
2. `npm run sim:check` verde — **golden hash idêntico OU re-baseline, conforme o caso, e documentado
   explicitamente qual dos dois aconteceu e por quê.** Se a revisão de D-09 só mexer em números que não
   tocam `sim/` (preços, renda — que vivem em `match/economia.ts` e `shop/catalogo.ts`), o hash deve
   permanecer idêntico. Se, por algum motivo, a revisão exigir mudança de magnitude de bônus que afete
   `sim/` via `itemBonus`, isso teria efeito de re-baseline e precisa da mesma disciplina de justificativa
   de `e3.6` (T-1) — mas essa não é a expectativa.

   **Reforço do @po, conferido no código: a expectativa é mais firme do que "não deveria".** Nenhuma rodada
   do `sim:check` usa item nenhum. `BASELINE` (`determinism.ts:78-84`) e `BUILD_BASELINE`
   (`determinism.ts:100`+) montam `PickSetup` só com `charId`/`abilityIndex`/`passiveIndex` — sem
   `itemBonus` —, e `world.ts:162` é `if (pick.itemBonus) addPartialBonus(...)`, um caminho que essas
   rodadas **não entram**. Somando: mudar `preco` ou magnitude `x` em `shop/catalogo.ts` não pode mover o
   golden hash, porque o hash não observa a loja; e mudar `ECONOMIA_PROVISORIA` em `match/economia.ts` também
   não, porque `match/` está fora do que o hash mede (§2.2). **Logo, o resultado esperado é "idêntico", e um
   hash diferente nesta story é sinal de que algo fora do escopo declarado do AC 10 foi tocado** — investigar
   antes de re-gravar, não re-gravar antes de investigar.
3. **Revisão dos números de D-09** (preços do catálogo, `rendaPorRodada`, `rendaAposATabela`,
   `jurosPorDezOuro`, `tetoDeJuros`, `precoTrocaDeBuild`, magnitudes `x` dos itens) feita **exclusivamente
   com dados reais da telemetria** de `e3.5` (P3.1-P3.3, distribuição de compra, trocas de build, ouro
   guardado vs. gasto) — **não inventar números finais sem essa evidência**. Se a telemetria coletada até
   este ponto for insuficiente para uma revisão responsável, a story deve declarar isso e manter os
   provisórios como estão, documentando o motivo.
4. **Renda das rodadas 6 e 7 (R-04, `docs/architecture-e3.md` §14):** a tabela de 5 entradas
   (`rendaPorRodada`) e o fallback `rendaAposATabela` continuam sendo a política ("repete o último valor"),
   a menos que a telemetria e o @pm decidam por dois números explícitos para as rodadas 6 e 7 — decisão de
   produto, não desta story isoladamente.
5. **Randomização da ordem de exibição de build (D-06) — condicional, não obrigatória.** Se a telemetria de
   `e3.5` (evento `buildPadrao`) mostrar que a default determinística está entrando com frequência alta o
   suficiente para ser considerada "meta" (jogadores deixando o timer estourar de propósito para pegar a
   primeira opção), implementar a randomização da ordem de exibição das opções de build.
6. Se implementada, a randomização **consome o stream 5 ou 6** da tabela de `e3.2`/§2.5 (política de
   partida do bot) — **nunca** o `world.rng` — porque ela afeta apenas a ordem de exibição na UI, não a
   simulação.

   **Observação do @po, para o @dev não ser pego de surpresa (não altera o AC):** os streams 5 e 6 são, pela
   tabela de §2.5, os da **política de partida do bot** (`e3.3`), e o bot consome deles na mesma partida em
   que a UI randomizaria. Compartilhar um stream entre dois consumidores torna o consumo de cada um função do
   outro — o mesmo tipo de acoplamento que D-08 existe para evitar. A tabela de §2.5 lista o **stream 7 como
   "livre"**, e ele seria o destino natural. O AC fica como está porque §4.2 diz "stream 5/6" com essas
   palavras e Artigo IV não me autoriza a trocar o número: **se** esta randomização for implementada (ela é
   condicional ao AC 5), levar a questão ao @architect junto da medição, e registrar a decisão no Dev Agent
   Record. Se a randomização não for implementada — o caso esperado — a questão não se materializa.
7. **Se a telemetria NÃO mostrar que D-06 virou meta, não implementar a randomização** — D-06 já registra
   isso como condicional: "se a default virar meta, randomizar a ordem de exibição". Implementar sem essa
   evidência seria antecipar uma decisão que a arquitetura reservou para medição.
8. **Esta story NÃO bloqueia o portão da Fase 3.** P3.1 a P3.4 já foram avaliados com os dados produzidos
   por `e3.5` e o ajuste de `e3.6`; esta story é fechamento de provisórios com medição, posterior ao
   julgamento de portão, não anterior a ele.
9. **Não decidir R-05 (Risco #6, morte súbita) nesta story**, a menos que o @pm já tenha tomado a decisão
   com base em P3.2 de `e3.6` e peça para esta story apenas **implementar** o que foi decidido (por exemplo,
   baixar o limiar de `SUDDEN_DEATH_MS` se a saída (b) for escolhida). Esta story não decide sozinha entre
   as saídas (a)/(b)/(c) de §14/R-05.
10. Nenhuma linha fora de `src/shop/catalogo.ts` (preços/magnitudes), `src/match/economia.ts`
    (`ECONOMIA_PROVISORIA`) e, condicionalmente, um novo módulo de randomização de ordem de build (cliente)
    é alterada — e cada mudança deve estar diretamente rastreável a um dado de telemetria citado no Dev
    Agent Record.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Balancing / Data revision
**Secondary Type(s)**: Frontend (se a randomização de D-06 for implementada)
**Complexity**: Low-Medium — a complexidade não está no código (edição de constantes já existentes, mais
possivelmente uma função de embaralhamento), mas em garantir que cada número revisado tem evidência de
telemetria por trás, não é escolha arbitrária.

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa (quality gate — valida que cada número revisado cita a evidência de telemetria correspondente, e que
  a randomização de D-06, se implementada, foi de fato motivada pelos dados)

**Supporting Agents**:
- @pm (decide se a revisão de renda das rodadas 6/7 vira dois números explícitos ou mantém "repete o
  último"; decide se D-06 randomiza)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Rodar antes de marcar a story como completa
- [ ] Pre-PR (@github-devops): Rodar antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Cada número revisado de D-09 (preço, renda, juros) cita a evidência de telemetria que o motivou — nenhum
  número "porque parece certo"
- Se D-06 randomizar: consome stream 5/6, nunca `world.rng`

**Secondary Focus**:
- `git diff --stat` restrito aos arquivos declarados no AC 10

## Tasks / Subtasks

- [ ] Task 1 — Coletar e analisar telemetria acumulada (AC: 3)
  - [ ] Reunir os exports de telemetria de `e3.5` (e de sessões jogadas durante `e3.4`/`e3.6`)
  - [ ] Calcular distribuição de compra (P3.3), ouro guardado vs. gasto, frequência de troca de build,
        frequência de `buildPadrao`

- [ ] Task 2 — Revisar D-09 (AC: 3, 4, 10)
  - [ ] Ajustar preços em `src/shop/catalogo.ts` e/ou `ECONOMIA_PROVISORIA` em `src/match/economia.ts`
        **somente** com base nos dados coletados
  - [ ] Decidir (com @pm) se a renda das rodadas 6/7 ganha números explícitos ou mantém o fallback
  - [ ] Documentar, para cada número alterado, qual dado de telemetria o motivou

- [ ] Task 3 — Avaliar D-06 (AC: 5, 6, 7)
  - [ ] Calcular a frequência de `buildPadrao` nos dados coletados
  - [ ] Se alta o suficiente para indicar "meta" (decisão a documentar com @pm): implementar randomização
        da ordem de exibição, consumindo stream 5/6
  - [ ] Se não: documentar a decisão de não implementar, com o número medido

- [ ] Task 4 — R-05, se já decidido (AC: 9)
  - [ ] Confirmar com @pm se há decisão tomada sobre morte súbita a partir de P3.2 de `e3.6`
  - [ ] Se sim: implementar a saída decidida (ex.: ajuste de `SUDDEN_DEATH_MS`)
  - [ ] Se não: não tocar, não decidir sozinho

- [ ] Task 5 — Verificação (AC: 1, 2, 8)
  - [ ] `npm run check` — 0 erros
  - [ ] `npm run sim:check` — reportar explicitamente se o hash ficou idêntico ou mudou, e por quê
  - [ ] Confirmar que esta story não reabre nem re-julga o portão P3.1-P3.4

## Dev Notes

### O passo, e por que ele não bloqueia (fonte: `docs/architecture-e3.md` §12, Anexo B)

> *(não bloqueia o portão)* revisão dos números de D-09 com os dados da §10; randomização da ordem de
> builds se a default de D-06 virou meta [...] idêntico ou re-baseline conforme o caso [...] Fecha os
> provisórios com medição em vez de raciocínio.

[Fonte: `docs/architecture-e3.md` §12, linha do passo 7]

### Os provisórios que esta story pode fechar (fonte: §6.2, §7.2)

```ts
// src/match/economia.ts — os valores que existem hoje, provisórios
export const ECONOMIA_PROVISORIA: ParametrosEconomia = {
  ouroInicial: 0,
  rendaPorRodada: [4, 5, 6, 7, 8],
  rendaAposATabela: 8,
  jurosPorDezOuro: 1,
  tetoDeJuros: 3,
  precoTrocaDeBuild: 5,
}
```

> D-09 permite valores provisórios "desde que explicitamente marcados". [...] O valor definitivo sai da
> telemetria da §10 com humano no controle, e a alteração é decisão de produto (@pm), não de arquitetura.

[Fonte: `docs/architecture-e3.md` §6.2]

### R-04 — renda das rodadas 6 e 7 (fonte: §14/R-04)

> A tabela do design tem 5 entradas; D-02 permite 7 rodadas (5,6% e 0,5% das partidas, §1.5). Adotei
> "repete o último valor", marcado como provisório. Precisa de confirmação, ou de dois números.

[Fonte: `docs/architecture-e3.md` §14/R-04]

### D-06 — a condição exata para randomizar (fonte: §4.2)

> A randomização da ordem de exibição fica **nomeada e não implementada**, como D-06 manda: se ela existir
> um dia, ela consome o stream 5/6 da tabela de §2.5, não o `world.rng`.

> a telemetria pode contar quantas vezes a default entrou, que é exatamente o sinal que D-06 diz que
> dispararia a mitigação ("se a default virar meta, randomizar a ordem de exibição").

[Fonte: `docs/architecture-e3.md` §4.2]

### R-05 — morte súbita, se já decidida por outra story (fonte: §14/R-05)

> As saídas: (a) aceitar como código morto e deixar `SUDDEN_DEATH_MS` como rede de segurança; (b) baixar o
> limiar para a faixa em que a cauda o alcança; (c) esperar P3.2 com humano no controle antes de decidir.
> Recomendo (c) e depois (a) ou (b) — mas o número é do @pm.

[Fonte: `docs/architecture-e3.md` §14/R-05 — esta story só implementa se @pm já tiver decidido entre (a) e
(b) depois de (c) ter sido cumprido em `e3.6`.]

### O que esta story explicitamente NÃO faz

- Não decide números de D-09 sem evidência de telemetria — se a evidência for insuficiente, a story
  documenta isso e não força uma revisão.
- Não implementa a randomização de D-06 "por precaução" — só se a telemetria mostrar o sinal.
- Não decide R-05 sozinha — só implementa se @pm já decidiu.
- Não reabre o julgamento do portão da Fase 3 — P3.1-P3.4 já foram avaliados antes desta story.
- Não toca `sim/` (a menos que uma revisão de magnitude de item prove necessário, o que não é a expectativa
  — ver AC 2).

### Testing

- `npm run check` — 0 erros.
- `npm run sim:check` — reportar explicitamente se o resultado foi "idêntico" ou "re-baseline", com a
  justificativa correspondente no Dev Agent Record.
- Para cada número de D-09 revisado: citar o dado de telemetria (arquivo exportado, métrica, valor) que
  motivou a mudança.
- Se a randomização de D-06 for implementada: teste dirigido confirmando que o `world.rng`/stream 0 nunca é
  consumido pela randomização, e que ela usa o stream 5 ou 6.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-07-29 | 1.0 | Story criada a partir de `docs/architecture-e3.md` §4.2, §6.2, §14/R-04, §14/R-05 e §12 (passo 7, não bloqueia o portão). Story explicitamente condicional em múltiplos ACs (D-06, R-05) — implementação real depende de dados de telemetria ainda não coletados no momento da criação desta story. | River (@sm) |
| 2026-07-29 | 1.1.0 | Validated GO (9/10) — Status: Draft → Ready. **Confirmado: a story descreve o MECANISMO de revisão, não números finais de D-09.** AC 3 exige que cada número revisado venha "exclusivamente com dados reais da telemetria" e — o ponto que mais importa — manda **declarar insuficiência e manter os provisórios** se a evidência não bastar, que é a saída honesta e a que impede o preenchimento por raciocínio. AC 4 devolve R-04 (renda das rodadas 6/7) ao @pm em vez de escolher dois números. ACs 5 e 7 fazem a randomização de D-06 **condicional ao sinal medido**, com o "não implementar sem evidência" escrito de forma explícita. AC 9 não decide R-05. Nenhum valor inventado em nenhum AC. **Should-Fix aplicado ao AC 2 (reforço factual):** a story dizia que mudar `x` num `ItemDef` "não **deveria** por si só mover o golden hash" — conferido no código, a garantia é mais forte que isso: `BASELINE` (`determinism.ts:78-84`) e `BUILD_BASELINE` (`determinism.ts:100`+) montam `PickSetup` **sem** `itemBonus`, e `world.ts:162` é `if (pick.itemBonus) addPartialBonus(...)` — um ramo em que essas rodadas nunca entram. O hash não observa a loja nem `match/` (§2.2). Portanto o resultado esperado é "idêntico" com firmeza, e hash diferente aqui é sintoma de escopo violado (AC 10), a investigar antes de re-gravar. **Nice-to-have registrado como observação, sem alterar o AC (Artigo IV):** o AC 6 manda a randomização de D-06 consumir stream 5/6, que §4.2 realmente diz com essas palavras — mas 5 e 6 são os streams da política de partida do bot (§2.5) e seriam compartilhados na mesma partida, exatamente o acoplamento que D-08 evita; §2.5 lista o stream 7 como "livre". Não troquei o número porque a fonte é literal; anotei para o @architect decidir **se** a randomização for implementada, que é o caso menos provável. Dependência de `e3.6` correta e bem argumentada (revisar preços antes do ajuste de duração mediria um jogo que ainda vai mudar de ritmo). AC 8 correto: esta story não bloqueia o portão. | @po (Pax) |
