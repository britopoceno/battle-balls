# Story e3.5: Telemetria local — coletor em `client/telemetria.ts`, exportação JSON (P3.1-P3.3)

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run check", "npm run sim:check", "uma partida jogada gera P3.1/P3.2/P3.3 a partir do arquivo exportado, sem cálculo manual", "revisão manual: só client/telemetria.ts toca localStorage"]
```

## Story

**Como** desenvolvedor executando o passo 5 do plano de construção da Fase 3 (`docs/architecture-e3.md`
§12),
**eu quero** criar `src/client/telemetria.ts` — um coletor que assina os `EventoPartida[]` já emitidos por
`match/` (`e3.2`), acumula em memória, persiste em `localStorage` sob chave versionada e oferece exportação
JSON,
**para que** as evidências P3.1 (mediana da rodada), P3.2 (% de rodadas de morte súbita) e P3.3
(distribuição de compra física × combate) do portão da Fase 3 sejam calculáveis a partir de uma partida
jogada de verdade, sem cálculo manual — e, de carona, o substrato sirva também ao indicador nunca
instrumentado do Risco #4 (RF-36), se o @pm decidir incluí-lo.

## Depende de

`e3.4` (cliente completo). O coletor assina eventos produzidos durante o fluxo real de partida no cliente —
sem o fluxo de `e3.4` funcionando (draft → builds → rodada → loja → placar), não há eventos de verdade para
coletar, só os tipos (que já existem desde `e3.2`).

## Acceptance Criteria

1. `npm run check` verde.
2. `npm run sim:check` verde, com **golden hash idêntico** — a telemetria não toca `sim/` nem `match/`
   (que já emite os eventos desde `e3.2`); esta story só adiciona o **coletor**.
3. `EventoPartida` (tipo já definido em `src/match/types.ts` desde `e3.2`) não é redefinido aqui — esta
   story consome o que já existe. Os cinco eventos, para referência:
   ```ts
   export type EventoPartida =
     | { t: 'rodadaFim'; rodada: number; duracaoMs: number; vencedor: Jogador | -1; atingiu60s: boolean; controle: ['humano'|'bot', 'humano'|'bot'] }
     | { t: 'compra';    rodada: number; jogador: Jogador; itemId: string; trilha: Trilha; preco: number; ouroDepois: number }
     | { t: 'trocaDeBuild'; rodada: number; jogador: Jogador; preco: number }
     | { t: 'buildPadrao';  rodada: number; jogador: Jogador }
     | { t: 'partidaFim';   rodadas: number; placar: [number, number] }
   ```
4. `src/client/telemetria.ts` (novo): assina os eventos emitidos pelas `Transicao` de `match/`, acumula em
   memória e persiste em `localStorage` sob chave **versionada**: `bb.telemetria.v1`.
5. Existe um **botão/atalho de exportar JSON** — todos os eventos acumulados, exportáveis como arquivo para
   análise externa.
6. **Pureza preservada:** `sim/` não ganha um contador; `match/` produz eventos, que é dado, não I/O. **Só**
   `src/client/telemetria.ts` toca `localStorage` — é a única peça desta seção que conhece DOM.
7. **P3.1** (mediana da rodada com humano no controle) é derivável de `rodadaFim.duracaoMs`, **filtrando
   pelo campo `controle`** — o campo existe precisamente porque partidas bot × bot (do teste headless de
   `e3.2`, passo 2) entram no mesmo fluxo e **não podem contaminar** a mediana.
8. **P3.2** (% de rodadas que atingem 60s) é derivável de `rodadaFim.atingiu60s` (`ticks × TICK_MS ≥
   SUDDEN_DEATH_MS`, a mesma conta do arnês).
9. **P3.3** (distribuição de compra física × combate) é derivável de `compra.trilha`.
10. Métricas adicionais, não bloqueantes do portão mas exigidas pela arquitetura: trocas de build por
    partida (D-01, via evento `trocaDeBuild`) e quantas vezes a default de D-06 entrou (via evento
    `buildPadrao` — sinal de que a randomização condicional de `e3.7` pode ser necessária).
11. **RF-36 de carona — R-07 JÁ FOI RESOLVIDA PELO USUÁRIO, está DENTRO do escopo desta story.** Correção do
    @po: o bloco "Resolução do usuário (2026-07-29)" de `docs/architecture-e3.md` §14 fecha R-07 de forma
    afirmativa — *"R-07 (telemetria RF-36) **entra de carona** no substrato de §10"* (linha 1155 do
    documento). A construção é a mesma das outras duas resoluções da mesma frase ("R-04 ... **como**
    provisório, marcado explicitamente; R-05 ... **decide** depois de medir com humano"): todas afirmam o
    caminho recomendado, não o mantêm em aberto. Portanto **não** há confirmação de @pm pendente, e esperar
    por ela deixaria RF-36 sem instrumentação por uma segunda fase seguida.

    Esta story **implementa** o evento `cast { ballIndex, ponteiro, anguloErro }`, alimentado por `input.ts`,
    junto do resto do substrato. Conferido no código: `src/client/input.ts` já expõe `Disparo` com
    `ballIndex: 0 | 1` (`input.ts:10-11`), já mantém "mira em andamento **por ponteiro**" em `Entrada`
    (`input.ts:21-22`) e já monta `ballIndex: mira.botao.ballIndex` no disparo (`input.ts:87`) — os três
    dados que o evento precisa existem na origem, e o custo é o de um evento a mais, como §10.3 previu.

    O que RF-36 pede (`docs/prd.md` §5, RF-36): *"% de rodadas com uma só mão; taxa de cast desperdiçado"* —
    o evento `cast` precisa ser suficiente para derivar **as duas** métricas do arquivo exportado, no mesmo
    critério do AC 12. Isto **não** reabre o portão de E1: é instrumentação nova em fase nova, exatamente
    como §10.3 registra.
12. Verificação da story: **uma partida jogada gera P3.1, P3.2 e P3.3 a partir do arquivo exportado, sem
    cálculo manual** — ou seja, o formato exportado deve ser diretamente processável (por exemplo, um
    script simples de agregação) sem transformação manual de dados.
13. **Escopo de arquivos (corrigido pelo @po — o AC anterior era autocontraditório).** A redação anterior
    dizia "nenhuma linha fora de `src/client/telemetria.ts` é alterada", o que é impossível de cumprir junto
    do AC 4 (o coletor tem de **assinar** os eventos que saem das `Transicao`, e quem chama `aplicar` é
    `main.ts`) e do AC 5 (um botão/atalho de exportar é DOM). Os arquivos que esta story pode tocar são,
    exatamente:
    - `src/client/telemetria.ts` — **novo**, o coletor; é o **único** lugar que toca `localStorage` (AC 6);
    - `src/client/main.ts` — **apenas** a ligação: entregar ao coletor os `Transicao.eventos` que o fluxo já
      produz, e ligar o gatilho de exportação. Nenhuma regra de jogo, nenhum cálculo de métrica aqui;
    - `src/client/input.ts` — **apenas** a emissão do evento `cast` do AC 11;
    - `index.html`/CSS — somente se o gatilho de exportação do AC 5 precisar de um elemento novo.

    **Continua proibido:** qualquer linha de `src/sim/`, `src/match/` (que já emite os eventos desde `e3.2`),
    `src/shop/`, `src/bot/` ou `src/chars/`. `git diff --stat` deve provar isso.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend / Instrumentation
**Secondary Type(s)**: —
**Complexity**: Low — um único arquivo novo, sem lógica de jogo; o risco está em não vazar I/O para fora
dele e em manter a chave de `localStorage` versionada.

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa (quality gate — a story existe para produzir evidência de portão; @qa valida que as métricas batem
  com o que a telemetria produz)

**Supporting Agents**:
- (nenhum necessário)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Rodar antes de marcar a story como completa
- [ ] Pre-PR (@github-devops): Rodar antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Nenhum I/O (`localStorage`, DOM) fora de `src/client/telemetria.ts`
- Chave de `localStorage` versionada (`bb.telemetria.v1`) — mudanças futuras de formato não devem corromper
  dados antigos silenciosamente

**Secondary Focus**:
- `controle: ['humano'|'bot', 'humano'|'bot']` corretamente preenchido para não contaminar P3.1 com
  partidas de teste headless

## Tasks / Subtasks

- [x] Task 1 — Coletor (AC: 4, 6)
  - [x] Criar `src/client/telemetria.ts`; assinar `EventoPartida[]` emitido pelas `Transicao` de `aplicar`
  - [x] Acumular eventos em memória
  - [x] Persistir em `localStorage` sob `bb.telemetria.v1`

- [x] Task 2 — Exportação (AC: 5, 12)
  - [x] Botão/atalho de exportar todos os eventos acumulados como JSON
  - [x] Confirmar que o formato exportado permite calcular P3.1/P3.2/P3.3 sem transformação manual

- [x] Task 3 — Métricas adicionais (AC: 10)
  - [x] Confirmar que trocas de build e entradas de `buildPadrao` são contáveis a partir dos eventos
        exportados

- [x] Task 4 — RF-36 de carona (AC: 11) — **EM ESCOPO, não condicional** (R-07 resolvida em §14)
  - [x] Adicionar evento `cast { ballIndex, ponteiro, anguloErro }` alimentado por `src/client/input.ts`
        (os três dados já existem lá: `input.ts:10-11`, `input.ts:21-22`, `input.ts:87`)
  - [x] Confirmar que o export permite derivar **as duas** métricas de RF-36: % de rodadas com uma só mão e
        taxa de cast desperdiçado
  - [x] **Não** consultar @pm nem bloquear — a decisão de escopo já está tomada

- [x] Task 5 — Verificação (AC: 1, 2, 7, 8, 9, 13)
  - [x] `npm run check` — 0 erros
  - [x] `npm run sim:check` — golden hash idêntico
  - [x] Jogar uma partida completa e confirmar que o JSON exportado permite calcular P3.1, P3.2 e P3.3
  - [x] `git diff --stat` — confirmar escopo (`src/client/telemetria.ts` + eventual `input.ts` se AC 11 for
        confirmado)

## Dev Notes

### De onde os dados saem (fonte: `docs/architecture-e3.md` §10.1)

> `match/` já produz `EventoPartida[]` em cada transição (§2.3), porque um redutor puro que não conta o que
> fez é um redutor que obriga o cliente a reconstruir a história. Os eventos são exatamente o que o portão
> precisa.

Tabela de origem de cada evidência do portão:

| Portão | Métrica | Origem |
|---|---|---|
| **P3.1** | mediana da rodada **com humano no controle** | `rodadaFim.duracaoMs`, filtrando `controle` — o campo existe porque partidas bot×bot (§12, passo 2) entram no mesmo fluxo e **não** podem contaminar a mediana |
| **P3.2** | % de rodadas que atingem 60s | `rodadaFim.atingiu60s` (`ticks × TICK_MS ≥ SUDDEN_DEATH_MS`, a mesma conta do arnês) |
| **P3.3** | distribuição física × combate | `compra.trilha` |
| — | trocas de build por partida (D-01) | `trocaDeBuild` |
| — | quantas vezes a default de D-06 entrou | `buildPadrao` |

[Fonte: `docs/architecture-e3.md` §10.1]

### Onde os dados são guardados (fonte: §10.2)

> `src/client/telemetria.ts`: assina os eventos, acumula em memória e persiste em `localStorage` sob chave
> versionada (`bb.telemetria.v1`), com um botão/atalho de **exportar JSON**. Três razões para não ser mais
> sofisticado que isso: não há servidor até a Fase 4; o volume é de dezenas de eventos por partida; e
> telemetria de jogadores reais é RF-49, **Fase 5**.
>
> **Pureza preservada:** `sim/` não ganha um contador. `match/` produz eventos, que é dado, não I/O. Só
> `client/telemetria.ts` toca `localStorage`, e ele é a única coisa desta seção que conhece DOM.

[Fonte: `docs/architecture-e3.md` §10.2]

### Dívida que este substrato paga de carona — RF-36 (fonte: §10.3)

> O indicador do **Risco #4** (P1.3: % de rodadas com uma só mão, taxa de cast desperdiçado) foi aprovado no
> PRD §6 com a instrução "instrumentar **junto com** a Fase 1". Verifiquei: **não existe telemetria nenhuma
> em `src/client/`**. [...] Não é escopo desta fase reabrir o portão de E1, e não estou propondo isso. Mas o
> substrato da §10.2 serve aos dois indicadores pelo mesmo preço: um evento `cast` com `{ ballIndex,
> ponteiro, anguloErro }` alimentado por `input.ts` fecha RF-36 de carona. Registro como **recomendação não
> bloqueante**, para o @pm decidir se entra na fase.

[Fonte: `docs/architecture-e3.md` §10.3 e §14/R-07]

### O que esta story explicitamente NÃO faz

- Não redefine `EventoPartida` — esse tipo já existe desde `e3.2`.
- Não implementa nenhum servidor de telemetria — é `localStorage` + export local, ponto final. Servidor é
  Fase 4.
- ~~Não implementa o evento `cast`/RF-36 por padrão~~ — **corrigido pelo @po:** R-07 foi resolvida
  afirmativamente pelo usuário em §14 ("entra de carona no substrato de §10"), então o evento `cast` **está
  em escopo** (AC 11). Não há confirmação de @pm pendente.
- Não calcula P3.1/P3.2/P3.3 automaticamente dentro do cliente — a arquitetura só exige que sejam
  **deriváveis do arquivo exportado**, não que existam num dashboard in-app.

### Testing

- Rodar `npm run sim:check` e confirmar golden hash idêntico.
- Rodar `npm run check` e confirmar 0 erros de `tsc`.
- Jogar uma partida completa (pode ser a mesma sessão do smoke visual de `e3.4`) e confirmar que:
  - o JSON exportado contém eventos `rodadaFim` para cada rodada, com `controle` corretamente marcado;
  - P3.1 (mediana de `duracaoMs` filtrando `controle` humano), P3.2 (`%` de `atingiu60s`) e P3.3
    (distribuição de `compra.trilha`) são calculáveis diretamente do arquivo, sem transformação manual.
- Confirmar por grep/leitura que `localStorage` só é tocado em `src/client/telemetria.ts`.

## Dev Agent Record

### File List

**Criados:**
- `src/client/telemetria.ts` — o coletor. Chave versionada `bb.telemetria.v1`, acúmulo em memória
  espelhado a cada escrita, exportação JSON, o tipo `EventoCast` de RF-36 e `anguloErroGraus`.
  **É o único arquivo do projeto que toca `localStorage`.**
- `src/tools/telemetria.ts` — o agregador que prova o AC 12. **Desvio de escopo registrado, ver abaixo.**

**Modificados (todos na lista do AC 13):**
- `src/client/main.ts` — só a ligação: entrega de `Transicao.eventos` ao coletor (nos DOIS pontos de
  saída de eventos de `match/` — `aplicar` e `registrarRodada`), emissão do `cast` e gatilho de
  exportação. Nenhum cálculo de métrica. **Correção do gate:** propaga `ladoDaTela` e `mag` de
  `Disparo` para o `EventoCast` registrado; `null` explícito no cast de teclado.
- `src/client/input.ts` — só `Disparo.ponteiro` (o `pointerId`) e a constante `TECLADO`. **Correção
  do gate:** `Disparo.ladoDaTela` (região do toque inicial), `LIMIAR_ARRASTO_PX` e `ARRASTO_MAX`
  exportados, doc de `ponteiro` corrigida (identifica um contato, não um dedo).
- `index.html` — o botão `#exportar` e o CSS dele.

**Modificados na correção do gate FAIL (fora da lista original, dentro do mesmo escopo de AC 13):**
- `src/client/telemetria.ts` — `EventoCast` ganha `ladoDaTela` e `mag`; docs corrigidas.
- `src/tools/telemetria.ts` — "uma só mão" agora deriva de `ladoDaTela`; taxa de desperdício exclui
  casts sem mira real e valores de ângulo inválidos; rótulo de P3.2 corrigido; `mag` ausente
  (arquivo de formato anterior) contabilizado à parte.

### Desvio de escopo do AC 13 — `src/tools/telemetria.ts`

O AC 13 lista os arquivos que a story pode tocar e este não está nela. Fica declarado, não escondido,
com o argumento para o @qa julgar:

- O AC 12 exige que P3.1/P3.2/P3.3 saiam do arquivo **"sem cálculo manual"** e **nomeia o
  instrumento**: *"por exemplo, um script simples de agregação"*. Sem ele, o AC 12 estaria provado só
  por leitura de código — que é exatamente o que a errata da Bíblia (§7) manda não aceitar.
- A proibição que o AC 13 protege está **intacta**: `git diff --stat` mostra zero linha em `src/sim/`,
  `src/match/`, `src/shop/`, `src/bot/` e `src/chars/`.
- Ele fica em `tools/` e não em `client/` pelo mesmo princípio de §10.2 que proíbe o coletor de
  calcular métrica: número agregado dentro do jogo é número que ninguém audita contra os eventos que
  o produziram.

### O erro que o agregador pegou em si mesmo — P3.3 estava contaminada

Registrado porque é o mesmo erro que `controle` existe para impedir em P3.1, cometido uma seção
abaixo, e porque só apareceu quando os números saíram de uma partida de verdade.

A primeira versão contava **todas** as compras em P3.3. Numa partida contra o bot, metade delas é do
bot — e a política de `bot/partida.ts` **alterna trilha por construção**, então ela empurra a
distribuição para 50/50 independentemente do que o humano prefira. A primeira execução imprimiu
`física 1 (50,0%) · combate 1 (50,0%)` sobre 2 compras: um resultado perfeitamente plausível e
completamente sem sentido, porque uma das duas era do bot.

O dado exportado já bastava para consertar sem recoletar: `rodadaFim.controle` é indexado por
JOGADOR (§10.1), então quem era humano naquela partida sai de um join com `compra.jogador`. O
agregador passou a fazê-lo — e **não** assume "humano é o jogador 0", que é convenção do cliente de
hoje e não fato registrado no arquivo. Depois do conserto: `física 0 · combate 1 (100%) — 1 compra(s)
de bot excluída(s)`, que bate com a única compra que o humano fez na sessão (Lâmina, trilha combate).

### Verificação executada

| Prova | Resultado |
|---|---|
| `npm run check` | 0 erros |
| `npm run sim:check` | verde, exit 0; **golden hash idêntico** nas 5 seeds e nas 5 variantes de build (AC 2) |
| `grep -rn localStorage src/` | uma só ocorrência de CÓDIGO, em `src/client/telemetria.ts`. A outra é um comentário em `match/types.ts` que já existia (AC 6) |
| `git diff --stat` | `index.html`, `src/client/input.ts`, `src/client/main.ts` — todos na lista do AC 13. Zero linha em `sim/`, `match/`, `shop/`, `bot/`, `chars/` (AC 13) |
| Partida completa jogada e exportada | 14 eventos · 1 partida · 3 rodadas · 2 compras · 8 casts, console limpo (só mensagens do Vite) |

**Saída real do agregador sobre o arquivo exportado (AC 12 — nenhuma transformação manual):**

```
P3.1  mediana da rodada com humano no controle
      14.4s (n=3)
P3.2  rodadas que atingem 60s (morte súbita)
      0.0% — 0 de 3
P3.3  distribuição de compra por trilha (só o humano)
      física 0 (0.0%) · combate 1 (100.0%) — 1 compra(s) de bot excluída(s)

extras
      trocas de build (D-01): 0 · 0.00 por partida
      default de D-06 entrou: 0 vez(es)

RF-36 Risco #4
      rodadas com uma só mão: 100.0% — 3 de 3
      cast desperdiçado (erro > 30°): 75.0% — 6 de 8 · erro mediano 104.0°
```

Conferência cruzada: as três durações que a tela de fim mostrou (11,7s · 15,3s · 14,4s) dão mediana
14,4s — bate com P3.1. Nenhuma rodada chegou a 60s — bate com P3.2.

**Ressalva honesta sobre os números de RF-36 desta sessão:** eles são de uma sessão de **mouse**, não
de polegares. "100% de rodadas com uma só mão" é literalmente verdade (um cursor é um ponteiro só) e
não diz nada sobre ergonomia de celular; "75% de cast desperdiçado, erro mediano 104°" reflete
arrastos em direções arbitrárias, não um jogador tentando acertar. O que esta sessão prova é que **as
duas métricas são deriváveis do arquivo**, que é o que o AC 11 pede. O número que vale para o Risco #4
sai da sessão no celular.

### Correção do gate FAIL (TEL-E35-006 bloqueante + TEL-E35-001/002/004 recomendados)

Aplicado via `apply-qa-fixes` sobre `docs/qa/gates/e3.5-telemetria-local.yml`. Nada do achado foi
questionado — a premissa errada de `ponteiro` estava mesmo escrita em três lugares, e a evidência de
toque real do próprio @qa provou.

**TEL-E35-006 (bloqueante) — corrigido.** `EventoCast` ganha `ladoDaTela: 'esq' | 'dir' | null`:
região da tela (metade esquerda/direita do canvas, em px de cliente) onde o TOQUE COMEÇOU
(`inicio.x`, não a posição de soltar — é onde a mão estava, não para onde o arrasto mirou).
`null` para casts de teclado. "% de rodadas com uma só mão" agora agrupa por `(partida, rodada)` e
conta REGIÕES distintas de `ladoDaTela`, não `pointerId` distintos — `tools/telemetria.ts:130-145`.
`ponteiro` foi mantido (multitoque simultâneo e separar teclado de toque, que é uma pergunta
diferente e legítima), mas a premissa "identifica o dedo" foi corrigida nos dois lugares de código
onde estava escrita: `input.ts` (doc de `Disparo.ponteiro`) e `telemetria.ts` (doc de `EventoCast` e
do arquivo). **Não corrigi a redação do AC 11 da story** — é campo do @po (`story-lifecycle.md`:
"Title, Description, AC, Scope | @po only"); fica registrado aqui como pendência de redação, não como
divergência de comportamento.

**Verificação independente, não aceitação por leitura:** escrevi um fixture sintético (`docs/qa`
não versionado, descartável) com valores conhecidos antes de rodar — 2 rodadas, uma com 4 casts todos
em `'esq'` (uma mão esperada) e outra com 2 casts em lados opostos (duas mãos esperada). Saída:
`50.0% — 1 de 2`, exato. Teste negativo (todos os `ladoDaTela: null`, simulando dado sem a correção):
`— — 0 de 0 (nenhuma rodada com cast por toque)` — degrada honestamente, sem `NaN%` e sem inventar
zero. Rodei também contra `docs/battle-balls-telemetria-1785816621271.json` (a evidência de toque real
do próprio gate, que antecede esta correção): confirma o que o gate já previu — o arquivo não tem
`ladoDaTela`, então a métrica sai `— — 0 de 0`, e não mais o `0,0%` falsamente confiante do FAIL.

**Rejogar a sessão de toque real fica como próximo passo, não coberto aqui.** Não há aparelho de
toque neste ambiente de execução; `docs/battle-balls-telemetria-1785816621271.json` continua sendo o
único dado de toque do projeto e agora está corretamente marcado como não-respondente à métrica (a),
em vez de aparentar uma resposta. Recomendo ao usuário jogar uma sessão de celular após este fix para
fechar o Risco #4 de fato — a métrica está pronta para receber esse dado.

**TEL-E35-001 (recomendado, aplicado) — `mag` no `EventoCast`.** Propagado de `Disparo.mag` (já
existia, só não ia para o evento). `input.ts` exporta `LIMIAR_ARRASTO_PX` (renomeado do literal `6`
inline) e `ARRASTO_MAX` para o agregador reconstruir o mesmo limiar sem duplicar o número. O
agregador exclui da taxa de desperdício os casts com `mag` abaixo do limiar (mira ainda no
placeholder `{dx:1,dy:0}` de `pointerdown`) e reporta a contagem excluída na própria linha, em vez de
misturá-los com casts de mira real.

**TEL-E35-002 (recomendado, aplicado) — guarda de `anguloErro` no agregador.** Troquei
`c.anguloErro >= 0` (que aceita `null` por coerção — `null >= 0` é `true` em JS) por
`Number.isFinite(c.anguloErro) && c.anguloErro >= 0`, com aviso de quantos foram descartados.
Verificado por NEG-2 do próprio gate: `anguloErro: null` já não zera a taxa silenciosamente, ela
avisa e exclui.

**TEL-E35-004 (recomendado, aplicado) — rótulo de P3.2.** Agora declara o mesmo filtro que P3.1 e
P3.3 já declaravam ("só com humano no controle") e o número de rodadas bot×bot excluídas, sem mudar o
cálculo (que já era filtrado).

**Efeito colateral descoberto durante a correção, corrigido de carona:** rodar o agregador contra o
arquivo de evidência antigo mostrou que casts sem o campo `mag` (formato anterior a esta correção)
desapareciam silenciosamente de `semMiraReal` e `comMiraReal` ao mesmo tempo (`undefined >= x` e
`undefined < x` são ambos `false`). Adicionado `magAusente` como categoria própria, com aviso
explícito — a mesma classe de erro que TEL-E35-002 aponta, encontrada pelo mesmo método (rodar contra
dado real, não confiar em leitura).

**TEL-E35-003 e TEL-E35-005 — não tocados**, conforme o próprio gate já havia decidido
(`correcoes_para_reabrir_o_gate.fora_do_escopo_desta_story`): TEL-E35-003 vai para
`architecture-e3.md` §12.1 como AC de `e3.6`; TEL-E35-005 pode ir junto de `e3.6`, que é quem opera as
sessões de medição.

**Verificação:**

| Prova | Resultado |
|---|---|
| `npm run check` | 0 erros |
| `npm run sim:check` | verde, exit 0; golden hash idêntico (5 seeds + 5 variantes) — nada em `sim/`/`match/` foi tocado |
| `git diff --stat` | `src/client/input.ts`, `src/client/main.ts`, `src/client/telemetria.ts`, `src/tools/telemetria.ts` — os quatro já cobertos pelo AC 13 (incluindo o desvio já julgado ACEITO). Zero arquivo novo, zero linha em `sim/`, `match/`, `shop/`, `bot/`, `chars/` |
| Fixture sintético (2 rodadas, valores conhecidos) | as 2 métricas de RF-36 batem exatamente com o esperado; teste negativo degrada sem `NaN%` |
| `docs/battle-balls-telemetria-1785816621271.json` (evidência real de toque, formato antigo) | agora reporta corretamente "sem dado" em vez do `0,0%` do FAIL — 23 casts sem `mag` explicitamente contabilizados e avisados |

**Higiene:** `docs/battle-balls-telemetria-1785816621271.json` continua untracked, como o gate já
notou — decisão do usuário sobre versionar ou apagar, não alterada aqui.

## QA Results

**Gate: FAIL** · Quinn (@qa, `quality_gate` declarado nesta story) · 2026-08-04 · gate completo em `docs/qa/gates/e3.5-telemetria-local.yml`

**12 dos 13 ACs passam**, tudo reexecutado de forma independente — nada aceito do Dev Agent Record. `npm run check` 0 erros; `npm run sim:check` verde com **golden hash idêntico** nas 5 seeds e nas 5 variantes de build. Escopo do AC 13 provado por `git show --stat eb0ef0c`: seis arquivos, **zero linha** em `src/sim/`, `src/match/`, `src/shop/`, `src/bot/`, `src/chars/`. AC 6 provado por varredura em HEAD sobre o repositório inteiro: `localStorage` aparece em **uma única linha de código** em todo o projeto (`src/client/telemetria.ts:100` e `:138`); a única outra ocorrência é comentário em `match/types.ts:159`, que já existia.

**FAIL por um único achado, de alta severidade: TEL-E35-006 — o AC 11 não está cumprido.** A primeira das duas métricas de RF-36, *"% de rodadas com uma só mão"*, **não é derivável do arquivo exportado em aparelho de toque** — que é o único aparelho para o qual a pergunta do Risco #4 faz sentido. O evento grava `ponteiro = PointerEvent.pointerId` sob a premissa, escrita em três lugares (AC 11 da story, `input.ts:17`, `telemetria.ts:34-37`) e na mensagem do commit, de que ele identifica *o dedo*. Ele não identifica: identifica **um contato**, e num touchscreen cada toque novo recebe um `pointerId` novo.

Não é dedução — está **medido sobre uma sessão de toque real deste build**, o arquivo `docs/battle-balls-telemetria-1785816621271.json` (untracked, exportado em 2026-08-04T04:10:21Z): **23 casts, 23 `pointerId` distintos**, em blocos sequenciais (14-18, 25-26, 30-34, 44-47, 60-66). O agregador imprime `rodadas com uma só mão: 0.0% — 0 de 4`. A métrica só consegue dizer "uma mão" numa rodada com **exatamente um** cast; com 2, 3 ou 7 casts ela diz "duas mãos" sempre, use o jogador uma mão ou duas. Conferi se algum outro campo resgata a resposta: **nenhum** — `ballIndex` é o único candidato, foi corretamente descartado pelo próprio @dev, e na sessão real alterna `0,1,0,1,1,1,1`, compatível com uma mão e com duas indistintamente.

A sessão de mouse do @dev imprimiu `100%` e pareceu perfeita **porque o mouse mantém um `pointerId` estável**. A métrica acertou por acidente no aparelho que não interessa e é estruturalmente incapaz de acertar no que interessa. O @dev chegou perto — a ressalva dele diz que o `100%` "não diz nada sobre ergonomia de celular" — mas concluiu que o número bom sairia do celular. Sai `0,0%`, igualmente vazio e **mais perigoso**, porque parece uma medição alarmante em vez de uma tautologia. É o modo de falha §7.5 da Bíblia na forma mais pura, e a mesma classe de erro que o @dev pegou em P3.3 — desta vez não contaminando o número, mas tornando-o incapaz de variar.

**Correção para reabrir o gate:** gravar no `EventoCast` a região/coordenada do toque (ou um `ladoDaTela`), que é o proxy honesto de "mão", e derivar a métrica dela; o dado já existe na origem (`input.ts` calcula `[x,y]` em `pos(e)` e guarda o ponto inicial em `inicio`). **Manter `ponteiro`** — ele responde uma pergunta legítima e diferente (contatos simultâneos) e separa teclado de toque; o que não pode é continuar sendo lido como "mãos". Corrigir a premissa nos três lugares. E **rejogar a sessão de toque depois**: o dado atual não responde à métrica e não é recuperável por reanálise, porque a informação nunca entrou no arquivo.

**AC 12 provado com instrumento meu, não com o arquivo do @dev:** escrevi um `ArquivoTelemetria` sintético do zero a partir do tipo declarado no código (20 eventos, 2 partidas, os 6 tipos de evento), com os valores escolhidos para que eu soubesse as 8 respostas antes de executar. As 8 bateram — inclusive a mediana com desempate par e a exclusão do sentinela `anguloErro: -1`. Nunca abri o export do @dev, o que prova de carona que o agregador é genérico e não calibrado para uma sessão específica.

**A correção de P3.3 que o Dev Agent Record relata está viva no código e foi provada por teste negativo, não por leitura.** O arquivo sintético carrega 5 compras `combate` do BOT dentro de uma partida com humano, mais 1 numa partida bot×bot. Saída: `física 1 (100,0%) · combate 0 · 6 compra(s) de bot excluída(s)`. Se o join com `rodadaFim.controle` fosse código morto, imprimiria `física 2 (28,6%) · combate 5 (71,4%)`. Segundo negativo (NEG-1): virando o `controle` de todas as rodadas para `['bot','bot']`, P3.1 colapsa para `sem rodada com humano` e P3.3 exclui as 7 compras. Os filtros mordem.

**Desvio do AC 13 julgado e ACEITO** (`src/tools/telemetria.ts`): o AC 12 exige prova por execução e **nomeia o instrumento** ("um script simples de agregação") — uma story não pode exigir prova por execução e proibir o executor. O invariante que o AC 13 existe para proteger está intacto e verificado por mim; o arquivo é inerte para o jogo (importa só `node:fs` e um `import type`, ninguém em `src/` o importa); e `tools/` é o lugar certo pelo mesmo princípio de §10.2 que proíbe o coletor de calcular métrica. Bloquear teria custado a única evidência real e forçado o AC 12 a ser provado por leitura de código, que é o que a errata da Bíblia §7 manda não aceitar. AC 13 fica CONCERNS (não PASS) para o desvio não entrar no registro como se a lista fechada tivesse sido cumprida. Correção estrutural para o @po: quando um AC exige prova por execução, o AC de escopo tem de dar lugar ao instrumento.

Os outros quatro achados são `medium`/`low` e **não sustentariam FAIL sozinhos**:

- **TEL-E35-001 (medium).** Um toque sem arrasto dispara um cast real cujo `anguloErro` é medido contra uma direção **inventada**: a mira nasce `{dx:1, dy:0}` (`input.ts:74`) e só é reescrita com arrasto > 6px (`input.ts:89`); `disparar` não tem guarda de `mag` e `world.ts:449` executa a habilidade com mag 0. Como `EventoCast` **não grava `mag`**, "arrastou com cuidado e errou 104°" e "encostou no botão" são a mesma linha no JSON. É a mesma classe de contaminação que o @dev pegou em P3.3, uma métrica adiante — e cai justamente sobre a sessão de celular que deve produzir o número do Risco #4, onde o toque curto é *mais* frequente que no mouse. **Pelo próprio critério da story** (dado bruto no arquivo, decisão de análise no agregador — a razão de o limiar de 30° ficar fora do arquivo), `mag` pertence ao arquivo. Custo de **recoleta**, não de reanálise: dado colhido sem isso tem de ser rejogado.
- **TEL-E35-002 (medium):** o agregador não valida que `anguloErro` seja finito, e `null >= 0` é `true` em JS. Medido (NEG-2): com `anguloErro: null` a saída vira `0.0% — 0 de 6 · erro mediano 0.0°` — confiante, precisa e falsa, sem um aviso. `null` é o que `NaN`/`undefined` viram ao atravessar `JSON.stringify`, e o agregador consome um arquivo baixado, editável e possivelmente de um coletor `v2` futuro. **Não alcançável pelo cliente de hoje** (reconferi: mira nasce unitária, teclado tem `Math.hypot(...) || 1`) — por isso medium e não high. O que o torna digno de registro é a assimetria: `ler()` **é** defensivo e cita a Bíblia §7 ao explicar por quê; o agregador, escrito na mesma sessão, não é — e é ele quem produz o número do portão.
- **TEL-E35-003 (medium):** `agregar()` e `anguloErroGraus()` não têm nenhum consumidor em `src/` — `sim:check` não sabe que existem, e os dois arquivos poderiam ser apagados hoje sem uma linha vermelha. A verificação do AC 12 foi uma sessão manual irrepetível. Quarta vez que este projeto registra a mesma forma. A story está **correta** em não ter feito diferente (o AC 13 fixa o escopo). Recomendação: levar para `architecture-e3.md` §12.1 — não deixar só no YAML —, seguindo a regra que o gate de `e3.3` estabeleceu depois de ARCH-E32-001 atravessar uma story inteira sem ser vista. `agregar()` é pura e recebe um array: um fixture sintético versionado no `sim:check` cobre de uma vez o filtro de `controle`, o join de P3.3 e o denominador de RF-36.
- **TEL-E35-004 / TEL-E35-005 (low):** P3.2 é o único dos três que não declara o próprio filtro no rótulo, embora o cálculo seja filtrado por humano; e `limpar()`/`contagem()` estão expostos sem nenhum chamador, sem caminho de UI para zerar o acúmulo — o export da sessão de celular vai vir misturado com a sessão de mouse já gravada em `bb.telemetria.v1`, sem marcador de sessão para separar depois. O segundo interage com TEL-E35-001: sujam a mesma métrica pela mesma via.

Julgamentos confirmados, e são a maior parte da story: ligar o coletor nos **dois** pontos de saída de eventos de `match/` era necessário e foi feito — só `aplicar` deixaria o portão sem P3.1 e sem P3.2, e a story pareceria pronta; `registrar` depois do `if` de rejeição em `aplicarDecisao` garante que decisão recusada não vire evidência; o botão de exportar viver fora do `#overlay` está certo (`desenharTela` recria o overlay a cada fase); manter o limiar de 30° no agregador e o ângulo bruto no arquivo está certo (mudar o limiar vira reanálise, não recoleta — e é esse mesmo critério que TEL-E35-001 e TEL-E35-006 aplicam de volta contra a story); e o agregador **não** assume "humano é o jogador 0", o que importa de verdade aqui porque §5.3 alterna o lado a cada rodada.

Sobre `ponteiro` vs `ballIndex`: o **raciocínio** do @dev está certo — o mesmo polegar alternando entre os dois botões continua sendo uma mão, e contar botões responderia outra pergunta. O que falha é a **premissa** sobre o que `pointerId` entrega. Rejeitar `ballIndex` foi acertado; adotar `pointerId` como substituto não, e as duas coisas são independentes.

**Nota de método, porque é a parte deste gate que quase não aconteceu:** meu arquivo sintético plantou ponteiros "realistas" (3, 7, -1) porque reproduzi a intenção declarada do campo — e imprimiu `50,0% — 1 de 2`. Eu teria fechado o AC 11 como PASS. O que expôs TEL-E35-006 foi rodar o agregador contra um arquivo de **toque real que eu não escrevi**. Quando o dado vem de uma borda do navegador, o sintético herda a premissa errada do implementador, porque quem escreve o sintético lê a mesma documentação. Só o aparelho responde. Isso também é o argumento mais forte de TEL-E35-003: o fixture permanente que eu recomendo tem de incluir um caso de toque, senão nasce com o mesmo ponto cego.

**Higiene:** `docs/battle-balls-telemetria-1785816621271.json` está untracked em `docs/`. É agora a evidência que sustenta TEL-E35-006 e o primeiro dado de toque do projeto — recomendo versioná-lo deliberadamente (por exemplo em `docs/qa/evidencias/`) ou apagá-lo conscientemente depois do achado fechado. Não o movi nem o apaguei.

`e3.4` não foi julgada por este gate e permanece `InProgress`, aguardando o teste em celular real — esperado, não é falha.

---

### Re-review — 2026-08-05

**Gate: CONCERNS** · Quinn (@qa) · 2026-08-05 · gate completo em `docs/qa/gates/e3.5-telemetria-local.yml`

**TEL-E35-006 (bloqueante) confirmado corrigido, verificação independente, não aceita por leitura.** Escrevi um fixture sintético NOVO — não o do @dev, não o meu da rodada anterior — com uma rodada de toque nos dois lados da tela, uma rodada com todos os toques do mesmo lado, e uma rodada só de teclado (para confirmar que ela é **excluída** do denominador, não contada como "duas mãos" por default de `ladoDaTela: null`). As oito métricas bateram exatamente com o previsto antes de rodar, incluindo o denominador correto (`comToque.length = 2`, não 3 — a rodada de teclado não aparece). Rodei também contra `docs/battle-balls-telemetria-1785816621271.json` (a evidência de toque real que expôs o FAIL): agora reporta honestamente `— — 0 de 0 (nenhuma rodada com cast por toque)` em vez do `0,0%` falsamente confiante, porque o arquivo antecede a correção e não tem `ladoDaTela`.

TEL-E35-001, TEL-E35-002 e TEL-E35-004 (recomendados na mesma passada) também verificados: `mag` propagado e usado para excluir casts sem mira real; `Number.isFinite` substitui a comparação que aceitava `null` por coerção; rótulo de P3.2 corrigido. Testei especificamente um `anguloErro: -5` (inválido, fora do sentinela `-1`) — descartado com aviso explícito, não mais silenciosamente zerado.

**CONCERNS, não PASS limpo, por dois achados novos de severidade baixa encontrados durante esta verificação, nenhum bloqueante:**

- **TEL-E35-007** — o guard `naoFinito` (`!Number.isFinite(x) || x < -1`) e o guard `medidos` (`x >= 0`) deixam uma lacuna estreita: um `anguloErro` no intervalo aberto `(-1, 0)` não é capturado por nenhum dos dois — não gera aviso, não entra na contagem, simplesmente some. Medido com `anguloErro: -0.5` no meu fixture. **Não alcançável pelo cliente atual**: `anguloErroGraus()` só emite `-1` ou um valor em `[0,180]`. Mesma classe hipotética de TEL-E35-002 original, lacuna mais estreita ainda — por isso `low`.
- **TEL-E35-008** — fronteira de "mira real" é `mag < limiar` (estrito) no agregador, mas `input.ts` decide não reescrever a direção quando `d <= LIMIAR_ARRASTO_PX` (o complemento de `d > LIMIAR_ARRASTO_PX`). No ponto exato em que `mag` bate o limiar, o agregador trata como "mira real" quando `input.ts` na verdade manteve o placeholder. Medido com `mag` ajustado para o valor exato do limiar. **Praticamente inatingível** com dado real (`mag` vem de ponto flutuante; bater o valor exato do limiar por acaso é medida zero) — `low`.
- **TEL-E35-009** (arquitetura, informativo) — `agregar()` ganhou dois `console.warn`, deixando de ser puramente uma função de array-para-array. Não quebra nada hoje (`agregar()` não é chamada de `sim:check`), mas quem implementar TEL-E35-003 (cobertura permanente, `e3.6`) precisa decidir explicitamente se os avisos fazem parte do comportamento esperado do teste ou se devem ser mockados — registrado para não ser lido como ruído inesperado depois.

**Nenhum dos três achados novos é alcançável pelo cliente hoje**, e nenhum reabre o portão — ficam registrados para limpeza de baixo custo, possivelmente junto de TEL-E35-003/005 em `e3.6`, que é quem já ia mexer na mesma vizinhança de código.

**Regressão verificada, não assumida:** `npm run check` 0 erros; `npm run sim:check` golden hash idêntico (5 seeds + 5 variantes) — reexecutado por mim; `grep -rn localStorage` ainda exatamente 2 ocorrências de código, ambas em `telemetria.ts`; `git diff --stat` desta rodada — quatro arquivos, todos dentro do escopo já coberto pelo AC 13 (o desvio de `tools/telemetria.ts` continua CONCERNS pela mesma razão da rodada anterior, não reaberto).

`e3.4` continua fora do escopo deste gate, `InProgress`, aguardando teste em celular real.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-05 | 1.5.0 | QA Gate CONCERNS — Status: InReview → Done. Re-review independente confirma TEL-E35-006 corrigido (fixture novo, valores conhecidos, denominador correto ao excluir rodada só de teclado) e TEL-E35-001/002/004 aplicados. CONCERNS por dois achados novos de baixa severidade, nenhum bloqueante e nenhum alcançável pelo cliente atual: TEL-E35-007 (lacuna estreita no guard de `anguloErro` para valores em `(-1,0)`) e TEL-E35-008 (fronteira `<` vs `<=` do limiar de mira real, inatingível na prática por ponto flutuante); mais TEL-E35-009 (informativo: `agregar()` ganhou `console.warn`, deixou de ser pura — nota para quem implementar TEL-E35-003 em `e3.6`). Golden hash idêntico e `check` 0 erros reexecutados. Ver gate completo em `docs/qa/gates/e3.5-telemetria-local.yml`. | Quinn (@qa) |
| 2026-08-04 | 1.4.0 | **Correção do gate FAIL — Status: InProgress → InReview.** Aplicados via `apply-qa-fixes` sobre `docs/qa/gates/e3.5-telemetria-local.yml`. **TEL-E35-006 (bloqueante) corrigido:** `EventoCast` ganha `ladoDaTela: 'esq'\|'dir'\|null` (região do TOQUE INICIAL, não da posição de soltar), propagado de `Disparo.ladoDaTela` (`input.ts`) via `main.ts`. "% de rodadas com uma só mão" agora agrupa por região distinta, não por `pointerId` distinto (`tools/telemetria.ts`). `ponteiro` mantido só para multitoque simultâneo/separar teclado; a premissa "identifica o dedo" corrigida nos comentários de `input.ts` e `telemetria.ts` (a AC 11 da story é campo do @po, não alterada aqui). Verificado com fixture sintético de valores conhecidos (`50,0% — 1 de 2`, exato) e teste negativo (degrada para `— — 0 de 0` sem `NaN%`); rodado também contra a evidência de toque real do gate, que agora reporta corretamente "sem dado" em vez do `0,0%` falso. **Rejogar a sessão de toque real com o fix aplicado continua pendente** — sem aparelho neste ambiente. **Recomendados aplicados na mesma passada:** TEL-E35-001 (`EventoCast.mag`, exportado `LIMIAR_ARRASTO_PX`/`ARRASTO_MAX` de `input.ts`, agregador exclui casts sem mira real da taxa de desperdício); TEL-E35-002 (`Number.isFinite` substitui `>= 0` no agregador — `null >= 0` não engana mais); TEL-E35-004 (rótulo de P3.2 declara o filtro "só com humano", como P3.1/P3.3). Efeito colateral encontrado e corrigido de carona: casts do formato antigo (sem `mag`) desapareciam silenciosamente de ambos os buckets do agregador — agora contados em `magAusente` com aviso explícito. TEL-E35-003 e TEL-E35-005 não tocados, por decisão já registrada no próprio gate (fora do escopo desta story). `npm run check` 0 erros; `npm run sim:check` golden hash idêntico; `git diff --stat` — quatro arquivos, todos já cobertos pelo AC 13 (incluindo o desvio de `tools/telemetria.ts` já julgado ACEITO), zero linha em `sim/`/`match/`/`shop/`/`bot/`/`chars/`. | @dev |
| 2026-08-04 | 1.3.0 | **Gate FAIL — Status: InReview → InProgress.** Reexecução independente completa; nada aceito do Dev Agent Record. **12 dos 13 ACs passam:** `npm run check` 0 erros e `npm run sim:check` verde com **golden hash idêntico** (5 seeds + 5 variantes); escopo do AC 13 provado por `git show --stat eb0ef0c` (zero linha em `sim/`, `match/`, `shop/`, `bot/`, `chars/`) e AC 6 por varredura em HEAD (uma única linha de código com `localStorage` no projeto inteiro); AC 12 provado com arquivo sintético escrito por mim, 8 respostas conhecidas antes de executar, 8 exatas; **a correção de P3.3 foi provada VIVA por teste negativo** (5 compras de bot plantadas são excluídas — contaminado imprimiria 28,6/71,4). Desvio do AC 13 (`src/tools/telemetria.ts`) julgado e **ACEITO** — o AC 12 exige prova por execução e nomeia o instrumento, o invariante protegido está intacto e o arquivo é inerte para o jogo. **FAIL por TEL-E35-006 (high): o AC 11 não está cumprido.** A métrica "% de rodadas com uma só mão" não é derivável do arquivo em aparelho de toque. `ponteiro` grava `PointerEvent.pointerId`, que identifica um **contato**, não um dedo — num touchscreen cada toque novo recebe um id novo. Medido sobre sessão de toque real deste build (`docs/battle-balls-telemetria-1785816621271.json`): **23 casts, 23 pointerId distintos**, saída `rodadas com uma só mão: 0.0% — 0 de 4`; a métrica só pode dizer "uma mão" numa rodada com exatamente 1 cast. A sessão de mouse imprimiu 100% e pareceu perfeita porque o mouse mantém id estável — acertou por acidente no aparelho que não interessa. Nenhum outro campo do arquivo resgata a resposta. Correção: gravar região/coordenada do toque no `EventoCast` (o dado já existe em `input.ts`), manter `ponteiro` para simultaneidade, corrigir a premissa nos três lugares onde está escrita, e **rejogar a sessão de toque** — o dado atual não é recuperável por reanálise. Outros quatro achados, nenhum bloqueante sozinho: TEL-E35-001 (medium, toque sem arrasto grava `anguloErro` contra a direção placeholder e `mag` não vai para o arquivo), TEL-E35-002 (medium, `null >= 0` é `true` — agregador reporta `0.0%` confiante e falso), TEL-E35-003 (medium, telemetria sem cobertura permanente; 4ª ocorrência da mesma forma no projeto), TEL-E35-004 e TEL-E35-005 (low). Ver QA Results acima e o gate completo em `docs/qa/gates/e3.5-telemetria-local.yml`. | Quinn (@qa) |
| 2026-08-04 | 1.2.0 | Status: Ready → InProgress → InReview. Implementação completa: `src/client/telemetria.ts` (coletor, chave `bb.telemetria.v1`, exportação JSON, evento `cast` de RF-36), ligação em `main.ts` nos dois pontos de saída de eventos de `match/`, `Disparo.ponteiro` em `input.ts`, botão `#exportar` + atalho `T`. `check` 0 erros e `sim:check` verde com golden hash idêntico. AC 12 provado por execução, não por leitura: uma partida real foi jogada, exportada e agregada — as três evidências do portão saíram do arquivo sem transformação manual, e a mediana bate com o que a tela de fim mostrou. Um desvio do AC 13 declarado (`src/tools/telemetria.ts`, o agregador que o próprio AC 12 nomeia); a proibição de `sim/`/`match/`/`shop/`/`bot/`/`chars/` está intacta e provada por `git diff --stat`. O agregador pegou um erro nele mesmo durante a verificação: P3.3 contava as compras do BOT, cuja política alterna trilha por construção e empurrava a distribuição para 50/50 — corrigido com join contra `rodadaFim.controle`, sem recoletar. | @dev |
| 2026-07-29 | 1.0 | Story criada a partir de `docs/architecture-e3.md` §10 (inteira) e §12 (passo 5). AC 11 (RF-36 de carona) marcado explicitamente como condicional à decisão de escopo do @pm, conforme §14/R-07 — não implementado por padrão. | River (@sm) |
| 2026-07-29 | 1.1.0 | Validated GO (8/10) — Status: Draft → Ready. **Dois Should-Fix aplicados, o primeiro deles um desalinhamento com a resolução do usuário.** (1) **AC 11 lia R-07 como pendente quando o usuário já a resolveu.** Conferido no documento, linha 1155 de `docs/architecture-e3.md`: *"R-07 (telemetria RF-36) **entra de carona** no substrato de §10"*, dentro do bloco "Resolução do usuário (2026-07-29)". A frase é afirmativa e paralela às outras duas da mesma sentença (R-04 "**como** provisório, marcado explicitamente"; R-05 "**decide** depois de medir com humano") — as três afirmam o caminho, nenhuma o mantém aberto. A story fazia o evento `cast` depender de uma confirmação de @pm que já aconteceu, e o efeito prático seria RF-36 atravessar uma segunda fase sem instrumentação, depois de já ter sido aprovado no PRD §6 com a instrução "instrumentar **junto com** a Fase 1" (`prd.md:310`). Corrigido: `cast` está em escopo, Task 4 deixou de ser condicional. Conferido que os dados existem na origem: `input.ts:10-11` (`Disparo.ballIndex: 0 | 1`), `input.ts:21-22` (mira em andamento **por ponteiro**), `input.ts:87` (`ballIndex: mira.botao.ballIndex`) — o evento custa mesmo "um evento a mais", como §10.3 afirma. (2) **AC 13 era autocontraditório com AC 4 e AC 5:** exigia que nenhuma linha fora de `telemetria.ts` mudasse, mas assinar as `Transicao` exige ligação em `main.ts` (que é quem chama `aplicar`) e o botão de exportar é DOM. Reescrito com a lista exata de arquivos permitidos e a lista do que continua proibido. Outros fatos conferidos: **não existe telemetria nenhuma em `src/client/` hoje** — `grep localStorage src/client/` não retorna nada, o que confirma a afirmação de §10.3/R-07 e o motivo de a chave versionada `bb.telemetria.v1` do AC 4 nascer aqui; `EventoPartida` do AC 3 é idêntico ao de §10.1 e ao que `e3.2`/AC 26 declara, sem redefinição. Dependência de `e3.4` correta. Nenhum número de D-05/D-09 tocado. | @po (Pax) |
