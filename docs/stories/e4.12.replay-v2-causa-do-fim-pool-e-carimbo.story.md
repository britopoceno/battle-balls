# Story e4.12: `bb.replay.v2` — causa do fim da rodada e da partida, pool e carimbo de código

## Status

Draft

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run check && npm run sim:check — golden hash idêntico; a reprodução de rodada natural não muda", "npm run build verde", "cenário discriminante W-2: Bo5 com W.O. numa rodada QUE NÃO É A 0 dá rc=0 em npm run replay:check, com a linha de W.O. e o hash conferido no tick do corte", "contrafactual obrigatório do cenário acima: o mesmo arquivo com aoVivo.ticks - 1 tem de reprovar", "uma partida com 'anular' no estouro da pausa dá rc=0 com a linha 'interrompida'", "uma v1 gravada pela e4.6 continua verificando (leitor aceita v1 e v2)", "mutações de aceite, cada uma numa cópia descartável, têm de FALHAR: classificar toda rodada como natural; para 'wo', reproduzir até o fim natural em vez de aoVivo.ticks; aceitar aoVivo.vencedor também para rodadas natural; ignorar o pool do arquivo", "E46-TST-001 absorvido: canário na ÚLTIMA rodada (dx trocado que muda o hash dela) e canário com uma rodada a mais no arquivo; Q5 e Q15 têm de dar rc=1", "E46-TST-002 absorvido: depois da queda no draft, a sala joga até o fim com escolha diferente da anterior, e o replay passa por verificarReplay; Q12 tem de dar rc=1", "E46-TST-003 absorvido: duas Bo5 simultâneas no mesmo servidor, os dois replays verificados; Q8 (gravação compartilhada) tem de dar FALHA", "git status --short src/server/main.ts vazio antes de editar, na mesma disciplina de sequência de tools/determinism.ts usada em e4.10", "git show --stat <commit de implementação> — só os arquivos do AC de escopo"]
```

## Story

**Como** desenvolvedor fechando o achado `E46-ARC-001` (medium) e a errata `E46-ARC-002` do gate de `e4.6` (`docs/qa/gates/e4.6-gravador-de-replay.yml`, decidido pelo @architect em `docs/architecture-e4.md` §7.3, commit `3e46e3d`),

**eu quero** um formato de replay `bb.replay.v2` que grave a causa do fim de cada rodada (`natural`/`wo`) e da partida (`fim`/`interrompida`), o `pool` usado e um carimbo do código que gravou (commit + sujo), com o leitor continuando a aceitar `bb.replay.v1`,

**para que** uma partida com W.O. ou interrompida por R-02 deixe de reprovar `replay:check` como se fosse quebra de determinismo, e P4.3 possa fechar sem o asterisco "exceto partidas com W.O. ou encerradas antes do fim".

## Depende de

Não depende de nenhuma story em aberto para **começar**. Não é pré-condição de **coleta** da `e4.7` — a regra de exclusão de evidência de P4.3 (AC 12 abaixo) já cobre partidas com W.O. durante a coleta, e a `e4.7` já identifica quedas pelo próprio registro de quedas do AC 8 dela (v1.8.0). É **pré-condição de fechar a Fase 4**, pela linha P4.3 do Anexo B de `architecture-e4.md`: sem esta story, P4.3 fecha com o asterisco "exceto partidas com W.O. ou interrompidas", numa política que já está em produção no servidor.

**Pode rodar em paralelo com a `e4.7`.** As duas abrem `src/server/main.ts` — a `e4.7` pelo AC 11 dela (comentário sobre o L-3 e a frase de contrato de `main.ts:240-243` da §6.3), esta pelo carimbo de commit/sujo (AC 4 abaixo). Ver "Sequência" no AC 11.

**Origem, sem invenção:** `docs/architecture-e4.md` §7.3 "A rodada de W.O. e a partida interrompida no replay" (decisão `bb.replay.v2`, `E46-ARC-001`/`E46-ARC-002`, gate de `e4.6`, commit `3e46e3d`); errata de custo de armazenamento nas §1.3/§7.1 do mesmo commit; os achados `E46-TST-001`, `E46-TST-002` e `E46-TST-003` do gate de `e4.6` (`docs/qa/gates/e4.6-gravador-de-replay.yml`), que a §7.3 recomenda absorver aqui.

## Acceptance Criteria

1. **`encerramento: 'natural' | 'wo'` por rodada**, classificado em `gravarPasso` pela regra medida em W-2 (§7.3): uma rodada que fecha sem `w.over` e com `w.tick < tetoDeTicks` só pode ter fechado por `estourarPausa` — é `'wo'`. Para `'wo'`, o verificador reproduz **até `aoVivo.ticks`**, compara `hash` e `ticks` **nesse tick**, e registra a rodada com o `aoVivo.vencedor`. O vencedor de um W.O. é um fato de relógio, gravado como dado, igual a uma decisão produzida pela sala (`buildPadrao`/`pronto`). O verificador confere que o vencedor não é `-1` e imprime a linha como W.O. (por exemplo, "rodada 1: W.O. no tick 90, hash ✓"), **nunca** como divergência.

2. **`encerramento: 'fim' | 'interrompida'` por partida**, lido da `partida.fase` da sala na montagem do replay. Para `'interrompida'`, o verificador confere as rodadas fechadas e as decisões até a última delas, e sai com **rc=0** e a linha "partida interrompida antes do fim: N rodada(s) verificada(s)". Ele **não** compara placar final nem vencedor da partida, que não existem para uma partida interrompida.

3. **`pool`, o `injetado.pool` da sala, gravado no replay.** A reprodução em `tools/partida.ts` passa a usar o `pool` do arquivo, e não o `POOL` fixo de `tools/partida.ts:58` (`['golem','vex']`).

4. **`codigo: { commit: string | null; sujo: boolean | null }`, injetado por `src/server/main.ts`**, que lê o git **uma vez na subida** (`git rev-parse HEAD` e `git status --porcelain`), com `null` nos dois campos se o git não estiver disponível. `src/net/replay.ts` continua **puro**: recebe o carimbo como recebe o `hash` — não lê git, não roda subprocesso. Quando o commit gravado difere do commit de quem verifica, o verificador **avisa** e **não reprova**: linha "replay gravado em X, verificado em Y", e — numa divergência de hash — acrescenta "pode ser mudança de código, não de determinismo".

5. **O leitor aceita v1 e v2.** Um arquivo `bb.replay.v1` é lido como "todas as rodadas `natural`, partida `fim`, pool `['golem','vex']`, código desconhecido" — exatamente o que o leitor de hoje já supõe implicitamente. Qualquer formato que não seja v1 nem v2 continua recusado alto, na mesma disciplina de `e4.6`/AC 8 (`lerReplay` recusa versão desconhecida).

6. **AC de verificação, cenário discriminante de W-2 (medido na §7.3, com um driver puro, seed 12345, jogador 1 caindo no tick 60 da rodada 1):** uma Bo5 com W.O. numa rodada **que não é a rodada 0** dá `replay:check` **rc=0**, com a linha de W.O. e o hash conferido no tick do corte (`9aa1b500` no tick 90, na medição da fonte). **Contrafactual obrigatório:** o mesmo arquivo, com `aoVivo.ticks − 1` no lugar de `aoVivo.ticks`, tem de **reprovar** (é o tick 89 de W-2, que a medição confirmou divergente). Uma partida com `'anular'` no estouro da pausa dá `rc=0` com a linha "interrompida". Uma v1 gravada pela `e4.6` continua verificando sem mudança.

7. **Mutações de aceite que têm de FALHAR** (cada uma numa cópia descartável, nunca na árvore compartilhada):
   - classificar **toda** rodada como `'natural'` — volta o sintoma de `E46-ARC-001` (rc=1 com "hash reproduzido ≠ … / ticks ≠ …" numa rodada de W.O.);
   - para `'wo'`, reproduzir até o **fim natural** em vez de `aoVivo.ticks`;
   - aceitar o `aoVivo.vencedor` também para rodadas `'natural'` — deixaria de verificar o vencedor de uma rodada que terminou normalmente;
   - ignorar o `pool` do arquivo, usando sempre `['golem','vex']`.

8. **`npm run sim:check` mantém o golden hash idêntico.** A reprodução de rodada `'natural'` **não muda** — só ganha a classificação e o dado de causa. Esta story não toca `sim/`, `match/`, `shop/`, `chars/`, `bot/` nem `client/`.

9. **Absorção dos três achados LOW de teste do gate de `e4.6`** (`docs/qa/gates/e4.6-gravador-de-replay.yml`), recomendada pela própria §7.3 ("é o mesmo trabalho" — a guarda da v2 já precisa exercitar W.O. numa rodada que não é a 0):
   - **`E46-TST-001`.** Hoje os canários de poder discriminante da guarda ficam os dois na **rodada 0** e só exercitam o campo `hash` (a mutação `Q5`, `Math.min(1, …)` no laço de comparação por rodada, passa verde e aprova uma divergência real na rodada 2; `Q15`, contagem de rodadas desligada, também passa verde e aprova um arquivo com uma rodada a mais). Esta story acrescenta à guarda um canário na **última** rodada (um `dx` trocado num comando que mude o hash dela) e outro com **uma rodada a mais** no arquivo. Mutações de aceite: `Q5` e `Q15` têm de dar **rc=1**.
   - **`E46-TST-002`.** `gravarDecisoes` tem um ramo especial para a queda no draft (a sala zera o log e recria a partida, e a gravação recomeça); a guarda hoje só confere que esse ramo não lança, nunca verifica o replay gravado depois dele (`Q12`, o ramo nunca é tomado, passa verde). Esta story acrescenta: depois da queda no draft, a sala joga até o fim com uma escolha **diferente** da de antes da queda, e o replay resultante passa por `verificarReplay`. Mutação de aceite: `Q12` tem de dar **rc=1**.
   - **`E46-TST-003`.** O roteiro de partida real usado no gate de `e4.6` joga **uma** sala por servidor (`Q8`, uma `GravacaoDeReplay` só, compartilhada por todas as salas, passava verde e 8/8). Esta story acrescenta à guarda **duas Bo5 simultâneas** no mesmo servidor, com os dois replays verificados. Mutação de aceite: `Q8` (gravação compartilhada) tem de dar **FALHA**.

10. **Golden hash idêntico também depois da absorção do item 9** — as guardas novas são acréscimo ao bloco de replay do `sim:check`, sem mudar o texto de nenhuma linha existente que não seja a própria linha de replay.

11. **Sequência com a `e4.7` em `src/server/main.ts`.** As duas stories abrem esse arquivo por motivos diferentes: esta pelo carimbo `codigo` do AC 4; a `e4.7`, pelo AC 11 dela, por um comentário sobre o L-3 (`E44-DOC-001`) e pela frase de contrato de `main.ts:240-243` que a §6.3 exige. **Proposta desta story, decisão final do @po:** `e4.12` primeiro. O carimbo de commit/sujo é um bloco pequeno e isolado (leitura de git na subida, sem tocar o resto do arquivo), e rodar antes evita que a `e4.7` precise rebasear um comentário sobre um carimbo que ainda não existe. Quem começar por último confere `git status --short src/server/main.ts` vazio antes de editar, na mesma disciplina de sequência já usada para `src/tools/determinism.ts` em `e4.10` (Dev Notes, "Sequência de `src/tools/determinism.ts`").

12. **Nota de evidência para a `e4.7` (não é AC desta story — o @po decide se e como editar a story de `e4.7`).** Enquanto `bb.replay.v2` não estiver no commit do servidor usado na coleta, partidas com rodada de W.O. ou encerradas antes do fim ficam **fora da prova de replay** de P4.3: não contam como falha nem como acerto, são identificadas pelo mesmo registro de quedas do AC 8 da `e4.7` (v1.8.0), e o README da coleta registra o `replay:check` delas como "excluída: W.O. na rodada k" ou "interrompida". A prova de P4.3 exige ao menos uma Bo5 completa sem W.O. com `replay:check` rc=0; toda partida sem W.O. tem de dar rc=0, e uma que reprove é achado de determinismo. Com a v2 no servidor, a exclusão cai, e a partida com W.O. entra na prova pela linha de W.O.

13. **Restrição de desenho para a opção de R-02 "bot assume" (item já respondido na lista de R-02, `e4.4`/AC 14, registrado pela §7.3 — não é decisão pendente do usuário).** Se a política final de R-02 for "bot assume o assento vago", os comandos do bot **têm de entrar pela mesma porta dos humanos** — o `pendentes` da rodada, carimbados pela sala — para que a gravação os veja como vê os outros. Um bot que mexa no `World` por fora quebraria o replay de novo. Esta story **não implementa** "bot assume" (a política de R-02 continua provisória); só registra a restrição, para quem implementar essa opção não reabrir o formato.

14. **Escopo de arquivos, fechado.** Alterados, e só: `src/net/replay.ts`, `src/tools/replay-check.ts`, `src/tools/partida.ts` (a reprodução ganha "parar no tick T com vencedor dado" e "parar depois da rodada N", e passa a usar o `pool` recebido do arquivo), `src/server/main.ts` (o carimbo do AC 4) e `src/tools/determinism.ts` (a guarda dos ACs 6, 7, 9, 10). Nenhum arquivo novo esperado. **Proibidos, sem exceção:** `src/sim/`, `src/match/`, `src/shop/`, `src/chars/`, `src/bot/`, `src/client/`. A prova de escopo é `git show --stat <commit de implementação>`.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Architecture (versionamento de formato de dado — `bb.replay.v1` → `v2`)
**Secondary Type(s)**: Test infrastructure (absorção de `E46-TST-001/002/003` no bloco de replay do `sim:check`)
**Complexity**: **Medium**. Cinco arquivos já existentes e bem delimitados (nenhum novo), mas o modo de falha caro é sutil: um verificador que classifique a rodada errada, ou que reproduza W.O. até o fim natural, volta a confundir um W.O. legítimo com quebra de determinismo — exatamente o sintoma que esta story existe para eliminar (`E46-ARC-001`).

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa (quality gate — o cenário W-2 e o contrafactual `aoVivo.ticks − 1` são invariantes de conteúdo, e só teste com dado real, não de forma, prova)

**Supporting Agents**:
- @architect (consulta, só se a forma de `bb.replay.v2` (os campos `encerramento`, `pool`, `codigo`) precisar mudar — é mudança de formato de dado já decidido em §7.3, não refatoração livre)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Rodar antes de marcar a story como completa
- [ ] Pre-PR (@github-devops): Rodar antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Classificação de `encerramento` que trata toda rodada como `'natural'`, ou que confia em relógio de parede em vez da regra `w.over || w.tick >= tetoDeTicks` (AC 1, AC 7) — reabre `E46-ARC-001`
- Reprodução de rodada `'wo'` indo até o fim natural em vez de parar em `aoVivo.ticks` (AC 1, AC 7)
- `pool` do arquivo ignorado, com `tools/partida.ts` continuando a usar o `POOL` fixo (AC 3, AC 7)
- `src/net/replay.ts` deixando de ser puro — qualquer leitura de git, `fs` ou `process.env` **fora** de `server/main.ts` (AC 4)
- Comparação de commit tratada como reprovação em vez de aviso (AC 4)
- Leitor recusando `bb.replay.v1` depois desta story (AC 5)

**Secondary Focus**:
- Canários de `E46-TST-001` ficando só na rodada 0, sem cobrir a última rodada (AC 9)
- Ramo de recomeço da queda no draft (`E46-TST-002`) sem verificação de saída no `sim:check` (AC 9)
- Guarda continuando a testar só uma sala por servidor, sem o cenário de `E46-TST-003` (AC 9)
- `golden hash` mudando fora do bloco de replay do `sim:check` (AC 8, AC 10)

## Tasks / Subtasks

- [ ] Task 1 — `net/replay.ts`: forma do `bb.replay.v2` (AC: 1, 2, 3, 4, 5)
  - [ ] Campo `encerramento: 'natural' | 'wo'` por rodada, classificado em `gravarPasso` pela regra de W-2
  - [ ] Campo `encerramento: 'fim' | 'interrompida'` por partida, lido de `partida.fase` na montagem
  - [ ] Campo `pool` gravado a partir de `injetado.pool` da sala
  - [ ] Campo `codigo: { commit: string | null; sujo: boolean | null }`, recebido de fora (injetado), sem leitura de git dentro de `net/replay.ts`
  - [ ] Confirmar que `net/replay.ts` continua sem `fs`, `node:`, `ws`, `document`, `window`, `localStorage`, `Math.random`, `Date.now`, `performance.now` (mesma forma do grep de pureza de `e4.6`/AC 11)

- [ ] Task 2 — `server/main.ts`: carimbo de código (AC: 4, 11)
  - [ ] Ler `git rev-parse HEAD` e `git status --porcelain` uma vez na subida do servidor
  - [ ] `null` nos dois campos se o git não estiver disponível
  - [ ] Passar o carimbo para a montagem do replay, do mesmo jeito que o `hash` já é passado
  - [ ] Confirmar `git status --short src/server/main.ts` vazio antes de editar (Sequência, AC 11)

- [ ] Task 3 — `tools/partida.ts`: reprodução parametrizada (AC: 1, 2, 3, 6, 7)
  - [ ] "Parar no tick T com vencedor dado", para rodadas `'wo'`
  - [ ] "Parar depois da rodada N", para partidas `'interrompida'`
  - [ ] Usar o `pool` recebido em vez do `POOL` fixo (`tools/partida.ts:58`)

- [ ] Task 4 — `tools/replay-check.ts`: leitor e mensagens (AC: 1, 2, 4, 5, 6)
  - [ ] `lerReplay` aceita v1 (com os defaults do AC 5) e v2
  - [ ] Linha de W.O. no formato "rodada N: W.O. no tick T, hash ✓", nunca como divergência
  - [ ] Linha "partida interrompida antes do fim: N rodada(s) verificada(s))" para `'interrompida'`
  - [ ] Aviso de commit divergente, sem reprovar por isso sozinho
  - [ ] Confirmar que a reprodução continua delegando a `tools/partida.ts` (sem reimplementar, mesma disciplina do AC 3 de `e4.6`)

- [ ] Task 5 — `tools/determinism.ts`: guarda da v2 e absorção dos LOW (AC: 6, 7, 8, 9, 10)
  - [ ] Guarda W-2: Bo5 com W.O. numa rodada que não é a 0, rc=0, linha de W.O., hash no tick do corte
  - [ ] Contrafactual: mesmo arquivo com `aoVivo.ticks − 1`, rc≠0
  - [ ] Guarda de `'anular'`: rc=0 com "interrompida"
  - [ ] Guarda de compatibilidade: uma v1 gravada continua verificando
  - [ ] Quatro mutações do AC 7, cada uma numa cópia descartável, rc≠0
  - [ ] `E46-TST-001`: canário na última rodada + canário de rodada a mais; `Q5`/`Q15` rc=1
  - [ ] `E46-TST-002`: verificação do replay pós-recomeço na queda no draft; `Q12` rc=1
  - [ ] `E46-TST-003`: duas Bo5 simultâneas, dois replays verificados; `Q8` FALHA
  - [ ] `npm run sim:check` — golden hash idêntico, só a linha de replay muda de texto

- [ ] Task 6 — Forma e escopo (AC: 8, 10, 14)
  - [ ] `npm run check && npm run sim:check`
  - [ ] `npm run build` verde
  - [ ] `git show --stat <commit de implementação>` — só os cinco arquivos do AC 14

## Dev Notes

### Por que esta story existe (fonte: `architecture-e4.md` §7.3)

O gate de `e4.6` (`E46-ARC-001`, medium) mediu que `estourarPausa` → `encerrarRodada(presente)` fecha a rodada com o hash e os ticks do mundo **naquele instante** (relógio de parede), que não está na receita (seed + decisões + comandos). O `bb.replay.v1` não tem como dizer que a rodada acabou por W.O., e `reproduzirPartida` roda até o fim natural — o resultado é um `replay:check` rc=1 indistinguível de quebra de determinismo. O @architect mediu (driver puro, seed 12345, jogador 1 cai no tick 60 da rodada 1) que **a rodada de W.O. é reprodutível até o tick em que foi cortada, com hash idêntico** — a simulação não tem defeito; falta ao formato o dado de onde e por que a rodada parou.

### O que já está certo, e não muda (§7.3)

O W.O. das fases `builds` e `loja` **já se reproduz** — a sala o produz como decisão (`buildPadrao`, `pronto`, `sala.ts:671-676`), e decisões produzidas pela sala já entram no log gravado (desvio 6 de `e4.6`, conferido no gate). Só a **rodada cortada** não tem esse caminho. Esta story não muda `src/net/sala.ts`.

### Medições da fonte, citadas por inteiro (para o @dev não precisar reabrir os documentos)

| # | Cenário | `replay:check` v1 | Regra candidata da v2 | Reprodução até `aoVivo.ticks` |
|---|---|---|---|---|
| W-1 | Partida limpa, 7 rodadas | ok | 7/7 `natural` | — |
| W-2 | W.O. na rodada 1 (cai, pausa estoura, volta na rodada 2) | 5 problemas, "rodada 1: ticks reproduzido 5057 != 90" e placar invertido | rodada 1 é `wo`; as outras 6 são `natural` | hash igual no tick 90 (`9aa1b500`); contrafactual no tick 89 dá diferente |
| W-3 | `'anular'` no estouro | "a reprodução não chegou ao fim da partida" | a rodada fechada é `natural` | — (a partida para na fase `rodada`; a rodada em curso não entra no histórico) |

- A causa sai da condição de fim natural de `avancarUmTick` (`w.over || w.tick >= tetoDeTicks`, `sala.ts:600`): uma rodada fechada **sem** essa condição só pode ter fechado por `estourarPausa`. A gravação classifica sozinha, **sem mudar `sala.ts`**.
- `FORMATO_DO_REPLAY = 'bb.replay.v1'` está hoje em `src/net/replay.ts`; `lerReplay` (que recusa outra versão) está em `src/tools/replay-check.ts` (decisão de `e4.6`, "onde mora a conferência da versão").
- `net/replay.ts` de hoje: `criarGravacao`, `marcarAntesDoPasso`, `gravarPasso`, `montarReplay`, `serializarReplay` — todos puros, sem `fs`/`tools/`. `tools/replay-check.ts` de hoje: `lerReplay`, `verificarReplay` (delega a `reproduzirPartida`), CLI `npm run replay:check`.
- Custo de armazenamento medido (errata `E46-ARC-002`, §1.3/§7.1): **115,5 B por comando** (não os 26,3 B da tupla quantizada do fio, que é outra forma), **~6,3 kB por rodada e ~32 kB por Bo5 de 5 rodadas**. Um replay não é um vídeo, é uma receita; os comandos são ~99,5% do arquivo.
- `E46-ARC-002 (a)`: hoje o pool não é gravado — `reproduzirPartida` usa `POOL = ['golem','vex']` fixo, e o servidor usa `Object.keys(CHARS)` (hoje as mesmas duas chaves, mas sem garantia futura).

### O que esta story explicitamente NÃO faz

- Não decide a política de R-02 (duração da pausa, "bot assume" vs. outras opções) — continua com o usuário/@pm.
- Não implementa "bot assume". Só registra a restrição de desenho (AC 13) para quando essa opção for escolhida.
- Não muda `src/net/sala.ts`, `src/sim/`, `src/match/`, `src/shop/`, `src/chars/`, `src/bot/` nem `src/client/`.
- Não é pré-condição de coleta da `e4.7` (é pré-condição de **fechar a fase**, AC 12 explica a diferença).

### Testing

- `npm run check` — 0 erros. `npm run check && npm run sim:check` sempre em par.
- `npm run sim:check` — golden hash idêntico; só a linha de replay do bloco `sala pura` muda de texto.
- Cenário W-2 (Bo5 com W.O. numa rodada que não é a 0) e o contrafactual `aoVivo.ticks − 1`, na guarda.
- Cenário `'anular'` (partida interrompida).
- Compatibilidade: uma v1 gravada pela `e4.6` continua verificando sem mudança.
- Quatro mutações do AC 7 e as três absorções do AC 9 (`Q5`, `Q15`, `Q12`, `Q8`), cada uma numa cópia descartável, com o rc registrado no Dev Agent Record.
- `git show --stat <commit de implementação>` prova o escopo de cinco arquivos (AC 14).

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-09-22 | 1.0.0 | Story criada a partir de `docs/architecture-e4.md` §7.3 (decisão `bb.replay.v2`, `E46-ARC-001`/`E46-ARC-002`, gate de `e4.6`, commit `3e46e3d`) e dos achados `E46-TST-001`, `E46-TST-002` e `E46-TST-003` do gate de `e4.6` (`docs/qa/gates/e4.6-gravador-de-replay.yml`), que a §7.3 recomenda absorver aqui. Formato v2: `encerramento` por rodada (`natural`/`wo`) e por partida (`fim`/`interrompida`), `pool` gravado, carimbo `codigo` (commit + sujo) injetado por `server/main.ts`; leitor aceita v1 e v2. Não é pré-condição de coleta da `e4.7`; é pré-condição de fechar a fase (P4.3 do Anexo B). Compartilha `server/main.ts` com a `e4.7` (AC 11 dela) — proposta de ordem `e4.12` → `e4.7` registrada no AC 11, decisão final do @po. Restrição de desenho para "bot assume" (R-02) registrada no AC 13, sem implementá-la. | @sm |

## Dev Agent Record

*A preencher pelo @dev durante a implementação.*

## QA Results

*A preencher pelo @qa após a implementação.*
