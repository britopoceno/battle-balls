# Story e4.3: `net/sala.ts` — a máquina de estados da sala, pura, com relógio injetado

## Status

Ready for Review

> **Ready, com pré-condição de início (v1.4.0).** @dev: esta story começa **depois do commit de
> implementação de `e4.8`** (`src/net/codec.ts` com `parseDoCliente` precisa existir: a guarda do AC 16
> o chama) **e depois do commit de implementação de `debt.11`**, se ela estiver Ready ou em curso. As três
> editam `src/tools/determinism.ts` numa árvore compartilhada. Ver "Depende de" → "Sequência".
> *(v1.5.0: a story do gate de `e4.8`, sugestão `debt.12`, vem **depois** desta, não antes. Esta não
> espera por ela.)*
> **(v1.6.0) E também depois do commit de implementação da story da versão do fio** (sugestão `e4.9`,
> a criar pelo @sm; `architecture-e4.md` §11.6.1). Ela põe `versao`, `assento` e `snapshotHz` no tipo de
> `{t:'sala'}`, e esta story os preenche. Ordem de `determinism.ts`: `e4.8` → `debt.11` → `e4.9` →
> `e4.3` → `debt.12` → `e4.6`.

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run check", "npm run sim:check", "guarda nova: partida completa conduzida pela Sala reproduz o placar de tools/partida.ts", "guarda nova: flush de fim de rodada — último snap com over:true e roundEnd antes de rodadaFim; events concatenados = world.events (AC 10/11)", "guarda nova: decisão com d.jogador alheio recusada só ao remetente, e a Bo5 inteira passando por parseDoCliente(JSON.parse(JSON.stringify(msg))) (AC 16)", "guarda nova: todo {t:'sala'} sai por assento, com o segredo do próprio destinatário, versao === VERSAO_DO_FIO e snapshotHz da configuração, e é o primeiro envio a um assento recém-assentado (AC 11 e)", "grep -rnwE \"ws|document|window|localStorage|Math\\.random\" src/net/ → exit 1", "grep -rnE \"Date\\.now|performance\\.now\" src/net/ → exit 1", "git show --stat <commit de implementação desta story>"]
```

## Story

**Como** desenvolvedor executando o **passo 3** do plano de construção da Fase 4
(`docs/architecture-e4.md` §10),
**eu quero** criar `src/net/sala.ts` — a máquina de estados de uma sala (assentos, fases da partida,
rodada em curso, carimbo de input, cadência de snapshot e o relógio de RF-04), **pura**, recebendo `agora`
como parâmetro e devolvendo as mensagens de saída como **dado** —,
**para que** a partida 1v1 inteira possa ser exercitada dentro do `sim:check`, sem socket e sem navegador,
antes de existir servidor; e para que fique provado, por teste e não por argumento, que a sala **não
acrescenta regra de jogo**: o mesmo placar sai por ela e pelo caminho headless que já existe.

## Depende de

`e4.0` (protocolo: `DoCliente`, `DoServidor`, `ATRASO_ALVO_TICKS`, `SNAPSHOT_HZ`) e `e4.2` (Done), que
entregou em `src/net/snapshot.ts`:
- `estaticoDaRodada(world)`, para montar `{t:'rodadaInicio'}`;
- a fábrica `criarProdutorDeSnapshot(world): ProdutorDeSnapshot`, **uma por rodada**, com
  `observar(world)`, chamado **depois de cada `step`**, e `snapshot(world)`, chamado quando a cadência
  pedir e no tick de término (AC 10). O produtor acumula os `world.events` entre quadros e **lança** se um
  tick for pulado, observado duas vezes ou fotografado sem ter sido observado
  (`snapshot.ts:141-189`; contrato em `architecture-e4.md` §5.6).

*(Até a v1.2.0 esta linha citava uma função `snapshot()` solta, que não existe. Corrigido pelo E42-REQ-002
do gate de `e4.2`.)*

**`e4.8` (`docs/stories/e4.8.codec-do-fio.story.md`, Ready), em código e em ordem de commit.** A guarda
do AC 16 chama `parseDoCliente` de `src/net/codec.ts`, que é `e4.8`/AC 4. Sem o commit de implementação
de `e4.8`, a guarda não compila.

**Story da versão do fio (sugestão `e4.9`, a criar pelo @sm; v1.6.0), em código e em ordem de commit.**
`architecture-e4.md` §11.6.1 (E48-ARC-003, commit `bed4603`) decidiu que o `{t:'sala'}` ganha `versao:
typeof VERSAO_DO_FIO` e `assento: string` (o segredo do achado M-4), e o @po acrescentou `snapshotHz:
number` (decisão O-1, v1.6.0). Essa story muda `net/protocolo.ts` e a metade de saída de `net/codec.ts`,
que o AC 15 desta proíbe. Por isso vem antes: com os campos já no tipo, a sala os preenche ao nascer, e
esta story não toca `protocolo.ts`. Sem o commit de implementação dela, o `{t:'sala'}` que a sala produz
não compila. A spec para o @sm está no Change Log de `e4.8`, linha v1.5.0.

### Corte executado (v1.4.0)

O corte decidido na v1.3.0 (commit `1564ae4`) foi executado pelo @sm em `e4.8` (commit `6c2d580`) e
revalidado pelo @po junto com esta versão. Foram para `e4.8`, com o texto da v1.3.0:
- a parte do parser do antigo AC 16 → `e4.8`/AC 4;
- o antigo AC 17 (codec de saída, `architecture-e4.md` §5.5) → `e4.8`/AC 5;
- o antigo AC 18 (alcance da guarda do fio, E42-TST-003/004) → `e4.8`/AC 6;
- a parte `codec.ts` dos AC 3, 14 e 15 → `e4.8`/AC 3, 7 e 8;
- a Task 9 sem a subtarefa da sala, e as Tasks 10 e 11 → `e4.8`/Tasks 1, 2 e 3.

**Fica aqui:** a sala inteira, inclusive o flush de fim de rodada dos AC 10/11, e o AC 16 reduzido à
checagem **assento → `d.jogador`** em decisões, ao caso de guarda "→ sala" e ao "Caminho feliz", em que
a Bo5 do AC 11 passa por `parseDoCliente(JSON.parse(JSON.stringify(msg)))`. Os ACs 1–15 **mantêm a
numeração**, e por isso as referências de `e4.8` a "AC 4/6/7 de `e4.3`" continuam certas.

### Sequência (decisão do @po, v1.4.0)

`src/tools/determinism.ts` é disputado por `e4.8`, `debt.11`, esta story e `e4.6`, numa árvore de
trabalho só. A ordem é **`e4.8` → `debt.11` → `e4.3`**. `e4.6` depende desta e de `e4.4`, e por isso
já vem depois.
- **`e4.8` primeiro,** porque esta depende dela em código.
- **`debt.11` antes desta,** e não depois. Ela é pequena: um import, uma chamada, uma linha de seção e um
  `throw` em `determinism.ts` (`architecture-e4.md` §2.2/R1). Tem prazo que esta não tem: antes da
  primeira coleta humana usada como evidência (R10). Posta depois da maior story da fase, ela esperaria
  a Bo5 inteira. E a R9 do @architect já manda que ela comece depois do commit de implementação da story
  em curso: com `e4.8` fechada e esta ainda não iniciada, a janela é essa.
- **Esta story começa** depois do commit de implementação de `e4.8` e do de `debt.11`. Se `debt.11`
  ainda estiver em Draft quando `e4.8` fechar, esta **não** espera por ela: começa, e `debt.11` passa a
  começar depois do commit de implementação desta (R9). Nos dois casos, a pré-condição de início é
  `git status --short src/tools/determinism.ts` vazio.
- **Conferência de escopo:** `git show --stat <commit de implementação desta story>`, **nunca**
  `git diff --stat` na árvore.
- **Depois desta (v1.5.0): a story do gate de `e4.8`** (E48-TST-001/002 e a fixture do fio de
  E48-ARC-003; sugestão `debt.12`, a criar pelo @sm). Ela também edita `determinism.ts` e tem de ter
  commit de implementação antes de `e4.4` começar. Como `e4.4` já depende desta, pô-la depois não atrasa
  nada. A ordem do arquivo fica **`e4.8` → `debt.11` → `e4.3` → `debt.12` → `e4.6`**.
- **Regra geral do arquivo (v1.5.0):** toda story que edita `src/tools/determinism.ts` só começa com
  `git status --short src/tools/determinism.ts` vazio, ou seja, sem mudança não commitada de outra
  story. Vale para esta, para `debt.12` e para `e4.6`.
- **Antes desta (v1.6.0): a story da versão do fio** (sugestão `e4.9`, `architecture-e4.md` §11.6.1).
  Ela edita `determinism.ts` (a amostra `sala` da guarda `não-snap` e a guarda da versão), depende de
  `debt.11` e é pré-condição desta. **A ordem vigente do arquivo é `e4.8` → `debt.11` → `e4.9` → `e4.3`
  → `debt.12` → `e4.6`**, e substitui a das duas linhas acima. A regra geral do arquivo vale para
  `e4.9` também. Esta story começa depois do commit de implementação de `e4.9`, e o caso "se `debt.11`
  ainda estiver em Draft, esta não espera" deixou de existir: `debt.11` já tem commit de implementação
  (`971686b`), e `e4.9` depende dela.

**O segundo corte registrado (máquina de fases × rodada e cadência) NÃO é feito agora.** A guarda do
AC 11 conduz uma Bo5 inteira, com draft, builds, rodadas e placar. Separar as fases da rodada deixaria a
primeira metade sem a guarda que justifica a story. Ele continua como contingência, se o @dev encontrar
atrito.

**Não depende do servidor.** É deliberado, e é a razão de `net/` e `server/` serem pastas diferentes
(§2.3): se a sala precisasse de socket para ser testada, o `sim:check` — processo único, sem rede — não
poderia cobri-la.

## Acceptance Criteria

1. `npm run check` verde.
2. `npm run sim:check` verde, com **golden hash idêntico**. A sala orquestra o motor; não contém regra de
   jogo, e portanto não pode mover o hash.
3. `src/net/sala.ts` (novo) é **puro**, verificável por grep: sem `ws`, sem
   DOM, sem `Date.now()`, sem `performance.now()`, sem `Math.random()`, sem I/O. **O relógio entra como
   parâmetro** — é a mesma disciplina de `match/` (`architecture-e3.md` §2.6), agora um nível acima.
   **A forma do grep é fixa (E40-TST-003), e o critério é o exit code, não a saída vazia:**
   ```sh
   grep -rnwE "ws|document|window|localStorage|Math\.random" src/net/   # exige exit 1
   grep -rnE "Date\.now|performance\.now" src/net/                        # exige exit 1
   ```
   `-r` porque o alvo é diretório (sem ele o grep sai com 2 e stdout vazio passaria em vazio); `-w`
   porque `ws` solto casa com "draws", "news", "rows" (e obrigou `protocolo.ts:19-20` a contornar o nome
   num comentário). Exit 0 é
   achado; exit 2 é comando quebrado, e também reprova.
4. A forma pública é a de §3.2, e o retorno é **dado, não efeito**:
   ```ts
   export function passo(s: Sala, agora: number, entrada: EntradaDaSala[]): ResultadoDoPasso
   ```
   onde `EntradaDaSala` casa uma `DoCliente` com o **assento** que a enviou, e `ResultadoDoPasso` devolve
   `{ sala, envios: { assento: string; msg: DoServidor }[] }`. Quem escreve no socket é `server/` (`e4.4`).
   É a mesma forma de `Transicao` em `match/` (estado + eventos), pelo mesmo motivo: uma função que devolve
   o que fazer é testável; uma que faz não é.
5. **Assentos e o mapeamento que é a única linha nova de anti-cheat da fase** (§8.1):
   `ballIndex → ballId` é resolvido pela sala sabendo (i) qual `Jogador` ocupa o assento e (ii) qual lado
   `ladosDaRodada` deu a esse jogador **nesta** rodada. Uma conexão **não pode** mover bola do outro
   jogador, e o teste do AC 11 deve incluir uma tentativa explícita disso, esperando recusa.
   **Jogador ≠ lado** (`architecture-e3.md` §5.3) — filtrar por `team === 0` aqui é o bug que a alternância
   de lado esconde até a segunda rodada.
6. **O carimbo do tick acontece aqui** (§4.1): `{t:'cast'}` chega sem `tick`, e a sala produz o `Command`
   com `tick = tickDaRodada + ATRASO_ALVO_TICKS`. Nenhum caminho aceita `tick` vindo do cliente. Um
   `{t:'cast'}` de bola morta, fora da fase `rodada`, ou de assento vazio é **descartado com
   `{t:'erro'}`**, nunca aplicado pela metade.
7. **Decisão ilegal usa a validação que já existe.** `{t:'decisao'}` é repassada a `aplicar()`; se
   `Transicao.erro` vier preenchido, a sala emite `{t:'erro', motivo}` **para o assento que enviou** e o
   estado fica intacto. A sala não reimplementa nenhuma regra de `match/`.
8. **A projeção é por assento.** Depois de cada mudança de `EstadoPartida`, a sala emite
   `{t:'visao', v: visaoPara(estado, jogador)}` **individualmente para cada assento** — nunca um broadcast
   único. É isto que fecha a ressalva §13.6 de E3: a build não revelada do oponente não está no socket
   porque não está no objeto. Um broadcast idêntico para os dois assentos reprova a story.
9. **O relógio de RF-04 mora aqui agora** (§3.4). A sala segura o prazo de 30s da fase `builds` a partir do
   `agora` injetado; no estouro ela **produz a decisão** `{t:'buildPadrao', jogador}` e a passa a
   `aplicar()`. `match/` continua sem relógio e **não muda uma linha**. A sala também emite
   `{t:'prazo', terminaEmMs}` para que o cliente desenhe a contagem sem ter relógio de autoridade.

   O motivo de o dono mudar não é elegância: com relógio no cliente, **fechar o notebook estagna a partida
   do outro para sempre**.
10. **Cadência de snapshot** (§5.2): a sala emite `{t:'snap'}` a cada `60 / SNAPSHOT_HZ` ticks, com `seq`
    monotônico por rodada. `SNAPSHOT_HZ` vem de `net/protocolo.ts` e **não é redefinido aqui**.

    **Acréscimo (v1.6.0, decisão O-1 do @po sobre `architecture-e4.md` §11.6.1):** *"A taxa efetiva é um
    campo `snapshotHz` da configuração da sala, no mesmo lugar da política de desconexão do AC 13, com
    default `SNAPSHOT_HZ`. A cadência usa `60 / snapshotHz`, e todo `{t:'sala'}` leva esse mesmo valor
    em `snapshotHz`."* A constante continua sem ser redefinida: o campo é a porta pela qual o override de
    operação de `e4.7`/AC 4 chega à cadência, vindo de `server/main.ts`, sem editar `sala.ts`. Sem essa
    porta, o override do servidor não tinha como mudar a cadência, que mora aqui.

    **Acréscimo (v1.3.0, `architecture-e4.md` §5.5/§5.6; fecha E42-REQ-002 do gate de `e4.2`):**
    *"A sala cria **um** `ProdutorDeSnapshot` por rodada, chama `observar(world)` depois de **cada**
    `step` (inclusive os de um `passo()` que avance vários ticks) e emite, além da cadência, **um
    `{t:'snap'}` no tick em que a rodada termina** (`world.over` ou o teto de ticks), mesmo fora da
    cadência. Na lista de `envios` desse passo, o `snap` final vem **antes** de `{t:'rodadaFim'}`. Ver
    `architecture-e4.md` §5.6."*

    Sem o snap final, o golpe que mata, o `death` e o `roundEnd` ficam no acúmulo do produtor e nunca
    chegam ao fio. O produtor **não** lança nesse caso: esquecer o flush passa em silêncio, e só a guarda
    do AC 11 o pega.
11. **A guarda que justifica a story existir, no `sim:check`:** uma partida Bo5 **completa**, conduzida
    inteiramente por `passo()` — com `agora` sintético e mensagens sintéticas nos dois assentos —, produz
    **o mesmo placar, a mesma sequência de vencedores e os mesmos hashes de rodada** que o caminho headless
    já existente (`tools/partida.ts`). Se divergir, a sala acrescentou regra de jogo, e isso é o defeito que
    esta guarda existe para pegar.

    **Correção do @po na validação — este AC, como estava escrito, era inimplementável, e a razão é o
    próprio AC 6.** `tools/partida.ts` grava `Command[]` com o tick que o bot carimbou (`view.tick`, via
    `rodarRodada`/`reproduzirRodada`), e `step` só executa comando cujo `tick === world.tick`. Se a guarda
    reapresentar cada cast gravado no tick em que ele foi gravado, a sala vai **re-carimbá-lo 6 ticks
    adiante** (AC 6) e a rodada diverge — hashes diferentes, por construção, sem nenhum defeito na sala.

    A guarda deve, portanto, **submeter cada cast gravado `ATRASO_ALVO_TICKS` ticks ANTES** do tick
    gravado, de modo que o carimbo da sala o faça cair exatamente onde o arnês o executou. Isso não é
    contorno: é o que transforma o AC 11 em **teste do AC 6** — se o carimbo estiver errado por um tick,
    o hash denuncia.

    Dois detalhes que a implementação tem de tratar, e que o @qa deve conferir:
    - comando gravado com `tick < ATRASO_ALVO_TICKS` não tem como ser submetido cedo o bastante. A guarda
      deve **afirmar que não existe nenhum** na gravação usada (e escolher outra seed se existir), em vez
      de silenciosamente clampar — clampar reintroduz a divergência que este AC existe para detectar;
    - a submissão antecipada vale só para `{t:'cast'}`. `{t:'decisao'}` não tem tick e entra na fase, não
      no tick.

    A guarda deve cobrir também: (a) a tentativa de mover bola alheia do AC 5, esperando recusa; (b) uma
    decisão ilegal, esperando `{t:'erro'}` e estado intacto; (c) o estouro do prazo do AC 9, esperando
    `buildPadrao` no log de decisões; e (d) **o flush de fim de rodada** (acréscimo v1.3.0,
    `architecture-e4.md` §5.5): *"Em toda rodada da Bo5: o último `{t:'snap'}` tem `over: true`, contém o
    evento `roundEnd` e precede `{t:'rodadaFim'}` nos envios; e a concatenação dos `events` de todos os
    `snap` da rodada é idêntica, em ordem, à sequência de `world.events` que a rodada produziu."*

    O item (d) discrimina na rodada real: 4 das 5 rodadas da §1 terminam em tick ímpar (4 773, 4 699,
    4 363 e 5 249), fora da cadência de 30 Hz. Sem o flush, o `roundEnd` some em 4 de 5. É a mesma
    checagem de sequência que a guarda `fio snapshot` de `e4.2` já faz para o produtor
    (`determinism.ts`), agora aplicada à **sala**. A mutação Q6 do gate de `e4.2`, a guarda sem flush,
    perdeu 3 eventos por rodada.

    **(e) O `{t:'sala'}` (acréscimo v1.6.0, `architecture-e4.md` §11.6.1).** A sala preenche, em todo
    `{t:'sala'}`, `versao: VERSAO_DO_FIO` (importado de `net/protocolo.ts`, seta que o AC 14 já permite),
    `assento` com a chave do assento **de destino** (a mesma string de `envios[i].assento`; o `e4.4` usa
    o segredo de assento como essa chave) e `snapshotHz` da configuração (AC 10). A guarda confere, em
    toda a Bo5:
    - todo `{t:'sala'}` sai **por assento** e carrega o segredo do **próprio** destinatário, nunca o do
      outro. É a regra do AC 8 aplicada ao segredo: um broadcast de `{t:'sala'}` entregaria o segredo de
      um jogador ao outro, e com ele o outro reassentaria no lugar dele (`e4.4`/AC 7);
    - todo `{t:'sala'}` tem `versao === VERSAO_DO_FIO` e `snapshotHz` igual ao da configuração;
    - o **primeiro** envio a um assento recém-assentado é o `{t:'sala'}`, tanto na primeira entrada quanto
      num reassentamento. Assim a versão é conferida pelo cliente antes do primeiro `snap` ser
      decodificado. O assentamento chega à sala pela mesma porta pela qual chega a queda do AC 13
      (`e4.4`/AC 14), e a forma dessa entrada é do @dev.
12. **`registrarRodada` recebe `controle` correto.** A assinatura tem default `['bot','bot']`
    (`match/redutor.ts:437-440`), e aceitar o default numa sala 1v1 **contamina P3.1** — a mediana de
    duração com humano no controle é justamente o que o campo existe para isolar
    (`architecture-e3.md` §10.1). A sala passa o `controle` real de cada assento. A guarda do AC 11, que
    roda com dois "humanos" sintéticos, deve conferir que os eventos `rodadaFim` saem com
    `controle: ['humano','humano']`.
13. **R-02 (desconexão) é MECANISMO parametrizado, não decisão tomada.** `architecture-e4.md` §12/R-02
    devolveu ao @pm quatro opções, e **nenhuma foi escolhida**. Esta story implementa o mecanismo
    (detecção de assento vago, prazo, e as transições), com a política num campo de configuração da sala
    e o **default provisório** = *pausa com prazo curto, W.O. no estouro* (a recomendação de §12/R-02).

    A marcação é **estrutural**, no padrão já usado pelo projeto para número não decidido
    (`chars/tuning.ts` e `match/economia.ts`): a política mora num lugar só, com o aviso no topo dizendo
    que é provisória e que a decisão é do @pm. Escolher em silêncio um comportamento que cria incentivo
    perverso — a opção "anula a partida" premia derrubar a própria rede — é exatamente o que este AC
    impede.
14. **Setas** (§2.2): `net/sala.ts` importa de `sim/`, `match/`, `net/protocolo.ts` e `net/snapshot.ts`.
    **Não** importa `ws`, `client/`, `tools/`, `chars/` nem `bot/`. O roster entra **injetado**
    (`Record<string, CharDef>`), pela mesma porta de `createWorld` e `criarPartida`.
    `net/sala.ts` **não** importa `net/codec.ts`: a sala recebe `DoCliente` já parseado e devolve
    `DoServidor` como dado. Quem chama `parseDoCliente` e `codificarDoServidor` é a fronteira, `server/`
    (`e4.4`), e, no `sim:check`, a guarda do AC 16 em `tools/determinism.ts` (`e4.8`/AC 7 fixa a outra
    metade: o codec não importa a sala).
15. **Escopo de arquivos.** Criado: `src/net/sala.ts`. Alterado: `src/tools/determinism.ts` (só as
    guardas dos AC 11 e 16). Proibido: `src/net/protocolo.ts`, `src/net/codec.ts` (`e4.8`: se a Bo5 do
    AC 16 esbarrar num parser estrito ou frouxo demais, é achado contra `e4.8`, roteado ao @po, e não
    edição aqui), `src/net/snapshot.ts` e `src/net/projecao.ts` (`e4.2`, Done), `src/sim/`, `src/match/`,
    `src/shop/`, `src/bot/`, `src/chars/`, `src/client/`, `docs/`.

    **A prova de escopo é `git show --stat <commit de implementação desta story>`**, que tem de listar só
    `src/net/sala.ts` (novo) e `src/tools/determinism.ts`. Não `git diff --stat` na árvore: `e4.8`,
    `debt.11`, `e4.9`, `debt.12` e `e4.6` também editam `determinism.ts` (ver "Depende de" → "Sequência").
16. **Assento → `jogador` em decisões, e a Bo5 inteira pelo parser.** O parser `parseDoCliente` (forma
    do fio em runtime, E40-SEC-001) é de `e4.8`/AC 4, e esta story o **usa** na guarda, sem alterá-lo.
    Fica aqui a parte que depende da sala existir:

    **Assento → `jogador` em decisões (a metade do anti-cheat que o AC 5 não cobria).** `Decisao`
    carrega `jogador`, e `aplicar()` confia nele — `redutor.ts:224` só confere se é a vez de
    `d.jogador`. Sem checagem, o assento 0 drafta pelo jogador 1 na vez dele, com uma decisão de forma
    perfeita. A sala recusa com `{t:'erro'}` **para o assento que enviou**, estado intacto, toda
    `{t:'decisao'}` cujo `d.jogador` difira do `Jogador` desse assento. É o AC 5 aplicado a decisões.

    **Casos na guarda do `sim:check`** (em `determinism.ts`, junto do AC 11; os casos "→ `null`" e
    "→ parse OK, com descarte" são de `e4.8`/AC 4 e não se repetem aqui):
    - **→ sala:** decisão com `d.jogador` alheio devolve `{t:'erro'}` só ao remetente, estado intacto.
    - **Caminho feliz:** a Bo5 do AC 11 submete **toda** mensagem sintética por
      `parseDoCliente(JSON.parse(JSON.stringify(msg)))`. Um parser estrito demais quebra a guarda do
      placar; um frouxo demais quebra os negativos de `e4.8`. Se a Bo5 esbarrar no parser, é achado
      contra `e4.8` (AC 15).

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: State machine / Orchestration
**Secondary Type(s)**: Test infrastructure (a guarda do AC 11)
**Complexity**: **High** — é a peça com mais superfície da fase. Ela toca as quatro fases da partida, a
rodada, o relógio, os assentos e a cadência de rede, e o modo de falha caro é sutil: acrescentar regra de
jogo sem perceber. O AC 11 existe porque revisão manual não pega isso.

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa (quality gate — os AC 5, 8, 11 e 12 são invariantes que só teste pega)

**Supporting Agents**:
- @architect (consulta, se o AC 11 divergir: divergência aqui é sinal de que o corte da §2.2 está errado,
  não de que o teste está)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Rodar antes de marcar a story como completa
- [ ] Pre-PR (@github-devops): Rodar antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- `{t:'visao'}` emitida como broadcast idêntico em vez de projeção por assento (AC 8) — é o vazamento de
  segredo que a fase inteira existe para fechar
- `{t:'sala'}` emitido como broadcast, ou com o segredo de outro assento (AC 11 e) — entrega o assento do
  oponente a quem o receber
- Filtro por `team === 0` em vez de `ladosDaRodada` (AC 5) — o bug fica invisível na primeira rodada
- Qualquer relógio próprio dentro de `net/` (AC 3)
- `tick` aceito do cliente em qualquer caminho (AC 6)
- `{t:'decisao'}` aplicada sem conferir `d.jogador` contra o assento (AC 16)
- Guarda Bo5 que monta a `EntradaDaSala` direto do literal, sem passar por
  `parseDoCliente(JSON.parse(JSON.stringify(msg)))` (AC 16)
- `net/sala.ts` importando `net/codec.ts`, ou editando-o (AC 14/15; o codec é de `e4.8`)
- Rodada que termina fora da cadência sem o `{t:'snap'}` final, ou com o `snap` final depois de
  `{t:'rodadaFim'}` na lista de envios (AC 10)

**Secondary Focus**:
- `registrarRodada` chamado sem `controle` (AC 12) — o default silencioso contamina P3.1
- Política de desconexão escolhida em vez de parametrizada (AC 13)

## Tasks / Subtasks

- [x] Task 1 — Forma e pureza (AC: 3, 4, 14)
  - [x] `Sala`, `EntradaDaSala`, `ResultadoDoPasso`, `passo(s, agora, entrada)`
  - [x] Relógio injetado; roster injetado; grep de pureza limpo

- [x] Task 2 — Assentos e autoridade (AC: 5, 6, 7)
  - [x] Mapeamento assento → `Jogador` → lado da rodada (`ladosDaRodada`)
  - [x] `ballIndex → ballId` com recusa de bola alheia
  - [x] Carimbo `tick = tickDaRodada + ATRASO_ALVO_TICKS`
  - [x] `{t:'decisao'}` → `aplicar()`; `Transicao.erro` → `{t:'erro'}` só para quem enviou

- [x] Task 3 — Projeção e cadência (AC: 8, 10, 11 e)
  - [x] `{t:'visao'}` por assento, via `visaoPara(estado, jogador)`
  - [x] `{t:'sala'}` por assento, com `versao: VERSAO_DO_FIO`, `assento` = chave do destinatário e
        `snapshotHz` da configuração, como primeiro envio de todo assentamento (AC 11 e, v1.6.0)
  - [x] `{t:'rodadaInicio'}` com `estaticoDaRodada`; `{t:'snap'}` a cada `60/snapshotHz` ticks, com `seq`;
        `snapshotHz` na configuração da sala, default `SNAPSHOT_HZ` (AC 10, v1.6.0)
  - [x] Um `criarProdutorDeSnapshot(world)` por rodada; `observar(world)` depois de **cada** `step`,
        inclusive num `passo()` que avance vários ticks
  - [x] `{t:'snap'}` final no tick de término (`world.over` ou teto), fora da cadência se preciso,
        **antes** de `{t:'rodadaFim'}` nos envios

- [x] Task 4 — Relógio de RF-04 (AC: 9)
  - [x] Prazo de 30s da fase `builds` a partir de `agora`
  - [x] Estouro produz `{t:'buildPadrao'}` como decisão; `match/` intacto
  - [x] `{t:'prazo', terminaEmMs}` emitido para os assentos

- [x] Task 5 — Fim de rodada (AC: 12)
  - [x] `registrarRodada(estado, resultado, controleReal)` — **nunca** o default
  - [x] `{t:'rodadaFim'}` para os assentos

- [x] Task 6 — R-02 parametrizada (AC: 13)
  - [x] Campo de política de desconexão, num lugar só, com aviso de provisório no padrão de `tuning.ts`
  - [x] Default: pausa com prazo curto → W.O. no estouro
  - [x] **Não** escolher por conta própria; a decisão é do @pm

- [x] Task 7 — A guarda (AC: 2, 11)
  - [x] Partida Bo5 completa por `passo()`, com `agora` e mensagens sintéticas
  - [x] **Submeter cada `{t:'cast'}` gravado `ATRASO_ALVO_TICKS` ticks antes do tick gravado** (correção
        do @po — sem isso o carimbo do AC 6 desloca a rodada e o AC 11 é inimplementável)
  - [x] Afirmar que a gravação usada não tem comando com `tick < ATRASO_ALVO_TICKS`; se tiver, trocar de
        seed — **não clampar**
  - [x] Conferir placar, sequência de vencedores e hashes contra `tools/partida.ts`
  - [x] Cobrir bola alheia, decisão ilegal e estouro de prazo
  - [x] Flush (AC 11 d): último `snap` com `over: true` e `roundEnd`, antes de `rodadaFim`; `events`
        concatenados idênticos, em ordem, a `world.events` da rodada
  - [x] `{t:'sala'}` (AC 11 e): por assento, com o segredo do próprio destinatário, `versao ===
        VERSAO_DO_FIO`, `snapshotHz` da configuração, e primeiro envio a todo assento recém-assentado
  - [x] `sim:check` verde com golden hash idêntico

- [x] Task 8 — Escopo (AC: 14, 15)
  - [x] Antes de começar: commit de implementação de `e4.8` presente (`parseDoCliente` exportado de
        `src/net/codec.ts`), commit de implementação de `e4.9` presente (`VERSAO_DO_FIO` exportado de
        `src/net/protocolo.ts`, e `{t:'sala'}` com `versao`, `assento` e `snapshotHz`) e
        `git status --short src/tools/determinism.ts` vazio
  - [x] `git show --stat <commit de implementação>` — só `src/net/sala.ts` (novo) e
        `src/tools/determinism.ts` (guardas dos AC 11 e 16); `protocolo.ts`, `codec.ts`, `snapshot.ts` e
        `projecao.ts` intactos
  - [x] `net/sala.ts` sem import de `net/codec.ts`
  - [x] Grep de pureza na forma do AC 3 — exit 1 nos dois comandos

- [x] Task 9 — Assento → `d.jogador` e a Bo5 pelo parser (AC: 16)
  - [x] Na sala: `{t:'decisao'}` com `d.jogador` ≠ jogador do assento → `{t:'erro'}` só ao remetente,
        estado intacto
  - [x] Guarda "→ sala": decisão com `d.jogador` alheio recusada, estado intacto
  - [x] Guarda "Caminho feliz": toda mensagem sintética da Bo5 do AC 11 passa por
        `parseDoCliente(JSON.parse(JSON.stringify(msg)))` (de `e4.8`) antes de virar `EntradaDaSala`

## Dev Notes

### Por que a sala é pura, e o que isso paga (fonte: §2.3, §3.2)

O precedente do projeto é consistente e já rendeu três vezes: `sim/` puro deu o arnês; `bot/` puro deu o
teste de replay; `match/` puro deu a partida headless dentro do `sim:check` (`e3.2`). A sala pura dá a
**partida 1v1 inteira testável sem rede** — que é a única forma de saber que o servidor está errado antes
de ter dois celulares na mão.

### A sala não acrescenta regra (fonte: §3.3)

Nada em `match/` muda nesta fase; muda **quem chama**. A tabela de §3.3 é o roteiro:

| hoje (`client/main.ts`) | na sala |
|---|---|
| `criarPartida({seed, pool})` | idem, com a seed vinda do servidor |
| `aplicar(partida, decisao)` | idem, com a decisão recebida do assento |
| `visaoPara(partida, HUMANO)` | idem, **duas vezes** — uma por assento |
| `setupDaRodada` + `createWorld` + laço | idem |
| `registrarRodada(...)` | idem, com `controle` real (AC 12) |

O AC 11 é a prova de que essa tabela foi seguida à risca: se a sala tivesse acrescentado qualquer regra, o
placar divergiria do caminho headless.

### `visaoPara` era para uma tela local e virou a peça de rede mais importante da fase

`architecture-e4.md` §3.3 registra a ironia útil: a projeção que escondia a build do **bot** em `e3.4` é
exatamente a projeção que impede a build do oponente **humano** de existir no socket. Por isso o AC 8 é
categórico contra broadcast: o segredo é uma propriedade de *quem recebe o quê*, não de *o que o cliente
decide desenhar*.

### O relógio, e por que ele muda de dono (fonte: §3.4)

`architecture-e3.md` §2.6 pôs o relógio fora de `match/` e o deu ao cliente — decisão certa para um jogo
local. Em rede ela vira buraco: quem não deixa o timer estourar estagna a partida alheia. O relógio muda de
dono; a **natureza** dele não muda. Continua produzindo `{t:'buildPadrao'}` como decisão explícita no log,
e o replay continua reproduzindo a partida sem saber quanto tempo o humano levou.

### R-02 continua aberta, e esta story não a fecha

As quatro opções de `architecture-e4.md` §12/R-02, com o custo de cada uma, seguem na mesa do @pm. O que
esta story entrega é o **mecanismo** e um default provisório marcado. A quarta opção ("anula a partida")
está registrada como incentivo perverso — quem está perdendo ganha motivo para derrubar a própria rede — e
é o exemplo de por que escolher em silêncio seria pior que parametrizar.

### O que esta story explicitamente NÃO faz

- **Não abre socket.** `ws`, assentos reais e laço de relógio de parede são `e4.4` — que está **bloqueada**
  em R-05 (ratificação da primeira dependência de runtime do projeto).
- **Não interpola.** Buffer e interpolação são do cliente (`e4.5`).
- **Não grava replay.** É `e4.6`.
- **Não decide `SNAPSHOT_HZ`** (R-03) **nem a política de desconexão** (R-02).

### Testing

- `npm run check` — 0 erros.
- `npm run sim:check` — golden hash idêntico **e** a guarda nova verde: partida Bo5 completa pela sala bate
  placar, vencedores e hashes com `tools/partida.ts`.
- Casos negativos na guarda: bola alheia recusada; decisão ilegal devolve `{t:'erro'}` com estado intacto;
  estouro de prazo produz `buildPadrao`.
- Conferir que os eventos `rodadaFim` da guarda saem com `controle: ['humano','humano']`.
- Casos do AC 16 na guarda: decisão com `d.jogador` alheio é recusada; a Bo5 inteira passa pelo parser
  de `e4.8`. (Os negativos de forma do parser são de `e4.8`/AC 4.)
- Flush (AC 11 d): nas rodadas que terminam fora da cadência, o `roundEnd` chega no último `snap`,
  antes de `rodadaFim`.
- `{t:'sala'}` (AC 11 e): nenhum sai em broadcast nem com o segredo do outro assento; todos têm
  `versao === VERSAO_DO_FIO` e o `snapshotHz` da configuração; é o primeiro envio depois de todo
  assentamento, inclusive no reassentamento.
- Grep de pureza na forma do AC 3 (exit 1 nos dois comandos) e grep de setas em `src/net/sala.ts`
  (sem `net/codec.ts`).
- `git show --stat <commit de implementação>` prova o escopo (não `git diff --stat` na árvore, AC 15).

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (1M context) — Dex (@dev), modo YOLO.

### Debug Log References

- **Pré-condições (Task 8):** `git status --short src/tools/determinism.ts` vazio no início (HEAD `c4b22db`,
  depois `1fa6301` com `debt.13` commitada durante a leitura). `parseDoCliente` exportado de
  `src/net/codec.ts:126` (`e4.8`, `910add8`); `VERSAO_DO_FIO` e o `{t:'sala'}` com `versao`/`assento`/`snapshotHz`
  em `src/net/protocolo.ts` (`e4.9`, `6f2f56c`).
- **Commit de implementação:** `99f4ee3` (pai `a5d7858`, commit do @po feito em paralelo na mesma árvore).
  `git show --stat 99f4ee3`:
  ```
   src/net/sala.ts          | 664 +++++++++++++++++++++++++++++++++++++++++++++++
   src/tools/determinism.ts | 661 ++++++++++++++++++++++++++++++++++++++++++++++
   2 files changed, 1325 insertions(+)
  ```
  Só inserções em `determinism.ts` (1558 → 2219 linhas): nenhuma linha existente mudou. Esta story foi
  commitada à parte, porque o AC 15 proíbe `docs/` no commit de implementação.
- `npm run check` → rc=0. `npm run build` → rc=0. `npm run lint` não existe neste projeto (`package.json` só
  tem `check`); o `tsc --noEmit` com `strict` e `noUnusedLocals` é o gate estático.
- `npm run sim:check` → rc=0, `golden hash    ✓ ok — 6 seeds batem o baseline` (linha 7, idêntica). O `diff`
  da saída inteira contra a de antes (capturada em `1fa6301`, depois de `debt.13`) é **só inserção**:
  `63a64,70`, 7 linhas acrescentadas, 0 removidas, depois da seção da telemetria:
  ```

  sala pura      ✓ ok — net/sala.ts: a Bo5 inteira por passo() reproduz tools/partida.ts; a sala não acrescenta regra de jogo (e4.3)
    bo5          ✓ matchSeed 1 · 3 rodada(s) · placar 0-3 · venc/rodada [1 1 1] · hash/ticks/lado iguais ao arnês em 3/3 · 141 cast(s) submetidos em T−6 e carimbados em T (0 com tick < 6) · 2 decisão(ões) ilegal(is) → {t:'erro'} ao remetente, estado intacto · buildPadrao pelo prazo de RF-04 · controle [humano,humano]
    flush        ✓ 3/3 rodadas: último snap com over:true e roundEnd antes de rodadaFim · events de 5683 snaps = world.events (1088 eventos, em ordem) · 2 rodada(s) terminam fora da cadência de 2 ticks
    assentos     ✓ 21 {t:'sala'} por assento, cada um com o segredo e o jogador do próprio destinatário, v1 e o snapshotHz da configuração · primeiro envio em 12 assentamento(s), com reassentamento no meio da rodada · 76 {t:'visao'} = visaoPara(estado, jogador do assento)
    autoridade   ✓ decisão com d.jogador alheio recusada só ao remetente, estado intacto (AC 16) · cast de bola morta descartado · 176 mensagens pela fronteira (parseDoCliente(JSON.parse(JSON.stringify(msg))), AC 16)
    negativos    ✓ assento vazio, sala cheia, decisão com a sala aguardando e cast fora da rodada → {t:'erro'} · bola alheia: ballId contrabandeado some no parser e a sala move a própria; ballIndex 2 recusado · 20 Hz: 10 snaps a cada 3 ticks · R-02 W.O.: rodada → vitória do presente, loja → pronto do ausente · anular → encerrada · draft → aguardando (§6)
  ```
- **Pureza (AC 3), na forma fixa:** `grep -rnwE "ws|document|window|localStorage|Math\.random" src/net/` →
  exit 1; `grep -rnE "Date\.now|performance\.now" src/net/` → exit 1.
- **Setas (AC 14):** `grep -n "codec" src/net/sala.ts` → exit 1 (nem em comentário);
  `grep -nE "from '\.\./(client|tools|chars|bot)/" src/net/sala.ts` → exit 1. Imports de `sala.ts`:
  `sim/world.ts`, `sim/types.ts` (tipos), `match/index.ts`, `./protocolo.ts`, `./snapshot.ts`.
  `grep -rn "from '\.\./client/" src/tools/` continua devolvendo só os dois arquivos da lista fechada da §2.2.
- **CodeRabbit:** indisponível nesta máquina (o gate por WSL não roda aqui). Os dois checkboxes de Quality
  Gate Tasks ficam sem marcar. Cobertura substituta: `npm run check`, `npm run sim:check` e a bateria abaixo.
- **Bateria negativa (mutações em `src/net/sala.ts`).** Um script descartável em Node aplica cada troca de
  texto, confere que o alvo casou exatamente 1 vez, roda `npm run sim:check` e restaura o arquivo no
  `finally`. Restauração conferida por sha256: idêntico. **15/15 reprovam (rc=1):**

  | Mutação | rc | O que a guarda acusou (primeira linha) |
  |---|---|---|
  | M1 carimbo `+1` tick | 1 | `rodada 0 tick 169: a sala carimbou [{"tick":176,…}], o arnês executou [{"tick":175,…}]` |
  | M1b carimbo `−1` tick | 1 | `rodada 0 tick 169: a sala carimbou [{"tick":174,…}]` |
  | M2 sem `snap` final | 1 | `rodada 0: o último snap não tem over:true com roundEnd` e `361 evento(s) nos snaps != 364 em world.events` |
  | M2b `snap` final depois do `rodadaFim` | 1 | `snap final (índice 168) não precede rodadaFim (índice 166) nos envios` |
  | M3 um `{t:'sala'}` só, para os dois, com o segredo do assento 0 (a mutação pedida pelo gate de `e4.9`) | 1 | `o MESMO objeto {t:'sala'} foi endereçado a mais de um assento — broadcast` e `endereçado a 'segredo-do-assento-1' carrega o segredo 'segredo-do-assento-0'` |
  | M3b o outro assento recebe o `{t:'sala'}` do recém-assentado | 1 | `endereçado a 'segredo-do-assento-0' carrega o segredo 'segredo-do-assento-1'` |
  | M4 decisão com `d.jogador` alheio aceita | 1 | `decisão com d.jogador alheio chegou a aplicar() / mudou o estado (AC 16)` |
  | M5 cadência lendo `SNAPSHOT_HZ` em vez de `config.snapshotHz` | 1 | `a 20 Hz a sala emitiu snaps nos ticks [2,4,…,30] …, esperado a cada 3 ticks` |
  | **M6 `jogador` trocado entre os assentos** (adendo do @architect) | 1 | `{t:'sala'} endereçado ao assento do jogador 0 diz jogador 1 (AC 11 e, §11.6.1)` |
  | M7 `lado = jogador` (o bug de `team === 0`) | 1 | `rodada 1 tick 269: a sala carimbou [{…"ballId":1…}]` — verde na rodada 0, vermelho na 1 |
  | M8 `registrarRodada` com o `controle` default | 1 | `rodadaFim da rodada 0 com controle [bot,bot] (AC 12)` |
  | M9 visão do jogador 0 para os dois assentos | 1 | `{t:'visao'} endereçado a 'segredo-do-assento-1' não é visaoPara(estado, jogador 1)` |
  | M10 prazo de RF-04 com `>` em vez de `>=` | 1 | `no estouro do prazo a sala produziu [], esperado [{"t":"buildPadrao","jogador":1}]` |
  | M11 reassentamento com `{t:'visao'}` antes do `{t:'sala'}` | 1 | `primeiro envio ao assento recém-assentado 'segredo-do-assento-1' foi {t:'visao'}` |
  | M12 pausa de R-02 sem congelar o mundo | 1 | `o mundo andou 300 tick(s) durante a pausa (AC 13)` |

### Completion Notes List

- **Adendo do @architect implementado (`architecture-e4.md` §11.6.1, commit `6c6b9a0`, pedido do coordenador
  durante a implementação):** no laço do AC 11 (e), cada `{t:'sala'}` confere também o `jogador` **do próprio
  assento de destino** (`msg.jogador === s.assentos.indexOf(envio.assento)`). É o bug de servidor que o
  decoder não vê. Mutação M6 (troca de `jogador` entre os assentos) → rc=1. O texto do AC não foi editado:
  o delta de AC é do @po.
- **Seed da Bo5:** `matchSeed 1`, a primeira candidata (`[1, 2, 12345]`, as `MATCH_SEEDS` de `partida.ts`). Nenhum
  comando gravado com `tick < 6`. A guarda percorre as candidatas e **pula** qualquer uma que tenha, com o
  motivo impresso. Não clampa. 2 das 3 rodadas terminam fora da cadência de 2 ticks, então o item (d)
  discrimina nesta gravação (M2 perde 3 eventos na rodada 0).
- **A guarda confere o carimbo campo a campo, além do hash.** Cada lote de casts vai num passo com `agora`
  parado. Os `Command` que a sala acrescentou a `pendentes` são comparados com os que o arnês executou
  (`tick`, `ballId`, `slot`, `dx`, `dy`, `mag`). Por isso M1 e M7 acusam o tick e a bola exatos, não só um hash
  diferente no fim.
- **[AUTO-DECISION] Forma da entrada de assentamento/queda (AC 11 e: "a forma dessa entrada é do @dev") →**
  `EntradaDaSala = { assento; msg: DoCliente } | { assento; conexao: 'assentou' | 'caiu' }` (motivo: a
  fronteira conhece o segredo e decide quem assenta, `e4.4`/AC 7. A sala só registra e responde, e as duas
  coisas chegam pela mesma porta, como o AC pede). Um `{t:'entrar'}` que chegue como `msg` recebe
  `{t:'erro'}` (é da fronteira). `{t:'pong'}` é ignorado (RTT é do servidor, `e4.4`/AC 9).
- **[AUTO-DECISION] O `hash` da rodada é injetado (`OpcoesDaSala.hashDoMundo`) →** (motivo: a definição única é
  `hash()` de `tools/harness.ts`, e o AC 14 proíbe `tools/`. Com o mesmo `hash` injetado, o AC 11 compara
  hashes iguais, não parecidos). **O teto de ticks** (`tetoDeTicks = 180 * TICK_HZ`) é repetido na configuração
  pelo mesmo motivo. A guarda tem uma tripwire `CONFIG_PADRAO_DA_SALA.tetoDeTicks === MAX_ROUND_TICKS`.
- **[AUTO-DECISION] Quem transforma `agora` em ticks →** a sala: `devidos = floor((agora − inícioDaRodada) · 60 /
  1000)`, e um `passo()` avança quantos ticks faltarem, observando cada um (motivo: o AC 10 prevê "um `passo()`
  que avance vários ticks", e assim o servidor só chama `passo` com o relógio dele. Um atraso de agendamento
  vira recuperação, não câmera lenta, §3.2). A guarda da Bo5 exercita isso: cada avanço é um salto até a
  próxima submissão, de dezenas a centenas de ticks.
- **[AUTO-DECISION] `{t:'prazo'}.terminaEmMs` é o RESTANTE em ms, não um instante →** (motivo: o relógio do
  cliente não é o do servidor. Um instante absoluto obrigaria sincronizar relógio só para desenhar a contagem.
  O nome do campo já lê "termina em"). A entrada na fase `builds` manda `terminaEmMs: 30000` aos dois assentos,
  e o reassentamento em `builds` manda o que resta.
- **[AUTO-DECISION] R-02 (AC 13), semântica do default provisório →** documentada no aviso de
  `POLITICA_DE_DESCONEXAO_PROVISORIA`, no topo de `sala.ts`. A pausa congela só a simulação da rodada.
  No estouro, `'wo'` faz o ausente perder o passo em curso: a rodada (registrada com o presente como
  vencedor), a `builds` (`buildPadrao`, a decisão que o próprio RF-04 já produz) ou a `loja` (`pronto`).
  Se continuar ausente, o prazo recomeça. A partida termina pelas regras de `match/`. `'anular'` existe como
  valor possível, com o aviso de incentivo perverso de §12/R-02. "Bot assume" **não é representável** sem
  injetar `bot/`, que o AC 14 proíbe. `prazoMs: 20_000` é ponto de partida, não medição. Desconexão com a sala
  `aguardando`, ou `jogando` ainda no draft, segue a seta de §6 ("desconexão antes do início") e não a
  política: libera o assento, devolve a sala a `aguardando` e recria a partida.
- **[AUTO-DECISION] Envios só a assentos conectados →** (motivo: assento vago não tem socket. O
  reassentamento ressincroniza com `{t:'sala'}` + `{t:'visao'}` + (`{t:'prazo'}`) + (`{t:'rodadaInicio'}`), e o
  próximo `snap` sai pela cadência, §6/`e4.4`/AC 8).
- **[AUTO-DECISION] O `buildPadrao` do roteiro não é enviado pela guarda →** o relógio da sala o produz no
  estouro. A guarda confere que 1 ms antes do prazo nada acontece, e que no prazo a sala produz exatamente
  `[{t:'buildPadrao', jogador: 1}]`. Como o `pronto` do jogador 0 vem antes do estouro, o log da sala tem
  `[…build, build, pronto j0, buildPadrao j1]`, contra `[…build, build, buildPadrao j1, pronto j0]` no arnês. O
  estado final é o mesmo (RF-04: a ordem não muda o resultado), e os hashes das três rodadas provam isso. A
  guarda compara o log da sala com "o que ela enviou + o que o prazo produziu", o que pega decisão inventada
  ou perdida.
- **Estado da `Sala`:** `passo()` devolve um objeto novo no nível de cima, mas `RodadaEmCurso` (o `World`, o
  produtor, os pendentes) é mutável, porque `step()` muta o mundo no lugar. Está documentado no cabeçalho: a
  `Sala` de entrada é consumida.
- **Self-critique (checkpoints 5.5 e 6.5), achados que ficam:**
  - A guarda não consegue provar, por mutação, que ela mesma passa as mensagens pelo parser. Se
    `pelaFronteira` fosse trocada por um cast direto, a Bo5 continuaria verde, porque a sala ignora campos
    extras. A garantia é a leitura do código: toda `EntradaDaSala` com `msg` da guarda da Bo5 sai de
    `pelaFronteira`, e o `ballIndex: 2` por fora do parser está nos negativos, isolado e rotulado.
  - `'anular'` é exercitado só com `prazoMs: 0`, e o W.O. de `builds` não tem caso próprio. O caminho é o
    mesmo `decidir(buildPadrao)` do relógio, que a Bo5 cobre.
- **Itens em aberto (para roteamento, nenhum bloqueia esta story):**
  1. **`e4.4` e o `hash`.** A sala recebe `hashDoMundo` injetado, e `e4.4`/AC 12 proíbe `server/ → tools/`.
     Hoje o servidor não tem de onde tirar `hash()` sem uma seta nova (`server/ → tools/harness.ts`) ou sem
     mover o `hash`. Decisão do @architect.
  2. **A pausa de R-02 não aparece no fio.** `{t:'sala'}.estado` só tem `aguardando | jogando | encerrada`, e o
     vocabulário é fechado. O jogador presente vê os `snap` pararem, sem motivo na tela. Uma variante ou um
     estado novo é mudança de protocolo (`VERSAO_DO_FIO`), para quando o @pm decidir R-02.
  3. **`EventoPartida` não tem variante no fio.** A §9 diz que ele "passa a chegar pelo fio", mas `DoServidor`
     não o carrega. A sala guarda a telemetria em `Sala.eventos` (é ali que a guarda confere o `controle` do
     AC 12), e a entrega ao cliente fica para quem abrir o protocolo.
  4. **R-02 continua aberta.** O default é a recomendação de §12/R-02, marcado como provisório. `prazoMs` não
     foi medido.

### File List

| Arquivo | Ação | Commit |
|---|---|---|
| `src/net/sala.ts` | criado — a sala pura (AC 3–10, 12, 13, 16) | `99f4ee3` |
| `src/tools/determinism.ts` | alterado, só inserção — guarda "sala pura" (AC 11 e 16), com a checagem de `jogador` por assento do adendo do @architect | `99f4ee3` |
| `docs/stories/e4.3.sala-pura.story.md` | alterado — Status, checkboxes, Dev Agent Record, File List, Change Log | commit de story, separado |

## QA Results

_(a preencher pelo @qa)_

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-08-19 | 1.0.0 | Story criada a partir do passo 3 de `docs/architecture-e4.md` §10 | @sm |
| 2026-08-19 | 1.1.0 | **Validação @po: GO 9/10** (7/10 antes da correção). **Status: Draft → Ready.** Achado bloqueante: **o AC 11 era inimplementável como escrito, e quem o tornava impossível era o AC 6 da própria story.** `tools/partida.ts` grava `Command[]` com o tick carimbado pelo bot (`view.tick`), e `sim/step` só executa comando cujo `tick === world.tick`; se a guarda reapresentasse cada cast no tick gravado, a sala o re-carimbaria 6 ticks adiante e a rodada divergiria por construção — hash diferente sem defeito nenhum na sala. Corrigido: a guarda submete cada `{t:'cast'}` **`ATRASO_ALVO_TICKS` ticks antes** do tick gravado, o que converte o AC 11 em teste do AC 6 (carimbo errado por um tick → hash denuncia). Registrados os dois casos de borda: comando gravado com `tick < 6` deve **falhar a seleção de seed**, nunca ser clampado; e a antecipação vale só para casts, não para decisões. Ponto não somado (9/10, não 10/10): a estimativa de complexidade "High" é honesta, mas a story continua sendo a maior da fase e o @po registra que ela é a candidata natural a ser quebrada em duas se o @dev encontrar atrito — o corte sugerido é *máquina de fases* × *rodada e cadência*. | @po |
| 2026-09-21 | 1.2.0 | **E40-SEC-001 ganha dono aqui, e E40-TST-003 é aplicado (gate `docs/qa/gates/e4.0-net-protocolo.yml`). Status permanece Ready.** Mesmo precedente de QA-D8-01 anexada à `e4.2` (`cf9402b`). **Por que aqui e não na `e4.4`:** a `e4.4` está Draft e bloqueada em R-05, o AC 13 dela **proíbe** `src/net/`, e o gate dela é manual (duas abas). Aqui o parser é puro, cabe no `sim:check`, e a guarda do AC 11 já exercita todas as mensagens do fio. Conferido na fonte antes de escrever: `aimFrom` em `sim/world.ts:445-451` (`JSON.parse('1e999')` → `Infinity` → `dx = NaN`, reproduzido em Node), o `else` de ult em `:459`, e `match/redutor.ts:224/273`. **AC 16 novo:** `src/net/codec.ts` com `parseDoCliente(raw: unknown): DoCliente | null`. Ele reconstrói a mensagem só com campos conhecidos (`tick` e extras somem por construção), recusa não-finitos, `t` desconhecido e slot desconhecido, lista os slots por extenso com `satisfies Record<Command['slot'], true>` (E40-INF-004) e reconstrói `decisao.d` por variante. `EntradaDaSala` continua levando o tipo parseado; quem chama o parser é `server/`. **Achado do @po ao validar, somado ao AC 16:** `Decisao` carrega `jogador`, e `aplicar()` só confere se é a vez de `d.jogador` (`redutor.ts:224`), então o assento 0 draftaria pelo jogador 1. O AC 5 cobria só casts; agora a sala recusa decisão com `d.jogador` alheio. Sem o parser, `jogador: 2` nem chegaria a ser `Transicao.erro`: seria `TypeError` em `redutor.ts:273`. ACs 3, 14 e 15, Task 8/9, Focus Areas e Testing ajustados: `codec.ts` entra no escopo, `protocolo.ts` fica explicitamente proibido e o grep de pureza passa à forma recursiva e com fronteira de palavra, exigindo exit 1. **Tamanho:** 16 ACs e 9 tasks. O acréscimo é um arquivo pequeno, puro, sem acoplamento com a máquina de estados e especificado caso a caso, e por isso **não invalida a prontidão**. Mas reforça o aviso de 1.1.0: a `e4.3` é, com folga, a maior story da fase. Se o @dev encontrar atrito, o **primeiro** corte passa a ser tirar o parser (Task 9 inteira, mais a guarda de negativos) para uma story própria, que não depende de nada da sala. Esse corte vem antes do corte *máquina de fases* × *rodada e cadência*, e a ligação `d.jogador` → assento fica com a metade que tiver `{t:'decisao'}`. | @po |
| 2026-09-21 | 1.3.0 | **Deltas da §5.5/§5.6 de `architecture-e4.md` (commits `48e53cc` e `c37943b`) aplicados, e achados do gate de `e4.2` roteados. Status: Ready → Draft, por corte decidido, não por defeito.** **Conferido na fonte antes de escrever:** `src/net/snapshot.ts:141-190` (`ProdutorDeSnapshot` com `observar`/`snapshot`, fábrica `criarProdutorDeSnapshot(world)`, exceção em tick pulado, repetido ou não observado); `determinism.ts:52-53` (composição fixa `[golem, vex]` 0/0), `:147-163` (`BUILD_BASELINE`, 5 variantes), `:431` (`SEEDS_FIO`), `:593-703` (canário de cobertura) e `:718` (linha-resumo com "sequência idêntica" fixo). **(1) "Depende de" corrigido (E42-REQ-002):** citava `snapshot()`, que não existe. Agora cita a fábrica e o contrato. **(2) AC 10, acréscimo (E42-REQ-002, fechado aqui):** um produtor por rodada, `observar` depois de cada `step` e `snap` final no tick de término, antes de `rodadaFim`. **(3) AC 11 (d), acréscimo:** o último `snap` tem `over: true` e `roundEnd` e precede `rodadaFim`, e os `events` concatenados são idênticos a `world.events`. Discrimina porque 4 de 5 rodadas terminam em tick ímpar. **(4) AC 17 novo (E42-ARC-001, decidido pelo @architect):** `codificarDoServidor`/`decodificarDoServidor`, com texto literal da §5.5, e Task 10. **(5) AC 18 novo, E42-TST-003 + E42-TST-004 anexados aqui**, e não a uma story debt: a guarda que eles corrigem é o laço `fio snapshot` de `determinism.ts`, o mesmo sobre o qual a guarda (a) do AC 17 roda. Estender a composição uma vez serve às duas, e uma story separada abriria o mesmo bloco do mesmo arquivo por umas 20 linhas. Task 11 correspondente. **(6) Correção a uma afirmação da §5.5:** a §5.5 diz que o "AC 15 não muda", mas o parêntese do AC 15 limitava `determinism.ts` às "guardas dos AC 11 e 16". A guarda do AC 17 ficaria fora do escopo. O parêntese passa a incluir 17 e 18. **(7) CORTE DECIDIDO:** com 18 ACs e 11 tasks, e dois componentes sem acoplamento (sala × fio) sob um só gate, a v1.2.0 já dizia que o primeiro corte seria o parser, e a §5.5 manda o codec de saída junto. Ele é executado **agora**, antes do desenvolvimento, e não "se houver atrito": o codec ganhou uma guarda de cinco partes com contrafactual, e uma falha dela seguraria o gate da sala, e vice-versa. O detalhe exato do que vai para a story nova e do que fica aqui está em "Depende de" → "Corte decidido". **Ação do @sm:** `*create-story` da story do codec (sugestão `e4.8`), levando AC 16-parser, 17 e 18 com o texto desta versão. **Ação do @po depois disso:** revalidar as duas, retirar daqui o que migrou, renumerar e devolver esta a Ready. Esta passa a depender, em código e em sequência de commit, da story do codec. O segundo corte (fases × rodada) **não** é feito: separaria a Bo5 da guarda do AC 11. | @po |
| 2026-09-21 | 1.4.0 | **Revalidação @po depois do corte: GO 9/10. Status: Draft → Ready**, com a pré-condição de início no topo. **Corte executado:** `e4.8` (commit `6c2d580`, validada GO 10/10 na mesma rodada) recebeu, com texto conferido por diff de palavras contra a v1.3.0, a parte do parser do AC 16 (→ `e4.8`/AC 4), o AC 17 (→ AC 5), o AC 18 (→ AC 6), a parte `codec.ts` dos AC 3/14/15 (→ AC 3/7/8), a Task 9 sem a subtarefa da sala e as Tasks 10 e 11 (→ Tasks 1–3). **Retirado daqui:** os AC 17 e 18, os itens (a)–(g) do AC 16, os casos de guarda "→ `null`" e "→ parse OK, com descarte", as Tasks 10 e 11, as subtarefas do parser na Task 9, as linhas de parser/codec em Focus Areas, Testing e `quality_gate_tools`, e `codec.ts` dos AC 3 e 15. **Numeração:** os ACs 1–15 mantêm os números, e o AC 16 continua 16. Por isso as referências de `e4.8` a "AC 4", "AC 6", "AC 7" e "AC 16 de `e4.3`" seguem certas, conferidas uma a uma. **Fica aqui, como pedido:** a sala; o flush de fim de rodada dos AC 10/11; a checagem assento → `d.jogador` (AC 16); e a guarda Bo5 que passa toda mensagem por `parseDoCliente(JSON.parse(JSON.stringify(msg)))`. Os casos "→ sala" e "Caminho feliz" ficam aqui com razão: precisam da `Sala` e do arnês Bo5 do AC 11. **Mudanças além da retirada:** **(1)** "Depende de" ganha `e4.8`, em código e em ordem de commit, com a seção "Sequência": `e4.8` → `debt.11` → `e4.3`, e `e4.6` já vem depois porque depende desta e de `e4.4`. A pré-condição de início é `git status --short src/tools/determinism.ts` vazio. `debt.11` vem antes desta porque é pequena (§2.2/R1), tem prazo (R10) e, pela R9, começa depois do commit da story em curso. Se ainda estiver em Draft quando `e4.8` fechar, esta não espera. **(2)** AC 14: `net/sala.ts` **não** importa `net/codec.ts`. Quem chama o parser é `server/` e, no `sim:check`, a guarda. Isso fecha a metade que faltava da seta que `e4.8`/AC 7 fixa do outro lado. **(3)** AC 15: `codec.ts` passa a ser proibido, e um parser que atrapalhe a Bo5 vira achado contra `e4.8`. `snapshot.ts` e `projecao.ts` (`e4.2`, Done) também entram na lista de proibidos. A prova de escopo é `git show --stat <commit de implementação>`, nunca `git diff --stat` na árvore. **(4)** "Depende de": `snapshot.ts:141-190` → `:141-189`, porque o arquivo tem 189 linhas. **Fatos conferidos:** `redutor.ts:224` (`if (vez !== d.jogador)`), `:273` (indexação sem faixa), `:437-440` (`registrarRodada` com default `['bot','bot']`); `snapshot.ts:68/151/161`; `protocolo.ts:38` (`ATRASO_ALVO_TICKS = 6`), `:49` (`SNAPSHOT_HZ = 30`); `e4.6` "Depende de" = `e4.3` e `e4.4`. **Tamanho:** 16 ACs e 9 tasks, a mesma contagem da v1.2.0, mas o AC 16 agora tem uma regra e dois casos de guarda, em vez de um parser inteiro. É razoável para uma story "High". O ponto não somado é o de sempre: continua sendo a maior story da fase, e o segundo corte (fases × rodada) segue como contingência. | @po |
| 2026-09-21 | 1.5.0 | **Sequência de `determinism.ts` atualizada com a story do gate de `e4.8` (`docs/qa/gates/e4.8-codec-do-fio.yml`, commit `438740b`). Status permanece Ready. Nenhum AC muda.** **(1)** E48-TST-001 (medium, prazo "antes de a `e4.4` começar"), E48-TST-002 e a fixture do fio de E48-ARC-003 **não** foram anexados aqui. Esta é a maior story da fase, e o motivo do corte da v1.3.0 foi justamente não pôr a guarda do fio e a da sala sob um só gate. Vão para uma story debt nova (sugestão `debt.12`, a criar pelo @sm). **(2) "Depende de" → "Sequência":** `debt.12` entra **depois** desta. O prazo dela é `e4.4`, que já depende desta, então a ordem não atrasa nada. Ordem do arquivo: `e4.8` → `debt.11` → `e4.3` → `debt.12` → `e4.6`. **(3)** A regra "só começa com `git status --short src/tools/determinism.ts` vazio" passa a valer para toda story que edita o arquivo, e não só para esta. Nota no Status acrescentada. **Fatos conferidos:** `determinism.ts:1046-1194` tem o bloco do codec de `e4.8` que `debt.12` estende. `git status --porcelain src/` estava vazio na hora desta emenda. Os greps de story mostram `determinism.ts` citado, entre as stories não fechadas, só por `debt.11`, esta e `e4.6`. | @po |
| 2026-09-21 | 1.6.0 | **Deltas da §11.6.1 de `architecture-e4.md` (E48-ARC-003, commit `bed4603`) aplicados, e decisão O-1 do @po. Status permanece Ready. O escopo de arquivos não muda** (AC 15: `sala.ts` novo e `determinism.ts`, só as guardas dos AC 11 e 16). **(1) "Depende de":** a story da versão do fio (sugestão `e4.9`, a criar pelo @sm; spec no Change Log de `e4.8`, v1.5.0) entra em código e em ordem de commit. Ela põe `versao`, `assento` e `snapshotHz` no tipo de `{t:'sala'}`, em `protocolo.ts`, que o AC 15 daqui proíbe. Nota no Status, Task 8 e "Sequência" ajustadas. **(2) Sequência:** a ordem de `determinism.ts` passa a `e4.8` → `debt.11` → `e4.9` → `e4.3` → `debt.12` → `e4.6`. O caso "se `debt.11` estiver em Draft, esta não espera" caiu, porque `debt.11` já tem commit de implementação (`971686b`, em gate do @qa) e `e4.9` depende dela. AC 15 cita `e4.9` e `debt.12` entre as que editam o arquivo. **(3) AC 11 (e), novo:** a sala preenche `versao: VERSAO_DO_FIO`, `assento` (a chave do assento de destino) e `snapshotHz` em todo `{t:'sala'}`. A guarda confere na Bo5 que todo `{t:'sala'}` sai por assento, com o segredo do próprio destinatário e nunca o do outro; que tem a versão certa; e que é o primeiro envio a um assento recém-assentado, na primeira entrada e no reassentamento. A regra de segurança é a do AC 8: um broadcast entregaria o segredo, e com ele o assento (`e4.4`/AC 7). **(4) AC 10, acréscimo (O-1):** a taxa efetiva é o campo `snapshotHz` da configuração da sala, com default `SNAPSHOT_HZ`, no mesmo lugar da política do AC 13. A cadência usa esse campo, e o `{t:'sala'}` leva o mesmo valor. **Achado do @po ao aplicar O-1:** sem esse campo, o override de operação de `e4.7`/AC 4 não tinha como mudar a cadência. A cadência mora em `sala.ts`, lia a constante, e o AC 11 de `e4.7` só abre `server/main.ts`. O campo fecha isso sem mudar o escopo de nenhuma das duas. **[AUTO-DECISION] O-1: taxa efetiva no `{t:'sala'}` (a) ou buffer do cliente fixado pela menor taxa da varredura (b)? → (a)** (motivo: (b) só troca o viés de lado. Com o buffer de 20 Hz, 30 e 60 Hz pagam ~17 e ~33 ms a mais de latência sentida, que é o eixo que a varredura julga, e (b) ainda exigiria editar `client/`, que o AC 11 de `e4.7` proíbe. Além disso, (a) continua certo depois da medição: um override sem rebuild em produção deixaria o cliente com o buffer errado para sempre. O custo de (a) é um campo na story que já abre a variante, contra uma versão nova do fio depois). Focus Areas, Task 3/7/8 e Testing ajustados. **Fatos conferidos:** `DoServidor['sala']` = `{ t; jogador; estado }` em `src/net/protocolo.ts:136`; comentário do segredo "devolvido no primeiro `{ t: 'sala' }`" em `:104`; `SNAPSHOT_HZ = 30` em `:49`; `amostras.sala` da guarda `não-snap` em `src/tools/determinism.ts:1157-1158`; AC 14 daqui já permite importar `net/protocolo.ts`. | @po |
| 2026-09-21 | 1.7.0 | **Implementada pelo @dev. Status: Ready → InProgress → Ready for Review.** Commit de implementação `99f4ee3`: `git show --stat` lista só `src/net/sala.ts` (novo, 664 linhas) e `src/tools/determinism.ts` (+661, só inserção). `npm run check` e `npm run build` rc=0. `npm run sim:check` rc=0, golden hash idêntico, saída só com inserção (7 linhas depois da telemetria). Bo5 `matchSeed 1` pela sala = `tools/partida.ts`: placar 0-3, vencedores [1 1 1], hash/ticks/lado iguais em 3/3, 141 casts carimbados em T. Greps de pureza exit 1 e 1. Bateria de 15 mutações em `sala.ts`: 15/15 reprovam, arquivo restaurado (sha256). **Adendo do @architect aplicado na guarda do AC 11 (e)** (§11.6.1, `6c6b9a0`): o `{t:'sala'}` de cada destinatário leva o `jogador` daquele assento. A mutação M6, `jogador` trocado entre os assentos, dá rc=1. O texto do AC não mudou; o delta é do @po. Decisões autônomas e 4 itens em aberto no Dev Agent Record: o `hash` injetado sem fonte para `e4.4`; a pausa de R-02 invisível no fio; `EventoPartida` sem variante no fio; R-02 ainda provisória. CodeRabbit indisponível nesta máquina. | @dev |
