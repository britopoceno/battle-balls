# Story e3.4: Cliente — fluxo draft → builds → rodada → loja → placar, `heuristic` no lugar do `dummy`, lado por jogador

## Status

InProgress

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run check", "npm run sim:check", "smoke visual P3.4 no celular real: partida completa cobrindo as 6 superficies (a)-(f) do AC 3, sem erro de console", "revisão manual: main.ts não usa mais meuTime fixo, minhasBolas filtra por lado"]
```

## Story

**Como** desenvolvedor executando o passo 4 do plano de construção da Fase 3 (`docs/architecture-e3.md`
§12),
**eu quero** implementar o fluxo completo de telas do cliente — draft, builds (com timer), rodada (canvas),
placar/loja — espelhando `FaseDaPartida` de `match/`, trocando o driver `dummy` pelo `heuristic` real e
filtrando `minhasBolas` pelo lado do jogador (que agora alterna a cada rodada, R-06),
**para que** exista, pela primeira vez, uma partida jogável do início ao fim contra o bot real — o cliente
que os portões P3.1 a P3.4 vão medir com humano no controle.

## Depende de

`e3.1` (`src/shop/` — catálogo e preview de stat efetivo), `e3.2` (`src/match/` — `EstadoPartida`,
`visaoPara`, redutor `aplicar`) e `e3.3` (`src/bot/partida.ts` — política de partida do bot). O cliente
precisa das três: da loja para desenhar preço/catálogo, de `match/` para orquestrar o fluxo de fases, e do
bot de partida para ter um oponente que joga a partida inteira, não só a rodada.

## Acceptance Criteria

**Verificação de compatibilidade (herdada, todas as stories):**

1. `npm run check` verde.
2. `npm run sim:check` verde, com **golden hash idêntico** ao baseline nas seeds 1, 2, 3, 7, 11.
   `sim:check` **não vê o cliente** — este AC prova que o motor não regrediu, não que o cliente funciona; a
   prova do cliente é o AC 3.

**Smoke visual — a prova que `sim:check` não dá (§12, passo 4; Anexo B, P3.4):**

3. **Smoke visual (P3.4):** uma partida completa jogada manualmente **no dispositivo — celular real**, do
   draft ao fim, **sem erro de console**. O padrão é o de P1.2, e P1.2 enumera as superfícies exercitadas
   (`docs/prd.md` §2, E1: *"Smoke visual próprio no dispositivo — seleção de build, arena, física, HUD,
   cooldown, arrasto nas duas mãos, fim de rodada, sem erro de console"*); `docs/prd.md` §2, E3 define P3.4
   como *"Smoke visual no dispositivo, **como em P1.2**"*. Seguindo esse padrão, a execução precisa
   exercitar, na mesma sessão, cada superfície **nova** desta fase:

   | # | Superfície a exercitar na sessão | Por que ela precisa estar na lista |
   |---|---|---|
   | a | tela de **draft** → escolha registrada | primeira tela do jogo; nunca existiu antes |
   | b | tela de **builds** com escolha manual, **e** um estouro de timer de 30s produzindo `buildPadrao` | o caminho do timeout (D-06) só existe aqui; é o único jeito de ver que `match/` recebe a decisão |
   | c | **rodada** no canvas, com arrasto nas duas mãos (herdado de P1.2) | é a superfície que P1.2 já cobria e que não pode ter regredido |
   | d | **placar** e **loja**: ao menos uma **compra** concluída e o **valor efetivo** exibido (AC 6) | é a decisão econômica, o motivo da fase existir |
   | e | ao menos uma rodada jogada **do lado 1** (alternância de R-06, AC 13) | input e HUD invertidos é o modo de falha mais provável desta story |
   | f | **fim de partida** (Bo5 resolvido, não abandonado no meio) | o fim de rodada era estado morto antes (AC 11) |

   Ausência de erro de console é **necessária e não suficiente**: uma superfície da tabela que não foi
   aberta na sessão não conta como coberta. Navegador desktop serve para desenvolvimento e depuração, mas
   **não substitui** a execução no celular — foi exatamente essa a lição de P1.2 (`docs/prd.md`: *"P1.2 não
   é redundante com `npm run sim:check`"*), e a Seção Testing desta story já exigia celular.

**Máquina de estados da UI (§11.1):**

4. O fluxo de tela segue exatamente:
   ```
   draft ──▶ builds ──▶ rodada ──▶ [placar] ──▶ loja ──▶ builds? ──▶ rodada ... ──▶ fim
   ```
   `FaseDaPartida` (de `match/types.ts`, `e3.2`) é a **fonte** — a UI não tem estado de fase próprio, só
   desenha o que a fase diz. Isso evita a classe de bug "a tela acha que está na loja e a partida acha que
   está na rodada".
5. Tabela de telas, superfície e I/O (§11.1):

   | Tela | Superfície | Lê | Escreve |
   |---|---|---|---|
   | Draft | DOM (overlay) | `visaoPara(e, humano).draft` | `{t:'draft'}` |
   | Builds | DOM — reaproveita os cards de `montarSeletor` que já existem (`main.ts:153-181`) | `visaoPara(...).eu.personagens` | `{t:'build'}`, `{t:'pronto'}`, timeout → `{t:'buildPadrao'}` |
   | Rodada | canvas — o laço de hoje, sem mudança estrutural | `World` | `Command[]` |
   | Placar / loja | DOM (overlay) | `visaoPara(...)`, catálogo, preços efetivos | `{t:'compra'}`, `{t:'trocaDeBuild'}`, `{t:'pronto'}` |

**Preview de valor efetivo — nunca reimplementar a fórmula (§7.3, §13.3):**

6. A tela de loja mostra o **valor efetivo** do item (não o valor nominal), calculado chamando
   `recomputeStats` sobre uma bola sintética — usando o **helper único** extraído em `e3.1`. **Nunca**
   reimplementar a fórmula de stats no cliente — isso criaria uma terceira fonte de verdade.

**Mudanças em `main.ts` (§11.2):**

7. `world` deixa de ser variável global de módulo e passa a ser criado por
   `setupDaRodada(estadoPartida)` (de `match/`, `e3.2`).
8. `meuTime` (`main.ts:28`) **some**: a composição vem do draft, e o lado vem de `ladoDoJogador`
   (`ResultadoRodada.ladoDoJogador`, `e3.2`/§5.3).
9. `minhasBolas()` passa a filtrar por `meuLado`, **não** por `team === 0` — literal:
   `balls.filter(b => b.team === meuLado)`.
10. `dummyCommands` é substituído por `botCommands` + `createBot(seedDaRodada, ladoDoBot)`, **recriado a
    cada rodada** (invariante M-1, `e3.2`).
11. O fim da rodada deixa de ser um estado morto (`world.over` e nada acontece): passa a emitir
    `registrarRodada(...)` (de `match/`).
12. `INPUT_DELAY_TICKS = 0` **continua 0** — é desvio consciente já registrado na arquitetura; P4.1 é da
    Fase 4. Não mexer nisso nesta story.

**Alternância de lado (§5.3, R-06 opção b aprovada):**

13. O cliente sabe renderizar e comandar o time 1 — o layout já é simétrico (`layout.ts`), então nenhuma
    mudança estrutural de layout é esperada, só a leitura correta de `meuLado` por rodada.

**Compras não são secretas (§4.1, nota explícita):**

14. `PersonagemVisivel.itens` é visível nas duas variantes do tipo de `visaoPara` — a tela de loja/placar
    **não** deve esconder os itens comprados pelo oponente. RF-04 declara segredo só para a *seleção de
    build*.

**`render.ts` (§11.2):**

15. `render.ts` não muda estruturalmente, exceto o HUD do placar (`desenharHud` já existe e deve ser
    conectado ao placar real) e o fato de que o time do humano pode ser 1.

**Draft com composição fixa (herdado de `e3.2`, R-01 opção B):**

16. A tela de draft existe e é funcional (lê `pool`/`ordem`, escreve `{t:'draft'}`), mas a composição
    exercitada é fixa `[golem, vex]` para os dois jogadores — não implementar escolha de personagem com
    efeito real no resultado; a estrutura é genérica, o conteúdo é fixo nesta fase (R-01).

**Dívidas herdadas dos gates de `e3.2` e `e3.3` (`docs/architecture-e3.md` §12.1) — entradas
obrigatórias desta story:**

> Estes quatro ACs foram acrescentados depois da validação de 1.1.0, quando §12.1 foi escrita. Eles
> não ampliam o escopo por invenção: cada um é a linha literal da tabela de §12.1, que declara as
> quatro dívidas como *"entradas obrigatórias da story"* do passo 4. O motivo de §12.1 existir é
> precisamente este — ARCH-E32-001 foi registrado como bloqueante do gate de `e3.3` e atravessou a
> story inteira sem ser visto, porque uma obrigação que só existe num YAML de gate é invisível para
> quem escreve a story seguinte.

17. **(D-a / ARCH-E32-001)** `medirM1` (`src/tools/partida.ts`) compara `createBot(s1)` novo contra
    `createBot(s0)` sujo — muda a **seed** e a **sujeira** juntas e credita a divergência à causa
    errada. Medido no gate: um bot limpo com `s0` já diverge de um limpo com `s1` em 8/8 seeds, logo o
    ramo de falha da guarda é inalcançável e a linha `invariante M-1 … contamina em 3/3` **não mede o
    que afirma**. Corrigir para **dois pares de bots com a mesma seed, sujando só um**. A guarda
    precisa continuar verde — e agora por medir contaminação, não diferença de seed.
18. **(D-b / ARCH-E32-004)** Os literais `3` e `7` da guarda de D-02 em `verificarPartida`
    (`src/tools/partida.ts:713-714`) passam a sair de `REGRAS_PADRAO` (ou de `e.regras` da partida
    gravada). Hoje, se `e3.7` mexer em `vitoriasParaVencer`, a guarda vira **falso FAIL** num bloco que
    derruba o `sim:check` inteiro.
19. **(D-c / ARCH-E33-001)** `src/bot/partida.ts` ganha cobertura no `sim:check`: hoje não tem **um
    único consumidor em `src/`** e o arquivo inteiro pode ser apagado sem uma linha vermelha. O bloco de
    política é **somado** ao lado do `ROTEIRO_*` fixo, **sem substituí-lo** — o roteiro exercita
    `buildPadrao`, `trocaDeBuild` e o caminho de rejeição por saldo, que a política v1 nunca produz.
    O cliente desta story é o primeiro consumidor de produção, e é por isso que a dívida vence aqui.
20. **(D-d / ARCH-E33-004)** As duas invariantes de chamador hoje **declaradas em comentário e sem
    guarda** ganham guarda executável: (1) **um `BotState` por rodada** (M-1, §2.5) e (2) **uma política
    por PARTIDA**, não por rodada — recriar a política a cada visita à loja muda as compras em 8/8 seeds
    (medido no gate de `e3.3`). O cliente é o primeiro chamador de produção das duas e é onde elas podem
    ser quebradas em silêncio, então o cliente precisa respeitá-las **e** a guarda precisa provar que
    quebrá-las é detectável.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend / Integration
**Secondary Type(s)**: Architecture (integração com `match/`, `shop/`, `bot/`)
**Complexity**: Medium-High — não é código novo complexo por fórmula (como `e3.2`/`e2.3`), mas é a primeira
story da fase que produz uma experiência jogável ponta a ponta, com risco concentrado em coordenação de
estado entre `main.ts` e `match/`.

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa (quality gate — comportamento observável em dispositivo real, não só `tsc`/`sim:check`)

**Supporting Agents**:
- @ux-design-expert (não é dono desta story — pixel/cor/ergonomia são dele, mas não bloqueiam esta entrega;
  ver Dev Notes)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Rodar antes de marcar a story como completa
- [ ] Pre-PR (@github-devops): Rodar antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Nenhuma reimplementação da fórmula de stats no cliente — a tela de loja usa o helper de bola sintética de
  `e3.1`
- `minhasBolas` filtra por `meuLado`, nunca hardcoded `team === 0`
- `BotState` recriado a cada rodada (invariante M-1) — nenhuma reutilização entre rodadas

**Secondary Focus**:
- `FaseDaPartida` como única fonte de verdade de estado de tela (sem estado de fase duplicado no cliente)
- Compras visíveis para ambos os jogadores (não esconder itens do oponente)

## Tasks / Subtasks

- [x] Task 1 — Máquina de estados da UI (AC: 4, 5)
  - [x] Conectar o render de tela a `FaseDaPartida` — sem estado de fase próprio no cliente

- [x] Task 2 — Tela de Draft (AC: 5, 16)
  - [x] DOM overlay lendo `visaoPara(e, humano).draft`, escrevendo `{t:'draft'}`
  - [x] Composição exercitada fixa `[golem,vex]`

- [x] Task 3 — Tela de Builds (AC: 5)
  - [x] Reaproveitar cards de `montarSeletor` (`main.ts:153-181`)
  - [x] Timer de 30s no cliente (relógio de parede vive aqui, não em `match/`); estouro emite
        `{t:'buildPadrao'}`

- [x] Task 4 — Tela de Rodada (AC: 5, 7, 9, 10)
  - [x] `world` criado por `setupDaRodada(estadoPartida)`
  - [x] `minhasBolas()` filtra por `meuLado`
  - [x] `dummyCommands` → `botCommands` + `createBot(seedDaRodada, ladoDoBot)` recriado por rodada

- [x] Task 5 — Tela de Placar / Loja (AC: 5, 6, 14)
  - [x] DOM overlay lendo `visaoPara`, catálogo (`e3.1`) e preços efetivos via helper de bola sintética
  - [x] Escreve `{t:'compra'}`, `{t:'trocaDeBuild'}`, `{t:'pronto'}`
  - [x] Itens comprados pelo oponente visíveis (não esconder)

- [x] Task 6 — `main.ts` (AC: 7, 8, 9, 10, 11, 12)
  - [x] Remover `meuTime` fixo; ler `ladoDoJogador` da rodada corrente
  - [x] Fim de rodada emite `registrarRodada(...)`
  - [x] Confirmar `INPUT_DELAY_TICKS = 0` inalterado

- [x] Task 7 — `render.ts` (AC: 13, 15)
  - [x] Conectar `desenharHud` ao placar real
  - [x] Confirmar renderização correta quando o humano é o time 1

- [x] Task 8 — Dívidas de §12.1 no arnês (AC: 17, 18, 19, 20)
  - [x] D-a: `medirM1` com dois pares de bots de **mesma seed**, sujando só um
  - [x] D-b: guarda de D-02 lendo `REGRAS_PADRAO`/`e.regras` no lugar dos literais `3` e `7`
  - [x] D-c: bloco de política **somado** ao `ROTEIRO_*` fixo, dando consumidor a `bot/partida.ts`
  - [x] D-d: guarda para "um `BotState` por rodada" e "uma política por PARTIDA"

- [ ] Task 9 — Verificação (AC: 1, 2, 3)
  - [x] `npm run check` — 0 erros
  - [x] `npm run sim:check` — golden hash idêntico
  - [ ] **PENDENTE — não executável pelo @dev.** Smoke visual (P3.4) **no celular real**, partida
        completa, sem erro de console — percorrendo as seis superfícies (a)-(f) da tabela do AC 3 e
        registrando cada uma como exercitada. As seis foram exercitadas em **navegador desktop** com
        console limpo (ver Dev Agent Record), e o AC 3 é explícito em que isso **não substitui** o
        celular: é a lição de P1.2, e a versão 1.1.0 desta story rejeitou "celular **ou** desktop"
        como abrandamento não autorizado. A execução no dispositivo é do usuário.

## Dev Notes

### A máquina de estados (fonte: `docs/architecture-e3.md` §11.1)

```
draft ──▶ builds ──▶ rodada ──▶ [placar] ──▶ loja ──▶ builds? ──▶ rodada ... ──▶ fim
  │         │           │                      │
  DOM       DOM       canvas                  DOM
```

> `FaseDaPartida` (§2.3) é a fonte; a UI não tem estado de fase próprio, só desenha o que a fase diz. Isso
> mata a classe inteira de bug "a tela acha que está na loja e a partida acha que está na rodada".

> **Por que DOM para as telas e canvas para a rodada:** o overlay, os cards e o CSS já existem e já foram
> validados em celular real na Fase 1 (P1.1/P1.2). Recriar isso em canvas seria trabalho novo para um
> resultado pior em acessibilidade e em rolagem — e a rodada continua sendo canvas porque é onde há 60Hz.

[Fonte: `docs/architecture-e3.md` §11.1]

### O que muda em `main.ts` (fonte: §11.2, literal)

> Hoje ele é um laço de rodada única com um seletor de build de bancada. Depois:
> - `world` deixa de ser variável global de módulo e passa a ser criado por `setupDaRodada(estadoPartida)`;
> - `meuTime` (`main.ts:28`) some: a composição vem do draft, e o lado vem de `ladoDoJogador` (§5.3);
> - `minhasBolas()` passa a filtrar por `meuLado`, não por `team === 0`;
> - `dummyCommands` → `botCommands` + `createBot(seedDaRodada, ladoDoBot)`, recriado por rodada (M-1);
> - o fim da rodada deixa de ser um estado morto (`world.over` e nada acontece) e emite
>   `registrarRodada(...)`;
> - `INPUT_DELAY_TICKS = 0` continua 0 — é desvio consciente registrado, e P4.1 é da Fase 4.
>
> Nada disso muda `render.ts` estruturalmente, exceto o HUD do placar (`desenharHud` já existe) e o fato de
> que o time do humano pode ser 1.

[Fonte: `docs/architecture-e3.md` §11.2]

### O preview de stat da loja — a quase-terceira fonte de verdade (fonte: §7.3, §13.3)

> Se um jogador comprar dois itens do mesmo campo e o teto morder, ele **paga por um bônus que não
> recebe**. A tela da loja precisa mostrar o valor **efetivo**, e o único jeito honesto de calculá-lo é
> chamar `recomputeStats` sobre uma bola sintética — **nunca** reimplementar a fórmula no cliente.

[Fonte: `docs/architecture-e3.md` §7.3 — o helper em si já foi extraído em `e3.1`; esta story só o
**consome** na tela de loja.]

### Compras não são secretas (fonte: §4.1)

> Nota registrada, porque o texto aprovado não fala disso: as compras não são secretas. RF-04 declara
> segredo para a *seleção de build* e mais nada; esconder também a loja seria regra que ninguém escreveu
> (Artigo IV). `PersonagemVisivel.itens` é visível nas duas variantes do tipo.

[Fonte: `docs/architecture-e3.md` §4.1]

### Relógio de parede — onde ele mora (fonte: §2.6)

> RF-04 pede um timer de 30 segundos. [...] `match/` só conhece decisões explícitas. O cliente é dono do
> relógio e, quando ele estoura, **emite uma decisão** (`{ t: 'buildPadrao' }`).

[Fonte: `docs/architecture-e3.md` §2.6 — o timer em si é responsabilidade desta story (cliente); `match/`
(`e3.2`) só sabe reagir à decisão que o timeout produz.]

### Pixel/cor/layout não são desta story (fonte: §11, preâmbulo)

> Arquitetura de dados e de fluxo. Pixel, cor e ergonomia são @ux-design-expert e não estão aqui.

[Fonte: `docs/architecture-e3.md` §11] — esta story implementa a estrutura funcional das telas reaproveitando
o que já existe (`montarSeletor`, overlay, CSS de P1.1/P1.2); refinamento visual fino não é bloqueante para
o smoke visual do AC 3, que exige apenas ausência de erro de console numa partida jogável.

### O que esta story explicitamente NÃO faz

- Não implementa a política de compra/draft/build do bot — isso é `e3.3` (consumida aqui, não criada aqui).
- Não implementa o coletor de telemetria — isso é `e3.5` (mas os eventos que `aplicar` emite, de `e3.2`, já
  existem; o cliente não precisa fazer nada especial para "gerar" eventos, eles nascem do redutor).
- Não corrige a fórmula de resolução de dano nem qualquer coisa de `sim/` — nenhuma linha de `sim/` deve ser
  tocada.
- Não decide números finais de D-09 — usa o que `e3.2` já definiu como provisório.
- Não refina pixel/cor/ergonomia além do necessário para reaproveitar os componentes existentes.

### Testing

- Rodar `npm run sim:check` e confirmar golden hash idêntico — o cliente não deve ter regredido o motor.
- Rodar `npm run check` e confirmar 0 erros de `tsc`.
- **Smoke visual (P3.4, obrigatório, não substituível por teste automatizado):** jogar uma partida completa
  no dispositivo real (celular, seguindo o precedente de P1.1/P1.2 da Fase 1) do draft ao fim, sem erro de
  console. `sim:check` não cobre o cliente — esta é a única prova que cobre.
- Teste manual dirigido: confirmar que, numa rodada em que o humano é o time 1 (após alternância), o input
  e o HUD funcionam corretamente (não há inversão de controle nem HUD trocado).
- Teste manual dirigido: na tela de loja, comprar um item que estoura o teto de um campo e confirmar que o
  valor efetivo exibido reflete o clamp (não o valor nominal).

## Dev Agent Record

### File List

**Criados:**
- `src/client/telas.ts` — as quatro telas de DOM (draft, builds, loja/placar, fim) como função de
  `VisaoPartida`. Sem estado de fase próprio (AC 4).

**Modificados:**
- `src/client/main.ts` — reescrito como CONDUTOR da partida: `meuTime` removido, `minhasBolas` por
  `meuLado`, `world` por `setupDaRodada`, `botCommands`+`createBot` por rodada, `registrarRodada` no
  fim da rodada, timer de 30s, política criada uma vez por partida.
- `src/client/render.ts` — `OpcoesRender` ganhou `placar`, `meuLado` e `vitoriasParaVencer`;
  `desenharHud` desenha o placar real e o relógio desceu para não colidir com ele. Sem outra mudança
  estrutural (AC 15).
- `src/tools/partida.ts` — D-a (`medirM1`), D-b (guarda de D-02), D-c + D-d
  (`jogarPartidaComPolitica`, `decisoesPorPolitica`, `invariantePolitica`).
- `index.html` — o overlay virou contêiner vazio (o conteúdo é de `telas.ts`); CSS para o card de
  draft e para `:disabled`; título "fase 3".
- `docs/stories/e3.4.cliente-fluxo-partida.story.md` — ACs 17-20, Task 8, checkboxes, este registro.

**Não tocados, e confirmado:** `src/sim/` (nenhuma linha), `src/match/`, `src/shop/`,
`src/bot/heuristic.ts`, `src/bot/partida.ts`, `src/bot/dummy.ts`.

### Debug Log — o travamento que a primeira versão produziu

Registrado porque o modo de falha é instrutivo e não teria aparecido em `tsc` nem em `sim:check`.

A primeira versão fazia `decidir` → `render` → `decidirPeloBot` → `decidir`. Parecia terminar (toda
decisão aceita avança o estado), e **não terminava**: emitir `{t:'build'}` **não** marca `prontos`,
então cada reentrada na fase `builds` reemitia a mesma build e descia um nível. A aba congelou —
`Page.captureScreenshot` estourou 30s no renderizador.

O conserto separou as três responsabilidades que estavam fundidas: `aplicarDecisao` (só escreve o
estado), `avancarBot` (laço **iterativo** com teto `MAX_JOGADAS_DO_BOT`, que termina porque cada
volta ou avança `draft.passo` ou marca `prontos[BOT]`) e `render` (só desenha). É a mesma disciplina
que `tools/partida.ts` já usava com `MAX_PASSOS` — o cliente tinha inventado um condutor recursivo em
vez de espelhar o que o arnês fazia, e a lição é a da §12.1: divergir do condutor provado é como se
perde a prova.

### Verificação executada

| Prova | Resultado |
|---|---|
| `npm run check` | 0 erros |
| `npm run sim:check` | verde; **golden hash idêntico** nas 5 seeds e nas 5 variantes de build |
| `invariante M-1` (D-a) | 3/3 divergem — e agora por MEDIR contaminação: com os dois pares na mesma seed, o braço de controle mudou (932/836/842 ticks contra 1059/948/829 antes), o de tratamento não (963/980/1006) |
| **bateria negativa de D-a** | removida a linha que suja o par de tratamento, a guarda dá `IGUAL ✗` em 3/3 e a bateria acusa. **Antes de D-a esse ramo era inalcançável** — a diferença de seed sozinha já produzia divergência em 8/8, então a mutação passava verde |
| `política bot` (D-c/D-d) | verde: 3 partidas conduzidas por `bot/partida.ts`, reproduzidas sem bot; 6/2/6 compras; recriar a política por visita à loja diverge em 3/3 |
| Cobertura somada, não trocada (AC 19) | as partidas por política dão 5/3/5 rodadas contra 3/3/4 do roteiro fixo — são caminhos diferentes, e o roteiro segue cobrindo `buildPadrao`, `trocaDeBuild` e rejeição por saldo, que a política v1 nunca emite |

### Smoke visual — o que foi exercitado, e onde

**Em navegador desktop (Chrome, `localhost:5173`), partida completa 0-3, console limpo** — 2
mensagens, ambas do Vite (`connecting` / `connected`), **zero** erro, warning ou decisão rejeitada.

| # | Superfície | Observado |
|---|---|---|
| a | draft | 4 escolhas na ordem snake `[0,1,1,0]`; as do bot saíram da política |
| b | builds manual **e** estouro de timer | Tremor selecionado à mão; timer correu de 30s a 0 e `{t:'buildPadrao'}` devolveu o Golem a Impacto Sísmico (ativa 0) |
| c | rodada no canvas, arrasto nas duas mãos | dois arrastos, um de cada botão inferior |
| d | loja com compra concluída e valor efetivo | Couraça comprada: ouro 9 → 3 (débito == preço), Golem passou a "1 item(ns) · couraca". O efetivo é por personagem — Chumbo lê `mass +0.64` no Golem e `+0.18` no Vex, o que só é possível chamando `recomputeStats` sobre a base real de cada um |
| e | rodada jogada do lado 1 | rodada 2 com `meuLado === 1`: o HUD desenhou "VOCÊ" na cor do time 1 e os botões comandaram as bolas da direita. A rodada 3 voltou ao lado 0 — a alternância de R-06 foi observada nos dois sentidos |
| f | fim de partida | Bo5 resolvido em 3 vitórias (D-02), não abandonado. Tabela final com lados `01 → 10 → 01` e um hash por rodada |

### Passagem em viewport de celular (emulação do Chrome) — dois defeitos achados e corrigidos

Feita **antes** da sessão no dispositivo, para não queimá-la com defeito de layout. Pixel 9 deitado,
768×425 CSS, dpr 2. **Emulação não é celular e não conta para o AC 3** (ver abaixo); ela mede
viewport, transbordo e tamanho de alvo, e foi para isso que serviu.

| # | Defeito medido | Correção |
|---|---|---|
| 1 | **A ação primária nascia 158px ABAIXO da dobra na tela de builds** (conteúdo 642px, viewport 425px) — com o timer de 30s de RF-04 correndo. Quem não descobrisse que a tela rola veria o tempo acabar e a `{t:'buildPadrao'}` sobrescrever a build recém-escolhida. Na loja é o mesmo botão, conteúdo de 1160px, sem timer | `#start` virou `position: sticky; bottom: 46px`. `sticky` e não `fixed` para voltar ao fluxo quando a tela cabe inteira. O `46px` empilha com o `#exportar`, que é fixo no rodapé central, em vez de colidir |
| 2 | **Os quatro botões de "trocar build" da loja tinham 35px de altura**, abaixo do mínimo de toque usual de 44px. São os únicos `.op` sem linha de descrição — e são os que custam 5 de ouro por D-01, então errar o dedo ali tem preço | `min-height: 44px` no `.op`. Os demais já vinham com 46-67px, então só esses quatro subiram |

Depois das duas: menor alvo 44px e **zero** botão abaixo de 44 (21 alvos na loja); ação primária
visível sem rolar nas duas telas; sem colisão com o `#exportar`; sem transbordo horizontal.

**Limite da emulação que importa registrar:** `navigator.maxTouchPoints === 1`. O Chrome emula **um**
ponteiro, então o **arrasto com os dois polegares — a interação central do jogo, e a que P1.2
validou explicitamente** — é intestável aqui. Pelo mesmo motivo, a métrica "% de rodadas com uma só
mão" de RF-36 sempre lerá 100% em emulação, e o número só significa alguma coisa vindo do aparelho.

**Isto NÃO fecha o AC 3.** O AC exige celular real, e a versão 1.1.0 desta story rejeitou
explicitamente "celular **ou** navegador desktop" como abrandamento não autorizado (Artigo IV),
citando P1.2. O desktop serviu para desenvolvimento e depuração — que é o uso que o próprio AC lhe
concede — e as seis superfícies ficam acima para que a sessão no dispositivo saiba o que percorrer.
**Task 9 permanece aberta e a story não pode ir a Done sem ela.**

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-07-29 | 1.0 | Story criada a partir de `docs/architecture-e3.md` §2.6, §4.1, §7.3, §11 (inteira), §13.3 e §12 (passo 4). Quality gate atribuído a @qa por ser story de comportamento observável/cliente, seguindo o mesmo critério aplicado em E2 (stories com smoke visual e superfície de UI vão a @qa; infraestrutura pura vai a @architect). | River (@sm) |
| 2026-08-04 | 1.5.0 | **Dois defeitos de layout de celular achados em emulação (Pixel 9 deitado, 768×425) e corrigidos**, antes da sessão no dispositivo para não queimá-la: (1) a ação primária nascia 158px abaixo da dobra na tela de builds com o timer de 30s correndo — quem não rolasse perdia a build para `buildPadrao`; corrigido com `#start` sticky, empilhado com o `#exportar`; (2) os quatro botões de troca de build da loja tinham 35px, abaixo do mínimo de toque de 44px, e são os que custam 5 de ouro por D-01; corrigido com `min-height: 44px` no `.op`. Só CSS em `index.html` — `check` 0 erros e golden hash idêntico. Registrado que a emulação expõe `maxTouchPoints: 1` e portanto **não** testa o arrasto com dois polegares nem produz número válido para "% de rodadas com uma só mão" (RF-36): AC 3 segue aberto e só o aparelho o fecha. | @dev |
| 2026-08-04 | 1.4.0 | Implementação concluída **exceto o AC 3**. `src/client/telas.ts` criado; `main.ts` reescrito como condutor da partida; `render.ts` com o placar real; as quatro dívidas de §12.1 pagas em `src/tools/partida.ts`. `check` 0 erros e `sim:check` verde com golden hash idêntico. As seis superfícies (a)-(f) foram exercitadas em navegador desktop com console limpo, o que **não** fecha o AC 3 (celular real) — Task 9 fica aberta e a story **não** vai a InReview até o smoke no dispositivo. Um travamento de renderizador foi encontrado e corrigido durante o smoke (condutor recursivo `decidir`→`render`→`bot`; ver Debug Log). | @dev |
| 2026-08-04 | 1.3.0 | Status: Ready → InProgress. Implementação iniciada. | @dev |
| 2026-08-04 | 1.2.0 | **ACs 17-20 acrescentados: as quatro dívidas de `docs/architecture-e3.md` §12.1 (D-a a D-d), que a §12.1 declara como "entradas obrigatórias da story" do passo 4.** A story foi validada em 1.1.0 no dia 2026-07-29; §12.1 foi escrita em 2026-07-30, depois dos gates de `e3.2` e `e3.3`, e portanto não pôde ser lida pela validação. Nenhum AC novo inventa requisito (Artigo IV): cada um é a linha literal da tabela de §12.1, com a origem (ARCH-E32-001, ARCH-E32-004, ARCH-E33-001, ARCH-E33-004) citada no próprio AC. Task 8 criada para os quatro; a Task de verificação passou a ser a 9. O motivo de a promoção existir está registrado no preâmbulo de §12.1 e vale como lição de processo: ARCH-E32-001 foi registrado como entrada bloqueante do gate de `e3.3` e atravessou aquela story inteira sem ser visto, porque o AC 15 de `e3.3` fixou os arquivos tocados em dois e nenhum deles era `src/tools/partida.ts`. Uma obrigação que só vive num YAML de gate é invisível para quem escreve a story seguinte. | @dev |
| 2026-07-29 | 1.1.0 | Validated GO (8/10) — Status: Draft → Ready. **Should-Fix aplicado ao AC 3 (o AC de smoke visual estava mais fraco que o portão que ele deve provar).** Duas falhas, ambas conferidas contra a fonte: (1) o AC permitia *"celular **ou** navegador desktop"*, mas `docs/prd.md` §2/E3 define P3.4 como "Smoke visual no dispositivo, **como em P1.2**", e P1.2 (`prd.md` §2/E1) é "Smoke visual **próprio no dispositivo**" — o desktop como alternativa era abrandamento não autorizado (Artigo IV), e contradizia a própria seção Testing desta story, que já pedia celular. Corrigido para celular real. (2) P1.2 **enumera as superfícies** exercitadas ("seleção de build, arena, física, HUD, cooldown, arrasto nas duas mãos, fim de rodada"); o AC 3 dizia só "partida completa do draft ao fim", o que deixaria passar uma sessão que nunca abriu a loja nem jogou do lado 1. Adicionada a tabela (a)-(f) com as seis superfícies novas — incluindo **estouro de timer produzindo `buildPadrao`** e **uma rodada jogada do lado 1**, que são os dois caminhos com maior chance de falha silenciosa. Fatos conferidos no código vivo, todos os alvos citados existindo nas linhas afirmadas: `main.ts:20` `const INPUT_DELAY_TICKS = 0` (AC 12 ✓), `main.ts:28` `const meuTime: PickSetup[]` (AC 8 ✓), `main.ts:55` `world.balls.filter((b) => b.team === 0)` (AC 9 ✓ — o hardcode existe mesmo), `main.ts:116` `...dummyCommands(world, 1)` (AC 10 ✓), `montarSeletor()` de `main.ts:153` a `main.ts:181` (AC 5 ✓ — a faixa citada está exata), `desenharHud` em `render.ts:283`, chamado de `render.ts:44` (AC 15 ✓). A alegação de AC 13 ("o layout já é simétrico") foi verificada e é mais forte do que a story diz: `src/client/layout.ts` tem 50 linhas e **nenhuma menção a `team`** — é puramente geométrico, portanto agnóstico de lado por construção. Confirmado também que a story não assume UI inexistente: o overlay/cards/CSS que ela reaproveita existem de fato, e o que ela declara como novo (`telas.ts`, loja, draft) não existe hoje. `sim/` intocado. | @po (Pax) |
