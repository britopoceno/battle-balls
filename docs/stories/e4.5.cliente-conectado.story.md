# Story e4.5: `client/rede.ts` + o modo conectado — buffer, interpolação, e o cliente que não simula

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run check", "npm run sim:check", "duas abas jogam uma Bo5 completa", "grep: nenhuma chamada a step() no caminho conectado", "grep: nenhum JSON.parse(...) as DoServidor; entrada por decodificarDoServidor (AC 3)", "decode que lança (t desconhecido, aridade errada): console.error + conexão fechada, sem reconexão em laço (AC 3)", "servidor local com VERSAO_DO_FIO + 1: cliente loga, fecha, não reconecta e mostra na tela 'recarregue'; servidor com versão menor ou sem versão: mostra 'aguarde o servidor' (AC 3 iv)", "três ramos do descompasso rodados separadamente (servidor +1, servidor numérico menor, servidor sem versao) e snapshotHz fora de forma fechando sem mensagem de versão; grep: ramo por instanceof DescompassoDeVersao, nenhum e.message decidindo ramo ou indo para a tela (AC 3 iv, v1.6.0)", "ATRASO_DE_BUFFER dimensionado pelo snapshotHz do {t:'sala'}, não pela constante compilada (AC 7)", "exports das duas abas: nenhum compra/trocaDeBuild/buildPadrao com jogador de outro assento; seed da última visao; controle não reescrito (AC 10, v1.7.0)", "modo escolhido pela URL: sem #/sala/{id} o modo local de hoje; com ele, {t:'entrar', sala: id} e nenhuma tela de criação (AC 4, v1.9.0)", "link com id inventado: {t:'erro'} antes de qualquer {t:'sala'} mostra mensagem na tela, sem tela preta e sem reconexão em laço (AC 3 v, v1.9.0)", "as duas abas abrem o mesmo link, copiado da linha de log do servidor (AC 13, v1.9.0)", "grep -nE '^(export )?const PORTA_DO_SERVIDOR = [0-9]+$' src/client/rede.ts src/server/main.ts: exatamente duas linhas, uma por arquivo, com o mesmo literal (L-3, §6.2, v1.10.0)", "grep -rnE 'location\\.host\\b|import\\.meta\\.env|ws://localhost|wss?://127\\.|[?&]servidor=' src/client/ sai com 1 (§6.2, v1.10.0)", "servidor desligado e porta errada: mensagem na tela com o endereço tentado, sem tela preta e sem reconexão em laço (AC 3 vi, v1.10.0)", "git diff --stat: render.ts/telas.ts/input.ts/layout.ts intactos"]
```

## Story

**Como** desenvolvedor executando o **passo 5** do plano de construção da Fase 4
(`docs/architecture-e4.md` §10),
**eu quero** criar `src/client/rede.ts` (WebSocket do navegador, buffer de snapshots, interpolação,
reconexão por segredo de assento) e dar a `client/main.ts` **dois modos** — `local`, o de hoje, e
`conectado` —,
**para que** o jogo apareça na tela de dois jogadores ao mesmo tempo com o servidor como única fonte de
verdade; e para que **P4.2 fique satisfeito por subtração**: no modo conectado o cliente não instancia
`World`, não chama `step()` e não tem autoridade sobre nada além de qual pixel pintar.

## Depende de

`e4.0` (protocolo), `e4.2` (`projecao.ts`), **`e4.8`** (codec do fio: `decodificarDoServidor(texto:
string): DoServidor` em `src/net/codec.ts`, implementada em `910add8` e em QA na data da v1.4.0) e `e4.4`
(servidor). *(v1.4.0: antes apontava para `e4.3`/AC 17, que o corte decidido lá levou para `e4.8`.)*
*(v1.5.0: e da story da versão do fio, sugestão `e4.9`, `architecture-e4.md` §11.6.1, que vem antes de
`e4.3` e portanto já existe quando esta começar. Desta story, `e4.9` entrega a checagem de `versao` dentro
de `decodificarDoServidor`, o erro de descompasso com as duas versões em campos, e os campos `assento` e
`snapshotHz` do `{t:'sala'}`.)* *(v1.7.0: e de **`e4.10`**, a story nova da variante `{t:'evento', e:
EventoPartida}` (`architecture-e4.md` §11.6.2, E43-ARC-002, commit `188998e`; a criar pelo @sm, spec no
Change Log de `e4.3` v1.10.0). Desta story, `e4.10` entrega a variante em `DoServidor`, `evento` na lista
fechada do decodificador, `VERSAO_DO_FIO = 2` e a sala mandando cada `EventoPartida` por assento: um
evento sem `jogador` vai aos dois, um com `jogador` vai só ao dono. Sem ela, o AC 10 não tem de onde ler
os eventos, porque hoje a sala os guarda em `Sala.eventos` e não os envia. `e4.10` não depende de R-05 e
vem antes desta. O AC 14 não muda.)* **Sequencialmente bloqueada por `e4.4`**
— não há contra quem conectar antes disso. *(v1.8.0: R-05 foi ratificada pelo usuário em 2026-09-21,
`architecture-e4.md` §12/R-05, Resolução, e a `e4.4` não espera mais decisão. Resta a ordem: esta story
começa depois da `e4.4`.)* Isto é bloqueio de ordem, não de decisão: nada nesta story espera resposta de
ninguém. *(v1.9.0: deixou de ser verdade para uma parte. Ver o bloco abaixo.)* *(v1.10.0: voltou a ser
verdade. O L-3 foi decidido na §6.2, e nada nesta story espera mais resposta de ninguém.)*

**L-3, o endereço do servidor: DECIDIDO (v1.10.0, `architecture-e4.md` §6.2, commit `5495657`).** A
subtarefa da Task 1 que abre o socket, travada na v1.9.0, **destrava**. Nenhum AC reabre, e o AC 14 fica
como está. A recomendação da §6.1 virou regra, com as três precisões da §6.2:
1. **O endereço sai de `location`, e só dele.** Esquema `wss:` se `location.protocol === 'https:'`, e `ws:`
   se não. Host `location.hostname`, **não** `location.host`, que traz a porta do Vite. Porta numa
   constante nomeada de `client/rede.ts`, com o mesmo valor da constante de escuta de `src/server/main.ts`.
   Nenhum `ws://localhost` escrito à mão, nenhum `import.meta.env` e nenhum parâmetro de servidor no link
   (um `?servidor=` deixaria um link apontar o cliente para o servidor de qualquer um, §6.2, Segurança).
2. O servidor não passa `host` ao `WebSocketServer`, e por isso escuta em todas as interfaces. É isso que
   faz a mesma regra servir às duas abas desta story e ao celular de `e4.7`. É da `e4.4`, e não desta story.
3. A porta de escuta não tem override na Fase 4. Um override deixaria o cliente na porta antiga, e a falha
   seria "não conecta", sem pista.

[AUTO-DECISION] da v1.9.0, confirmado pelo @architect com a tabela de cinco lugares da §6.2: **duas
cópias da porta**, uma em `client/rede.ts` e a de `src/server/main.ts`, cada uma citando a outra no
comentário (arquivo e nome da constante). Nenhum dos cinco lugares de uma cópia única serve no escopo
atual. `src/net/protocolo.ts` está proibido aqui (AC 14) e na `e4.4` (AC 13). `client/` importando
`server/main.ts` quebraria na carga da página, porque o `ws` resolvido para navegador lança
(`node_modules/ws/browser.js`). `server/` não importa `client/` (`e4.4`/AC 12). Um arquivo novo pediria duas
setas novas na §2.2. E `import.meta.env` ainda deixaria dois defaults. A divergência entre as duas cópias não
passa calada: o cliente não conecta, e o caso (vi) do AC 3 põe o erro na tela. **[AUTO-DECISION] Nome da
constante do cliente? → `PORTA_DO_SERVIDOR`, o mesmo nome da do servidor** (motivo: um nome só faz o grep
do gate achar as duas cópias com um padrão, e quem ler uma acha a outra pelo nome). Hoje a do servidor é
`const PORTA_DO_SERVIDOR = 5179` em `src/server/main.ts:38`, e o número não é 5173 nem 4173, as portas
padrão do Vite, como a §6.2 exige.

**O cliente do Pages não conecta, e isso é esperado (§6.2).** A regra aponta um cliente do Pages para
`wss://<usuario>.github.io:<porta>`, onde o servidor não está. A conexão falha, e o caso (vi) do AC 3 mostra
o erro. Sem `#/sala/{id}` na URL, o Pages continua sendo o modo local de hoje. O caminho `wss:` por um host
com certificado só volta ao @architect se a §12/R-08, que é do usuário, disser que o portão precisa de um
aparelho pela internet. Não é desta story.

## Acceptance Criteria

1. `npm run check` verde.
2. `npm run sim:check` verde, com **golden hash idêntico**. O cliente não é coberto pelo `sim:check` e não
   deve passar a ser; o que se exige aqui é que nada tenha vazado para as camadas que são.
3. `src/client/rede.ts` (novo): `WebSocket` do navegador, fila de saída, buffer de snapshots recebidos,
   reconexão com o segredo de assento que `e4.4` emite. É o **único** arquivo novo do cliente.

   **Acréscimo (v1.3.0, `architecture-e4.md` §5.5):** *"Toda mensagem recebida passa por
   `decodificarDoServidor(texto)` (`net/codec.ts`); o cliente não faz `JSON.parse(...) as DoServidor`. O
   buffer guarda o `Snapshot` decodificado, e é dele que `projetar()` e o AC 10 leem."* O `{t:'snap'}`
   chega como tupla posicional. Um `JSON.parse` com cast devolveria a tupla com cara de `Snapshot`, e o
   `tsc` não reclamaria. O decodificador **lança** em aridade divergente (§11.6), e esse erro tem de
   aparecer, não ser engolido. `net/codec.ts` é só **usado** aqui: o AC 14 continua proibindo `src/net/`.

   **Os três casos em que `decodificarDoServidor` lança (v1.4.0, conferido em `e4.8`/`910add8`):**
   (i) texto que não é JSON (o `JSON.parse` interno); (ii) mensagem sem `t` ou com `t` fora da lista
   **fechada** de `DoServidor`; (iii) tupla do `snap` com aridade diferente do layout, ou campo que devia
   ser lista e não é. Os três significam a mesma coisa: servidor e cliente com versões diferentes do
   protocolo, ou fio corrompido. **Decisão do @po sobre o tratamento: logar e fechar a conexão, não
   descartar o quadro.** Toda chamada a `decodificarDoServidor` em `client/rede.ts` fica dentro de
   `try`. No `catch`: `console.error` com a mensagem do erro, fechamento da conexão, e **nenhuma
   reconexão automática** por causa desse erro. A reconexão por segredo de assento continua valendo para
   queda de rede, que é outro caminho. Motivo: num descompasso de versão, **todo** `snap` falha. Descartar
   quadro a quadro deixaria a tela congelada no último estado bom, que é a degradação silenciosa que este
   AC proíbe. Reconectar sozinho repetiria o erro em laço. *(v1.5.0: a frase "mensagem na tela é
   opcional" que estava aqui foi substituída pelo caso (iv) abaixo.)*

   **Quarto caso (iv), v1.5.0 (`architecture-e4.md` §11.6.1):** `{t:'sala'}` com `versao` diferente de
   `VERSAO_DO_FIO`, ou sem `versao`. O decodificador lança (`e4.9`), e o tratamento é o mesmo dos outros
   três: `console.error`, conexão fechada, nenhuma reconexão automática. Como o `{t:'sala'}` é o primeiro
   envio de toda conexão assentada (`e4.4`/AC 8), a conexão fecha antes de o primeiro `snap` ser
   decodificado. Este caso não acrescenta código ao `try`/`catch` que já existe.

   **Mensagem na tela no descompasso de versão: OBRIGATÓRIA (decisão do @po, v1.5.0).** Quando o erro é
   o descompasso de versão de `e4.9`, que traz as duas versões em campos, o cliente mostra na tela uma
   mensagem que separa os dois sentidos, porque o remédio é oposto:
   - **servidor com versão maior que a do cliente** → aba velha: a mensagem diz para **recarregar** a
     página;
   - **servidor com versão menor, ou sem versão** → cliente na frente do servidor (o Pages republica a
     cada push em `master`): a mensagem diz que recarregar **não** resolve e que é preciso **esperar o
     servidor ser atualizado**.
   As duas mensagens mostram os dois números. Para os casos (i)–(iii), que não são descompasso de versão,
   basta o `console.error`. A mensagem é escrita a partir dos **campos** do erro, nunca por parse do texto
   da mensagem de erro. Mora em `client/main.ts` (o `overlay` que ele já controla, `main.ts:66`), ou em
   `client/rede.ts`, sem tocar os cinco arquivos do AC 12. **O AC 14 não muda.**

   **Como o `catch` escolhe o ramo (v1.6.0, gate de `e4.9`, observação para o gate de `e4.5`).** O erro de
   `e4.9` é a classe `DescompassoDeVersao`, exportada de `src/net/codec.ts` com os campos
   `readonly servidor: unknown` e `readonly cliente: number`. Importá-la é uso, como o de
   `decodificarDoServidor`, e o AC 14 continua valendo. A regra:
   - o descompasso é reconhecido **só** por `e instanceof DescompassoDeVersao`. Nunca por `e.name`, nunca
     por `e.message` (nem `includes`, nem regex), e o texto de `e.message` nunca vai para a tela no lugar das
     duas mensagens. Um `catch (e) { mostrar(e.message) }` reprova este AC, porque deixa os campos
     decorativos;
   - com o descompasso reconhecido, o ramo sai **só** dos campos, e são **três**:
     1. `typeof e.servidor === 'number' && e.servidor > e.cliente` → servidor mais novo: "recarregue";
     2. `typeof e.servidor === 'number' && e.servidor < e.cliente` → servidor mais velho: "aguarde o
        servidor";
     3. `e.servidor` não é número (ausente, que é o servidor de antes de `e4.9`, ou de outro tipo, como
        `"1"`) → "aguarde o servidor", com o número do cliente e o servidor mostrado como "sem versão".
   - qualquer outro erro (casos i a iii, e o erro comum que `e4.9` lança para `snapshotHz`/`assento` fora de
     forma) não é descompasso: só o `console.error` e o fechamento, sem mensagem de versão.
   [Fonte: `docs/qa/gates/e4.9-versao-do-fio-e-assento.yml`, `observacoes` (gate de `e4.5`);
   `src/net/codec.ts` `class DescompassoDeVersao` em `6f2f56c`]

   **Quinto caso (v), v1.9.0 (`architecture-e4.md` §6.1): `{t:'erro'}` antes de qualquer `{t:'sala'}`.**
   Com a O-2 decidida, esse caminho passa a existir: id desconhecido (link inventado, ou de uma sala que já
   acabou) ou sala cheia. O servidor responde `{t:'erro'}` e não assenta a conexão, então nenhum `{t:'sala'}`
   chega. O decodificador **aceita** esse quadro, porque só o `sala` passa por `conferirSala`
   (`src/net/codec.ts:481`). Então o caso não cai no `catch` dos casos (i) a (iv), e precisa de ramo
   próprio.
   - Um `{t:'erro'}` recebido **antes** do primeiro `{t:'sala'}` da conexão faz o cliente mostrar uma
     mensagem **na tela**, no mesmo `overlay` do caso (iv), dizendo que o link não abre uma sala (ela não
     existe, já acabou ou está cheia) e que é preciso pedir um link novo a quem subiu o servidor. O cliente
     fecha a conexão e **não reconecta** sozinho. Tela preta, ou tela local parada sem aviso, reprova.
   - [AUTO-DECISION] O texto da tela vem do `motivo` do servidor ou é fixo do cliente? → **fixo do
     cliente**, e o `motivo` vai só para o `console.error` (motivo: o `motivo` de hoje é texto de
     desenvolvedor, com referências de story, como em `src/net/sala.ts:449`. Também é a mesma regra do caso
     (iv), em que o texto do erro nunca vai para a tela).
   - Um `{t:'erro'}` recebido **depois** do `{t:'sala'}` (por exemplo, uma decisão recusada pela sala) é
     outro caso: só `console.error`, e a conexão **continua aberta**. Fechar ali tiraria da partida um
     jogador que só mandou um clique fora de hora.

   **Sexto caso (vi), v1.10.0 (`architecture-e4.md` §6.2): a conexão fecha antes do primeiro
   `{t:'sala'}`, sem que o cliente a tenha fechado.** Servidor desligado, porta errada (as duas cópias de
   `PORTA_DO_SERVIDOR` divergindo), firewall no caminho do celular, ou o cliente do Pages apontando para um
   host sem servidor. O navegador dispara `error` e `close` sem nenhuma mensagem, então o caso não passa pelo
   decodificador nem pelo ramo do caso (v).
   - O cliente mostra uma mensagem **na tela**, no mesmo `overlay` dos casos (iv) e (v), dizendo que não
     alcançou o servidor. **Tela preta, ou tela local parada sem aviso, reprova**, como no caso (v). É o
     mesmo teste do id inventado, pelo outro lado.
   - [AUTO-DECISION] A mensagem mostra o endereço tentado? → **sim**, o `esquema//hostname:porta` montado
     pela regra do "Depende de" (motivo: no celular não há console, e o endereço é o que separa porta errada
     de rede errada na Task 1 de `e4.7`. O endereço não tem segredo nem id de sala). O texto é fixo do
     cliente.
   - O cliente **não reconecta sozinho** neste caso. A reconexão por segredo de assento do AC 11 é do
     jogador já assentado, depois de um `{t:'sala'}`, e não muda. Quem nunca assentou recarrega a página.
   - Quando o próprio cliente fecha a conexão pelos casos (i) a (v), a mensagem do caso (vi) **não**
     aparece por cima da mensagem do caso que fechou.
4. **`client/main.ts` ganha dois modos, e a diferença entre eles é quem tem o estado:**

   | | `local` (hoje) | `conectado` |
   |---|---|---|
   | `criarPartida` / `aplicar` / `registrarRodada` | cliente | **servidor** |
   | `setupDaRodada` / `createWorld` / `step` | cliente | **servidor** |
   | `criarPolitica` / `botCommands` | cliente | **não existe** (não há bot em 1v1) |
   | acumulador de tick | cliente | **não existe** |
   | render, input, telas, telemetria | cliente | cliente |

   **Acréscimo (v1.9.0, `architecture-e4.md` §6 e §6.1): como o cliente escolhe o modo.** Até aqui nenhum
   AC dizia isso.
   - O modo é escolhido **pela URL, na carga da página**. Se `location.hash` tem a forma `#/sala/{id}`
     (§6), com `id` não vazio, o cliente abre no modo `conectado`, e a primeira mensagem que manda na
     conexão é `{t:'entrar', sala: id}`, com o `assento` do AC 11 quando houver um guardado. Se não tem essa
     forma, o cliente abre no modo `local` de hoje, **sem mudança nenhuma**: nenhuma conexão é aberta e
     nenhuma tela nova aparece.
   - **O cliente não cria sala e não ganha tela de criação.** Quem cria é o servidor, e o id chega pelo
     log de operação dele (§6.1). O cliente só lê o id do link. Uma tela de "criar sala" seria tela que
     nenhum documento desenhou (§0), e `telas.ts` está no AC 12.
   - O AC 14 não reabre: `DoCliente` já tem `{t:'entrar'; sala; assento?}` (`src/net/protocolo.ts:134`).

5. **A verificação de P4.2 é um grep, e ela é AC:** no caminho conectado não existe chamada a `step()`,
   nem a `createWorld()`, nem a `aplicar()`. Se existir, o cliente tem autoridade e o critério do portão
   não está satisfeito — por mais que o placar pareça certo.
6. **`disparar()` passa a emitir `{t:'cast'}` sem `tick`** (§4.1). A mira local continua imediata
   (RF-34) porque ela é `client/input.ts` e **não passa por rede** — `input.ts` não muda nem uma linha.
7. **Interpolação** (§5.3): o cliente desenha o instante `agora − ATRASO_DE_BUFFER`, com
   `ATRASO_DE_BUFFER ≥ 1 intervalo de snapshot` + margem de jitter. A constante tem nome e comentário
   dizendo que ela **soma** à latência sentida (§4.4, R-01) — quem a aumentar para suavizar está pagando
   com responsividade, e isso precisa estar escrito onde a pessoa vai mexer.

   **Acréscimo (v1.5.0, decisão O-1 do @po sobre `architecture-e4.md` §11.6.1):** *"O intervalo de
   snapshot que dimensiona `ATRASO_DE_BUFFER` é `1000 / snapshotHz` ms, com `snapshotHz` lido do último
   `{t:'sala'}` recebido, e **não** do `SNAPSHOT_HZ` compilado no bundle. A margem de jitter continua
   sendo uma constante nomeada."* Motivo: a taxa é lever de operação, sobrescrita na subida do servidor
   sem rebuild (`e4.7`/AC 4). Com a constante compilada, a 20 Hz o cliente dimensionaria o buffer para 30,
   esvaziaria o buffer e tremeria, e a varredura de `e4.7` atribuiria o tremor à taxa. Como o `{t:'sala'}`
   vem antes do primeiro `snap` (`e4.4`/AC 8), o cliente sempre sabe a taxa quando começa a interpolar.
8. **O que NÃO interpola, e cada item tem um modo de falha concreto:**
   - **`world.events`** — são de um tick só. Chegam acumulados no snapshot (`e4.2`/AC 5) e são aplicados
     **na chegada**, não interpolados. Interpolar faria os números de dano flutuarem de lugar nenhum.
     (v1.3.0, `architecture-e4.md` §5.6: os `events` do `snap` final da rodada, com o golpe que mata,
     o `death` e o `roundEnd`, são aplicados **antes** de trocar para a tela de fim de rodada. É a
     metade cliente do contrato cujas metades servidor são `e4.3`/AC 10 e `e4.4`/AC 16.)
   - **`alive` e `hp`** — booleano não interpola; morte não é meio-caminho.
   - **início e fim de zona** — uma Muralha existe ou não existe. Interpolar `expiresAt` produz parede
     fantasma.
9. **A prova de compatibilidade de tipo, adiada de `e4.2`/AC 12, acontece aqui.** Com a correção do @po em
   `e4.2`/AC 6, `desenhar` passa a receber `world: VisaoDoMundo`, e a chamada do modo conectado
   — `desenhar(g, cw, ch, projetar(snap, estatico, CHARS), opcoes)` — **é** a prova: se a projeção
   divergir do que o renderizador lê, `tsc` reprova em compilação em vez de a tela ficar preta.

   *(A redação anterior propunha `const _: Parameters<typeof desenhar>[3] = projetar(...)`. Isso só
   funcionaria depois do estreitamento de `e4.2`; antes dele o parâmetro é `World` completo e a asserção
   falharia sempre. Mantida a ideia, removida a asserção redundante: a chamada real já cobre.)*
10. **A telemetria de `e3.5` continua funcionando, e continua local.** `EventoPartida` chega em
    `{t:'evento', e}` (`e4.10`), e `client/rede.ts`/`client/main.ts` entregam `e` ao coletor, como
    `telemetria.registrar(seed, [e])`, com `seed` da última `visao`. O coletor (`client/telemetria.ts`)
    **não muda**. Ele mede o jogador, não a partida. O `controle` do `rodadaFim` vem do servidor e **não é
    reescrito** pelo cliente. *(v1.7.0, `architecture-e4.md` §11.6.2; o texto anterior dizia que
    `EventoPartida` "passa a chegar pelo fio em vez de sair do redutor local", sem variante no fio que o
    levasse.)*

    **Verificação (v1.7.0):** o export de uma partida conectada contém o `rodadaFim` de cada rodada, o
    `partidaFim`, os `cast` e as compras **do próprio** jogador, e **nenhum** evento `compra`,
    `trocaDeBuild` ou `buildPadrao` com `jogador` diferente do jogador do assento (o `jogador` do
    `{t:'sala'}`). Conferido nos exports **das duas abas**, uma no assento do jogador 0 e outra no do
    jogador 1, com o comando e o resultado no Dev Agent Record. Uma conferência só na aba do jogador 0 não
    distingue o filtro certo de um cliente que assume `HUMANO = 0`. Exceção esperada, que não reprova: o
    `rodadaFim` de uma rodada perdida por W.O. enquanto aquele assento estava vago não chega a esse export,
    porque a sala não reenvia eventos no reassentamento (§11.6.2).

    **Detalhe apontado pelo @po, e é onde o AC quebraria em silêncio:** o evento `cast` de RF-36 não vem
    do fio — ele nasce em `main.ts:disparar()`, que hoje lê `world.balls` para achar a bola do jogador e
    os inimigos vivos, e passa isso a `anguloErroGraus` (`client/telemetria.ts:212`). **No modo conectado
    não existe `world`.** A fonte passa a ser a **projeção interpolada** do último snapshot. Isso é
    correto e suficiente — as posições que o jogador estava vendo são exatamente as que devem medir o erro
    de mira dele —, mas precisa ser feito de propósito. Se `disparar()` for adaptado sem isso, `anguloErro`
    sai `null` ou incorreto e RF-36 morre calada no modo que mais importa.
11. **Reconexão exercitada:** fechar a aba no meio de uma rodada e reabrir com o segredo devolve o jogador
    ao mesmo assento, com o placar e a rodada corretos, **sem nenhuma reconciliação no cliente** (§6). Se
    esta story precisar escrever lógica de reconciliação, alguma outra vazou autoridade — parar e escalar.
12. **Arquivos do cliente que NÃO mudam nesta story:** `render.ts`, `telas.ts`, `input.ts`, `layout.ts`,
    `telemetria.ts`. `git diff --stat` prova. *(`render.ts` já terá mudado em `e4.2`, **só em anotações de
    tipo**: o import, `OpcoesRender.minhasBolas`, o `world` de `desenhar` e dos 6 auxiliares, e o `b` de
    `desenharBola`, ver `e4.2`/AC 6 v1.4.0. Aqui ele não muda de novo. Se esta story precisar mexer nele,
    o estreitamento de `e4.2` ficou incompleto e é lá que se corrige.)*

    **Nota do @po (2026-09-21): `OpcoesRender.minhasBolas` é de `e4.2`, não desta story.** Na forma de
    hoje, `minhasBolas: Ball[]` (`render.ts:21`) exige objetos `Ball` do motor, que o modo conectado não
    tem como fornecer, porque não existe `World` no cliente. Estreitar o **tipo** (`Ball[]` →
    `BolaVisivel[]`) é uma anotação em `render.ts` e foi para `e4.2`/AC 6, pela regra desta própria nota
    de rodapé. **O que fica aqui é a FONTE do valor**, em `client/main.ts`: hoje
    `minhasBolas = (): Ball[] => world.balls.filter(...)` (`main.ts:298`) alimenta `desenhar` (`:420`) e
    `disparar()` (`:302`, `:356`). No modo conectado ela passa a sair da projeção interpolada, filtrada por
    `meuLado`, pela mesma razão e da mesma fonte que o AC 10 já exige para o `anguloErro`. Se sair de outro
    lugar, a mira e os botões de habilidade desenham a bola errada, ou nenhuma bola.
13. **Verificação de ponta a ponta:** duas abas completam uma Bo5 inteira — draft, builds, rodadas, loja,
    placar — cada uma vendo apenas a própria build até a largada (RF-05).
    *(v1.9.0, `architecture-e4.md` §6.1.)* As duas abas abrem o **mesmo link**, montado com o id copiado
    da linha de log do servidor (`e4.4`/AC 6, item 2). O Dev Agent Record registra a linha usada (ela tem
    só o id, sem segredo e sem seed). **Armadilha:** quando a primeira aba senta, o servidor cria a próxima
    sala livre e escreve outra linha. A segunda aba usa o link da primeira, e não a última linha do log.
14. **Escopo de arquivos.** Criado: `src/client/rede.ts`. Alterado: `src/client/main.ts`. Proibido:
    `src/sim/`, `src/match/`, `src/shop/`, `src/bot/`, `src/chars/`, `src/net/`, `src/server/`,
    `src/tools/`, `docs/`, e os cinco arquivos do AC 12.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend / Networking
**Secondary Type(s)**: Gate criterion (P4.2)
**Complexity**: Medium-High — o risco não é o WebSocket, é o `main.ts` virar dois programas mal separados.
O modo local precisa continuar existindo inteiro: ele é o modo de treino (RF-43) e o único jogável sem
servidor.

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa (quality gate — AC 5, AC 12 e AC 8 são conferíveis mecanicamente e é onde a regressão se esconde)

**Supporting Agents**:
- @ux-design-expert (consulta, se a interpolação exigir mudança visual perceptível — não previsto)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Rodar antes de marcar a story como completa
- [ ] Pre-PR (@github-devops): Rodar antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Qualquer `step()`, `createWorld()` ou `aplicar()` alcançável no modo conectado (AC 5)
- `hp`, `alive` ou zonas sendo interpolados (AC 8)
- Regressão no modo local — ele precisa continuar jogável inteiro

**Secondary Focus**:
- `ATRASO_DE_BUFFER` sem comentário sobre o custo em latência (AC 7)
- `ATRASO_DE_BUFFER` calculado a partir de `SNAPSHOT_HZ` importado, em vez do `snapshotHz` recebido (AC 7)
- Descompasso de versão sem mensagem na tela, ou com a mensagem escolhida por parse do texto do erro (AC 3 iv)
- Descompasso reconhecido por algo que não seja `instanceof DescompassoDeVersao`, `servidor` não numérico
  caindo em "recarregue", ou `e.message` mostrado na tela (AC 3 iv, v1.6.0)
- Alteração acidental em `input.ts` (AC 6, AC 12)
- `HUMANO = 0` assumido no caminho conectado, ou `controle` do `rodadaFim` reescrito no cliente (AC 10, v1.7.0)
- `{t:'erro'}` antes do `{t:'sala'}` sem mensagem na tela, com o `motivo` do servidor na tela, ou com
  reconexão em laço; `{t:'erro'}` depois do `{t:'sala'}` fechando a conexão (AC 3 v, v1.9.0)
- Modo local mudando de comportamento sem `#/sala/{id}` na URL, ou tela de criação de sala (AC 4, v1.9.0)
- Endereço do servidor escrito à mão (`ws://localhost`...) em vez de sair de `location`, ou porta sem
  constante nomeada (L-3, v1.9.0; decidido na §6.2, v1.10.0)
- *(v1.10.0, §6.2)* `location.host` no lugar de `location.hostname` (a URL sairia com a porta do Vite
  grudada no host); `import.meta.env` lendo endereço ou porta; parâmetro de servidor no link (`?servidor=`
  ou parecido); comentário de `PORTA_DO_SERVIDOR` em `client/rede.ts` sem citar `src/server/main.ts` e o nome
  da constante de lá
- *(v1.10.0, AC 3 vi)* Conexão que fecha antes do `{t:'sala'}` sem mensagem na tela, com reconexão em laço,
  ou com a mensagem do caso (vi) por cima da de um caso (i)–(v)

## Tasks / Subtasks

- [x] Task 1 — `client/rede.ts` (AC: 3, 11)
  - [x] *(v1.10.0, L-3 decidido na §6.2, destravada)* Abrir o socket: esquema `wss:` se
        `location.protocol === 'https:'`, e `ws:` se não; host `location.hostname`; porta na constante
        `PORTA_DO_SERVIDOR` de `client/rede.ts`, com o mesmo literal de `src/server/main.ts:38` e um
        comentário que cita `src/server/main.ts` e o nome da constante de lá. Sem `import.meta.env` e sem
        parâmetro de servidor no link ("Depende de")
  - [x] *(v1.10.0)* Conexão que fecha antes do primeiro `{t:'sala'}` sem o cliente ter fechado: mensagem
        fixa na tela com o endereço tentado, sem reconexão automática (AC 3 vi)
  - [x] Conexão, fila de saída, buffer de snapshots
  - [x] Toda mensagem recebida por `decodificarDoServidor(texto)`; nenhum `JSON.parse(...) as DoServidor`;
        o buffer guarda o `Snapshot` decodificado (AC 3)
  - [x] `decodificarDoServidor` dentro de `try`; no `catch`, `console.error` + fechar a conexão, sem
        reconexão automática por esse motivo (AC 3, v1.4.0)
  - [x] Descompasso de versão (AC 3 iv, v1.5.0): mesmo `catch`, e mensagem na tela escolhida pelos
        campos do erro de `e4.9` — "recarregue" se o servidor é mais novo, "aguarde o servidor" se é mais
        velho ou não tem versão — com as duas versões; dentro de `rede.ts`/`main.ts`
  - [x] *(v1.6.0)* Ramo por `e instanceof DescompassoDeVersao`, nunca por `name`/`message`; três ramos
        pelos campos (`servidor` número maior, número menor, não número) (AC 3 iv)
  - [x] *(v1.9.0)* `{t:'erro'}` antes do primeiro `{t:'sala'}`: mensagem fixa na tela, `motivo` só no
        `console.error`, conexão fechada, sem reconexão automática; `{t:'erro'}` depois do `{t:'sala'}`:
        só `console.error`, conexão aberta (AC 3 v)
  - [x] Reconexão por segredo de assento, sem reconciliação

- [x] Task 2 — Dois modos em `main.ts` (AC: 4, 5, 6)
  - [x] *(v1.9.0)* Modo pela URL na carga: `#/sala/{id}` → `conectado` e `{t:'entrar', sala: id}`; sem
        isso, `local` sem mudança. Nenhuma tela de criação de sala (AC 4)
  - [x] Separar o que é do modo local do que é comum
  - [x] Caminho conectado sem `step`/`createWorld`/`aplicar`
  - [x] `disparar()` → `{t:'cast'}` sem tick
  - [x] No modo conectado, `minhasBolas` sai da projeção interpolada, filtrada por `meuLado` (nota do
        @po no AC 12). O tipo já é `BolaVisivel[]` desde `e4.2`

- [x] Task 3 — Interpolação (AC: 7, 8)
  - [x] `ATRASO_DE_BUFFER` nomeada e comentada
  - [x] Intervalo de snapshot = `1000 / snapshotHz` do último `{t:'sala'}`, não o `SNAPSHOT_HZ` compilado;
        margem de jitter em constante nomeada (AC 7, v1.5.0)
  - [x] Interpolar posição, velocidade e ângulo
  - [x] **Não** interpolar eventos, `alive`/`hp`, início/fim de zona

- [x] Task 4 — Prova de tipo (AC: 9)
  - [x] A chamada real `desenhar(g, cw, ch, projetar(snap, estatico, CHARS), opcoes)` compila sem cast
        (AC 9; a asserção `Parameters<typeof desenhar>[3]` foi removida na v1.1.0 por ser redundante)

- [x] Task 5 — Telemetria (AC: 10)
  - [x] Eventos do fio alimentam o coletor existente, sem tocá-lo
  - [x] *(v1.7.0)* `{t:'evento', e}` → `telemetria.registrar(seed, [e])`, `seed` da última `visao`;
        `controle` do `rodadaFim` não reescrito; nenhum `HUMANO = 0` no caminho conectado
  - [x] *(v1.7.0)* Exports das duas abas conferidos: nenhum `compra`/`trocaDeBuild`/`buildPadrao` com
        `jogador` de outro assento

- [x] Task 6 — Verificação (AC: 1, 2, 12, 13, 14)
  - [x] `check` e `sim:check` verdes
  - [x] Duas abas, Bo5 completa, segredo de build preservado até a largada. *(v1.9.0: as duas com o mesmo
        link, id copiado da linha de log do servidor; linha registrada no Dev Agent Record, AC 13)*
  - [x] `git diff --stat` prova os cinco arquivos intactos

## Dev Notes

### Por que P4.2 sai por subtração (fonte: §8.1)

A forma cara de passar em *"o cliente não decide dano"* é validar o que o cliente manda. A forma barata é
não haver o que validar: sem `World` e sem `step()`, divergir o cliente artificialmente altera **o que
aquele jogador vê**, e nada mais. O AC 5 é essa afirmação transformada em grep.

### O modo local não é legado — é requisito

RF-43 diz que o mesmo bot serve a balanceamento, modo treino e oponente solo. O modo local é o treino, e é
o único jogável sem servidor. Esta story **não** o substitui; ela o mantém intacto ao lado do conectado. Um
`main.ts` em que o modo local deixou de funcionar reprova, mesmo com o conectado perfeito.

### A latência sentida se acumula, e o buffer é a terceira parcela (fonte: §4.4, R-01)

```
latência sentida = ida (RTT/2) + 100ms (ATRASO_ALVO_TICKS) + ATRASO_DE_BUFFER
```

O comentário do AC 7 existe porque a tentação, ao ver movimento tremido, é aumentar o buffer — e o preço
sai do bolso da responsividade, que é o que P4.4 vai julgar. R-01 já está devolvida ao @pm com esse número.

### Eventos são o caso que a interpolação estraga em silêncio

Com `SNAPSHOT_HZ = 30`, cada snapshot carrega os eventos de dois ticks (`e4.2`/AC 5). Se o cliente os
interpolar junto da posição, os números de dano aparecem em posições que nunca existiram; se os descartar,
somem. Aplicar na chegada é a única leitura correta, e é por isso que o AC 8 é explícito em vez de óbvio.

### No modo conectado, o jogador local pode ser o 1 (v1.7.0, fonte: §11.6.2)

`client/main.ts:61` fixa `const HUMANO: Jogador = 0`, e o modo local continua assim. No modo conectado, o
jogador local é o `jogador` do `{t:'sala'}`, e pode ser o 1. Nada do caminho conectado assume
`HUMANO = 0`: nem as decisões que o cliente monta (`draft`, `build`, `pronto`, `compra`, `trocaDeBuild`,
`main.ts:250-257`), nem `meuLado` (`:195`), nem o filtro de `minhasBolas`, nem a leitura dos eventos. A sala
recusa uma decisão com `d.jogador` de outro assento (`e4.3`/AC 16), então um cliente que assuma 0 no
assento 1 não joga. E o `controle` do `rodadaFim` chega do servidor e não é reescrito.

### O que esta story explicitamente NÃO faz

- **Não grava replay** (`e4.6`).
- **Não decide `SNAPSHOT_HZ`** (R-03) nem liga a compensação de RTT (R-01) — as duas saem de `e4.7`/P4.4.
- **Não escolhe política de desconexão** (R-02) — o cliente só reage ao que a sala mandar.
- **Não muda o renderizador nem a camada de telas.**

### Testing

- `npm run check` e `npm run sim:check` verdes.
- Duas abas: Bo5 completa; cada jogador vê só a própria build até a largada.
- Reconexão no meio da rodada volta ao mesmo assento e ao mesmo placar.
- Grep: `step(`, `createWorld(`, `aplicar(` inalcançáveis no caminho conectado.
- Grep: nenhum `JSON.parse` com cast para `DoServidor` em `src/client/`; a entrada passa por
  `decodificarDoServidor` (AC 3).
- Decode que lança (AC 3): mandar do devtools, ou de um servidor adulterado, uma mensagem com `t`
  desconhecido e um `snap` com uma posição a menos. Nos dois casos, `console.error` aparece, a conexão
  fecha e o cliente não tenta reconectar em laço.
- Descompasso de versão (AC 3 iv, v1.5.0), verificação manual: um servidor local rodando com
  `VERSAO_DO_FIO + 1` (cópia descartável ou mudança local não commitada) faz o cliente logar, fechar a
  conexão, **não** reconectar e mostrar a mensagem de recarregar, com as duas versões. Repetir com um
  servidor de versão menor (ou sem o campo) e confirmar a mensagem de esperar o servidor. Registrar no
  Dev Agent Record.
- *(v1.6.0)* Os **três** ramos do AC 3 (iv) são rodados **separadamente**, não dois: (1) servidor com
  `VERSAO_DO_FIO + 1` → "recarregue"; (2) servidor com versão numérica menor (`0` basta, enquanto
  `VERSAO_DO_FIO` for 1; depois de `e4.10`, que o sobe para 2, `0` e `1` servem, v1.7.0) → "aguarde o servidor"; (3) servidor **sem** `versao` no `{t:'sala'}` →
  "aguarde o servidor" com "sem versão". Um quarto caso, `snapshotHz: 7` com a versão certa, tem de
  fechar a conexão **sem** mensagem de versão (é erro comum, não descompasso). Os quatro vão para o Dev
  Agent Record.
- *(v1.6.0)* Grep em `src/client/`: o ramo do descompasso usa `instanceof DescompassoDeVersao`, e nenhum
  `.message` do erro de decodificação decide o ramo nem vai para a tela (fora do `console.error`).
- Taxa (AC 7, v1.5.0): com o servidor subido a 20 Hz pelo override de `e4.7` (ou por mudança local), o
  cliente dimensiona o buffer para 50 ms + margem, e não para o intervalo de 30 Hz.
- Fim de rodada: o número flutuante do golpe que mata aparece antes da tela de fim de rodada (AC 8).
- Export de telemetria de uma partida conectada contém `rodadaFim`, `compra` e `cast`. *(v1.7.0: nas duas
  abas, cada export contém o `rodadaFim` de cada rodada, o `partidaFim`, os `cast` e as compras do
  próprio jogador, e nenhum `compra`/`trocaDeBuild`/`buildPadrao` com `jogador` diferente do jogador do
  assento, AC 10.)*
- `git diff --stat` prova os cinco arquivos intactos do AC 12.
- Modo local jogado uma vez, para confirmar que não regrediu.
- *(v1.9.0, AC 4)* Sem `#/sala/{id}` na URL, o modo local abre como hoje, e nenhuma conexão aparece na
  aba Rede do devtools. Com o link, a primeira mensagem enviada é `{t:'entrar', sala: id}`.
- *(v1.9.0, AC 3 v)* **Link com id inventado** (`#/sala/xxxxxxxx`): a tela mostra a mensagem de link
  inválido, **não fica preta**, e o cliente não tenta reconectar. Repetir com o link de uma sala cheia
  (uma terceira aba) e com o de uma sala que já acabou. Depois, já assentado, mandar uma decisão fora de
  hora (um `{t:'cast'}` fora da rodada, que a sala recusa em `src/net/sala.ts:491`): o `{t:'erro'}` sai
  só no console, e a partida continua.
- *(v1.9.0, AC 13)* As duas abas com o mesmo link, montado com o id da linha de log. A linha copiada fica
  no Dev Agent Record.
- *(v1.10.0, AC 3 vi, §6.2)* **Servidor desligado:** abrir um link `#/sala/{id}` sem o servidor de pé. A
  tela mostra a mensagem de servidor não alcançado com o endereço tentado, **não fica preta**, e o cliente
  não tenta reconectar em laço (aba Rede do devtools). **Porta errada:** repetir com o servidor de pé e a
  `PORTA_DO_SERVIDOR` do cliente trocada por outro número numa mudança local não commitada. É assim que uma
  divergência entre as duas cópias aparece. As duas rodadas vão para o Dev Agent Record.
- *(v1.10.0, §6.2)* **Grep das duas cópias:**
  `grep -nE '^(export )?const PORTA_DO_SERVIDOR = [0-9]+$' src/client/rede.ts src/server/main.ts` imprime
  **exatamente duas linhas**, uma por arquivo, com o **mesmo** número. E
  `grep -rnE 'location\.host\b|import\.meta\.env|ws://localhost|wss?://127\.|[?&]servidor=' src/client/`
  sai com 1 (nenhuma ocorrência). O `\b` depois de `host` deixa `location.hostname` passar.
- *(v1.10.0)* A URL montada, conferida no devtools em três origens: `http://localhost:<porta-do-vite>` dá
  `ws://localhost:<PORTA_DO_SERVIDOR>`, sem a porta do Vite; o IP da LAN com `npm run dev -- --host` dá
  `ws://<ip>:<PORTA_DO_SERVIDOR>`; e `[::1]`, se testado, sai com colchetes.

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (1M context), como Dex (@dev), modo YOLO, em 2026-09-22.

### File List

Commit de implementação: **`205e693`**, pai `1d75b8d`. `git show --stat 205e693` lista só os dois arquivos abaixo
(813 inserções, 5 remoções).

| Arquivo | Ação | O quê |
|---|---|---|
| `src/client/rede.ts` | **criado** (AC 3, 7, 8, 11) | endereço só de `location` e `PORTA_DO_SERVIDOR = 5179` (cópia da de `src/server/main.ts`), `{t:'entrar'}` como primeira mensagem, fila de saída, `pong`, `decodificarDoServidor` em `try` com os casos i a vi, `avisoDeVersao` pelos campos, buffer + `amostrar`/`interpolar`, `atrasoDeBuffer(snapshotHz)`, reconexão por segredo em `sessionStorage` |
| `src/client/main.ts` | alterado (AC 4, 5, 6, 9, 10, 12) | `lerModo` pela URL; seção `INÍCIO/FIM DO MODO CONECTADO` com as ações pelo fio, a tela pela última `visao`, os avisos, o laço de desenho pela projeção interpolada, as bolas do jogador, o cast e a telemetria; no código local, 4 desvios de uma linha (`disparar`, espaço, `if (!world)` e a bola do atalho), mais os imports e o parágrafo do cabeçalho |

Intactos, conferido com `git diff --stat` (saída vazia): `render.ts`, `telas.ts`, `input.ts`, `layout.ts`,
`telemetria.ts`, `src/sim/`, `src/match/`, `src/shop/`, `src/bot/`, `src/chars/`, `src/net/`, `src/server/`, `src/tools/`,
`package.json`. Os arquivos de `e4.6` (`src/server/main.ts`, `src/net/replay.ts`, `src/tools/*`, `package.json`),
que outro @dev editava ao mesmo tempo, **não** foram adicionados ao commit.

### Debug Log References

Os scripts e as saídas ficaram no scratchpad da sessão, fora do repositório: `rede-harness.mjs`, `cdp.mjs`,
`e2e.mjs`, `neg.mjs`, `analisa.mjs`, `e2e/`, `e2e2/` e `neg/`, com `relatorio.txt`, `dump.json`, os dois exports
e os screenshots de fim de rodada.

### Completion Notes

**Gates, na árvore do commit.** Uma cópia descartável com `git archive 1d75b8d` e os dois arquivos do commit,
o que dá exatamente a árvore de `205e693`. Usei a cópia porque o `e4.6` estava com `src/server/main.ts` pela
metade na árvore compartilhada.

| gate | resultado |
|---|---|
| `npm run check` | exit 0 na cópia. Na árvore compartilhada, exit 0 antes do commit. Numa rodada anterior, falhou só em `src/server/main.ts`, por imports ainda sem uso da `e4.6` em andamento |
| `npm run sim:check` | exit 0, **`golden hash ✓ ok — 6 seeds batem o baseline`**, 0 linhas `✗` (AC 2) |
| `npm run build` | exit 0 (`index-*.js` 65,39 kB), com a saída na cópia |
| `grep -nE '^(export )?const PORTA_DO_SERVIDOR = [0-9]+$' src/client/rede.ts src/server/main.ts` | exatamente duas linhas, `src/client/rede.ts:36:const PORTA_DO_SERVIDOR = 5179` e `src/server/main.ts:38:const PORTA_DO_SERVIDOR = 5179` (em `205e693`; na árvore compartilhada, com a `e4.6` em andamento, é a linha 43) |
| `grep -rnE 'location\.host\b\|import\.meta\.env\|ws://localhost\|wss?://127\.\|[?&]servidor=' src/client/` | **exit 1** |
| P4.2, AC 5: `sed -n '/INÍCIO DO MODO CONECTADO/,/FIM DO MODO CONECTADO/p' src/client/main.ts \| grep -cE '\b(step\|createWorld\|aplicar)\('` e o mesmo grep em `src/client/rede.ts` | `0` e `0` |
| AC 3: `grep -n 'JSON.parse' src/client/rede.ts` | nenhuma linha. A entrada é só `decodificarDoServidor` |
| AC 3 (iv): `instanceof DescompassoDeVersao` | `rede.ts:259` é o único ramo. `e.message` aparece só em `rede.ts:256`, dentro do `console.error` |
| AC 7: `SNAPSHOT_HZ` em `rede.ts` | só em 2 comentários, sem import. O atraso é `atrasoDeBuffer(snapshotHz)`, com o `snapshotHz` do `{t:'sala'}` |
| CodeRabbit (Quality Gate Task pre-commit) | **não executável aqui**: `wsl --status` respondeu "O Subsistema do Windows para Linux não está instalado" (reconfirmado em 2026-09-22). Coberto pelos gates acima, pela bateria dirigida e pelo self-critique |

**Método da verificação de navegador (registrado conforme pedido).** As ferramentas `claude-in-chrome` não estavam
disponíveis nesta sessão. Usei **Chrome 153 headless dirigido por CDP** (`--remote-debugging-port`, scripts Node
24 com `WebSocket` nativo). Cada aba abre numa janela própria, com foco emulado. O servidor sobe por `npm run server`
e o cliente por `npm run dev -- --port 5191 --strictPort`, os dois a partir da cópia descartável. Todos os
processos que subi (servidores, Vite, Chrome) foram mortos com `taskkill /T /F`. Depois conferi que nenhuma das
portas 5188, 5189, 5191, 5192, 9333 e 9334 ficou em escuta e que nenhum `chrome.exe` com os meus perfis ficou vivo.
**A porta 5179 estava ocupada pelo `npm run server` da `e4.6`** (PID de outro agente, não mexi). Por isso, nas
corridas de navegador, as **duas** cópias da constante foram trocadas para 5189 na cópia descartável, com o
mesmo literal dos dois lados. A regra do endereço com o literal real 5179 está provada pela bateria dirigida
abaixo (`ws://localhost:5179`) e pelo grep dos literais.

**1. Bateria dirigida de `rede.ts`** (`rede-harness.mjs`: `WebSocket`, `location`, `sessionStorage` e relógio
falsos, importando o `rede.ts` real). **77 ok, 0 falhas.** Cobre:
- **Endereço (§6.2):** `http:`/`localhost` dá `ws://localhost:5179`, sem a porta do Vite. `https:` no Pages dá
  `wss://usuario.github.io:5179`, `[::1]` dá `ws://[::1]:5179`, e a LAN dá `ws://192.168.0.10:5179`.
- **AC 4:** a primeira mensagem é `{"t":"entrar","sala":"ABC"}`. Depois de um `{t:'sala'}`, recarregar manda
  `{"t":"entrar","sala":"ABC","assento":"S1"}`.
- **Fila e cast:** a decisão fica na fila até o `{t:'sala'}`, e o cast sem assento é descartado. O cast
  assentado sai sem `tick` (AC 6). `ping` recebe `pong` com o mesmo `id`.
- **Casos i, ii e iii** (texto que não é JSON, `t` desconhecido, `snap` com uma posição a menos): `console.error`,
  conexão fechada pelo cliente (1000), um socket só em 1,3 s (sem reconexão) e nenhum aviso.
- **Caso iv, três ramos pelos campos:** versão 3 dá `recarregue` com `3/2`. Versões 1 e 0 dão `aguarde` com `1/2`
  e `0/2`. Sem `versao`, e com `"3"` em string, dá `aguarde` com `servidor: null`. `snapshotHz: 7` com a versão
  certa fecha sem aviso de versão.
- **Caso v:** um `{t:'erro'}` antes do `{t:'sala'}` dá o aviso `linkInvalido`. O `motivo` vai só ao
  `console.error` e não entra no aviso, e a conexão fecha sem reconexão. Um `{t:'erro'}` depois do `{t:'sala'}`
  gera só `console.error`, e a conexão fica aberta.
- **Caso vi:** um `close` antes do `{t:'sala'}` dá o aviso `semServidor` com o endereço, sem reconexão. Quando o
  próprio cliente fechou pelos casos ii ou iv, o aviso do caso vi **não** aparece por cima.
- **AC 11:** quem já estava sentado e cai tenta de novo em ~1 s, reapresenta o segredo e reassenta sem aviso. O
  teto é de 5 tentativas seguidas, e depois vem o aviso `semServidor`. O código 4000 dá o aviso
  `assentoEmOutraAba`, sem reconexão. Uma queda depois de `encerrada` não reconecta nem avisa.
- **AC 7:** a 20 Hz o cliente desenha em `agora − 150 ms` (1000/20 + 50), e a 30 Hz em `agora − 83,33 ms`. Com
  os mesmos snapshots, o atraso muda só pelo `snapshotHz` do `{t:'sala'}`.
- **AC 8:** os eventos chegam ao receptor **na chegada**, e o snapshot desenhado tem `events: []`. `hp`, `alive` e
  `ultCharge` vêm do snapshot anterior, sem interpolar. Uma zona que só existe no seguinte não aparece antes, sem
  parede fantasma. O ângulo 3,1 → −3,1 passa por π, pelo caminho curto. O instante desenhado não volta atrás.
  Depois de uma pausa de 5 s (R-02), o relógio realinha e volta a interpolar.

**2. Duas abas, Bo5 completa (AC 13, 4, 5, 6, 8, 10, 12; RF-05).** Foram duas corridas (`e2e.mjs`).
- **Linha de log usada** (a primeira, e não a última):
  `[op 2026-09-22T04:56:20.327Z] sala criada: id=xtlsUQEiK5mLZcINvKtMLA caminho=/#/sala/xtlsUQEiK5mLZcINvKtMLA`.
  Quando a aba A sentou, o servidor escreveu
  `sala criada: id=n5XVK1OM3FyNqWKJTNe9Iw …`, e a aba B abriu o link da primeira linha, como pede a armadilha
  do AC 13. Na primeira corrida, a linha foi `id=FwY45UZ3wQcCPgyc9C-8TQ`.
- **Assentos:** A é o jogador 0 e B é o jogador 1. As duas mandaram
  `{"t":"entrar","sala":"xtlsUQEiK5mLZcINvKtMLA"}` como primeira mensagem ao servidor do jogo.
- **Partida:** draft, builds, 3 rodadas, loja com compra nas duas abas, e fim com "Você perdeu 0 — 3" em A e
  "Você venceu 3 — 0" em B, nas duas corridas. Na rodada 2, o HUD de A pintou "VOCÊ" com a cor do lado 1,
  porque o lado alterna e o placar é o real. A sala foi "encerrada e descartada" no log.
- **RF-05:** nas 3 `visao` da fase builds de cada aba, 0 objetos do oponente com `abilityIndex`/`passiveIndex`.
- **Prazo de RF-04:** B deixou o prazo correr na 1ª rodada. A tela contou de "Builds 30S" até "Builds 1S", e a
  build padrão veio do servidor (`buildPadrao:j1` no export de B).
- **AC 6:** foram 280 e 288 `{t:'cast'}` de teclado, com 0 levando `tick` e 0 decisões com o jogador de outro
  assento.
- **AC 8:** os screenshots do `rodadaFim` (`e2e2/fim-rodada-*.png`) mostram o **canvas, e não o overlay**, com
  o número flutuante do golpe final ("16") na tela. O overlay estava fechado antes do screenshot nas 3 rodadas.
- **Erro depois de sentado (caso v, 2ª parte):** A clicou "Pronto" duas vezes. O console mostrou
  `[rede] o servidor recusou uma mensagem: jogador 0 já declarou pronto`, e A seguiu jogando. B teve um
  `bola 1 do jogador 1 está morta — cast descartado`, só no console.
- **Sala cheia:** a 3ª aba no mesmo link, durante o draft, recebeu na tela "Este link não abre uma sala — A
  sala não existe, já acabou ou está cheia. Peça um link novo a quem subiu o servidor.", com 1 socket ao jogo
  e o `motivo` só no console. **Sala acabada:** uma aba nova no link depois do fim mostrou a mesma tela.
- **AC 10, exports das duas abas** (botão `#exportar`, download pelo Chrome), na corrida sem recarga:
  - A (jogador 0): 285 eventos, com `rodadaFim` ×3, `partidaFim` ×1, `cast` ×280 e `compra:j0` ×1. Eventos com
    `jogador`: só `compra:j0`.
  - B (jogador 1): 294 eventos, com `buildPadrao:j1` ×1, `rodadaFim` ×3, `partidaFim` ×1, `cast` ×288 e
    `compra:j1` ×1. Eventos com `jogador`: só `buildPadrao:j1` e `compra:j1`.
  - Nas duas: a mesma seed (`3940912704`), da `visao`. Todo `rodadaFim` com `controle ["humano","humano"]`, como
    veio do servidor. Todo `cast` com `anguloErro` numérico (0 `null`, 0 `-1`), medido na projeção interpolada.
  - `{t:'evento'}` recebidos com o `jogador` do outro assento: 0 nas duas abas.
- **AC 11:** no meio da 2ª rodada, B saiu para `about:blank`, esperou 3 s e voltou ao mesmo link na mesma aba.
  - Antes: `jogador 1`, placar `[1,0]`, rodada 1.
  - Depois: `jogador 1`, placar `[1,0]`, rodada 1, fase `rodada`, com o overlay fechado e a partida seguindo.
    A primeira mensagem foi `{"t":"entrar","sala":…,"assento":"<segredo>"}`, e chegou um `rodadaInicio` novo.
  - Nenhuma linha de reconciliação.
  - Servidor: `conexão #2 derrubada: assento reassentado pela conexão #4`, código 4000.
- **AC 4 (sem hash):** a aba sem `#/sala/{id}` abriu "Draft escolha 1 de 4", o modo local contra o bot. O único
  socket foi o do HMR do Vite (`ws://localhost:519x/?token=…`), com 0 sockets ao servidor do jogo.

**3. Bateria negativa em navegador real** (`neg.mjs`). O servidor roda de outra cópia, adulterada, e o cliente
não muda. Em todos os casos abaixo houve 1 socket ao jogo, fechado, e **nenhuma segunda tentativa**.
- **Servidor desligado (vi):** "Servidor não alcançado — Não foi possível conectar a ws://localhost:5189. Confira
  se o servidor está de pé e acessível desta rede, e recarregue a página." Console:
  `[rede] sem conexão com o servidor em ws://localhost:5189 (código 1006); sem reconexão automática`.
- **Porta errada (vi):** `rede.ts` da cópia servida pelo Vite trocado para 5188, com o servidor em 5189. A tela
  mostrou a mesma mensagem com `ws://localhost:5188`.
- **Versão 3 no servidor (iv-1):** `{ t: 'sala', versao: 3, … }` em `sala.ts` da cópia do servidor. A tela mostrou
  "Página desatualizada — Esta página fala a versão 2 do protocolo, e o servidor está na versão 3. Recarregue a
  página."
- **Versão 1 no servidor (iv-2):** "Servidor desatualizado — … o servidor está na versão 1. Recarregar não
  resolve: espere o servidor ser atualizado."
- **Servidor sem `versao` (iv-3):** "Servidor desatualizado — … o servidor está sem versão. Recarregar não
  resolve: …"
- **`snapshotHz: 7` com a versão certa:** `console.error` com
  `{t:'sala'} com snapshotHz 7 — tem de ser inteiro positivo divisor de 60`, conexão fechada e nenhuma mensagem
  de versão. A tela ficou no status "Conectando…" (ver F-6).
- **Controle, com o servidor sem adulteração:** "Aguardando o oponente…", sem erro e com a conexão aberta.
- **Id inventado (`#/sala/xxxxxxxx`, v):** "Este link não abre uma sala …", e o `motivo` do servidor só no
  console.
- **Servidor derrubado com dois jogadores sentados no draft (AC 11):** cada aba abriu 6 sockets, o original mais
  5 tentativas, e parou na tela "Servidor não alcançado". Não houve laço.

**[AUTO-DECISION]s desta implementação:**
1. Onde fica o segredo de assento? → **`sessionStorage`, por sala** (`battle-balls:assento:{id}`). Motivo: o AC 13
   põe duas abas do mesmo navegador no mesmo link. Com `localStorage` a segunda aba reapresentaria o segredo da
   primeira e a derrubaria (código 4000). `sessionStorage` sobrevive a recarregar, a sair e voltar na mesma aba e
   à reabertura da aba fechada. Uma aba nova e vazia é jogador novo.
2. Reconexão do jogador sentado → **1 s entre tentativas, no máximo 5 seguidas**. O contador zera a cada
   `{t:'sala'}`, e depois do teto vem o aviso do caso vi. Motivo: a story pede reconexão sem laço e não dá número.
3. Código 4000 (assento retomado em outra conexão, `e4.4`/AC 7) → **não reconecta, e a tela diz em texto fixo que
   a partida continua em outro lugar**. Motivo: reconectar derrubaria a outra aba, e as duas se derrubariam em laço.
4. Queda depois de `{t:'sala', estado:'encerrada'}` → **nem reconexão nem aviso**. A sala já saiu do servidor, e o
   aviso de link inválido cobriria a tela de fim.
5. `{t:'cast'}` sem assento → **descartado, e não enfileirado**. Mira é do instante. As decisões esperam na fila
   até o `{t:'sala'}`.
6. `MARGEM_DE_JITTER_MS = 50`, ponto de partida com o mesmo estatuto de R-03 (quem mede é `e4.7`), e
   realinhamento do relógio de exibição quando um snapshot chega mais de 500 ms além da menor distância vista (a
   pausa de R-02).
7. Interpolação: o estado discreto e as **listas** (projéteis, zonas) vêm do snapshot anterior, e só posição,
   ângulo, velocidade e `time` interpolam. É a leitura literal do AC 8.
8. A tela de fim de rodada espera o instante desenhado alcançar o snapshot final (`exibiuOUltimo`). É a metade
   cliente da §5.6, porque sem isso o `rodadaFim` e a `visao` da loja chegam no mesmo lote e o overlay cobriria o
   golpe final. Pela mesma razão, um cast é bloqueado entre o `rodadaFim` e a troca de tela, no papel do
   `world.over` local.
9. Status no overlay ("Conectando…", "Aguardando o oponente…") antes da primeira `visao`, para não ficar tela
   preta. Não é tela de criação de sala, e usa os mesmos estilos das telas.
10. "Nova partida" na tela de fim, no modo conectado → `location.reload()`. A sala acabou, o servidor responde que
    o id não existe, e o aviso do caso v diz para pedir um link novo. Nenhuma tela nova.
11. Espaço (pausa) no modo conectado → **sem efeito**. Pausar é parar a simulação local, e quem simula é o servidor.
12. `meuLado` no modo conectado → a mesma `ladosDaRodada` de `match/`, alimentada com `regras` e `rodada` da visão
    por um cast. Reescrever a regra aqui seria a segunda fonte de verdade de lado.

**Achados para @qa/@po** (nenhum mudou AC nem escopo):
- **F-1 (MEDIUM, desvio de implementação, dentro do AC 14): `telas.ts` desenha o draft com o humano fixo no
  jogador 0** (`telas.ts:102` e `:113`, `daVez === 0`). No assento do jogador 1, a tela dava a vez ao oponente e
  desabilitava os botões na vez dele, e o draft travava. Isso bloquearia o AC 13 e é exatamente o `HUMANO = 0` que
  as Dev Notes v1.7.0 proíbem. O AC 12 congela `telas.ts`. Por isso, `main.ts:visaoParaATela` entrega **só à
  tela, só no draft, e só no assento 1** uma cópia da visão com o jogador 0 e o jogador 1 trocados em
  `draft.ordem` e `draft.escolhas`. A decisão que vai ao servidor sai com o jogador verdadeiro, e no assento 0 a
  visão passa sem cópia. Efeito colateral: a dica do draft ("j0: golem") rotula "j0" como "você" no assento 1.
  Recomendação: uma story futura troca o `0` por `ctx.humano` em `telaDraft`, e o adaptador sai.
- **F-2 (MEDIUM, lacuna): na loja do modo conectado, o preview de valor efetivo mostra "efeito indisponível".**
  `basePorChar` só é colhido de um `World` local (`telas.ts:55-63`), e o AC 5 proíbe montar mundo no caminho
  conectado. Para o preview voltar, o `StatBlock` base teria de chegar por outra porta, ou a derivação de
  `makeBall` teria de ser extraída de `sim/`. É decisão para @architect/@po.
- **F-3 (LOW, cosmético): o HUD de `render.ts` escreve "BOT" para o oponente**, também no modo conectado.
  `render.ts` está congelado pelo AC 12.
- **F-4 (LOW, armadilha de verificação): duas abas do mesmo navegador dividem a chave de telemetria no
  `localStorage`, e cada escrita grava o array inteiro da aba** (`telemetria.ts:151`). Na primeira corrida, B
  recarregou no meio (AC 11) e importou o acúmulo que A tinha gravado. O export de B saiu com os casts e o
  `rodadaFim r0` de A no lugar dos dele, e o `buildPadrao:j1` se perdeu. Em dois aparelhos isso não acontece. A
  conferência do AC 10 acima é a da segunda corrida, sem recarga, e está limpa. O coletor é do AC 12 e não mudou.
- **F-5 (LOW, já conhecido): o comentário de `src/server/main.ts` sobre a porta ainda diz que o L-3 está "em
  aberto"**. O Change Log v1.10.0 já roteou isso à `e4.4`, fora do AC 14.
- **F-6 (LOW, observação): nos casos i a iii e no `snapshotHz` fora de forma, a tela fica no status do momento**
  ("Conectando…" ou a última tela), como o AC 3 pede ("basta o `console.error`"). Se o @po quiser um aviso
  genérico também nesses casos, é uma linha em `rede.ts`.
- **F-7 (info): no AC 11 a aba foi "fechada" navegando para fora e voltando na mesma aba.** O `sessionStorage`
  é por aba, e é o que guarda o segredo. O Chrome devolve o `sessionStorage` ao reabrir uma aba fechada (Ctrl+Shift+T),
  mas uma aba nova e vazia não tem segredo e entra como jogador novo, de propósito (decisão 1). O socket da página
  que saiu ficou aberto ~3 s, e o servidor o derrubou com 4000 quando a volta reassentou. Nesse intervalo, o
  console da aba registrou um `conexão sem sala` vindo da página antiga.

**Self-critique (Step 5.5 e 6.5, inline por causa do AC 14):**
- AC 5 por construção: o modo conectado não chama nada que simule, e o grep da seção dá 0.
- O modo local não mudou de comportamento. Os 4 desvios são a primeira linha de funções compartilhadas. O `frame`,
  o `render()` e as decisões locais estão intactos. A aba sem hash abriu o draft contra o bot sem socket ao jogo.
- `HUMANO` não aparece na seção conectada. O jogador sai do `{t:'sala'}`, e o assento 1 foi exercitado de verdade
  (B), com compra e build padrão no próprio export.
- Nenhum texto de erro ou `motivo` vai para a tela: os avisos são dados estruturados, e os textos são fixos em
  `main.ts:mostrarAviso`.
- Nenhum `location.host`, variável de ambiente do bundler ou parâmetro de servidor, nem em comentário (grep exit 1).
- Risco residual: a interpolação sob jitter real de celular ainda não foi medida. `MARGEM_DE_JITTER_MS` é o
  lever, e `e4.7` mede.

**DoD (`story-dod-checklist`):** requisitos e ACs 1–14 atendidos com evidência acima, e o F-1 é desvio registrado
dentro do escopo. Padrões seguidos: sem dependência nova, imports relativos como no resto do cliente,
comentários no padrão do projeto. Testes: não há suíte de unidade no cliente; a cobertura é a bateria dirigida
(77/77), duas Bo5 em navegador e a bateria negativa. O `sim:check` continua verde e não passou a cobrir o cliente
(AC 2). Build verde. File List completa. CodeRabbit não executável, com o motivo registrado. Nada pushado.

## QA Results

### Review Date: 2026-09-22

### Reviewed By: Quinn (Test Architect)

**Gate: CONCERNS** → `docs/qa/gates/e4.5-cliente-conectado.yml`. **Status: InReview → Done.**

Revisão de `205e693` (pai `1d75b8d`). Durante o gate o HEAD andou para `fbbf313` (e4.6), e
`git diff 205e693 HEAD -- src/client` sai vazio. `205e693` ainda não está em `origin/master`.

**Os 14 ACs estão MET.** Refiz as verificações do zero, numa cópia descartável:
- **Gates de build:** `check`, `sim:check` e `build` deram rc=0, e o golden hash não mudou. O escopo tem só os
  dois arquivos do AC 14.
- **Greps:** os do L-3 dão 2 linhas de 5179, e os padrões proibidos dão rc=1. O de P4.2, mais largo que o do
  AC 5, dá rc=1 na seção conectada e em `rede.ts`. `instanceof`, `.message` e `motivo` só aparecem dentro de
  `console.error`.
- **Baterias em Node:** a bateria do @dev deu 77/77 de novo, e a minha deu 9/9.
- **Bateria negativa:** 28/28 em Chrome headless contra um servidor falso de protocolo. Cobre os casos i a vi
  com a tela lida no DOM, os três ramos de versão, porta errada, servidor desligado, 4000, teto de
  reconexão e a escolha do modo pela URL.
- **Duas Bo5 contra o servidor real:** uma com duas abas no mesmo navegador e outra com perfis separados. As
  duas usaram o link da primeira linha de log e reconectaram os dois assentos: A no meio da rodada, B na
  loja.
- **Lado e bolas:** conferidos a cada rodada, nos dois assentos, contra o `ladoDoJogador` do `rodadaFim`, com
  0 erros.
- **Build e AC 8:** nenhuma visão de builds traz a build do oponente (RF-05). A tela de fim de rodada espera
  46–84 ms pelo snapshot final (AC 8).
- **AC 10:** exports limpos por assento, com perfis separados.
- **Mutações:** 21 em `rede.ts` e 3 em `main.ts`.

**Achados:**
- **E45-REQ-001, MEDIUM, @po/@architect (era o F-7).** O segredo em `sessionStorage` não sobrevive a FECHAR a
  aba e abrir o link de novo, que é o fluxo natural do celular da e4.7. Nesse caso a tela manda "pedir um
  link novo", enquanto o assento continua reservado. Com o segredo presente o AC 11 funciona: recarga,
  navegação e aba restaurada foram medidas. É preciso decidir antes da coleta da e4.7.
- **E45-ARC-001, MEDIUM, @architect → @po (era o F-2).** O preview de valor efetivo da loja fica
  "indisponível" no modo conectado. Não viola AC desta story, e o AC 5/12/14 proíbe todas as correções
  aqui. Mas é uma regressão da Fase 3 no modo em que a Fase 4 é jogada, e precisa de um canal para o
  `StatBlock` base.
- **E45-DEBT-001, LOW (era o F-1, rebaixado de MEDIUM).** A troca de `visaoParaATela` está correta. Só a
  visão da tela do draft no assento 1 muda, e as decisões saem com o jogador verdadeiro. Sem a troca, o
  draft trava (M1). Com o jogador 0 nas decisões, a sala recusa (M2). O débito é o `0` literal de
  `telaDraft`.
- **E45-UX-001, LOW (F-3):** o HUD mostra "BOT".
- **E45-TEL-001, LOW (F-4).** Reproduzido: B levou 106 casts de A e perdeu 95 dos seus. Os `cast` não têm
  `jogador`, e por isso o filtro do AC 10 não vê a contaminação. O protocolo de coleta da e4.7 precisa de
  aparelho ou perfil por assento.
- **E45-UX-002, LOW (F-6):** "Aguardando o oponente…" com a conexão morta.
- **E45-TST-001, LOW.** R4, R23 e R27 passam verdes na bateria do @dev. O cliente não tem teste no
  repositório.
- **F-5:** é o E44-DOC-001, ainda aberto em `fbbf313`.

As 12 decisões autônomas foram aceitas. As consequências estão no gate, em `avaliacoes_pedidas`.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-08-19 | 1.0.0 | Story criada a partir do passo 5 de `docs/architecture-e4.md` §10 | @sm |
| 2026-08-19 | 1.1.0 | **Validação @po: GO 9/10** (7/10 antes das correções). **Status: Draft → Ready** — bloqueio é de **ordem** (espera `e4.4`), não de decisão. Dois achados: **(a)** o AC 9 propunha provar compatibilidade com `Parameters<typeof desenhar>[3]`, mas `desenhar` recebe `world: World` **completo** (`render.ts:40-46`) e nenhuma projeção satisfaz esse tipo — a asserção falharia sempre. Isso expôs um defeito em `e4.2`, que foi reaberta (v1.2.0) para estreitar o parâmetro para `VisaoDoMundo`; aqui o AC virou a chamada real, que já é a prova. **(b)** o AC 10 dizia que o coletor de telemetria "não muda" e isso é verdade, mas o evento `cast` de RF-36 **não vem do fio** — nasce em `disparar()`, que hoje lê `world.balls` para calcular `anguloErro`, e no modo conectado não existe `world`. A fonte passa a ser a projeção interpolada; sem isso RF-36 morre calada justamente no modo que mais importa. Ponto não somado: a story concentra duas coisas grandes (dois modos em `main.ts` + interpolação) e é candidata a corte se o @dev encontrar atrito. | @po |
| 2026-09-21 | 1.2.0 | **Alinhamento a `e4.2` v1.4.0. Status permanece Ready**, sem tarefa nova de peso. **(1)** O AC 12 dizia que `render.ts` "já terá mudado uma linha" em `e4.2`. Isso era impossível (`desenhar` repassa `world` a 6 auxiliares anotados `World`, e trocar só `render.ts:44` dá 6 × TS2345, reproduzido pelo @po). Agora diz "só anotações de tipo". **(2) `OpcoesRender.minhasBolas: Ball[]` (`render.ts:21`) exige `Ball` do motor, que o modo conectado não fornece. Decisão do @po: o TIPO é de `e4.2`** (uma anotação a mais em `e4.2`/AC 6, pela regra do próprio AC 12 desta story: "é lá que se corrige"). **A FONTE do valor é desta story**: `main.ts:298` lê `world.balls`, e no modo conectado passa a ler a projeção interpolada, como o `anguloErro` do AC 10. Isso virou nota no AC 12 e subtarefa na Task 2. Motivo de não dar o tipo a esta story: o AC 12/14 proíbe `render.ts` aqui, e abrir essa porta numa story já candidata a corte custaria mais do que uma linha de anotação numa story que já está editando o arquivo. **(3)** A Task 4 ainda pedia `Parameters<typeof desenhar>[3]`, que o AC 9 removeu na v1.1.0. Agora pede a chamada real compilando sem cast. **(4) E41-TEL-002 (gate de `e4.1`) NÃO vem para cá**, embora o gate a sugerisse como candidata. O AC 10/12/14 proíbe tocar em `client/telemetria.ts` e `src/tools/`, esta story está bloqueada atrás de `e4.4` → R-05 e a contaminação dos dados acontece desde já. Ver o roteamento no Change Log de `e4.1` (v1.4.0). | @po |
| 2026-09-21 | 1.3.0 | **Delta da §5.5 de `architecture-e4.md` (commit `48e53cc`) e contrato da §5.6 (`c37943b`). Status permanece Ready.** O bloqueio de ordem não muda (`e4.4` → R-05), e o acréscimo é uma chamada de função, sem task nova de peso. **(1) AC 3, acréscimo:** toda mensagem recebida passa por `decodificarDoServidor(texto)`, sem `JSON.parse(...) as DoServidor`, e o buffer guarda o `Snapshot` decodificado, de onde `projetar()` e o AC 10 leem. Motivo: o `snap` agora é tupla posicional, e um parse com cast passaria no `tsc` com dado errado. **(2) AC 8, nota:** os `events` do `snap` final são aplicados antes da tela de fim de rodada. Essa é a linha `client/rede.ts` da tabela de responsabilidades da §5.6. Não é AC novo, porque o AC 8 já exigia "aplicar na chegada", e a nota só torna explícita a ordem em relação à troca de tela. **(3)** "Depende de" passa a citar `decodificarDoServidor` (`e4.3`/AC 17 v1.3.0, que o corte decidido lá leva para a story própria do codec). O AC 14 **não muda**: `net/codec.ts` é só usado aqui, e `src/net/` continua proibido. Task 1, `quality_gate_tools` e Testing ajustados. | @po |
| 2026-09-21 | 1.4.0 | **O codec aponta para `e4.8`, e o tratamento do decode que lança foi decidido. Status permanece Ready.** O bloqueio de ordem não muda (`e4.4` → R-05). **(1) "Depende de":** `decodificarDoServidor` passa de "`e4.3`/AC 17, a ir para uma story própria" a **`e4.8`**, implementada em `910add8` (Ready for Review, em QA). Assinatura conferida em `src/net/codec.ts:419`: `decodificarDoServidor(texto: string): DoServidor`. **(2) AC 3, acréscimo, a partir da implementação de `e4.8` (Dev Agent Record de lá, nota 6):** o decodificador não lança só em aridade divergente. Também lança com `t` ausente ou fora da lista fechada de `DoServidor` (`codec.ts:421-424`), em campo que devia ser lista (`:343`) e, pelo `JSON.parse` interno, em texto que não é JSON. **[AUTO-DECISION] Logar e descartar o quadro, ou fechar a conexão? → fechar** (motivo: num descompasso de versão todo `snap` falha, então descartar congelaria a tela calado, que é o que o AC 3 proíbe com "não ser engolido". Reconectar sozinho repetiria o erro em laço). Fica assim: `try` em volta de toda chamada, e no `catch` `console.error`, fechamento da conexão e nenhuma reconexão automática por esse motivo. A reconexão por queda de rede não muda. Mensagem na tela fica a critério do @dev, dentro do AC 12/14. Task 1, Testing e `quality_gate_tools` ajustados. O AC 14 **não muda**: `src/net/` continua proibido aqui. | @po |
| 2026-09-21 | 1.5.0 | **Deltas da §11.6.1 de `architecture-e4.md` (E48-ARC-003, commit `bed4603`) e decisão O-1 do @po. Status permanece Ready.** O bloqueio de ordem não muda (`e4.4` → R-05). **O AC 14 não muda:** `src/net/` continua proibido, e tudo abaixo mora em `client/rede.ts` e `client/main.ts`. **(1) AC 3, caso (iv):** `{t:'sala'}` com `versao` diferente de `VERSAO_DO_FIO`, ou ausente. O decodificador lança (story da versão do fio, sugestão `e4.9`), e o tratamento é o dos outros três casos: log, conexão fechada, sem reconexão automática. **(2) [AUTO-DECISION] A mensagem na tela que separa "recarregue" de "aguarde o servidor", recomendada pelo @architect: obrigatória ou opcional? → obrigatória** (motivo: o jogador desta fase está num celular, onde não há console, e o `console.error` sozinho deixaria uma tela parada sem explicação. Os dois remédios são opostos, e recarregar em laço contra um servidor atrasado não resolve nada. Depois da §11.6.1, o cliente do Pages fica permitido em `e4.7`, então o caso "cliente na frente do servidor" deixa de ser teórico, porque o Pages republica a cada push. O custo é pequeno: `main.ts` já controla um `overlay` (`src/client/main.ts:66`), e as duas versões chegam em campos do erro, que o @po pediu na spec de `e4.9` para que a mensagem não dependa de parse de texto). A frase "mensagem na tela é opcional" do AC 3 v1.4.0 foi substituída. Para os casos (i)–(iii), basta o log. **(3) AC 7, acréscimo (O-1):** o intervalo que dimensiona `ATRASO_DE_BUFFER` vem do `snapshotHz` do último `{t:'sala'}`, e não do `SNAPSHOT_HZ` compilado. A decisão (taxa efetiva no `{t:'sala'}`, e não buffer fixo pela menor taxa da varredura) e o motivo dela estão em `e4.3` v1.6.0. **(4) Testing:** verificação manual com um servidor local em `VERSAO_DO_FIO + 1`, em que o cliente loga, fecha, não reconecta e mostra "recarregue", e o caso inverso, que mostra "aguarde o servidor". Também a checagem do buffer a 20 Hz. "Depende de" ganha `e4.9`. Task 1/3, Focus Areas e `quality_gate_tools` ajustados. **Fatos conferidos:** `overlay` em `src/client/main.ts:66`, alternado por `classList` em `:270`/`:276`; `DoServidor['sala']` sem `versao` hoje (`src/net/protocolo.ts:136`); o decode devolve `m as DoServidor` sem checar campos (`src/net/codec.ts:426`, achado M-3 da §11.6.1). | @po |
| 2026-09-21 | 1.6.0 | **Observação do gate de `e4.9` para o gate de `e4.5` (`docs/qa/gates/e4.9-versao-do-fio-e-assento.yml`, `c4b22db`, `observacoes`). Status permanece Ready**, e o bloqueio de ordem não muda (`e4.4` → R-05). **Conferência pedida pelo lead:** o AC 3 (iv) v1.5.0 já exigia a mensagem "a partir dos **campos** do erro, nunca por parse do texto", mas **não** dizia como reconhecer o descompasso (`instanceof`), juntava "versão menor ou sem versão" num só teste manual e não cobria `servidor` de tipo errado. Faltavam três coisas, e elas foram acrescentadas. **(1) AC 3 (iv):** o descompasso é reconhecido só por `instanceof DescompassoDeVersao`, nunca por `name`/`message`, e `e.message` não vai para a tela. Os ramos são **três**, pelos campos: `servidor` número maior → "recarregue"; número menor → "aguarde o servidor"; não número (ausente ou de outro tipo) → "aguarde o servidor", com "sem versão". O erro comum de `snapshotHz`/`assento` fora de forma não é descompasso. **(2) Testing:** os três ramos são rodados separadamente, mais `snapshotHz: 7` com a versão certa fechando sem mensagem de versão, e um grep de `instanceof`/`.message` em `src/client/`. **(3)** Task 1, CodeRabbit Focus e `quality_gate_tools` ajustados. O AC 14 **não muda**: importar a classe de `net/codec.ts` é uso, como `decodificarDoServidor`. **Fatos conferidos em `6f2f56c`:** `export class DescompassoDeVersao extends Error` com `readonly servidor: unknown` e `readonly cliente: number` declarados no corpo e atribuídos no construtor. `conferirSala` lança a classe quando `versao !== VERSAO_DO_FIO` (então `versao: 0` e `versao: "1"` também caem nela) e lança `Error` comum para `snapshotHz`/`assento`. | Pax (@po) |
| 2026-09-21 | 1.7.0 | **Delta da §11.6.2 de `architecture-e4.md` (E43-ARC-002, commit `188998e`). Status permanece Ready**, e o bloqueio de ordem não muda (`e4.4` → R-05). **O AC 14 não muda:** a variante nova do fio é de `e4.10`, e aqui ela só é lida. **(1) "Depende de" ganha `e4.10`** (a criar pelo @sm, spec no Change Log de `e4.3` v1.10.0): a variante `{t:'evento', e: EventoPartida}`, uma mensagem por evento, por assento, com `VERSAO_DO_FIO` 1 → 2. Motivo do @architect: o AC 10 dizia que `EventoPartida` "passa a chegar pelo fio", mas `DoServidor` não tinha variante para isso, a sala guarda os eventos em `Sala.eventos` sem enviá-los, e o AC 14 proíbe esta story de abrir `src/net/`. **(2) AC 10 reescrito:** `{t:'evento', e}` → `telemetria.registrar(seed, [e])`, com `seed` da última `visao`; o `controle` do `rodadaFim` vem do servidor e não é reescrito; o coletor não muda. **Verificação nova:** o export contém `rodadaFim` de cada rodada, `partidaFim`, `cast` e as compras do próprio jogador, e **nenhum** evento com o `jogador` do outro. O @po acrescentou duas coisas ao texto da §11.6.2. A conferência roda nos exports **das duas abas**, porque só a aba do assento 1 separa o filtro certo de um cliente que assume `HUMANO = 0`. E a ausência do `rodadaFim` de uma rodada perdida por W.O. com o assento vago é exceção esperada, porque a §11.6.2 decide "sem reenvio no reassentamento". **(3) Dev Notes, subseção nova:** no modo conectado o jogador local pode ser o 1, e nada do caminho conectado assume `HUMANO = 0`. **(4)** Task 5, CodeRabbit Focus, Testing e `quality_gate_tools` ajustados. No Testing, o ramo "versão numérica menor" ganhou a nota de que, com `VERSAO_DO_FIO = 2` depois de `e4.10`, `0` e `1` servem. **Fatos conferidos em `188998e`:** `const HUMANO: Jogador = 0` em `src/client/main.ts:61`, `meuLado = lados[HUMANO]` em `:195`, decisões montadas com `jogador: HUMANO` em `:250-257`; `registrar: (partida: number, eventos: readonly EventoTelemetria[]) => void` em `src/client/telemetria.ts:132`, chamado hoje como `telemetria.registrar(partida.seed, t.eventos)` em `main.ts:134`/`:238`; `EventoPartida` em `src/match/types.ts:161-181`, onde `compra`, `trocaDeBuild` e `buildPadrao` têm `jogador` e `rodadaFim` e `partidaFim` não têm; `Sala.eventos` em `src/net/sala.ts:205`, alimentado em `:520` e `:609` e não enviado. Pela contagem do @po em `jogarPartida(1)` (Node 24.13.1, cópia de `188998e`): 7 eventos, 5 para o assento do jogador 0 e 6 para o do jogador 1, como a M-5 da §11.6.2. | Pax (@po) |
| 2026-09-21 | 1.8.0 | **R-05 ratificada pelo usuário (2026-09-21, `architecture-e4.md` §12/R-05, bloco "Resolução"). Status permanece Ready.** "Depende de" deixa de dizer que a `e4.4` está bloqueada em R-05, e a ordem continua: esta story começa depois da `e4.4`. Nenhum AC muda. **Duas coisas que a revalidação da `e4.4` (v1.9.0, GO 9/10, Ready) trouxe para cá:** **(1)** o AC 10 e o AC 16 da `e4.4` passam a ser verificados com clientes de protocolo, porque lá não existe cliente de navegador. As verificações de navegador que a `e4.4` citava são desta story: as duas abas no AC 13, o número flutuante do golpe final no AC 8 e o P4.2 no AC 5. Os três já estavam aqui, e nada foi acrescentado. **(2) O-2, o canal de criação de sala, está com o @architect** (`e4.4`/AC 6). Nenhum documento diz como o primeiro jogador obtém o id da sala, e a resposta vai decidir como este cliente abre ou recebe o link. Se ela tocar `client/`, entra aqui por emenda. Enquanto isso, o AC 14 não muda. | Pax (@po) |
| 2026-09-22 | 1.9.0 | **Deltas da §6.1 de `architecture-e4.md` (O-2, opção D, commit `c3418a6`). Revalidação @po: GO 8/10, porque os ACs mudaram de forma material (dois casos novos e uma pré-condição). Status permanece Ready.** **O AC 14 não reabre:** o cliente só lê o id do link e manda o `{t:'entrar'}` que `DoCliente` já tem. **(1) AC 4, acréscimo:** o modo é escolhido pela URL. Com `#/sala/{id}`, o cliente abre `conectado` e manda `{t:'entrar', sala: id}`. Sem isso, abre o `local` de hoje sem mudança nenhuma. Não há tela de criação de sala. Até aqui nenhum AC dizia como o modo é escolhido. **(2) AC 3, caso (v) novo:** `{t:'erro'}` antes de qualquer `{t:'sala'}` (id desconhecido, sala acabada ou cheia) mostra uma mensagem na tela e fecha a conexão, sem reconexão em laço. O decodificador aceita esse quadro (`src/net/codec.ts:481`), então ele não cai no `catch` dos casos (i) a (iv) e precisaria de ramo próprio de qualquer jeito. [AUTO-DECISION] Texto da tela a partir do `motivo` do servidor ou fixo do cliente? → **fixo** (motivo: o `motivo` de hoje é texto de desenvolvedor, com referência de story, em `src/net/sala.ts:449`, e o caso iv já proíbe texto de erro na tela). **Acréscimo do @po:** um `{t:'erro'}` **depois** do `{t:'sala'}` (a sala recusa cast fora da rodada em `:491` e decisão ilegal em `:532`) fica só no console, e a conexão **continua aberta**. Sem essa distinção, um cliente que fechasse em todo `{t:'erro'}` tiraria da partida um jogador por um clique fora de hora. **(3) AC 13:** as duas abas abrem o mesmo link, com o id copiado da linha de log do servidor. Entrou a mesma armadilha da `e4.4`/AC 10: a segunda aba não usa a última linha do log, porque o servidor cria outra sala quando a primeira aba senta. **(4) "Depende de", pré-condição nova: L-3, o endereço do servidor.** A §6.1 traz uma recomendação e não uma decisão (esquema por `location.protocol`, host `location.hostname`, porta numa constante nomeada), e ela não cobre o cliente do Pages. A pergunta está aberta com o @architect, e **só a subtarefa da Task 1 que abre o socket espera**. [AUTO-DECISION] Onde mora a porta, se a recomendação for confirmada? → duas cópias, uma em `client/rede.ts` e uma em `src/server/main.ts`, cada uma citando a outra (motivo: a cópia única exige `net/protocolo.ts`, que o AC 14 daqui e o AC 13 de `e4.4` proíbem, e o @architect deixou essa escolha com o @po). **(5)** Tasks 1, 2 e 6, CodeRabbit Focus, Testing e `quality_gate_tools` ajustados. Testing ganhou o link com id inventado (a tela não fica preta), a sala cheia, a sala acabada, o erro depois de assentado e o modo local sem hash. **Nota:** 8/10, contra 9/10 na v1.1.0. O ponto de antes continua perdido: a story concentra dois modos em `main.ts` e mais a interpolação, e é candidata a corte. O ponto novo é o L-3 em aberto, que trava uma subtarefa. Os dois são registro, e nenhum é correção que o @po possa fazer. **Fatos conferidos em `c3418a6`:** `{t:'entrar'; sala; assento?}` em `src/net/protocolo.ts:134`; `conferirSala` só para `t === 'sala'` em `src/net/codec.ts:481`; `erro()` da sala em `src/net/sala.ts:368`, chamado com sala cheia em `:393` e com cast fora de fase em `:491`; nenhum `location`, `ws://`, `wss://`, `VITE_` ou `import.meta.env` em `src/`, e `src/client/main.ts` não lê `location.hash` hoje. | Pax (@po) |
| 2026-09-22 | 1.10.0 | **L-3 decidido pelo @architect (`architecture-e4.md` §6.2, commit `5495657`). Revalidação @po: GO 9/10, e o ponto perdido na v1.9.0 pelo L-3 em aberto volta. Status permanece Ready.** A subtarefa da Task 1 que abre o socket destrava. **Nenhum AC reabre, e o AC 14 fica como está.** **(1) "Depende de":** a recomendação vira decisão, com as três precisões da §6.2. O endereço sai só de `location`: `wss:` em `https:` e `ws:` fora dele, `location.hostname` e não `location.host`, e a porta numa constante nomeada. Não há `import.meta.env` nem parâmetro de servidor no link. O servidor escuta sem `host`, e a porta não tem override. O [AUTO-DECISION] das duas cópias da v1.9.0 fica, agora com a tabela de cinco lugares da §6.2 como fonte. **[AUTO-DECISION] Nome da constante do cliente? → `PORTA_DO_SERVIDOR`, igual ao do servidor** (motivo: um padrão de grep acha as duas cópias, e quem lê uma acha a outra pelo nome). O bloco da pergunta aberta e a parada da Task 1 saíram. A frase "nada nesta story espera resposta de ninguém" volta a valer. **(2) AC 3, caso (vi) novo, a partir do acréscimo de Testing que o @architect recomendou:** a conexão fecha antes do primeiro `{t:'sala'}` sem que o cliente a tenha fechado (servidor desligado, porta errada, firewall, ou o cliente do Pages apontando para `github.io`). Nesse caso a tela mostra que o servidor não foi alcançado, com o endereço tentado, e o cliente não reconecta sozinho. Motivo de pôr no AC e não só no Testing: sem requisito, o teste de "servidor desligado" não tinha critério. Nenhum dos casos (i) a (v) cobre esse caminho, porque o navegador dá `error`/`close` sem mensagem, e é por ele que a divergência entre as duas cópias apareceria. [AUTO-DECISION] Mostrar o endereço tentado? → sim (motivo: no celular de `e4.7` não há console, e o endereço separa porta errada de rede errada. Ele não traz segredo nem id). **(3)** CodeRabbit Focus perde o "(se a recomendação for confirmada)" e ganha `location.host`, `import.meta.env`, parâmetro de servidor no link, comentário da constante sem citar o outro lado e o caso (vi). **(4) Testing e `quality_gate_tools`:** servidor desligado e porta errada (mudança local não commitada) com o erro na tela. Um grep dos dois literais (`^(export )?const PORTA_DO_SERVIDOR = [0-9]+$` em `src/client/rede.ts` e `src/server/main.ts`, exatamente duas linhas com o mesmo número) e um grep dos padrões proibidos em `src/client/`, que tem de sair com 1. Os dois foram rodados pelo @po. O de padrões proibidos acusou `location.host`, `import.meta.env`, `?servidor=` e `ws://localhost` numa amostra, deixou `location.hostname` passar, e hoje sai com 1 em `src/client/`. Também entrou a conferência da URL montada no devtools. **Fica fora desta story:** o comentário do lado do servidor (`src/server/main.ts:32-36`) ainda diz que o L-3 está "em aberto" e que "se o desenho confirmar duas cópias, [...] esta passa a citar a do cliente". Atualizá-lo para citar `src/client/rede.ts` e `PORTA_DO_SERVIDOR` exige abrir `src/server/`, que o AC 14 proíbe. Isso vai como nota para a `e4.4`, que está em gate, e o lead aplica depois do gate. Até lá, o grep dos literais é a conferência que vale. **Nota:** 9/10. O ponto que continua perdido é o da v1.1.0: dois modos em `main.ts` mais a interpolação fazem desta story candidata a corte. **Fatos conferidos em `77113d3`:** `const PORTA_DO_SERVIDOR = 5179` em `src/server/main.ts:38`, que não é 5173 nem 4173. `new WebSocketServer({ port, perMessageDeflate, maxPayload })` em `:389-393`, sem `host`. Nenhum `process.env` nem `argv` em `src/server/main.ts`, então a porta não tem override. `vite.config.ts` só define `base`, sem `server.port`, e `package.json:7` traz `"dev": "vite"`. `src/client/` ainda não tem `rede.ts`, e nenhuma ocorrência de `location.host`, `import.meta.env` ou `ws(s)://`. | Pax (@po) |
| 2026-09-22 | 1.10.1 | Development started (yolo mode) — Status: Ready → InProgress | @dev |
| 2026-09-22 | 1.11.0 | Development complete — Status: InProgress → InReview. Implementação em `205e693` (pai `1d75b8d`): `src/client/rede.ts` criado e `src/client/main.ts` com dois modos. `check`, `sim:check` (golden hash idêntico) e `build` verdes na árvore do commit; os dois greps do L-3 conferem; bateria dirigida de `rede.ts` 77/77; duas Bo5 completas em duas abas de Chrome headless por CDP, com o link da primeira linha de log; exports das duas abas conferidos (AC 10); bateria negativa em navegador com servidores adulterados em cópia descartável (casos iii-vi, 3 ramos de versão, porta errada, servidor derrubado). Achados F-1 a F-7 no Dev Agent Record, com F-1 (`telaDraft` fixa o humano no jogador 0, contornado em `main.ts` só para a tela) e F-2 (preview da loja indisponível no modo conectado) para @qa/@po. CodeRabbit não executável (WSL ausente) | @dev |
| 2026-09-22 | 1.12.0 | **QA gate: CONCERNS. Status: InReview → Done.** Gate em `docs/qa/gates/e4.5-cliente-conectado.yml`, revisão `205e693`. Os 14 ACs estão MET. `check`, `sim:check` (golden hash ✓) e `build` deram rc=0, e os greps do L-3, de P4.2 e do AC 3 batem. A bateria do @dev deu 77/77, a minha 9/9 e a negativa em navegador 28/28 (casos i–vi, porta errada e servidor desligado na tela). Duas Bo5 no servidor real reconectaram os dois assentos e conferiram o lado a cada rodada. Os exports saíram limpos por assento com perfis separados. Rodei 24 mutações. Dois MEDIUM ficam com dono fora da story: E45-REQ-001 (segredo em sessionStorage não sobrevive a fechar a aba, @po/@architect) e E45-ARC-001 (preview da loja no modo conectado, @architect). Há cinco LOW, e o F-1 foi rebaixado a LOW. | Quinn (@qa) |
