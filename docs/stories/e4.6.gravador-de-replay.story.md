# Story e4.6: P4.3 — gravador de replay no servidor e verificação por igualdade do hash arredondado, na mesma engine

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run check", "npm run sim:check", "replay de partida real reproduz os mesmos ResultadoRodada.hash (hash() arredondado, igualdade exata da string) gravados pela sala do servidor, não os da guarda do AC 7 (AC 5, v1.5.0; redação v1.10.0, E46-DOC-001)", "revisão manual: a verificação roda em Node, não no navegador — e o motivo está escrito", "git show --stat <commit de implementação desta story>"]
```

## Story

**Como** desenvolvedor executando o **passo 6** do plano de construção da Fase 4
(`docs/architecture-e4.md` §10),
**eu quero** que o servidor **grave** `matchSeed + Decisao[] + Command[] por rodada` de cada partida
jogada, e que exista um verificador que reproduz a partida gravada e compara os hashes,
**para que** **P4.3** — *"replay reconstrói a partida a partir de seed + linha do tempo de inputs, com
hash idêntico ao da execução ao vivo"* — seja satisfeito **sem asterisco**, e para que RF-41 exista como
capacidade e não como propriedade teórica.

## Depende de

`e4.3` (a sala é quem vê as decisões e carimba os comandos) e `e4.4` (o servidor é quem hospeda a sala e
persiste). **Sequencialmente bloqueada por `e4.4`**. *(v1.6.0: a `e4.4` não está mais bloqueada em
R-05, que o usuário ratificou em 2026-09-21, `architecture-e4.md` §12/R-05, Resolução. Resta a ordem:
esta story começa depois da `e4.4`.)*

**Sequência de `src/tools/determinism.ts` (v1.2.0).** Esta story edita o arquivo (AC 7, AC 12), que é
disputado numa árvore de trabalho só. A ordem registrada é `e4.8` → `debt.11` → a story da versão do
fio (sugestão `e4.9`, v1.3.0) → `e4.3` → a story do gate de `e4.8` (sugestão `debt.12`) → *(v1.4.0)* a
story do gate de `e4.3` (sugestão `debt.14`) → *(v1.5.0)* a story da variante `{t:'evento'}` (sugestão
`e4.10`, `architecture-e4.md` §11.6.2) → esta. Ordem vigente: `e4.8` → `debt.11` → `e4.9` → `e4.3` →
`debt.12` → `debt.14` → `e4.10` → **`e4.6`**. **Pré-condição de início:** `git status --short
src/tools/determinism.ts` vazio, ou seja, sem mudança não commitada de outra story. **Conferência de
escopo:** `git show --stat <commit de implementação desta story>`, nunca `git diff --stat` na árvore.

## Acceptance Criteria

1. `npm run check` verde.
2. `npm run sim:check` verde, com **golden hash idêntico**.
3. **O mecanismo já existe e não é reinventado.** `tools/partida.ts` já reproduz uma partida a partir de
   `matchSeed + Decisao[] + Command[]`, e `tools/determinism.ts:215-256` já prova o isolamento de stream
   que sustenta isso (D-08, resolvido na Fase 2). Esta story acrescenta o **gravador** e o **verificador de
   partida real** — se ela reimplementar reprodução, reprova.
4. **O que é gravado, por partida:** `matchSeed`, a lista de `Decisao` na ordem em que a sala as aceitou, e
   `Command[]` por rodada **na forma já carimbada** (com o `tick` que a sala atribuiu). Gravar a intenção
   crua do cliente em vez do comando carimbado tornaria o replay dependente de re-executar o carimbo, e
   qualquer mudança futura em `ATRASO_ALVO_TICKS` quebraria replays antigos em silêncio.
5. **Verificação por igualdade do hash arredondado, na mesma engine** (§7.2): reproduzir a partida gravada
   **no servidor Node** produz os mesmos hashes de rodada, o mesmo placar e a mesma sequência de vencedores
   que a execução ao vivo. Sem tolerância, sem ε: mesma engine, igualdade exata **do hash**.

   *(Redação v1.10.0, E46-DOC-001, gate `docs/qa/gates/e4.6-gravador-de-replay.yml`. O critério não muda.)*
   O instrumento testemunha que a string de `hash()` é igual (`src/tools/harness.ts`: FNV-1a sobre `tick`,
   `winner` e, por bola, `x`, `y`, `vx`, `vy` e `hp` arredondados por `toFixed(4)`, mais `alive`). Ele não
   testemunha cada bit do estado. Na mesma engine, com o comando gravado em JSON com o double inteiro, a
   reprodução é bit-exata por construção. Mas isso é propriedade da execução, e não o que o `replay:check`
   prova. O gate mediu a diferença: `dx + 1e-12` passa em 38 de 40 comandos, e o sinal trocado em 37 de 40,
   porque a maioria dos comandos cai em recarga. É a §11.1 aplicada a este instrumento, e a saída do
   `replay:check` já diz isso.

   **Acréscimo (v1.5.0, `architecture-e4.md` §2.2, E43-ARC-001):** *"Os hashes 'da execução ao vivo' são
   os `ResultadoRodada.hash` que a sala **do servidor** gravou (o `hash` injetado por `server/main.ts`), e
   não os que a guarda do AC 7 calcula. Esta comparação é a prova de ponta a ponta de E43-ARC-001: um
   `hashDoMundo` que não seja o `hash()` de `tools/harness.ts` reprova aqui."* A guarda do AC 7 não vê
   isso, porque injeta ela mesma o `hash` do arnês e nunca carrega `server/main.ts`. Por isso a partida
   deste AC é real, jogada contra o servidor de `e4.4`, e os hashes comparados saem do arquivo que o
   servidor gravou.
6. **A verificação NÃO roda no navegador, e o motivo entra em comentário no código** — não só na story.
   `architecture-e4.md` §1.5 mediu: Node e Chrome divergem em bits na mesma simulação
   (`Math.atan2`/`sin`/`cos` diferem entre engines; a divergência entra pelo ângulo da Muralha em
   `chars/golem.ts:140`). O `hash()` quantizado bateu em 5/5 seeds, mas §11.1 mostra que **quantização é
   arredondamento, não banda de tolerância** — os 5/5 são o resultado esperado de uma loteria com boas
   probabilidades, não prova de robustez.

   Portanto: **o instrumento do portão é o Node.** Um visualizador de replay no navegador é desejável e
   está fora de escopo; se um dia entrar, a comparação tem de deixar de ser `hash(a) === hash(b)` e virar
   `|a − b| < ε` com ε declarado.
7. **Guarda no `sim:check`:** uma partida gravada pela sala (a mesma da guarda de `e4.3`) é reproduzida
   pelo verificador e bate hash a hash. É o teste de `e4.3`/AC 11 fechando o círculo: lá se provou que a
   sala não acrescenta regra; aqui se prova que o que ela gravou reconstrói a partida.
8. **Formato versionado.** O arquivo de replay carrega uma versão de formato explícita, no mesmo espírito
   da chave `bb.telemetria.v1` de `e3.5`. Mudança de formato não pode corromper replay antigo em silêncio.
9. **Orçamento de armazenamento medido e registrado.** `architecture-e4.md` §1.3 mediu **54,6 comandos por
   rodada × 26,3 B ≈ 1,4 kB**, mais as decisões; uma Bo5 inteira deve caber em poucos kB. O @dev remede com
   o formato real e registra. Um replay não é vídeo; é receita. Se der ordens de grandeza a mais, algo
   está sendo gravado que não devia (estado, por exemplo).
10. **Onde os replays ficam é decisão de operação, não de arquitetura**, e esta story escolhe o mínimo:
    arquivo em disco no servidor, caminho configurável, sem banco e sem retenção automática. Persistência
    "de verdade" não tem requisito em RF-37 a RF-42 (Artigo IV) e não entra.
11. **Setas — e aqui havia um CICLO, encontrado pelo @po na validação.** A redação anterior punha
    montagem **e reprodução** em `src/net/replay.ts`, e mandava a reprodução delegar a `tools/partida.ts`
    (AC 3). Isso fecharia `tools/ → net/ → tools/`: a primeira seta foi criada em `e4.2`/AC 11 para
    hospedar a guarda de ida-e-volta, e a segunda seria criada aqui. Ciclo entre camadas é exatamente o
    que a tabela da §2.2 existe para impedir, e `match/types.ts` já registra o mesmo raciocínio para o
    caso de `ResultadoRodada.hash` (*"um `import ... from '../tools/...'` aqui inverteria a seta e criaria
    ciclo"*).

    A divisão correta, em três lugares:

    | peça | onde | por quê |
    |---|---|---|
    | tipo do replay + **montagem** a partir do que a sala vê | `src/net/replay.ts` — **puro**, sem `fs`, sem `tools/` | é dado, e a sala precisa produzi-lo |
    | **escrita em disco** | `src/server/main.ts` | I/O é do servidor; `net/` não ganha `fs` |
    | **reprodução e verificação** | `src/tools/replay-check.ts` — CLI, `npm run replay:check` | `tools/` já importa `sim/`, `match/`, `harness` e (desde `e4.2`) `net/`; é a camada certa e a única que não cria ciclo |

    Isso também casa melhor com o AC 6: a verificação é uma ferramenta de linha de comando em Node, que é
    exatamente o que `tools/` é. O servidor **não** verifica replay em linha — ele grava.
12. **Escopo de arquivos.** Criados: `src/net/replay.ts` (puro: tipo e montagem) e
    `src/tools/replay-check.ts` (reprodução e verificação, CLI). Alterados: `src/server/main.ts` (escrita
    em disco), `src/tools/determinism.ts` (guarda do AC 7) e `package.json` (script `replay:check`).
    Proibido: `src/sim/`, `src/match/`, `src/shop/`, `src/bot/`, `src/chars/`, `src/client/`, `docs/`.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Backend / Persistence
**Secondary Type(s)**: Gate criterion (P4.3)
**Complexity**: Low-Medium — a parte difícil (reprodução determinística) já existe e está provada desde a
Fase 2. O risco é gravar a coisa errada.

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa (quality gate — AC 5 e AC 6 são critério de portão e precisam de reexecução independente)

**Supporting Agents**:
- (nenhum necessário)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Rodar antes de marcar a story como completa
- [ ] Pre-PR (@github-devops): Rodar antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 20 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Gravar intenção crua em vez de comando carimbado (AC 4) — quebra replay antigo quando a constante mudar
- Gravar **estado** em vez de receita (AC 9) — infla o arquivo e desfaz o sentido de RF-41
- `fs` aparecendo em `src/net/` (AC 11)

**Secondary Focus**:
- Verificação escrita de forma a poder rodar no navegador sem o alerta de §1.5 (AC 6)
- Formato sem versão (AC 8)

## Tasks / Subtasks

- [x] Task 1 — `net/replay.ts` puro (AC: 3, 4, 8, 11)
  - [x] Tipo do replay e **montagem** a partir do que a sala já vê
  - [x] Versão de formato explícita
  - [x] **Sem `fs` e sem import de `tools/`** — a reprodução NÃO mora aqui (correção do @po: criaria ciclo)

- [x] Task 2 — Escrita em `server/` (AC: 10, 11, 12)
  - [x] Arquivo em disco, caminho configurável, sem banco

- [x] Task 3 — Verificador em `tools/` (AC: 5, 6, 11)
  - [x] `src/tools/replay-check.ts` + script `replay:check`
  - [x] Reprodução delegando a `tools/partida.ts` / `harness`, sem reimplementar
  - [x] Igualdade exata de hash, placar e vencedores
  - [x] Comentário no código explicando por que a verificação não roda no navegador (§1.5, §11.1)

- [x] Task 4 — Guarda e orçamento (AC: 7, 9)
  - [x] Guarda no `sim:check`: partida da sala → replay → hashes iguais
  - [x] Medir e registrar o tamanho real do replay de uma Bo5

- [x] Task 5 — Verificação (AC: 1, 2, 12)
  - [x] `check` e `sim:check` verdes, golden hash idêntico
  - [x] `git show --stat <commit de implementação desta story>` prova o escopo (nunca `git diff --stat` na árvore compartilhada)

## Dev Notes

### D-08 foi resolvido na Fase 2, e é por isso que esta story é pequena (fonte: §7.1)

O bot saca de stream próprio, semeado da seed da partida, e `sim:check` prova o isolamento reproduzindo a
rodada **sem bot nenhum**. `tools/partida.ts` já sobe isso ao nível da partida. O servidor, por
construção, é quem recebe as decisões e carimba os comandos — gravar é anexar duas listas que já passam
pelas mãos dele.

### Por que a verificação é do Node, e não do navegador (fonte: §1.5, §7.2, §11.1)

Medido nesta fase, pela primeira vez no projeto:

| onde o replay roda | veredito |
|---|---|
| mesma engine que gravou | **bit-exato** — P4.3 sem asterisco |
| engine diferente | `hash()` bateu em 5/5 seeds, mas o estado diverge em bits em 3/5 |

A maior divergência observada foi **1,6e-9 px** após 5 249 ticks, e o vencedor e a contagem de ticks
bateram em 5/5. Mesmo assim: quantização é arredondamento, e dois valores separados por 1e-9 caem em lados
opostos de uma fronteira com probabilidade pequena — **não nula**. Escolher o Node não é conservadorismo; é
usar o único instrumento em que a igualdade é exata por construção.

### O que esta story explicitamente NÃO faz

- **Não faz visualizador de replay.** Não há requisito, e §7.2 explica por que ele não seria o instrumento
  do portão.
- **Não persiste em banco, não faz retenção, não expõe API de replay.** Nada disso está em RF-37 a RF-42.
- **Não fecha a guarda de determinismo entre engines** — essa é **R-04**, devolvida ao backlog para antes
  da Fase 5.

### Testing

- `npm run check` e `npm run sim:check` verdes, golden hash idêntico.
- Guarda nova: partida conduzida pela sala → replay → hashes de todas as rodadas iguais, placar igual,
  sequência de vencedores igual.
- Partida real jogada em duas abas → replay gravado → verificação em Node dá os mesmos hashes (igualdade do
  hash arredondado, AC 5, redação v1.10.0). *(v1.5.0: os
  hashes "ao vivo" são os `ResultadoRodada.hash` que a sala do servidor gravou, AC 5.)*
- Tamanho do replay de uma Bo5 medido e registrado.
- Grep: `fs` não aparece em `src/net/`.

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (1M context), como Dex (@dev), modo YOLO.

### Pré-condições

- `git status --short src/tools/determinism.ts` vazio no início, sobre `1d75b8d` (gate da `e4.4`, Done). A fila de
  `determinism.ts` estava livre, e esta story é a última dela.
- `e4.4` Done: `src/server/main.ts` injeta `hashDoMundo: hash` (`grep -nwF "hashDoMundo: hash" src/server/main.ts` → exit 0).
- A `e4.5` foi implementada em paralelo, por outro @dev, só em `src/client/`. Nenhum arquivo em comum.

### Commit de implementação e escopo (AC 12)

- **`7850a7f`**. Pai: `e279c43`, que é só a story da `e4.5` (1 arquivo em `docs/`). O código-base é `205e693` (a
  implementação da `e4.5`), mais esta story.
- `git show --stat 7850a7f` lista 5 arquivos, os do AC 12 e nenhum outro: `package.json` (+2/−1), `src/net/replay.ts`
  (novo, 198), `src/server/main.ts` (+65/−3), `src/tools/determinism.ts` (+176/−3), `src/tools/replay-check.ts` (novo, 197).
  São 638 inserções e 7 remoções.
- Nenhum arquivo de `src/sim/`, `src/match/`, `src/shop/`, `src/bot/`, `src/chars/`, `src/client/` ou `docs/` no commit
  de implementação. A story vai num commit separado.

### Decisões

- **[AUTO-DECISION] "Decisões na ordem em que a sala as aceitou" (AC 4): quais entram?** → só as que `aplicar()`
  aceitou. Motivo: `Sala.decisoes` guarda também as recusadas, e `reproduzirPartida` só consome decisão fora da fase
  `rodada`. Um `{t:'pronto'}` mandado no meio da rodada é recusado ao vivo, mas no replay seria consumido na loja
  seguinte e aceito, e a partida reproduzida seria outra. A mutação M1 prova isso numa partida real: "a reprodução não
  chegou ao fim da partida". A classificação aplica `aplicar()` à `partida` de antes do passo, na ordem do log. É a
  mesma função pura, sobre o mesmo estado e na mesma ordem da sala, e não reproduz rodada nenhuma. Uma tripwire confere
  que as aceitas levam exatamente ao estado em que a sala terminou o passo.
- **[AUTO-DECISION] Como capturar os comandos carimbados sem editar `net/sala.ts`, que está fora do AC 12?** → com uma
  marca antes do passo e uma leitura depois (`marcarAntesDoPasso` / `gravarPasso`). A sala só acrescenta ao array
  `pendentes` (`castar` faz `push`) e, quando avança ticks, o troca por um array novo (`filter`). Então o array marcado
  guarda os comandos carimbados no passo, mesmo os que o próprio passo consumiu. A rodada que começa dentro do passo
  tem os comandos dela no `pendentes` inicial. O servidor e a guarda chamam as duas funções em volta de `passo()`.
- **[AUTO-DECISION] Os resultados ao vivo entram no arquivo?** → sim: o `ResultadoRodada` de cada rodada
  (`EstadoPartida.historico`), o placar e o vencedor. Motivo: o AC 5 diz que "os hashes comparados saem do arquivo que
  o servidor gravou". São dezenas de bytes por rodada, e não são estado (AC 9).
- **[AUTO-DECISION] Caminho configurável (AC 10)** → a variável de ambiente `BB_REPLAYS` na subida. Sem ela, a pasta
  é `replays/` no diretório de onde o servidor sobe, e ela é criada só na primeira gravação. Um arquivo por partida, com
  o nome `<ISO sem ':'>-<id da sala>.json`. O servidor não apaga nenhum. A porta continua sem override: a única leitura
  de `process.env` em `server/main.ts` é esta (cf. `e4.7`, AC 11).
- **[AUTO-DECISION] Se a gravação lançar no meio da partida** → a partida segue sem replay, e o log diz por quê
  (`gravacao = null`). Derrubar a sala por causa do gravador puniria os jogadores por um defeito de ferramenta. Gravar
  um replay faltando comando reproduziria outra partida.
- **[AUTO-DECISION] Onde mora a conferência da versão (AC 8)** → a constante `FORMATO_DO_REPLAY = 'bb.replay.v1'` fica
  em `net/replay.ts`, junto do tipo. O leitor que recusa outra versão (`lerReplay`) fica em `tools/replay-check.ts`, do
  lado de quem lê. Assim `net/` não ganha leitura de arquivo.
- **Sem reimplementar a reprodução (AC 3):** `verificarReplay` chama `reproduzirPartida` de `tools/partida.ts`, que
  pede uma `PartidaGravada` completa. O `setup` de cada rodada não é gravado (a reprodução o recalcula), e no adaptador
  ele é um getter que lança. Se a reprodução um dia passar a lê-lo, o verificador falha alto em vez de usar um setup
  inventado.
- **Setas:** `net/replay.ts` importa só `sim/types` (tipo `Command`), `match/` e tipos de `net/sala.ts`. Os greps
  `grep -rnw fs src/net/` e `grep -rnwE "ws|document|window|localStorage|Math\.random" src/net/` saem com exit 1, e não
  há `node:` em `src/net/`. `grep -rn "from '\.\./tools/" src/server/` devolve uma linha, a do `hash`. `server/main.ts`
  continua sem ler `Sala.partida`: quem lê é `montarReplay`, em `net/`.

### Verificação

- `npm run check` → rc 0. `npm run sim:check` → rc 0, com `golden hash ✓ ok — 6 seeds batem o baseline`.
  `npm run build` → rc 0. O projeto não tem script `lint`.
- **Guarda do AC 7** (linha `replay` sob `sala pura`): a Bo5 de `bo5PelaSala`, gravada passo a passo por `conduzir`,
  vira `bb.replay.v1` (16 570 B) e passa pelo mesmo `serializarReplay` do servidor, pelo `lerReplay` e pelo
  `verificarReplay`. Hash, ticks, vencedor, seed e lados batem em 3/3 rodadas, com placar 0-3 e vencedores [1 1 1].
  Vão junto conferências de valor: os hashes gravados são os do arnês; os 141 comandos são os carimbados pela guarda
  da `e4.3` e, rodada a rodada, comandos do arnês na ordem; as 14 decisões são o log da sala (16) menos as 2 recusadas.
  Também o poder discriminante: `dx` trocado de sinal e `hash` stub `''` reprovam, e formato `v0`, `v2` ou ausente
  lança na leitura. Numa sala à parte, um cast mandado no mesmo passo que abre a rodada é gravado no tick 6, e um
  `{t:'pronto'}` recusado no meio da rodada fica no log da sala e fora do replay. A gravação roda em todo passo de toda
  sala da guarda (W.O., `anular`, queda no draft, 20 Hz), e não lançou em nenhum.
- **AC 5, partida real contra o servidor da `e4.4`** (script abaixo, fora do repo, com `BB_REPLAYS` no scratchpad):
  foram 8 de 8 conferências. Dois clientes de protocolo jogaram uma Bo5 completa, placar 3-1 em 4 rodadas. Houve
  casts aleatórios dos dois lados, com `dx`/`dy` em precisão total, e 2 492 comandos carimbados. B mandou um
  `{t:'pronto'}` no meio de cada rodada, e os 4 foram recusados. A caiu e reassentou no meio da rodada 2 (pausa de R-02).
  - o `aoVivo` do arquivo é igual aos `rodadaFim` que os dois clientes receberam: hashes `6bb72d64 4b129f56 1850b2bb
    5d6ac69c`. Esses são os `ResultadoRodada.hash` calculados pelo `hash` injetado em `server/main.ts`;
  - `npm run replay:check -- <arquivo>` → rc 0, com hashes, placar e vencedores idênticos;
  - a seed está no arquivo e é a da `visao` dos clientes. Ela não aparece no log de operação;
  - arquivo `2026-09-22T04-52-32-401Z-eaaUX60dPflGJtL1l_W-ow.json`, sha256 `d0a80444…04cd5a`. `server/main.ts` e
    `net/replay.ts` não mudaram depois dessa execução. O verificador foi reexecutado sobre o mesmo arquivo com o código
    final (`7850a7f`), rc 0.
- **Bateria de 10 mutações.** Rodou numa cópia descartável (src/ + `package.json` + junction de `node_modules` no
  scratchpad), restaurada a cada mutação. O repositório não foi tocado. As 10 foram pegas.

  | # | mutação | `sim:check` | partida real + `replay:check` |
  |---|---|---|---|
  | M1 | gravar as decisões recusadas (filtro desligado) | rc 1 | rc 1 — "a reprodução não chegou ao fim da partida" |
  | M2 | descartar os comandos do passo em que a rodada começa | rc 1 (depois da sala à parte; antes passava, ver abaixo) | — |
  | M3 | gravar a intenção (tick − 6) em vez do carimbo | rc 1 | rc 1 — placar reproduzido 0-3 ≠ 1-3 |
  | M4 | regravar os pendentes a cada passo (`slice(0)`) | rc 1 | — |
  | M5 | `montarReplay` com `Sala.decisoes` inteiro | rc 1 | — |
  | M6 | verificador sem comparar o hash | rc 1 | — |
  | M7 | verificador compara o reproduzido com ele mesmo | rc 1 | — |
  | M8 | `lerReplay` sem conferir a versão | rc 1 | — |
  | M9 | servidor com `hashDoMundo: () => ''` (E43-ARC-001) | **rc 0** (esperado: a guarda injeta o `hash` dela) | rc 1 — hash reproduzido ≠ `''` ao vivo |
  | M10 | servidor sem `gravarPasso` | **rc 0** (esperado) | rc 1 — log "replay não montado: a sala fechou 5 rodada(s) e a gravação viu 0", nenhum arquivo |

  M9 e M10 passam no `sim:check` por construção, e é isso que o acréscimo do AC 5 (v1.5.0) diz: a guarda nunca carrega
  `server/main.ts`. Quem as pega é a partida real. **M2 passou verde na primeira rodada da bateria.** Nem a Bo5 nem a
  partida real mandam um cast no mesmo passo que abre a rodada. Por isso a sala à parte da guarda passou a mandar um. Com
  isso, M2 dá rc 1.

### Observações para o @qa

1. **Rodada de W.O. (R-02) não se reproduz.** No estouro da pausa, a sala registra a rodada com o hash e os ticks do
   mundo NAQUELE instante (`estourarPausa` → `encerrarRodada`). O replay roda a rodada até o fim natural, e o
   `replay:check` acusa a divergência. Não foi exercitado. A política de R-02 é provisória e está com o usuário/@pm, e
   nenhum AC daqui cobre o caso. A `e4.7` já marca as rodadas de W.O. à parte.
2. **Sala encerrada antes da fase `fim`** (`'anular'`, ou os dois assentos vagos): o arquivo é gravado com as rodadas
   fechadas, e o `replay:check` acusa "a reprodução não chegou ao fim da partida". Isso é verdade, e não é defeito.
3. **O pool não é gravado** (o AC 4 não o lista). `reproduzirPartida` usa o `POOL` dela (`['golem','vex']`), e o
   servidor usa `Object.keys(CHARS)`, que hoje é a mesma lista. Se o roster crescer, as duas fontes divergem, e não há
   tripwire. Fica registrado, sem conserto aqui.
4. **Quantização do `hash()`.** Sonda no scratchpad, sobre o replay real: 2 dos primeiros 40 comandos da rodada 0 mudam
   o resultado com `dx` trocado de sinal. Nos dois, `dx + 1e-12` passa no verificador. O verificador prova igualdade de
   hash, que é o que o AC 5 pede, e não igualdade de cada bit do estado. É a §11.1 medida neste instrumento. A saída
   do `replay:check` diz isso e não usa "bit a bit".
5. **`.gitignore` está fora do escopo.** Rodar `npm run server` sem `BB_REPLAYS` cria `replays/` na raiz do repo, sem
   ignorar. Sugestão para quem for dono do arquivo: acrescentar `replays/`.
6. **CodeRabbit** não roda nesta máquina (o gate via WSL não está instalado). A cobertura foi check, sim:check, build,
   a partida real e as 10 mutações. A caixa "Pre-Commit (@dev)" continua desmarcada.

### Self-critique (Step 5.5 / 6.5), inline por causa do AC 12

- **Reprodução reimplementada?** Não. `replay-check.ts` só traduz o formato e compara, e a reprodução é `reproduzirPartida`.
  O `aplicar()` em `net/replay.ts` classifica decisões e não roda rodada. O resultado de rodada vem da sala.
- **Estado gravado?** Não: 99,5% dos bytes da partida real são comandos (ver o orçamento).
- **Intenção crua em vez do carimbo?** Não: o que se grava é o `Command` que a sala pôs em `pendentes`, com `tick` e
  `ballId`. M3 prova que a troca seria pega.
- **Caminhos de erro:** gravação que lança (log, partida segue), montagem que lança (log, sem arquivo), disco que
  falha (log), leitor com JSON inválido ou formato alheio (lança com mensagem), caminho inexistente no CLI (rc 2).
- **Pendências:** as observações 1, 3 e 5 dependem de decisão de fora desta story. CodeRabbit está indisponível.

### Story DoD (resumo)

Os 12 ACs estão cobertos: AC 1, 2 e 7 pelo sim:check; AC 3, 4, 8, 10 e 11 pelo código, pelos greps e pela guarda;
AC 5 pela partida real; AC 6 pelo comentário em `tools/replay-check.ts`; AC 9 pelo orçamento abaixo; AC 12 pelo
`git show --stat 7850a7f`. TypeScript estrito, sem `any`, e só as setas do AC 11. Testes: check, sim:check e build
verdes, 8/8 na partida real e 10/10 mutações pegas.

### File List

| arquivo | mudança |
|---|---|
| `src/net/replay.ts` | **novo** — `FORMATO_DO_REPLAY`, tipos `Replay`/`RodadaDoReplay`, `criarGravacao`, `marcarAntesDoPasso`, `gravarPasso`, `montarReplay`, `serializarReplay`. Puro |
| `src/tools/replay-check.ts` | **novo** — `lerReplay` (versão, AC 8), `verificarReplay` (delega a `reproduzirPartida`, AC 5), CLI; comentário do AC 6 |
| `src/server/main.ts` | gravação em volta de `passo()`, `gravarReplay` na sala encerrada, `DIRETORIO_DE_REPLAYS` (`BB_REPLAYS`), cabeçalho "cinco coisas" |
| `src/tools/determinism.ts` | `Condutor.gravacao` em todo passo, `ResumoBo5.replay`, `guardaReplay` e `recusadaNaRodada` (AC 7) |
| `package.json` | script `replay:check` |

### Orçamento de armazenamento medido (AC 9)

| partida | bytes | rodadas | comandos | B/comando | B/rodada |
|---|---|---|---|---|---|
| guarda do `sim:check` (a Bo5 do arnês, com comandos do bot: 47/rodada) | 16 570 | 3 | 141 | ~116 | 5 523 |
| partida real (clientes de teste castando a cada 250 ms: 623/rodada) | 289 558 | 4 | 2 492 | 115,6 | 72 390 |

- **Composição na partida real:** comandos 288 059 B (99,5%), decisões 883 B (20 decisões), e o resto (~616 B) são os
  resultados ao vivo, o placar e o envelope. Nenhum `World`, nenhum snapshot.
- **Contra a §1.3** (54,6 comandos × 26,3 B ≈ 1,4 kB por rodada): o byte por comando é 4,4× maior, 115,6 B contra
  26,3 B. A causa é a codificação: JSON com nomes de campo e `dx`/`dy`/`mag` com precisão total de double (até 17
  dígitos). A tupla quantizada da §1.3 não serve aqui, porque arredondar o comando gravado mudaria o comando
  reproduzido em relação ao que a sala carimbou. Na taxa da §1.3, isso dá ≈ 6,3 kB por rodada e ≈ 32 kB numa Bo5 de 5
  rodadas. É uma ordem de grandeza **abaixo** de "ordens a mais", e o fator vem da forma, não do conteúdo.
- **A partida real pesa 72 kB por rodada** porque os clientes de teste mandam ~4 casts/s por lado, contra 0,35 da
  §1.3. O tamanho cresce linear com os comandos, e o byte por comando é o mesmo nas duas medições.
- Compactar, com tupla posicional sem nomes e ainda sem arredondar, é possível. Não foi feito nem medido, porque
  nenhum AC pede.

### Script da partida real (AC 5)

<details>
<summary><code>e46-partida-real.mjs</code> — fora do repositório; <code>node e46-partida-real.mjs &lt;raiz-do-repo&gt; &lt;pasta-de-saída&gt;</code> (porta 5179 livre)</summary>

```js
// e4.6 — partida REAL contra o servidor de e4.4, gravação do replay e verificação (AC 5, AC 9). FORA do repositório.
// Uso:  node e46-partida-real.mjs <raiz-do-repo> <pasta-de-saida>
// Sobe `node src/server/main.ts` com BB_REPLAYS=<pasta-de-saida>/replays, pega o id da PRIMEIRA linha "sala criada"
// (armadilha do e4.4/AC 10), e conduz dois clientes de protocolo (WebSocket global do Node 24, codec.ts) numa Bo5
// inteira: decisões pela política de bot/partida.ts, casts aleatórios dos dois lados, um {t:'pronto'} de B no meio de
// TODA rodada (recusado ao vivo: é o caso que o filtro de decisões do gravador existe para tirar do replay) e uma
// queda + reassentamento de A no meio da rodada 2 (pausa de R-02). No fim roda `npm run replay:check` no arquivo.
import { spawn, spawnSync } from 'node:child_process'
import { readdirSync, readFileSync, writeFileSync, mkdirSync } from 'node:fs'
import { join } from 'node:path'
import { pathToFileURL } from 'node:url'

const RAIZ = process.argv[2] ?? 'C:/Users/v-vbt/OneDrive/Documentos/Claude/jogo'
const SAIDA = process.argv[3] ?? '.'
const REPLAYS = join(SAIDA, 'replays')
mkdirSync(SAIDA, { recursive: true })
const url = (rel) => pathToFileURL(join(RAIZ, rel)).href
const { decodificarDoServidor } = await import(url('src/net/codec.ts'))
const { criarPolitica } = await import(url('src/bot/partida.ts'))

const ENDERECO = 'ws://127.0.0.1:5179'
const logServidor = []
const srv = spawn(process.execPath, ['src/server/main.ts'], { cwd: RAIZ, stdio: ['ignore', 'pipe', 'pipe'], env: { ...process.env, BB_REPLAYS: REPLAYS } })
let resto = ''
function aoTexto(buf) {
  resto += buf.toString('utf8')
  const linhas = resto.split('\n')
  resto = linhas.pop()
  for (const l of linhas) logServidor.push(l)
}
srv.stdout.on('data', aoTexto)
srv.stderr.on('data', aoTexto)
let servidorVivo = true
srv.on('exit', (c) => { servidorVivo = false; logServidor.push(`<<servidor saiu com ${c}>>`) })

const espera = (ms) => new Promise((r) => setTimeout(r, ms))
async function ate(pred, ms, oQue) {
  const t0 = Date.now()
  while (!pred()) {
    if (Date.now() - t0 > ms) throw new Error(`timeout esperando ${oQue}`)
    await espera(20)
  }
}
const achados = []
function confere(ok, oQue) {
  achados.push({ ok, oQue })
  console.log(`${ok ? 'OK  ' : 'FALHA'} ${oQue}`)
}

class Cliente {
  constructor(nome) {
    Object.assign(this, { nome, recebidas: [], erros: [], fechado: null, jogador: null, assento: null, visao: null, feito: new Set(), politica: null, rodadaEmCurso: false, prontosNaRodada: 0 })
  }
  abrir() {
    return new Promise((ok, falha) => {
      this.ws = new WebSocket(ENDERECO)
      this.ws.onopen = () => ok(this)
      this.ws.onerror = () => falha(new Error(`${this.nome}: erro de socket`))
      this.ws.onclose = (e) => { this.fechado = e.code }
      this.ws.onmessage = (ev) => this.receber(ev.data)
    })
  }
  mandar(obj) { if (this.ws.readyState === 1) this.ws.send(JSON.stringify(obj)) }
  receber(txt) {
    const m = decodificarDoServidor(txt)
    this.recebidas.push(m.t === 'snap' ? { t: 'snap', seq: m.seq } : m)
    switch (m.t) {
      case 'ping': this.mandar({ t: 'pong', id: m.id }); break
      case 'erro': this.erros.push(m.motivo); break
      case 'sala': this.jogador = m.jogador; this.assento = m.assento; break
      case 'visao': this.visao = m.v; this.agir(); break
      case 'rodadaInicio':
        this.rodadaEmCurso = true
        if (this.nome === 'B') {
          // recusado ao vivo por aplicar() (fase rodada); se o replay o gravasse, a loja seguinte o aceitaria
          this.mandar({ t: 'decisao', d: { t: 'pronto', jogador: this.jogador } })
          this.prontosNaRodada++
        }
        break
      case 'rodadaFim': this.rodadaEmCurso = false; break
    }
  }
  agir() {
    const v = this.visao
    if (this.jogador === null) return
    if (this.politica === null) this.politica = criarPolitica(v.seed, this.jogador)
    const { politica, rand } = this.politica
    const j = this.jogador
    if (v.fase === 'draft') {
      const chave = `draft:${v.draft.passo}`
      if (v.draft.ordem[v.draft.passo] === j && !this.feito.has(chave)) {
        this.feito.add(chave)
        this.mandar({ t: 'decisao', d: { t: 'draft', jogador: j, charId: politica.escolherDraft(v, rand) } })
      }
    } else if (v.fase === 'builds') {
      const chave = `builds:${v.rodada}`
      if (!v.prontos[j] && !this.feito.has(chave)) {
        this.feito.add(chave)
        for (const slot of [0, 1]) this.mandar({ t: 'decisao', d: { t: 'build', jogador: j, slot, ...politica.escolherBuild(v, slot, rand) } })
        this.mandar({ t: 'decisao', d: { t: 'pronto', jogador: j } })
      }
    } else if (v.fase === 'loja') {
      const chave = `loja:${v.rodada}`
      if (!v.prontos[j] && !this.feito.has(chave)) {
        this.feito.add(chave)
        for (const d of politica.comprar(v, rand)) this.mandar({ t: 'decisao', d })
        this.mandar({ t: 'decisao', d: { t: 'pronto', jogador: j } })
      }
    }
  }
  castar() {
    if (!this.rodadaEmCurso) return
    const ang = Math.random() * Math.PI * 2 // mira do cliente de teste; dx/dy com precisão total, de propósito
    this.mandar({ t: 'cast', ballIndex: Math.random() < 0.5 ? 0 : 1, slot: Math.random() < 0.8 ? 'ability' : 'ult', dx: Math.cos(ang), dy: Math.sin(ang), mag: Math.random() })
  }
}
async function conectar(nome, entrar) {
  const c = new Cliente(nome)
  await c.abrir()
  c.mandar(entrar)
  return c
}

let rc = 1
try {
  await ate(() => logServidor.some((l) => l.includes('sala criada: id=')), 15000, 'sala criada')
  const id = /sala criada: id=(\S+) /.exec(logServidor.find((l) => l.includes('sala criada: id=')))[1]
  const a = await conectar('A', { t: 'entrar', sala: id })
  await ate(() => a.jogador !== null, 3000, 'A assenta')
  const b = await conectar('B', { t: 'entrar', sala: id })
  await ate(() => b.jogador !== null, 3000, 'B assenta')
  const castador = setInterval(() => { a.castar(); b.castar() }, 250)

  // queda + reassentamento de A no meio da rodada 2 (pausa de R-02: o mundo congela e retoma)
  await ate(() => a.recebidas.filter((m) => m.t === 'rodadaInicio').length >= 2, 600000, 'rodada 2')
  await espera(1500)
  const segredo = a.assento
  a.ws.close()
  await espera(1500)
  const a2 = await conectar('A2', { t: 'entrar', sala: id, assento: segredo })
  await ate(() => a2.recebidas.some((m) => m.t === 'snap'), 5000, 'A2 recebe snap')
  a2.visao = a2.recebidas.find((m) => m.t === 'visao').v
  a2.feito = a.feito
  a2.politica = a.politica
  a2.rodadaEmCurso = true
  const castador2 = setInterval(() => a2.castar(), 250)

  await ate(() => b.recebidas.some((m) => m.t === 'sala' && m.estado === 'encerrada'), 900000, 'sala encerrada em B')
  clearInterval(castador); clearInterval(castador2)
  await ate(() => logServidor.some((l) => l.includes('replay gravado: ')), 5000, 'linha "replay gravado" no log')
  const arquivos = readdirSync(REPLAYS).filter((f) => f.endsWith('.json'))
  confere(arquivos.length === 1 && arquivos[0].endsWith(`-${id}.json`), `um replay gravado em BB_REPLAYS, com o id da sala no nome: ${arquivos.join(', ')}`)
  const caminho = join(REPLAYS, arquivos[0])
  const texto = readFileSync(caminho, 'utf8')
  const replay = JSON.parse(texto)

  // os hashes "ao vivo" do arquivo são os que os DOIS clientes receberam no rodadaFim (o hash injetado pelo servidor)
  const fins = (c) => c.recebidas.filter((m) => m.t === 'rodadaFim').map((m) => m.resultado)
  const noFioB = fins(b)
  const noFioA = [...fins(a), ...fins(a2)]
  const dedup = (l) => [...new Map(l.map((r) => [r.indice, r])).values()]
  confere(JSON.stringify(replay.rodadas.map((r) => r.aoVivo)) === JSON.stringify(noFioB) && JSON.stringify(dedup(noFioA)) === JSON.stringify(noFioB),
    `aoVivo do arquivo = rodadaFim recebidos pelos dois clientes (${noFioB.map((r) => r.hash).join(' ')})`)
  const fim = b.recebidas.find((m) => m.t === 'evento' && m.e.t === 'partidaFim')?.e
  confere(fim !== undefined && JSON.stringify(fim.placar) === JSON.stringify(replay.placar) && Math.max(...replay.placar) === 3, `placar do arquivo ${JSON.stringify(replay.placar)} = partidaFim ${JSON.stringify(fim?.placar)}`)
  const recusasB = b.erros.filter((e) => /pronto/.test(e)).length
  confere(b.prontosNaRodada === replay.rodadas.length && recusasB >= b.prontosNaRodada,
    `B mandou ${b.prontosNaRodada} {t:'pronto'} no meio da rodada, e os ${recusasB} foram recusados ao vivo`)
  const cmds = replay.rodadas.reduce((s, r) => s + r.comandos.length, 0)
  confere(cmds > 0 && replay.rodadas.every((r) => r.comandos.every((c) => Number.isInteger(c.tick) && Number.isInteger(c.ballId))),
    `${cmds} comandos carimbados, todos com tick e ballId inteiros (a forma carimbada, AC 4)`)

  // vazamento: a seed está no arquivo (AC 4) e NÃO no log de operação
  const seed = String(replay.matchSeed)
  confere(!logServidor.join('\n').includes(seed) && a.visao.seed === replay.matchSeed, `matchSeed ${seed} = a seed da visao dos clientes, e ausente do log de operação`)

  // AC 5 — o verificador em Node
  const chk = spawnSync(process.execPath === '' ? 'node' : 'npm', ['run', 'replay:check', '--', caminho], { cwd: RAIZ, encoding: 'utf8', shell: true })
  console.log(chk.stdout)
  confere(chk.status === 0, `npm run replay:check no replay da partida real → rc ${chk.status}`)

  // AC 9 — orçamento
  const bytesCmd = replay.rodadas.reduce((s, r) => s + r.comandos.reduce((q, c) => q + JSON.stringify(c).length + 1, 0), 0)
  const bytesDec = JSON.stringify(replay.decisoes).length
  console.log(`\norçamento: ${Buffer.byteLength(texto)} B total · ${replay.rodadas.length} rodadas · ${cmds} comandos (${(cmds / replay.rodadas.length).toFixed(1)}/rodada, ${(bytesCmd / cmds).toFixed(1)} B/comando, ${bytesCmd} B) · ${replay.decisoes.length} decisões (${bytesDec} B) · ${(Buffer.byteLength(texto) / replay.rodadas.length).toFixed(0)} B/rodada`)
  writeFileSync(join(SAIDA, 'e46-servidor.log'), logServidor.join('\n') + '\n')
  confere(servidorVivo, 'servidor de pé no fim')
  a2.ws.close(); b.ws.close()
  rc = achados.every((x) => x.ok) ? 0 : 1
} catch (err) {
  confere(false, `roteiro abortado: ${err.message}`)
  writeFileSync(join(SAIDA, 'e46-servidor.log'), logServidor.join('\n') + '\n')
} finally {
  srv.kill()
  console.log(`\n${achados.filter((x) => x.ok).length}/${achados.length} conferências OK`)
  process.exit(rc)
}
```

</details>

A bateria de mutações (`e46-mutacoes.mjs`) aplica as 10 trocas da tabela acima, uma de cada vez, a
`<cópia>/src/<arquivo>`. Cada padrão tem de casar exatamente uma vez. Depois ela roda `node src/tools/determinism.ts`
na cópia e, em M1, M3, M9 e M10, o script acima contra o servidor da cópia.

## QA Results

### Gate 2026-09-22 — Quinn (@qa): **CONCERNS** → Status Done

Arquivo do gate: `docs/qa/gates/e4.6-gravador-de-replay.yml`. Revisão: `7850a7f`. O HEAD andou para `197047e`
(gate da e4.5, só docs), e `git diff 7850a7f HEAD -- src package.json package-lock.json tsconfig.json` sai vazio.
`7850a7f` não está em `origin/master`.

**Os 12 ACs estão MET.** Refiz tudo numa cópia descartável de `7850a7f` (git archive + junction), sem tocar a árvore
compartilhada, nas portas 5261 a 5272.

| verificação | resultado |
|---|---|
| `check` / `sim:check` / `build` (AC 1, 2, 7) | rc 0 / rc 0 com "golden hash ✓ ok — 6 seeds" / rc 0. A linha `replay` da guarda deu 3/3 (16 570 B, 141 comandos, 14 de 16 decisões) |
| escopo (AC 12) | `git show --stat 7850a7f`: exatamente os 5 arquivos, 638/7 |
| pureza e setas (AC 11) | `fs`, `node:`, `ws/document/window/localStorage/Math.random/process` e `Date.now/performance.now` dão rc 1 em `src/net/`. Nenhum `from '../tools/'` em `src/net/`, então não há ciclo |
| partida real do @dev (AC 5, 4, 9) | 8/8. `replay:check` rc 0, [3,0] em 3 rodadas, 2 085 comandos, 115,5 B/comando |
| roteiro meu: duas Bo5 simultâneas + reconexão do assento 1 | 8/8. Os dois replays deram rc 0, com seeds distintas |
| hash stub no servidor (E43-ARC-001) | sim:check verde por construção. Na partida real, `replay:check` rc 1 nas 3 rodadas |
| só as decisões aceitas (desvio 6) | Correto. Conferi por leitura (recusa por identidade fora do log; nenhuma decisão legal em `rodada`) e por R1/Q2 |
| W.O. de R-02 (desvio 1), exercitado | servidor real: `replay:check` rc 1, "ticks reproduzido 4214 != 92". Driver puro: "placar 0-3 != 3-2" |
| mutações próprias (11) | pegas: R1, R9, Q2 e Q3 (Q3 pela guarda; o verificador acha a ordem entre bolas no mesmo tick equivalente). Sobreviventes que viraram achados: Q5, Q15, Q12, Q8. Q14 sobrevive, mas é redundante |

**Achados:**

- **E46-ARC-001 (medium, @architect + usuário via lista de R-02):** uma partida com rodada de W.O., ou encerrada
  antes de `fim`, grava um replay que nunca verifica, e a falha parece divergência de determinismo. A v1 não tem
  como marcar como a rodada acabou. A falha é alta e não silenciosa, e é aceitável aqui porque R-02 está em
  aberto. Mas P4.3 fica com esse asterisco.
- **E46-TST-001 (low):** os canários do verificador na guarda ficam na rodada 0, e só no campo hash. Q5 (comparar
  só a rodada 0) e Q15 (contagem de rodadas) passam verdes. Q5 aprova um dx trocado na rodada 2 do replay real.
- **E46-TST-002 (low):** o ramo de recomeço da gravação na queda no draft não tem conferência de saída. Q12 fica
  verde, e é nociva: 21/20 decisões, "placar 0-3 != 3-1".
- **E46-TST-003 (low):** o roteiro do @dev joga uma partida só. Uma gravação única compartilhada entre as salas
  (Q8) passa 8/8, e só o meu roteiro concorrente a pega.
- **E46-DOC-001 (low, @po):** "bit-exata/bit-a-bit" no título, no AC 5 e no `quality_gate_tools`. O instrumento
  prova igualdade de hash quantizado (dx+1e-12 aprovado em 38/40). Aceito a redação do @dev.
- **E46-ARC-002 (low):** o replay não carrega pool nem carimbo de build (fica preso à versão do código), e
  `replays/` não está no `.gitignore`.

**Desvios do @dev adjudicados:** (1) W.O. → aceitável nesta story, virou E46-ARC-001. (2) Pool não gravado →
aceitável hoje, as duas fontes dão `['golem','vex']`, e ficou em E46-ARC-002. (3) Hash e não bits → aceito,
E46-DOC-001. (4) `replays/` sem `.gitignore` → LOW, fora do AC 12. (5) 5,5 kB/rodada → **não** viola o AC 9: é
medido e registrado, 99,5% são comandos, e o fator de 4,4× vem da codificação (a §1.3 mediu tupla quantizada).
(6) Só decisões aceitas → correto e necessário.

CodeRabbit não roda nesta máquina. Não sobrou processo nem porta do gate, e o `node_modules` do repositório está
intacto.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-08-19 | 1.0.0 | Story criada a partir do passo 6 de `docs/architecture-e4.md` §10 (P4.3) | @sm |
| 2026-08-19 | 1.1.0 | **Validação @po: GO 10/10** (7/10 antes da correção). **Status: Draft → Ready** — bloqueio de ordem (`e4.4`), não de decisão. Achado bloqueante: **ciclo entre camadas.** O AC 11 punha montagem **e reprodução** em `src/net/replay.ts`, e o AC 3 mandava a reprodução delegar a `tools/partida.ts` — fechando `tools/ → net/ → tools/`, já que a primeira seta foi criada em `e4.2`/AC 11. `match/types.ts` registra exatamente esse raciocínio para o caso análogo de `ResultadoRodada.hash` (*"inverteria a seta e criaria ciclo"*). Corrigido com divisão em três: tipo e montagem em `net/replay.ts` (puro), escrita em disco em `server/`, e **reprodução/verificação em `src/tools/replay-check.ts`** como CLI (`npm run replay:check`). A correção também melhora o AC 6: a verificação passa a ser explicitamente uma ferramenta Node, que é o que `tools/` é. | @po |
| 2026-09-21 | 1.2.0 | **Sequência de `determinism.ts` registrada (roteamento do gate de `e4.8`, `docs/qa/gates/e4.8-codec-do-fio.yml`, commit `438740b`). Status permanece Ready. Nenhum AC de conteúdo muda.** O bloqueio de ordem não muda (`e4.4` → R-05). **(1) "Depende de":** esta story vem por último na fila de `src/tools/determinism.ts` (`e4.8` → `debt.11` → `e4.3` → `debt.12` sugerida → esta), e começa só com `git status --short src/tools/determinism.ts` vazio. **(2) Conferência de escopo:** `quality_gate_tools` e Task de verificação passam de `git diff --stat` a `git show --stat <commit de implementação desta story>`. Numa árvore compartilhada, `git diff --stat` absorveria mudanças de outra story em curso. É a mesma regra já aplicada em `e4.3`, `debt.10` e `debt.11`. **Fatos conferidos:** o AC 12 lista `src/tools/determinism.ts` entre os alterados (guarda do AC 7). Entre as stories não fechadas, só `debt.11`, `e4.3` e esta citam o arquivo. | @po |
| 2026-09-21 | 1.3.0 | **Sequência de `determinism.ts` atualizada pela §11.6.1 de `architecture-e4.md` (E48-ARC-003, commit `bed4603`). Status permanece Ready. Nenhum AC muda.** A story da versão do fio (sugestão `e4.9`, a criar pelo @sm; spec no Change Log de `e4.8`, v1.5.0) também edita `determinism.ts` e entra entre `debt.11` e `e4.3`. Ordem do arquivo: `e4.8` → `debt.11` → `e4.9` → `e4.3` → `debt.12` → esta. A pré-condição de início (`git status --short src/tools/determinism.ts` vazio) e a conferência de escopo por `git show --stat` não mudam. Como esta story já vem depois de `e4.3` e `e4.4`, a entrada de `e4.9` não a atrasa. | @po |
| 2026-09-21 | 1.4.0 | **Sequência de `determinism.ts` atualizada pelo roteamento do gate de `e4.3` (`docs/qa/gates/e4.3-sala-pura.yml`, `5f6c44f`). Status permanece Ready. Nenhum AC muda.** Os achados E43-TST-001/002/003 foram para uma story nova, sugestão `debt.14`, a criar pelo @sm (spec no Change Log de `e4.3` v1.9.0). Ela edita o bloco `sala pura` de `determinism.ts` e entra entre `debt.12` e esta. Ordem do arquivo: `e4.8` → `debt.11` → `e4.9` → `e4.3` → `debt.12` → `debt.14` → esta. A pré-condição de início (`git status --short src/tools/determinism.ts` vazio) e a conferência de escopo por `git show --stat` não mudam. Esta story já vem depois de `e4.4`, e `debt.14` é pré-condição de `e4.4` (v1.7.0), então a entrada de `debt.14` não a atrasa. **Nota para quando esta story for aberta:** a guarda do replay (AC 7, AC 12) vai conviver com as asserções de entrega por assento que `debt.14` acrescenta em `conduzir`. | @po |
| 2026-09-21 | 1.5.0 | **Decisões do @architect sobre o gate de `e4.3` aplicadas (`architecture-e4.md` §2.2 E43-ARC-001 e §11.6.2 E43-ARC-002, commit `188998e`). Status permanece Ready.** O bloqueio de ordem não muda (`e4.4` → R-05). O AC 12 (escopo) não muda. **(1) AC 5, acréscimo:** os hashes "da execução ao vivo" são os `ResultadoRodada.hash` que a sala do servidor gravou, com o `hash` injetado por `server/main.ts` (`e4.4`/AC 12 v1.8.0), e não os que a guarda do AC 7 calcula. É a prova de ponta a ponta de E43-ARC-001: um `hashDoMundo` que não seja o `hash()` de `tools/harness.ts` (um stub `''`, uma lambda sobre outra função) reprova aqui. A guarda do AC 7 não vê isso, porque injeta ela mesma o `hash` do arnês (`determinism.ts:1742` em `5766137`, `hashDoMundo: hash`) e nunca carrega `server/main.ts`. `quality_gate_tools` e Testing ajustados. **(2) "Depende de", sequência de `determinism.ts`:** a story nova da variante `{t:'evento'}` (sugestão `e4.10`, a criar pelo @sm; spec no Change Log de `e4.3` v1.10.0) edita o bloco `sala pura` do arquivo e entra antes desta. **Ordem vigente, escolhida pelo @po:** `e4.8` → `debt.11` → `e4.9` → `e4.3` → `debt.12` → `debt.14` → `e4.10` → `e4.6`. A §11.6.2 escreve `debt.12` → `e4.10` → `e4.6` porque foi redigida sem `debt.14` na fila. As duas formas concordam em "entre `debt.12` e `e4.6`", e o motivo de pôr `e4.10` depois de `debt.14` está no Change Log de `e4.3` v1.10.0. A pré-condição de início (`git status --short src/tools/determinism.ts` vazio) e a conferência por `git show --stat` não mudam. Esta story já vinha depois de `e4.4`, e `e4.10` não depende de R-05, então a entrada dela não atrasa esta. **Nota para quando esta story for aberta:** a guarda do replay (AC 7) vai conviver com as asserções de `{t:'evento'}` por assento que `e4.10` acrescenta em `conduzir`. **Fatos conferidos em `188998e` e `5766137`:** `criarSala({ id: 'guarda-e4.3', …, hashDoMundo: hash })` em `src/tools/determinism.ts:1448` (`:1742` depois do commit de `debt.12`); `hash: n.injetado.hashDoMundo(r.world)` em `src/net/sala.ts:603`; `src/server/` ainda não existe. | Pax (@po) |
| 2026-09-21 | 1.6.0 | **R-05 ratificada pelo usuário (2026-09-21, `architecture-e4.md` §12/R-05, bloco "Resolução"). Status permanece Ready.** "Depende de" deixa de dizer que a `e4.4` está bloqueada em R-05, e a ordem continua: esta story começa depois da `e4.4`, que foi revalidada e está Ready (v1.9.0). Nenhum AC muda. A ordem de `src/tools/determinism.ts` também não muda. | Pax (@po) |
| 2026-09-22 | 1.7.0 | Development started (yolo mode) — Status: Ready → InProgress | Dex (@dev) |
| 2026-09-22 | 1.8.0 | Development complete — Status: InProgress → InReview. Implementação em `7850a7f` (pai `e279c43`, docs da `e4.5`; código-base `205e693`): `src/net/replay.ts` e `src/tools/replay-check.ts` novos, `src/server/main.ts`, `src/tools/determinism.ts` e `package.json` alterados — os 5 do AC 12. `check`, `sim:check` (golden hash idêntico) e `build` verdes. Guarda do AC 7 verde (3/3 rodadas, poder discriminante e versão conferidos). Partida real contra o servidor: 8/8, `replay:check` rc 0 em 4 rodadas. 10/10 mutações pegas (M9/M10 só pela partida real, como o AC 5 prevê). Orçamento: ~116 B/comando, 5,5 kB/rodada na Bo5 do arnês. Observações ao @qa: W.O. de R-02 não se reproduz, pool não gravado, hash quantizado, `replays/` sem `.gitignore`. Detalhes no Dev Agent Record | Dex (@dev) |
| 2026-09-22 | 1.9.0 | **QA gate CONCERNS — Status: InReview → Done.** Os 12 ACs estão MET, com verificação independente em cópia descartável de `7850a7f`: check, sim:check (golden hash idêntico) e build verdes; partida real 8/8; duas partidas simultâneas 8/8; hash stub reprova no `replay:check`; W.O. de R-02 exercitado; 11 mutações. Achados: E46-ARC-001 (medium, W.O. e sala encerrada antes do fim não verificam na v1), E46-TST-001/002/003, E46-DOC-001 e E46-ARC-002 (low). Gate em `docs/qa/gates/e4.6-gravador-de-replay.yml` | Quinn (@qa) |
| 2026-09-22 | 1.10.0 | **E46-DOC-001 (low, @po, gate `docs/qa/gates/e4.6-gravador-de-replay.yml`, `7a7b548`): a redação passa de "bit-exata/bit-a-bit" para "igualdade do hash arredondado". Status permanece Done. Só o texto da story muda: nenhum critério, nenhum código, nenhum resultado do gate.** O gate mediu que o `replay:check` prova que a string de `hash()` é igual, junto com o placar e os vencedores, e não que cada bit do estado é igual. `dx + 1e-12` passa em 38 de 40 comandos. **Trocados:** o título; o AC 5, cujo cabeçalho passa a "Verificação por igualdade do hash arredondado" e que ganha uma nota com a composição do hash e a distinção entre a execução, bit-exata por construção na mesma engine, e o que o instrumento prova; o item de `quality_gate_tools` do AC 5; e a linha de Testing da partida real. **Não trocados, de propósito:** a tabela "Por que a verificação é do Node" nas Dev Notes ("mesma engine que gravou → bit-exato") e o "igualdade é exata por construção" logo abaixo dela. Os dois citam a §7.2 sobre a **execução** na mesma engine, e isso continua verdadeiro. O Dev Agent Record e o QA Results não são do @po, e ficaram como estão. **Fatos conferidos:** `hash()` em `src/tools/harness.ts:75-89` é FNV-1a sobre `tick`, `winner` e, por bola, `id`, `x`/`y`/`vx`/`vy`/`hp` com `toFixed(4)` e `alive`. A saída de `src/tools/replay-check.ts:192` já diz "prova igualdade de hash, não de cada bit do estado (§11.1)". O que o gate sugere ao @architect (um digest sem quantização na v2) não entra aqui: isso é da §7.3 e da `e4.12`. | Pax (@po) |
