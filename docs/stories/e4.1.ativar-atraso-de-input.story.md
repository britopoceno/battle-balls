# Story e4.1: P4.1 — ativar o atraso de input no modo local (`INPUT_DELAY_TICKS` 0 → 6)

## Status

InReview

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run check", "npm run sim:check", "smoke manual: cast sai ~100ms depois, mira continua imediata", "git diff --stat: só src/client/main.ts"]
```

## Story

**Como** desenvolvedor executando o **passo 1** do plano de construção da Fase 4
(`docs/architecture-e4.md` §10),
**eu quero** remover o literal `const INPUT_DELAY_TICKS = 0` de `src/client/main.ts` e passar a ler
`ATRASO_ALVO_TICKS` de `net/protocolo.ts`, com valor efetivo **6**,
**para que** o critério **P4.1** do portão da Fase 4 seja satisfeito, o desvio consciente registrado desde
a Fase 0 seja fechado, e o modo solo passe a ter a mesma sensação de input que o 1v1 vai ter — o que é o
que torna o treino contra o bot honesto.

## Depende de

`e4.0` (`net/protocolo.ts` existe e exporta `ATRASO_ALVO_TICKS = 6`). Sem ela não há de onde importar, e
recriar o literal aqui violaria a definição única do AC 4 de `e4.0`.

## Acceptance Criteria

1. `npm run check` verde.
2. **`npm run sim:check` verde, com golden hash IDÊNTICO.**
3. **AC 2 é uma armadilha deliberada, e o comportamento esperado dela está escrito aqui.**
   `architecture-e4.md` §4.2 afirma que esta mudança é **hash-neutra**, e dá o motivo: o arnês nunca usa
   a constante — os dois bots carimbam o tick corrente (`bot/heuristic.ts:583` e `bot/dummy.ts:30,43`:
   `tick: view.tick`), e `sim:check` roda só com bots. A constante só afeta input **humano**, que não
   existe em teste automatizado.

   **Se o hash mover, a story PARA.** Não re-gravar baseline, não ajustar o teste: existe um acoplamento
   que ninguém sabia que existia, e descobri-lo é o resultado valioso. Escalar para @architect com o diff
   do hash. *(A única story do projeto autorizada a mover o golden hash foi `e3.6`, por decisão explícita
   de D-05; esta não é ela.)*
4. `const INPUT_DELAY_TICKS = 0` some de `src/client/main.ts:58`. No lugar,
   `import { ATRASO_ALVO_TICKS } from '../net/protocolo.ts'`, e o ponto de uso (`main.ts:311`) passa a
   somar `ATRASO_ALVO_TICKS`.
5. O valor efetivo é **6** — ~100ms a 60 Hz. Nenhum literal `6` novo em `client/`: o número mora em
   `net/protocolo.ts` e mais lugar nenhum.
6. **RF-34 continua valendo, e é verificação manual obrigatória:** a mira aparece **imediatamente** na
   bola ao arrastar (feedback local, `client/input.ts`), mesmo com o efeito saindo ~100ms depois. Se o
   arrasto passar a ter lag visual, a mudança vazou de `main.ts` para o caminho de input, e isso reprova.
7. **Smoke manual, com o texto do que se espera sentir** — porque "parece igual" e "parece pior" são os
   dois resultados possíveis e ambos são informação:
   - jogar pelo menos uma rodada completa no modo local, no aparelho (o mesmo alvo de P3.4);
   - confirmar que o cast sai perceptivelmente depois do toque, e que isso **não** atrapalha — é o Pilar
     "você não pilota a bola" sendo testado pela primeira vez com atraso real;
   - registrar a impressão no Dev Agent Record, em uma frase. Não é métrica; é o dado que P4.4 vai
     precisar comparar depois.
8. **O registro histórico fecha.** `docs/architecture-e3.md` §11.2 diz *"`INPUT_DELAY_TICKS = 0` continua
   0 — é desvio consciente registrado, e P4.1 é da Fase 4"*, e `docs/GDD.md` §8 diz *"Hoje o cliente roda
   com `INPUT_DELAY_TICKS = 0` — desvio consciente da Fase 0, registrado, e ativá-lo em 6 é pré-condição
   do portão da Fase 4"*. Esta story **fecha os dois**, e o GDD §8 fica descrevendo código que não existe
   mais — um documento que descreve o código errado é pior que documento nenhum.

   **Correção do @po na validação: a emenda do GDD NÃO é trabalho do @dev.** `docs/GDD.md` é documento de
   design, e o precedente do projeto é claro — emendas de documento saem em commit próprio do @architect
   (`106de11`, *"docs(architect): errata §14"*). O que esta story exige do @dev é **registrar a pendência**
   no Dev Agent Record com a frase exata que ficou obsoleta; a emenda é handoff para @architect, e o @qa
   confere que o handoff foi registrado — não que o GDD foi editado.
9. **Escopo de arquivos.** `src/client/main.ts`, e **só ele**. Nenhuma linha de `src/sim/`, `src/match/`,
   `src/shop/`, `src/bot/`, `src/chars/`, `src/tools/`, `src/client/input.ts`, `src/client/render.ts` —
   nem de `docs/` (ver AC 8). `git diff --stat` prova.
10. **Esta story não implementa rede.** O atraso passa a existir no modo **local**. Quem carimba o tick no
    servidor é `e4.4`; o modo conectado é `e4.5`. O que se ganha aqui é P4.1 e a sensação, não transporte.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend / Game feel
**Secondary Type(s)**: Gate criterion (P4.1)
**Complexity**: Trivial em linhas, **não trivial em consequência** — é a primeira vez que o jogo é jogado
com atraso de input, e o julgamento humano do AC 7 é o único instrumento que existe para isso.

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa (quality gate — a hash-neutralidade do AC 3 precisa ser verificada por reexecução independente, não
  aceita por relato)

**Supporting Agents**:
- (nenhum necessário)

### Quality Gate Tasks

- [x] Pre-Commit (@dev): Rodar antes de marcar a story como completa — CodeRabbit indisponível nesta máquina (o gate via WSL não roda); coberto por `npm run check` + `npm run sim:check` + `npm run build`
- [ ] Pre-PR (@github-devops): Rodar antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 10 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Literal `6` reaparecendo em `client/` em vez do import (AC 5)
- Qualquer mudança em `client/input.ts` — a mira local não pode ganhar atraso (AC 6)

**Secondary Focus**:
- Baseline de golden hash re-gravado (AC 3 proíbe explicitamente)

## Tasks / Subtasks

- [x] Task 1 — A troca (AC: 4, 5)
  - [x] Remover `const INPUT_DELAY_TICKS = 0` de `src/client/main.ts`
  - [x] Importar `ATRASO_ALVO_TICKS` de `../net/protocolo.ts` e usá-lo em `main.ts:311` (depois da troca, `main.ts:309`)
  - [x] Conferir por grep que nenhum literal novo apareceu

- [x] Task 2 — Verificação automática (AC: 1, 2, 3)
  - [x] `npm run check` — 0 erros
  - [x] `npm run sim:check` — **golden hash idêntico** (saída completa igual byte a byte à de antes da troca)

- [ ] Task 3 — Smoke manual (AC: 6, 7) — **PENDENTE: exige um humano no aparelho** (ver Dev Agent Record)
  - [ ] Jogar uma rodada completa no aparelho
  - [ ] Confirmar mira imediata ao arrastar (RF-34)
  - [ ] Registrar a impressão sobre o atraso em uma frase no Dev Agent Record

- [x] Task 4 — Registrar a pendência documental (AC: 8) — **não editar docs**
  - [x] Anotar no Dev Agent Record a frase de `docs/GDD.md` §8 que ficou obsoleta, verbatim
  - [x] Handoff para @architect emendar em commit próprio
  - [x] *(`architecture-e3.md` §11.2 é registro histórico de uma fase fechada — **não** editar; ele
        descreve corretamente o que era verdade na Fase 3)*

- [x] Task 5 — Escopo (AC: 9)
  - [x] `git diff --stat` — só `src/client/main.ts`

## Dev Notes

### Por que isto é hash-neutro (fonte: `architecture-e4.md` §4.2)

Vale escrever a cadeia inteira, porque o instinto correto ao ver "mudar uma constante de tempo" é esperar
o hash mover:

- `sim:check` roda partidas **só com bots** (`dummy` e `heuristic`).
- Os dois bots carimbam o tick corrente: `bot/heuristic.ts:583` (`tick: view.tick`) e `bot/dummy.ts:30,43`.
- Nenhum deles lê `INPUT_DELAY_TICKS`. A constante vive em `client/main.ts` e só é somada no caminho de
  input **humano** (`main.ts:311`, dentro de `disparar()`).
- Logo, mudar 0 → 6 não altera nenhum `Command` que o arnês produz, e o golden hash não se move.

Se mover, uma das premissas acima é falsa — e essa é uma informação que vale mais que a story.

### Onde a latência vai parar depois (fonte: §4.4, R-01)

Esta story entrega 100ms de atraso **local**, sem rede. No modo conectado o número real será
`RTT/2 + 100ms + atraso de interpolação`, plausivelmente 150-250ms num celular em 4G — maior que o que o
diagrama do GDD §8 sugere. Isso está devolvido ao @pm como **R-01** e não bloqueia esta story: o que se
mede aqui é a sensação do atraso puro, sem o ruído da rede, e é justamente o baseline que P4.4 vai querer
comparar.

### Por que o atraso não incomoda (fonte: §4.3, e agora medido)

`architecture-e4.md` §1.3 mediu: **0,35 comando por segundo por lado**. Um atraso de 100ms num evento que
acontece a cada ~3 segundos é invisível; o mesmo atraso num eixo analógico amostrado 60×/s seria
intolerável. É o Pilar *"você não pilota a bola"* pagando dividendo, e o AC 7 é o teste dessa afirmação
com um humano no controle em vez de um argumento.

### O que esta story explicitamente NÃO faz

- **Não abre socket, não cria sala, não serializa nada.** Passos 4, 3 e 2 respectivamente.
- **Não mexe em `client/input.ts`.** A mira local é imediata hoje e continua imediata (AC 6).
- **Não decide a compensação de RTT** de §4.4 — é lever que só entra se P4.4 mostrar injustiça entre pings.
- **Não edita `architecture-e3.md`.** Aquele documento registra corretamente o que era verdade na Fase 3.

### Testing

- `npm run check` — 0 erros.
- `npm run sim:check` — golden hash idêntico. **Reexecutar independentemente no gate**, não aceitar relato.
- Grep: `INPUT_DELAY_TICKS` não existe mais em `src/`; `ATRASO_ALVO_TICKS` aparece em `net/protocolo.ts`
  (declaração) e `client/main.ts` (uso), e em nenhum outro lugar.
- Smoke manual no aparelho: uma rodada completa, mira imediata, impressão registrada.
- `git diff --stat` prova o escopo.

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (1M context), como @dev (Dex), em modo YOLO.

### File List

| Arquivo | Mudança |
|---|---|
| `src/client/main.ts` | Modificado. Saíram `const INPUT_DELAY_TICKS = 0` e o docblock dela. Entrou `import { ATRASO_ALVO_TICKS } from '../net/protocolo.ts'`, e o ponto de uso em `disparar()` passou a somar `ATRASO_ALVO_TICKS` |
| `docs/stories/e4.1.ativar-atraso-de-input.story.md` | Registro da story (status, checkboxes, Dev Agent Record, Change Log) |

### Referências de linha, reconferidas antes da troca

Todas batem com o que o @po validou: `client/main.ts:58` (`const INPUT_DELAY_TICKS = 0`),
`client/main.ts:311` (`tick: world.tick + INPUT_DELAY_TICKS`), `bot/heuristic.ts:583`
(`cmds.push({ tick: view.tick, ... })`), `bot/dummy.ts:30` e `:43` (`tick: view.tick`). Depois da troca, o
ponto de uso está em `client/main.ts:309`: o import soma 1 linha, o docblock removido tira 7 e o comentário
novo soma 4. Os bots não foram tocados.

### Setas (`architecture-e4.md` §2.2)

A tabela de camadas diz `client/ → todos`, então `client/ → net/` é permitido. `net/protocolo.ts` só importa
**tipos** de `match/types.ts` e `sim/types.ts`, então o import não traz nada de runtime para o cliente além
da própria constante. A direção inversa também foi conferida: `grep -rnwE "from '\.\./client/" src/net/`
dá exit 1 (limpo).

### Verificação automática (AC 1, 2, 3, 5, 9)

| Verificação | Resultado |
|---|---|
| `npm run check` (`tsc --noEmit`) | exit 0, 0 erros |
| `npm run sim:check` | exit 0: `golden hash    ✓ ok — 6 seeds batem o baseline` |
| Hash-neutralidade (AC 3) | `sim:check` rodado **antes** e **depois** da troca, e o `diff` das duas saídas completas deu idêntico, inclusive os hashes curtos impressos em `política bot` (ex.: `d12c2706`, `4bcb7488`). Nenhum diff em `src/tools/` (baseline intocado) |
| `npm run build` (Vite) | exit 0: o import de `net/` resolve no bundle |
| `grep -rnw "INPUT_DELAY_TICKS" src/` | exit 1: não existe mais em `src/` |
| `grep -rnw "ATRASO_ALVO_TICKS" src/` | só `net/protocolo.ts:38` (declaração, `= 6`) e `client/main.ts:8` (import) / `:309` (uso) |
| Nenhum literal `6` novo (AC 5) | linhas adicionadas no diff de `main.ts` filtradas por `grep -w 6`: exit 1 (nenhum) |
| `git diff --stat` (AC 9) | código: só `src/client/main.ts` (+6/−8) |

Os greps são recursivos e com fronteira de palavra (`-rnw`), como pedem E40-INF-004 e E40-TST-003, e exit 1
significa limpo.

A cadeia de hash-neutralidade de §4.2 se sustentou. Nenhuma premissa caiu e nenhum acoplamento escondido
apareceu, então não há nada a escalar para @architect.

### Impressão do smoke (AC 7)

**PENDENTE, não executado.** Este agente não tem aparelho nem humano no controle, e o AC 7 pede
justamente o julgamento de um humano jogando uma rodada completa no aparelho alvo de P3.4. Os AC 6 (mira
imediata, RF-34) e 7 (o cast sai ~100ms depois e não atrapalha) ficam **em aberto até o smoke manual**. A
frase de impressão deve ser escrita aqui por quem jogar.

O que dá para afirmar só lendo o código (e isso **não** substitui o AC 6): a mudança está toda dentro de
`disparar()` em `main.ts`, que só roda depois do disparo. `client/input.ts` e `client/render.ts` não têm
diff, então o caminho da mira local não foi tocado.

### Pendência documental: handoff para @architect (AC 8)

`docs/GDD.md` §8 (*Multiplayer e rede*, linhas 560-561) descreve código que não existe mais. A frase
obsoleta, verbatim:

> Hoje o cliente roda com `INPUT_DELAY_TICKS = 0` — desvio consciente da Fase 0, registrado, e ativá-lo
> em 6 é pré-condição do portão da Fase 4.

**Handoff:** @architect emenda em commit próprio (precedente `106de11`). Esta story não editou o GDD (AC 8, 9).
`docs/architecture-e3.md` §11.2 (linha 1105) **não** é pendência: é registro histórico da Fase 3 e era
correto na época (Task 4).

### Decisões

- [AUTO-DECISION] Pôr um comentário no ponto de uso? → Sim, 4 linhas dizendo por que o número vem de
  `net/`, que só o efeito atrasa (RF-34) e por que o hash não se move (razão: o docblock removido guardava
  o porquê da constante, e sem ele o próximo leitor não saberia que o solo e o servidor compartilham o
  atraso de propósito). O comentário diz "~100ms" e não o número de ticks, para não criar um segundo `6`.
- [AUTO-DECISION] Onde pôr o import → logo depois do import de `../tools/harness.ts`, junto dos outros
  imports de camadas vizinhas.
- [AUTO-DECISION] Status com AC 6/7 pendentes → "Ready for Review", como pedido, com a pendência manual
  declarada acima. O @qa decide se o gate espera o smoke ou sai como CONCERNS.

### Self-critique (checkpoints 5.5 / 6.5)

- Escopo: 1 arquivo de código, nenhuma feature fora dos AC. ✓
- Nenhum literal duplicado; a definição única continua única. ✓
- Nenhum re-baseline e nenhum ajuste de teste. ✓
- Nenhuma afirmação sobre a sensação de jogo feita sem jogar. ✓
- IDS: REUSE. A constante já existia em `net/protocolo.ts` (e4.0), e nada novo foi criado.

## QA Results

### Gate 2026-09-21 — Quinn (@qa) — **PENDING: o código está aprovado; a story fica em InReview até o smoke no aparelho**

Arquivo do gate: `docs/qa/gates/e4.1-ativar-atraso-de-input.yml` · revisão: `b8e8a41`

**O que foi verificado de forma independente (não aceitei o relato):**

| AC | Veredito | Evidência própria |
|---|---|---|
| 1 | MET | `npm run check` exit 0 |
| 2 | MET | `npm run sim:check` exit 0, golden hash ok; `npm run build` exit 0 |
| 3 | MET | saída completa do `sim:check` idêntica entre um worktree em `b8e8a41^` e `b8e8a41` (`diff` vazio, incluindo os 6 hashes curtos de `política bot`); bots carimbam `view.tick` (`dummy.ts:30,43`, `heuristic.ts:583`); `determinism.ts` não importa `client/` |
| 4 | MET | `INPUT_DELAY_TICKS` → `grep -rnw` rc=1 em `src/`; import em `main.ts:8`, uso em `main.ts:309` |
| 5 | MET | `ATRASO_ALVO_TICKS` só é declarado em `net/protocolo.ts:38`; nenhum `6` nas linhas adicionadas |
| 6 | **NOT MET: não executado** | exige humano no aparelho; ler o código (`input.ts`/`render.ts` sem diff) não substitui |
| 7 | **NOT MET: não executado** | a frase de impressão não existe |
| 8 | MET | frase do GDD §8 registrada verbatim com handoff; o @architect emendou em `261c4ab` |
| 9 | MET | `git show --stat b8e8a41`: código só em `src/client/main.ts` (+6/−8) |
| 10 | MET | nenhum transporte; mudança confinada a `disparar()` |

Setas (§2.2): `client/ → net/` é permitido; `protocolo.ts` só traz `import type`. O comentário em
`main.ts:305-308` está correto. Leitores do tick carimbado: `main.ts:403` regrava para `w.tick` na
entrega, então o valor só decide **quando** o comando entra. `abrirRodada` zera `pendentes`, e
`castCommand` protege morte/recarga/ult. A telemetria `cast` é gravada no instante do input e não tem
`tick`. Não existe replay de rodada humana. Nenhuma premissa `tick == world.tick` quebrou.

**Controle de mutação:** com `world.tick + 600` o `sim:check` sai idêntico. Ele é cego ao caminho
humano. O AC 2 prova que não há acoplamento, mas não guarda o valor 6 (E41-TST-003).

**Por que PENDING e não CONCERNS:** CONCERNS leva a story a Done (`story-lifecycle.md`), e o AC 6 é
"verificação manual obrigatória" que "reprova". Uma checagem capaz de reprovar não vira observação em
aberto. O precedente é a `e3.4` (smoke P3.4 pendente, "a story não pode ir a Done sem ela"). P4.1 é
critério do portão da Fase 4, e Done o faria parecer fechado sem que ninguém tivesse jogado com o
atraso. As saídas são duas: (a) o usuário joga, escreve a frase do AC 7 e o @qa refaz o gate curto; ou
(b) o usuário dispensa explicitamente os ACs 6/7, e o gate vira WAIVED, como na `e3.6`.

**Achados:**

| ID | Sev. | Dono | Resumo |
|---|---|---|---|
| E41-REQ-000 | high | usuário | ACs 6/7 não executados. É o que segura InReview. Não é retorno ao @dev |
| E41-TEL-002 | medium | @po / @architect; mitigação: usuário | a telemetria não distingue atraso 0 de atraso 6 (`versao` v1, sem campo, e o localStorage sobrevive a trocas de build). **Antes do smoke, exportar e zerar** (`localStorage.removeItem('bb.telemetria.v1')`) |
| E41-UX-001 | low | usuário (observar) / @architect (e4.5) | janela "em voo" de 6 ticks: o HUD mostra PRONTO, um 2º arrasto é recusado em silêncio pelo sim e ainda conta como `cast` na telemetria |
| E41-TST-003 | low | @architect | o `sim:check` não guarda o valor do atraso; a guarda da sala em e4.3/e4.4 deve afirmar `agendado − tick === 6` |
| E41-DOC-004 | low | @pm | `prd.md:219` ("hoje é 0") e `prd.md:317` (constante antiga) ficaram obsoletos, fora do inventário do AC 8 |

**Para o usuário, na rodada no aparelho:** (1) exportar e zerar a telemetria antes; (2) mira aparece
e segue o dedo sem lag (AC 6); (3) o efeito sai ~100ms depois, e isso atrapalha ou não? (AC 7); (4)
algum toque "sumiu" logo depois de um cast? (E41-UX-001); (5) escrever uma frase em "Impressão do
smoke (AC 7)" e acionar o @qa.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-08-19 | 1.0.0 | Story criada a partir do passo 1 de `docs/architecture-e4.md` §10 (P4.1) | @sm |
| 2026-08-19 | 1.1.0 | **Validação @po: GO 10/10** (9/10 antes da correção). **Status: Draft → Ready.** Achado: o AC 8 mandava o @dev emendar `docs/GDD.md`, e documento de design é do @architect — o precedente do projeto é commit próprio (`106de11`, *"docs(architect): errata §14"*). Virou registro de pendência + handoff, e o escopo do AC 9 encolheu para `src/client/main.ts` e mais nada. **As quatro referências de linha dos AC 3 e 4 foram conferidas na fonte e estão corretas:** `client/main.ts:58` (`const INPUT_DELAY_TICKS = 0`), `client/main.ts:311` (`tick: world.tick + INPUT_DELAY_TICKS`), `bot/heuristic.ts:583` e `bot/dummy.ts:30` (ambos `tick: view.tick`) — a cadeia de hash-neutralidade do AC 3 se sustenta. | @po |
| 2026-09-21 | 1.2.0 | **Implementação @dev.** `INPUT_DELAY_TICKS` removida de `client/main.ts`; o uso passa a ler `ATRASO_ALVO_TICKS` (6) de `net/protocolo.ts`. `check`, `sim:check` (golden hash idêntico, saída completa igual byte a byte à de antes) e `build` verdes. Smoke manual (AC 6, 7) **pendente, exige humano no aparelho**. Frase obsoleta do GDD §8 registrada como handoff para @architect. **Status: Ready → Ready for Review** (nomenclatura pedida pelo lead; equivale a InReview). | @dev |
| 2026-09-21 | 1.3.0 | **Gate @qa: PENDING.** Código aprovado (ACs 1-5 e 8-10 MET, verificados de forma independente, incluindo `sim:check` antes/depois idêntico e controle de mutação). ACs 6/7 (smoke no aparelho) não executados. **Status: Ready for Review → InReview** (normalizado para a nomenclatura de `story-lifecycle.md`; fica em InReview até o smoke ou até dispensa explícita do usuário). Achados: E41-REQ-000 (high, usuário), E41-TEL-002 (medium, @po/@architect), E41-UX-001, E41-TST-003, E41-DOC-004 (low). Gate: `docs/qa/gates/e4.1-ativar-atraso-de-input.yml`. | @qa |
