# Story e4.2: `net/snapshot.ts` + `net/projecao.ts` — `World → Snapshot → forma de render`, ida e volta

## Status

Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run check", "npm run sim:check", "guarda de ida-e-volta no sim:check", "orçamento de bytes remedido, com events: SimEvent[] de todos os tipos", "grep -rnwE \"ws|document|window|localStorage|Math\\.random\" src/net/ → exit 1", "grep -rnE \"Date\\.now|performance\\.now\" src/net/ → exit 1", "git diff --stat: render.ts com exatamente 1 linha; protocolo.ts só com justificativa (AC 14)"]
```

## Story

**Como** desenvolvedor executando o **passo 2** do plano de construção da Fase 4
(`docs/architecture-e4.md` §10),
**eu quero** criar `src/net/snapshot.ts` (produz `Snapshot` e `EstaticoDaRodada` a partir de `World`) e
`src/net/projecao.ts` (reconstrói, a partir de snapshot + estático + `CHARS`, exatamente a forma que
`client/render.ts` já aceita),
**para que** o cliente possa desenhar uma rodada **sem simular** — que é a propriedade da qual dependem
P4.2, o fechamento de §13.6 de E3, e a imunidade do modelo ao resultado de §1.5 —, e para que a prova
disso seja automática: uma guarda de ida-e-volta dentro do `sim:check`, sem servidor e sem navegador.

## Depende de

`e4.0` (`Snapshot` e `EstaticoDaRodada` declarados como tipo em `net/protocolo.ts`). Esta story os
**produz**; o protocolo continua sendo o dono do vocabulário.

**A forma já está decidida, e foi decidida em `e4.0`** (gate `docs/qa/gates/e4.0-net-protocolo.yml`,
E40-REQ-002). `e4.0` entregou `Snapshot` e `EstaticoDaRodada` **completos**, recortados por `Pick` de
`sim/types.ts` (`Ball`, `Projectile`, `Zone`, `Effect`, `World`), sem nenhum campo escrito à mão, e o
@qa conferiu campo a campo contra `client/render.ts`. Portanto "produz" quer dizer exatamente isto:
`estaticoDaRodada(world)` e `snapshot(world)` **devolvem valores desses tipos**. Esta story decide a
PRODUÇÃO (o que ler do `World`, a quantização com o ε do AC 8, a acumulação de eventos do AC 5, a
tripwire do AC 4) — não a FORMA. Se a produção esbarrar num campo que a forma não tem, ou que tem a
mais, a porta é o AC 14, não um tipo paralelo em `snapshot.ts`.

## Acceptance Criteria

1. `npm run check` verde.
2. `npm run sim:check` verde, com **golden hash idêntico**. Esta story lê `World`; não escreve nele.
3. `src/net/snapshot.ts` e `src/net/projecao.ts` (novos) são **puros**, verificável por grep: sem `ws`,
   sem DOM, sem `Date.now`, sem `performance.now`, sem `Math.random`, sem I/O. **A forma do grep é
   fixa (E40-TST-003), e o critério é o exit code, não a saída vazia:**
   ```sh
   grep -rnwE "ws|document|window|localStorage|Math\.random" src/net/   # exige exit 1
   grep -rnE "Date\.now|performance\.now" src/net/                        # exige exit 1
   ```
   `-r` porque o alvo é diretório (sem ele o grep sai com 2, "Is a directory", e stdout vazio passaria
   um gate que olhasse só a saída); `-w` porque `ws` solto casa com "draws", "news", "rows". Exit 0 é
   achado; exit 2 é comando quebrado, e também reprova.
4. **As três classes de campo da §5.1 são respeitadas, e a terceira é a que o gate confere:**

   | classe | campos (a forma é a de `net/protocolo.ts`, `e4.0`) | destino |
   |---|---|---|
   | estático da rodada | `id`, `charId`, `team`, `ultThreshold`, `abilityIndex`, `stat.maxHp`, `stat.radius` | `EstaticoDaRodada`, **uma vez** |
   | dinâmico que o render lê | `time`, `over`, `winner`, `arena`; por bola `id`, `x`, `y`, `facing`, `hp`, `alive`, `ultCharge`, `abilityReadyAt`, `effects[].kind`; projéteis, zonas; `events` | `Snapshot`, a cada quadro |
   | interno do motor | `memory`, `ax`, `ay`, `contact`, `base`, `bonusPassive`, `bonusItem`, `nextId`, `phase`, `rng`, `chars` | **nunca sai** |
   | conteúdo local do bundle | **cor da bola** (`chars[charId].color`) e o resto de `CharDef` | **não vai pelo fio** — `projecao.ts` lê do `CHARS` injetado (AC 10) |

   **Correção do @po (E40-REQ-002) — a cor NÃO está no `EstaticoDaRodada`.** A versão anterior desta
   tabela punha "cor" na linha do estático. `render.ts` lê a cor da bola **sempre** de `def.color`, com
   `def = chars[charId]` (`render.ts:166, 186, 274, 289, 314, 380, 386`, conferido pelo gate de `e4.0`),
   e o roster é conteúdo local do bundle (§5.4), não autoridade — por isso `e4.0` a deixou fora do tipo,
   e por isso `projetar()` recebe `CHARS`. O gate desta story confere que a cor chega pelo roster
   injetado, não que ela está no estático. (A cor de **projétil** e de **zona** — `color`, `ownerColor`
   — é outra coisa: é dado da entidade no `World` e já está no `Snapshot`.)

   A terceira linha é AC, não recomendação: **um teste automático deve falhar se qualquer um desses
   nomes aparecer no JSON serializado do snapshot.** O motivo está na arquitetura: um campo interno que
   vaze é um campo que o cliente pode exibir, e daí a dois passos de alguém achar que o cliente pode
   decidi-lo.

   **Correção do @po na validação — `stat.maxHp` e `stat.radius` são estáticos por VERIFICAÇÃO, não por
   natureza, e por isso viram tripwire.** `sim/stats.ts:14-15` classifica os dois como *"estruturais —
   recomputados só em evento explícito"*, o que é diferente de "constantes". E o mecanismo de mudança
   existe e está em uso: `chars/vex.ts:108` chama `ctx.addBonus(self, 'maxSpeed', 0.25)` **condicionalmente**
   (quando `hp/stat.maxHp < 0.4`), provando que `stat` muda no meio da rodada. Hoje nenhuma passiva toca
   `maxHp` nem `radius`, e `bonusItem` é congelado por invariante — então a classificação está correta
   **hoje**. Mas a Fase 5 traz 6 personagens novos.

   Portanto: a guarda do AC 7 deve **comparar, a cada tick, o `stat.maxHp` e o `stat.radius` do `World`
   contra os valores que foram para o `EstaticoDaRodada`, e falhar se divergirem**. Assim a suposição
   deixa de ser suposição: no dia em que alguém escrever uma passiva que muda raio, o `sim:check` apita
   em vez de o HP bar do adversário desenhar errado em produção.
5. **Eventos são acumulados, não descartados.** Com `SNAPSHOT_HZ < 60`, os `world.events` dos ticks
   intermediários precisam entrar no snapshot seguinte. Se forem descartados, metade dos acertos some da
   tela — o cliente consome `events` de tipo `hit` para os números flutuantes (`client/main.ts:407`).
   Teste: numa rodada inteira, a soma de eventos `hit` presentes nos snapshots é **igual** à soma de
   eventos `hit` que a rodada produziu.
6. **`client/render.ts` muda por EXATAMENTE uma anotação de tipo, e o corpo não muda nem uma linha.**

   **Segunda correção do @po (2026-08-19, revisão da rodada seguinte) — a redação anterior deste AC era
   factualmente impossível.** Ela dizia "não é alterado, nem uma linha", apoiada em
   `architecture-e4.md` §5.1 (*"O renderizador não muda"*). Mas a assinatura real é
   `desenhar(g, cw, ch, world: World, o)` (`client/render.ts:40-46`): o parâmetro pede um `World`
   **completo**, com `rng`, `phase`, `nextId`, e `memory`/`base`/`bonusItem` em cada bola — justamente os
   campos da **classe 3** que o AC 4 proíbe de sair. Uma projeção nunca satisfaz esse tipo, e as duas
   saídas ruins seriam fabricar um `World` falso no cliente (reintroduzindo a classe 3) ou reescrever o
   renderizador.

   A saída certa já tem precedente no próprio projeto: `WorldView = Omit<World, 'rng'>` (`debt.7`)
   estreitou um tipo sem tocar em ponto de chamada nenhum, porque `World` continua satisfazendo o tipo
   estreito **estruturalmente**. Faz-se o mesmo:
   - `net/projecao.ts` exporta `VisaoDoMundo` — o conjunto exato de campos que `render.ts` lê (a lista
     fechada do Dev Notes);
   - `client/render.ts` troca `world: World` por `world: VisaoDoMundo`. **Uma linha, no cabeçalho da
     função.** O corpo não muda;
   - o modo local continua passando o `World` de sempre, sem cast e sem adaptação, porque `World`
     satisfaz `VisaoDoMundo` estruturalmente. Se precisar de cast, o tipo foi mal desenhado.

   `git diff --stat` deve mostrar `client/render.ts` com **1 linha alterada** — nem zero, nem duas.

   **Handoff para @architect:** `architecture-e4.md` §5.1 afirma *"O renderizador não muda... Nenhuma
   linha de `render.ts` entra nesta fase"*. É otimista por uma linha. Merece emenda; o @dev registra o
   pedido, não edita o documento (mesma regra do AC 11).
7. **Guarda de ida-e-volta no `sim:check`** (nova, no padrão das guardas existentes `invariante M-1`,
   `política bot`, `segredo visão`): para uma rodada completa, a cada tick,
   `projetar(snapshot(world), estatico, CHARS)` produz, para **todo** campo que `render.ts` lê:
   - as mesmas grandezas do `World`, dentro do ε de quantização declarado no AC 8;
   - a mesma **presença**: mesmo número de bolas, projéteis e zonas, com os mesmos `id`.

   A lista de campos que `render.ts` lê é fechada e está no Dev Notes — a guarda deve cobrir todos.
8. **A quantização é declarada, não implícita.** O snapshot arredonda posições e ângulos; o ε escolhido
   fica numa constante nomeada em `net/snapshot.ts`, com comentário dizendo que é **precisão de
   exibição** e não tem relação com o quantum do `hash()` do arnês (§11.1 — que alerta que quantização
   não é banda de tolerância). Confundir os dois é o erro que este AC existe para impedir.
9. **Orçamento de bytes remedido, e o número vai para o Dev Agent Record.** `architecture-e4.md` §1.2
   mediu, com a mesma composição, **263 B/tick de média e 433 B de pico** para o snapshot de render. A
   story deve remedir com a implementação real e registrar. **Se passar de ~600 B/tick de média, algo da
   classe 3 vazou** — investigar antes de seguir, não ajustar o orçamento.

   **Correção do @po (E40-REQ-002) — a medição inclui `events: SimEvent[]` com TODOS os tipos de evento,
   e o número de `events` sai separado.** O tipo que `e4.0` fixou é `events: SimEvent[]`, e `SimEvent`
   (`sim/types.ts:119-125`) não é só `hit`: carrega também `cast` e `ult` (com `name: string`), `death`,
   `suddenDeath` e `roundEnd`. E, pelo AC 5, cada snapshot leva os eventos **acumulados** desde o
   anterior — a `SNAPSHOT_HZ = 30`, dois ticks de eventos por quadro. §1.2 mediu "o que `render.ts` lê",
   e `events` não é lido por `render.ts` (é `client/main.ts:407`), então não se pode supor que os 263 B
   os incluam. A medição, portanto:
   - mede o snapshot **como produzido** — por quadro, na cadência de `SNAPSHOT_HZ`, com eventos
     acumulados de todos os tipos — e converte para B/tick para comparar com §1.2;
   - registra **à parte** quantos bytes são de `events` (média e pico), para que um pico legítimo de
     eventos (morte súbita, ult com projéteis) não seja lido como vazamento de classe 3, nem o
     contrário;
   - se o @dev decidir **filtrar** eventos na produção (por exemplo, só `hit`, o único consumido hoje),
     isso tipa — um subconjunto de `SimEvent[]` cabe no tipo —, mas é decisão de PRODUÇÃO com
     consequência para `e4.5`/`e4.6` e vai registrada no Dev Agent Record com a regra do filtro e os
     dois números (com e sem filtro). O teste do AC 5 (soma de `hit`) vale em qualquer dos casos.
10. **Setas** (§2.2): `net/` importa só tipos de `sim/` e `match/`; `chars/` entra em `projecao.ts` como
    **dado injetado** (`Record<string, CharDef>`), pela mesma porta que `createWorld` já usa — não por
    import de `chars/`.
11. **Extensão declarada da tabela de setas:** `tools/` passa a poder importar de `net/` para hospedar a
    guarda do AC 7. É uma seta nova, deliberada, e coerente com a tabela (`tools/` já está acima de
    todos os módulos puros) — mas é uma seta que **ninguém aprovou ainda**, e inventá-la em silêncio é
    exatamente o que a §2.2 existe para impedir.

    **Correção do @po na validação:** o @dev **não** edita `docs/architecture-e4.md` — documento de
    arquitetura é do @architect, e o precedente do projeto é commit próprio (`106de11`). O @dev
    **implementa** a seta e **registra o pedido de emenda** no Dev Agent Record; a linha no Anexo A é
    handoff para @architect. O @qa confere que o handoff existe, não que o documento foi editado.
12. **A prova de compatibilidade de TIPO fica para `e4.5`** — mas com o AC 6 corrigido ela muda de
    natureza e fica mais forte. Com `VisaoDoMundo` no cabeçalho de `desenhar`, a compatibilidade passa a
    ser verificada pelo `tsc` **em toda chamada existente**: se `projetar()` não produzir a forma certa,
    `e4.5` não compila. Aqui a garantia continua sendo por **enumeração de campos** (AC 7), e a asserção
    explícita nasce em `e4.5`, do lado certo da seta (`client/` importa `net/` legitimamente).
13. **QA-D8-01 — follow-up herdado, cobrado aqui porque esta é a primeira story da Fase 4 a tocar
    `determinism.ts`.** O gate de `debt.8` deixou registrado: *"a próxima story que tocar
    `determinism.ts` deve adicionar a asserção estrutural «BASELINE contém ≥1 `winner === -1`»"*.

    O motivo é o defeito que `debt.8` corrigiu: a cobertura do caminho de empate foi perdida em silêncio
    quando o baseline foi re-gravado em `e3.6`, e ninguém percebeu porque nenhum teste afirmava que o
    empate **estava** coberto — os hashes batiam, e um baseline sem empate bate consigo mesmo
    perfeitamente. A asserção é de uma linha e transforma "temos uma seed de empate" de fato histórico
    em invariante verificada a cada `sim:check`.

    Esta story adiciona a asserção. Custo: uma linha. É follow-up barato de fase anterior sendo pago no
    primeiro momento em que o arquivo estava aberto de qualquer jeito.
14. **Escopo de arquivos.** Criados: `src/net/snapshot.ts`, `src/net/projecao.ts`. Alterados:
    `src/tools/determinism.ts` (só a guarda nova do AC 7) e `src/client/render.ts` (**exatamente a linha
    do AC 6** — a anotação de tipo do parâmetro). Proibido: `src/sim/`, `src/match/`, `src/shop/`,
    `src/bot/`, `src/chars/`, o resto de `src/client/` e **`docs/`** (ver AC 11).

    **`src/net/protocolo.ts` — alterado SÓ com justificativa (E40-REQ-002).** O default é não tocar: a
    forma é de `e4.0` e já cobre tudo o que `render.ts` lê. Mas a versão anterior deste AC proibia o
    arquivo por omissão, e isso deixaria a story sem saída legítima se a produção revelasse um defeito
    de forma. Fica permitido **um ajuste de forma de `Snapshot`/`EstaticoDaRodada`** quando, e só quando:
    - houver motivo concreto e citável — um campo que `render.ts` lê e a forma não tem, um campo que a
      guarda do AC 7 prova desnecessário, ou a medição do AC 9 —, registrado no Dev Agent Record com o
      motivo e o diff do tipo;
    - o ajuste continuar por `Pick`/indexação de `sim/types.ts`, sem campo escrito à mão (AC 8 de `e4.0`);
    - nenhum campo de classe 3 entrar (AC 4) e a cor da bola continuar fora (AC 4, correção acima).

    É a "alteração deliberada e revisada" do AC 10 de `e4.0`, e o @qa confere a justificativa, não só o
    diff. `DoCliente`/`DoServidor` e as duas constantes continuam **proibidos** aqui: esta story não tem
    motivo para tocá-los.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Serialization / Contract
**Secondary Type(s)**: Test infrastructure (a guarda do AC 7)
**Complexity**: Medium — a lógica é simples, mas o erro caro é silencioso: um campo a mais no snapshot não
quebra nada hoje e custa banda, superfície e confusão de autoridade depois.

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa (quality gate — o AC 4 e o AC 9 são exatamente o tipo de invariante que passa despercebido em
  revisão manual e é trivial de conferir por teste)

**Supporting Agents**:
- (nenhum necessário)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Rodar antes de marcar a story como completa
- [ ] Pre-PR (@github-devops): Rodar antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 20 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Campo da classe 3 (§5.1) aparecendo no snapshot — `memory`, `base`, `bonusItem`, `stat` inteiro (AC 4)
- Eventos de ticks intermediários sendo descartados (AC 5)
- `client/render.ts` alterado além da anotação de tipo do AC 6 (exatamente 1 linha)
- `src/net/protocolo.ts` alterado sem a justificativa exigida pelo AC 14

**Secondary Focus**:
- ε de quantização sem nome e sem comentário, ou confundido com o quantum do `hash()` (AC 8)
- `projecao.ts` importando `chars/` em vez de receber o roster injetado (AC 10)

## Tasks / Subtasks

- [ ] Task 1 — `net/snapshot.ts` (AC: 3, 4, 5, 8)
  - [ ] `estaticoDaRodada(world): EstaticoDaRodada` — classe 1
  - [ ] `snapshot(world): Snapshot` — classe 2, quantizado, com ε nomeado e comentado
  - [ ] Acumulação de eventos entre snapshots (AC 5)
  - [ ] Conferir por leitura que nenhum campo da classe 3 é lido

- [ ] Task 2 — `net/projecao.ts` (AC: 3, 6, 10)
  - [ ] Exportar `VisaoDoMundo` — a lista fechada de campos que `render.ts` lê
  - [ ] `projetar(snap, estatico, chars): VisaoDoMundo`
  - [ ] Trocar `world: World` por `world: VisaoDoMundo` em `client/render.ts` — **uma linha**
  - [ ] Confirmar que o modo local segue passando `World` sem cast (satisfação estrutural)
  - [ ] Registrar no Dev Agent Record o pedido de emenda de §5.1 ao @architect
  - [ ] Roster injetado como `Record<string, CharDef>`, não importado

- [ ] Task 3 — Guarda de ida-e-volta (AC: 4, 7, 11)
  - [ ] Nova guarda em `src/tools/determinism.ts`, no padrão das existentes
  - [ ] Cobrir todos os campos da lista fechada do Dev Notes
  - [ ] Cobrir presença: contagem e `id` de bolas, projéteis e zonas
  - [ ] **Tripwire de `stat.maxHp`/`stat.radius`:** comparar por tick contra o `EstaticoDaRodada` e
        falhar se divergirem (AC 4, correção do @po)
  - [ ] Registrar no Dev Agent Record o pedido de emenda do Anexo A (seta `tools/ → net/`) — **não**
        editar `docs/`

- [ ] Task 4 — Orçamento (AC: 9)
  - [ ] Medir bytes/tick médios e de pico com a implementação real, por quadro na cadência de
        `SNAPSHOT_HZ`, com `events` acumulados de **todos** os tipos de `SimEvent`
  - [ ] Registrar à parte a parcela de `events` (média e pico); se houver filtro de eventos, os dois
        números e a regra do filtro
  - [ ] Registrar no Dev Agent Record; comparar com 263 B / 433 B de §1.2

- [ ] Task 5 — QA-D8-01, follow-up herdado (AC: 13)
  - [ ] Asserção estrutural em `determinism.ts`: `BASELINE` contém ≥1 `winner === -1`

- [ ] Task 6 — Verificação (AC: 1, 2, 6, 14)
  - [ ] `npm run check` — 0 erros
  - [ ] `npm run sim:check` — golden hash idêntico, guarda nova verde, asserção de empate verde
  - [ ] Grep de pureza na forma do AC 3 — exit 1 nos dois comandos
  - [ ] `git diff --stat` — `render.ts` com exatamente 1 linha alterada, escopo respeitado;
        `protocolo.ts` intacto ou com a justificativa do AC 14 no Dev Agent Record

## Dev Notes

### A lista fechada de campos que `render.ts` lê (conferida no código nesta sessão)

De `world`: `arena` (`w`, `h`, `pad`), `balls`, `projectiles`, `zones`, `chars`, `time`, `over`, `winner`.

De cada `ball`: `id`, `charId`, `team`, `x`, `y`, `facing`, `hp`, `alive`, `ultCharge`, `ultThreshold`,
`effects` (só o `kind` — `render.ts:220-222` usa `.some()`), `stat.radius`, `stat.maxHp`,
`abilityReadyAt`, `abilityIndex`.

De cada `projectile`: `x`, `y`, `vx`, `vy` (usados em `render.ts:134` para o rastro), `radius`, `color`.

De cada `zone`: `kind`, `x`, `y`, `angle`, `halfLen`, `radius`, `pull`, `ownerColor`.

De `events`: só `hit` é consumido hoje (`client/main.ts:407`), com `x`, `y`, `amount`, `crit`.

**`facing` merece nota:** ele é escrito por `sim/physics.ts:95` (`Math.atan2`) e lido **só** por
`render.ts:198`. Não está no hash e não alimenta física — conferido por grep. É dado de exibição puro, e
por isso pode ser quantizado agressivamente sem consequência.

### Por que o snapshot não é o mundo (fonte: §1.2, §5.1)

Medido: `World` serializável inteiro custa **5 049 B/tick**; o que o render lê custa **263 B**. Fator
**19×**. A diferença são campos que não mudam durante a rodada (`base`, `bonusItem`, `stat`) ou que render
nenhum consome (`memory`, `ax/ay`, `nextId`, `phase`). Mandar `World` inteiro a 60 Hz custaria
**2 423,7 kbit/s** por cliente; o snapshot comprimido a 30 Hz custa **8,2 kbit/s**.

### Por que a guarda mora no `sim:check` (fonte: §2.2, §10 passo 3)

`net/` é puro exatamente para que isto seja possível: provar a serialização inteira sem subir servidor e
sem abrir navegador, no mesmo comando que já protege o golden hash. É o mesmo dividendo que `match/` puro
deu em `e3.2` (partida inteira headless) e que `tools/harness.ts` deu em `e2.0` (uma definição, dois
consumidores).

### O ε de exibição não é o quantum do `hash()` (fonte: §8, §11.1)

`architecture-e4.md` §11.1 mostra que o `toFixed(4)` do `hash()` **não** é banda de tolerância — é
arredondamento, e portanto uma loteria com boas probabilidades. Nada disso vale para o ε desta story: aqui
a quantização é de **exibição**, o consumidor é um canvas de ~1000 px, e errar o último bit de uma posição
não tem consequência nenhuma. Os dois números são independentes e o comentário do AC 8 existe para que
ninguém os amarre por engano no futuro.

### O que esta story explicitamente NÃO faz

- **Não interpola.** O buffer e a interpolação entre snapshots são `e4.5` (§5.3).
- **Não decide `SNAPSHOT_HZ`.** A story produz um snapshot por tick quando solicitada; quem escolhe a
  cadência é o servidor (`e4.4`), e o número final sai de P4.4 (R-03).
- **Não abre socket nem cria sala.** Passos 4 e 3.
- **Não prova compatibilidade de tipo com `desenhar`** — AC 12 explica por quê e para onde vai.

### Testing

- `npm run check` — 0 erros.
- `npm run sim:check` — golden hash idêntico **e** guarda de ida-e-volta verde numa rodada completa.
- Teste de vazamento: serializar o snapshot e confirmar que nenhum dos nomes da classe 3 aparece.
- Teste de eventos: soma de `hit` nos snapshots == soma de `hit` produzidos pela rodada.
- Medição de orçamento: bytes/tick médio e de pico, com `events` de todos os tipos e a parcela de
  `events` registrada à parte (AC 9), no Dev Agent Record.
- Grep de pureza na forma do AC 3, exigindo exit 1 nos dois comandos.
- `git diff --stat` prova que `client/render.ts` mudou **exatamente 1 linha** (AC 6) e que
  `src/net/protocolo.ts` está intacto — ou, se mudou, que a justificativa do AC 14 está no Dev Agent
  Record.

## Dev Agent Record

### File List

_(a preencher pelo @dev)_

### Orçamento de bytes medido (AC 9)

_(a preencher pelo @dev — média e pico, comparados com 263 B / 433 B de `architecture-e4.md` §1.2)_

## QA Results

_(a preencher pelo @qa)_

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-08-19 | 1.0.0 | Story criada a partir do passo 2 de `docs/architecture-e4.md` §10 | @sm |
| 2026-08-19 | 1.1.0 | **Validação @po: GO 10/10** (8/10 antes das correções — o achado (a) era um AC factualmente frágil, não só de forma). **Status: Draft → Ready.** Dois achados: **(a)** o AC 4 classificava `stat.maxHp` e `stat.radius` como estáticos da rodada *por natureza*. `sim/stats.ts:14-15` os chama **"estruturais — recomputados só em evento explícito"**, que não é o mesmo que constantes, e `chars/vex.ts:108` prova que `stat` muda no meio da rodada (`addBonus` condicional em `maxSpeed` abaixo de 40% de HP). Hoje nenhuma passiva toca `maxHp`/`radius` e `bonusItem` é congelado — a classificação está certa **hoje** —, mas a Fase 5 traz 6 personagens novos. Virou **tripwire** na guarda do AC 7: comparação por tick contra o `EstaticoDaRodada`, falhando se divergir. **(b)** o AC 11 mandava o @dev editar `docs/architecture-e4.md` — mesmo problema de autoridade de `e4.1`, virou handoff. Corrigida ainda a citação `client/main.ts:405` → `:407` (linha real do `ev.t === 'hit'`). | @po |
| 2026-08-19 | 1.2.0 | **Reabertura pelo @po durante a validação de `e4.5`** — a story continua **Ready**, mas o AC 6 estava factualmente impossível e teria reprovado no gate. `desenhar` recebe `world: World` **completo** (`client/render.ts:40-46`), incluindo `rng`, `phase`, `nextId` e os campos de classe 3 de cada bola; nenhuma projeção satisfaz esse tipo. As duas saídas ruins seriam fabricar um `World` falso no cliente (reintroduzindo a classe 3 que o AC 4 proíbe) ou reescrever o renderizador. A saída certa tem precedente no projeto — `WorldView = Omit<World,'rng'>` de `debt.7` estreitou um tipo sem tocar em ponto de chamada nenhum: `net/projecao.ts` passa a exportar `VisaoDoMundo`, e `render.ts` muda **exatamente uma linha** (a anotação do parâmetro). O modo local continua passando `World` sem cast, por satisfação estrutural. Efeito colateral bom: com o tipo estreito no cabeçalho, o `tsc` passa a verificar a compatibilidade em toda chamada, e o AC 12 fica mais forte do que era. **Handoff para @architect:** `architecture-e4.md` §5.1 afirma que "nenhuma linha de `render.ts` entra nesta fase" — é otimista por uma linha e merece emenda. | @po |
| 2026-09-21 | 1.3.0 | **Alinhamento ao shape de `e4.0` (E40-REQ-002 e E40-TST-003, gate `docs/qa/gates/e4.0-net-protocolo.yml`). Status permanece Ready**: nenhuma tarefa nova de implementação, só texto que teria tropeçado no gate. Conferido na fonte antes de emendar: `src/net/protocolo.ts:64-93` (forma completa por `Pick`), `render.ts:159/267/308/361` (`def = world.chars[charId]`) e `:166-386` (cor sempre de `def.color`), `sim/types.ts:119-125` (os 6 tipos de `SimEvent`). **(1)** "Depende de": a forma é dada por `e4.0`; "preenche" virou **produz**, e a story decide produção, não forma. **(2)** Tabela do AC 4: "cor" saiu da linha do estático — vem de `chars[charId].color` pelo `CHARS` injetado — e a tabela passou a listar os campos reais da forma (`id`, `abilityIndex`, `time`/`over`/`winner`/`arena`, `events`); nova linha "conteúdo local do bundle". **(3)** AC 9: a medição inclui `events: SimEvent[]` de **todos** os tipos, acumulados na cadência de `SNAPSHOT_HZ`, com a parcela de `events` registrada à parte; filtro de eventos é permitido, mas registrado. **(4)** AC 14: `src/net/protocolo.ts` pode ser alterado **só** para ajuste de forma de `Snapshot`/`EstaticoDaRodada` com motivo citável no Dev Agent Record, por `Pick`, sem classe 3 e sem cor — a porta que o AC 10 de `e4.0` exige. **(5)** AC 3: grep de pureza fixado na forma recursiva e com fronteira de palavra, exigindo exit 1 (sem `-r` o grep sai com 2 e passa em vazio; `ws` solto casa "draws"). Limpeza no mesmo passo: `quality_gate_tools`, Focus Areas e Testing ainda diziam "`render.ts` intacto", contradizendo o AC 6 corrigido em 1.2.0 — agora dizem "exatamente 1 linha". | @po |
