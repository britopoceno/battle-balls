# Story e4.2: `net/snapshot.ts` + `net/projecao.ts` — `World → Snapshot → forma de render`, ida e volta

## Status

Ready for Review

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run check", "npm run sim:check", "guarda de ida-e-volta no sim:check", "orçamento de bytes remedido, com events: SimEvent[] de todos os tipos", "grep -rnwE \"ws|document|window|localStorage|Math\\.random\" src/net/ → exit 1", "grep -rnE \"Date\\.now|performance\\.now\" src/net/ → exit 1", "git diff src/client/render.ts: só linhas de anotação de tipo (import + minhasBolas + world em desenhar e nos 6 auxiliares + b em desenharBola), nenhuma linha de corpo (AC 6); protocolo.ts só com justificativa (AC 14)"]
```

## Story

**Como** desenvolvedor executando o **passo 2** do plano de construção da Fase 4
(`docs/architecture-e4.md` §10),
**eu quero** criar `src/net/snapshot.ts` (produz `Snapshot` e `EstaticoDaRodada` a partir de `World`) e
`src/net/projecao.ts` (reconstrói, a partir de snapshot + estático + `CHARS`, exatamente a forma que
`client/render.ts` já aceita),
**para que** o cliente possa desenhar uma rodada **sem simular** — que é a propriedade da qual dependem
P4.2, o fechamento de §13.6 de E3, e a imunidade do modelo ao resultado de §1.5 —, e para que a prova
disso seja automática: uma guarda de ida-e-volta dentro do `sim:check`, sem servidor e sem navegador.

## Depende de

`e4.0` (`Snapshot` e `EstaticoDaRodada` declarados como tipo em `net/protocolo.ts`). Esta story os
**produz**; o protocolo continua sendo o dono do vocabulário.

**A forma já está decidida, e foi decidida em `e4.0`** (gate `docs/qa/gates/e4.0-net-protocolo.yml`,
E40-REQ-002). `e4.0` entregou `Snapshot` e `EstaticoDaRodada` **completos**, recortados por `Pick` de
`sim/types.ts` (`Ball`, `Projectile`, `Zone`, `Effect`, `World`), sem nenhum campo escrito à mão, e o
@qa conferiu campo a campo contra `client/render.ts`. Portanto "produz" quer dizer exatamente isto:
`estaticoDaRodada(world)` e `snapshot(world)` **devolvem valores desses tipos**. Esta story decide a
PRODUÇÃO (o que ler do `World`, a quantização com o ε do AC 8, a acumulação de eventos do AC 5, a
tripwire do AC 4) — não a FORMA. Se a produção esbarrar num campo que a forma não tem, ou que tem a
mais, a porta é o AC 14, não um tipo paralelo em `snapshot.ts`.

## Acceptance Criteria

1. `npm run check` verde.
2. `npm run sim:check` verde, com **golden hash idêntico**. Esta story lê `World`; não escreve nele.
3. `src/net/snapshot.ts` e `src/net/projecao.ts` (novos) são **puros**, verificável por grep: sem `ws`,
   sem DOM, sem `Date.now`, sem `performance.now`, sem `Math.random`, sem I/O. **A forma do grep é
   fixa (E40-TST-003), e o critério é o exit code, não a saída vazia:**
   ```sh
   grep -rnwE "ws|document|window|localStorage|Math\.random" src/net/   # exige exit 1
   grep -rnE "Date\.now|performance\.now" src/net/                        # exige exit 1
   ```
   `-r` porque o alvo é diretório (sem ele o grep sai com 2, "Is a directory", e stdout vazio passaria
   um gate que olhasse só a saída); `-w` porque `ws` solto casa com "draws", "news", "rows". Exit 0 é
   achado; exit 2 é comando quebrado, e também reprova.
4. **As três classes de campo da §5.1 são respeitadas, e a terceira é a que o gate confere:**

   | classe | campos (a forma é a de `net/protocolo.ts`, `e4.0`) | destino |
   |---|---|---|
   | estático da rodada | `id`, `charId`, `team`, `ultThreshold`, `abilityIndex`, `stat.maxHp`, `stat.radius` | `EstaticoDaRodada`, **uma vez** |
   | dinâmico que o render lê | `time`, `over`, `winner`, `arena`; por bola `id`, `x`, `y`, `facing`, `hp`, `alive`, `ultCharge`, `abilityReadyAt`, `effects[].kind`; projéteis, zonas; `events` | `Snapshot`, a cada quadro |
   | interno do motor | `memory`, `ax`, `ay`, `contact`, `base`, `bonusPassive`, `bonusItem`, `nextId`, `phase`, `rng`, `chars` | **nunca sai** |
   | conteúdo local do bundle | **cor da bola** (`chars[charId].color`) e o resto de `CharDef` | **não vai pelo fio** — `projecao.ts` lê do `CHARS` injetado (AC 10) |

   **Correção do @po (E40-REQ-002) — a cor NÃO está no `EstaticoDaRodada`.** A versão anterior desta
   tabela punha "cor" na linha do estático. `render.ts` lê a cor da bola **sempre** de `def.color`, com
   `def = chars[charId]` (`render.ts:166, 186, 274, 289, 314, 380, 386`, conferido pelo gate de `e4.0`),
   e o roster é conteúdo local do bundle (§5.4), não autoridade — por isso `e4.0` a deixou fora do tipo,
   e por isso `projetar()` recebe `CHARS`. O gate desta story confere que a cor chega pelo roster
   injetado, não que ela está no estático. (A cor de **projétil** e de **zona** — `color`, `ownerColor`
   — é outra coisa: é dado da entidade no `World` e já está no `Snapshot`.)

   A terceira linha é AC, não recomendação: **um teste automático deve falhar se qualquer um desses
   nomes aparecer no JSON serializado do snapshot.** O motivo está na arquitetura: um campo interno que
   vaze é um campo que o cliente pode exibir, e daí a dois passos de alguém achar que o cliente pode
   decidi-lo.

   **Correção do @po na validação — `stat.maxHp` e `stat.radius` são estáticos por VERIFICAÇÃO, não por
   natureza, e por isso viram tripwire.** `sim/stats.ts:14-15` classifica os dois como *"estruturais —
   recomputados só em evento explícito"*, o que é diferente de "constantes". E o mecanismo de mudança
   existe e está em uso: `chars/vex.ts:108` chama `ctx.addBonus(self, 'maxSpeed', 0.25)` **condicionalmente**
   (quando `hp/stat.maxHp < 0.4`), provando que `stat` muda no meio da rodada. Hoje nenhuma passiva toca
   `maxHp` nem `radius`, e `bonusItem` é congelado por invariante — então a classificação está correta
   **hoje**. Mas a Fase 5 traz 6 personagens novos.

   Portanto: a guarda do AC 7 deve **comparar, a cada tick, o `stat.maxHp` e o `stat.radius` do `World`
   contra os valores que foram para o `EstaticoDaRodada`, e falhar se divergirem**. Assim a suposição
   deixa de ser suposição: no dia em que alguém escrever uma passiva que muda raio, o `sim:check` apita
   em vez de o HP bar do adversário desenhar errado em produção.
5. **Eventos são acumulados, não descartados.** Com `SNAPSHOT_HZ < 60`, os `world.events` dos ticks
   intermediários precisam entrar no snapshot seguinte. Se forem descartados, metade dos acertos some da
   tela — o cliente consome `events` de tipo `hit` para os números flutuantes (`client/main.ts:407`).
   Teste: numa rodada inteira, a soma de eventos `hit` presentes nos snapshots é **igual** à soma de
   eventos `hit` que a rodada produziu.
6. **`client/render.ts` muda só em anotações de tipo — o import, `minhasBolas` de `OpcoesRender`, o
   `world` de `desenhar` e dos seus 6 auxiliares, e o `b` de `desenharBola` —, e `git diff` não mostra
   nenhuma linha de corpo de função alterada.**

   **Segunda correção do @po (2026-08-19, revisão da rodada seguinte) — a redação anterior deste AC era
   factualmente impossível.** Ela dizia "não é alterado, nem uma linha", apoiada em
   `architecture-e4.md` §5.1 (*"O renderizador não muda"*). Mas a assinatura real é
   `desenhar(g, cw, ch, world: World, o)` (`client/render.ts:40-46`): o parâmetro pede um `World`
   **completo**, com `rng`, `phase`, `nextId`, e `memory`/`base`/`bonusItem` em cada bola — justamente os
   campos da **classe 3** que o AC 4 proíbe de sair. Uma projeção nunca satisfaz esse tipo, e as duas
   saídas ruins seriam fabricar um `World` falso no cliente (reintroduzindo a classe 3) ou reescrever o
   renderizador.

   A saída certa já tem precedente no próprio projeto: `WorldView = Omit<World, 'rng'>` (`debt.7`)
   estreitou um tipo sem tocar em ponto de chamada nenhum, porque `World` continua satisfazendo o tipo
   estreito **estruturalmente**. Faz-se o mesmo:
   - `net/projecao.ts` exporta `VisaoDoMundo` — o conjunto exato de campos que `render.ts` lê (a lista
     fechada do Dev Notes) — e o tipo do elemento de `balls` (nome sugerido `BolaVisivel`);
   - `client/render.ts` troca as anotações, e **só as anotações** (tabela abaixo). O corpo não muda;
   - o modo local continua passando o `World` de sempre, e `client/main.ts` continua passando o seu
     `Ball[]` em `minhasBolas`, sem cast e sem adaptação, porque `World` satisfaz `VisaoDoMundo` e `Ball`
     satisfaz `BolaVisivel` estruturalmente. Se precisar de cast, o tipo foi mal desenhado.

   **Terceira correção do @po (2026-09-21, a pedido do @dev, que parou antes de escrever código): "uma
   linha" também era impossível.** A versão 1.2.0 trocou "nenhuma linha" por "exatamente uma", contando
   só o cabeçalho de `desenhar`. Mas `desenhar` repassa `world` a seis auxiliares, cada um anotado
   `world: World`: `desenharArena` (`render.ts:63`), `desenharZonas` (`:97`), `desenharProjeteis` (`:131`),
   `desenharBola` (`:155`, que ainda recebe `b: Ball` em `:156`), `desenharMiras` (`:261`) e `desenharHud`
   (`:301`). Trocar só a linha 44 reprova no `tsc` com **6 erros TS2345** (*"VisaoDoMundo is not
   assignable to World"*, um por chamada, `render.ts:52-58`). O @po **reproduziu** isso numa cópia de
   `src/` fora do repositório. A contagem de linhas nunca foi o critério certo; a **natureza** da linha é.

   O conjunto que compila, também reproduzido (`tsc` sem erro em `client/` e `net/`, `main.ts` intocado):

   | linha | antes | depois |
   |---|---|---|
   | 1 | `import type { Ball, World } from '../sim/types.ts'` | `import type { BolaVisivel, VisaoDoMundo } from '../net/projecao.ts'` |
   | 21 | `minhasBolas: Ball[]` | `minhasBolas: BolaVisivel[]` |
   | 44, 63, 97, 131, 155, 261, 301 | `world: World` | `world: VisaoDoMundo` |
   | 156 | `b: Ball,` | `b: BolaVisivel,` |

   `World` e `Ball` saem do import porque `noUnusedLocals` está ligado (`tsconfig.json`). Por isso o
   import de `sim/types.ts` vira, na mesma linha, o de `net/projecao.ts` (`client/ → net/` é permitido,
   §2.2). **`minhasBolas` entra no conjunto por decisão do @po, não do @dev.** `OpcoesRender.minhasBolas:
   Ball[]` (`render.ts:21`) exige objetos `Ball` do motor, que o modo conectado de `e4.5` não tem como
   fornecer. Os campos que `desenharMiras`/`desenharHud` leem desses objetos já estão todos na lista
   fechada. Estreitar aqui custa uma anotação e fecha o estreitamento de `render.ts` numa story só. Também
   mantém verdadeiro o AC 12 de `e4.5`, que já dizia *"se esta story precisar mexer nele, o
   estreitamento de `e4.2` ficou incompleto e é lá que se corrige"*.

   **Critério do gate:** `git diff src/client/render.ts` mostra **só** linhas de import e de anotação de
   tipo de parâmetro/campo, e nenhuma linha de corpo de função. A referência esperada é `git diff --numstat`
   = `10 10` (10 linhas trocadas no lugar, nenhuma acrescentada). O @dev pode escolher outro arranjo que
   também seja só de anotação, por exemplo `VisaoDoMundo['balls'][number]` inline em vez de um nome
   exportado, ou o import partido em duas linhas. Nesse caso a contagem muda e o AC continua cumprido. Uma
   linha de corpo alterada reprova, qualquer que seja a contagem.

   **Handoff para @architect:** `architecture-e4.md` §5.1 afirma *"O renderizador não muda... Nenhuma
   linha de `render.ts` entra nesta fase"*. Na verdade entram ~10 linhas de anotação de tipo e nenhuma de
   corpo: o comportamento do renderizador não muda, mas a assinatura muda. A emenda merece ser feita nesses
   termos. O @dev registra o pedido e não edita o documento (mesma regra do AC 11).
7. **Guarda de ida-e-volta no `sim:check`** (nova, no padrão das guardas existentes `invariante M-1`,
   `política bot`, `segredo visão`): para uma rodada completa, a cada tick,
   `projetar(snapshot(world), estatico, CHARS)` produz, para **todo** campo que `render.ts` lê:
   - as mesmas grandezas do `World`, dentro do ε de quantização declarado no AC 8;
   - a mesma **presença**: mesmo número de bolas, projéteis e zonas, com os mesmos `id`.

   A lista de campos que `render.ts` lê é fechada e está no Dev Notes — a guarda deve cobrir todos.
8. **A quantização é declarada, não implícita.** O snapshot arredonda posições e ângulos; o ε escolhido
   fica numa constante nomeada em `net/snapshot.ts`, com comentário dizendo que é **precisão de
   exibição** e não tem relação com o quantum do `hash()` do arnês (§11.1 — que alerta que quantização
   não é banda de tolerância). Confundir os dois é o erro que este AC existe para impedir.
9. **Orçamento de bytes remedido, e o número vai para o Dev Agent Record.** `architecture-e4.md` §1.2
   mediu, com a mesma composição, **263 B/tick de média e 433 B de pico** para o snapshot de render. A
   story deve remedir com a implementação real e registrar. **Se passar de ~600 B/tick de média, algo da
   classe 3 vazou** — investigar antes de seguir, não ajustar o orçamento.

   **Correção do @po (E40-REQ-002) — a medição inclui `events: SimEvent[]` com TODOS os tipos de evento,
   e o número de `events` sai separado.** O tipo que `e4.0` fixou é `events: SimEvent[]`, e `SimEvent`
   (`sim/types.ts:119-125`) não é só `hit`: carrega também `cast` e `ult` (com `name: string`), `death`,
   `suddenDeath` e `roundEnd`. E, pelo AC 5, cada snapshot leva os eventos **acumulados** desde o
   anterior — a `SNAPSHOT_HZ = 30`, dois ticks de eventos por quadro. §1.2 mediu "o que `render.ts` lê",
   e `events` não é lido por `render.ts` (é `client/main.ts:407`), então não se pode supor que os 263 B
   os incluam. A medição, portanto:
   - mede o snapshot **como produzido** — por quadro, na cadência de `SNAPSHOT_HZ`, com eventos
     acumulados de todos os tipos — e converte para B/tick para comparar com §1.2;
   - registra **à parte** quantos bytes são de `events` (média e pico), para que um pico legítimo de
     eventos (morte súbita, ult com projéteis) não seja lido como vazamento de classe 3, nem o
     contrário;
   - se o @dev decidir **filtrar** eventos na produção (por exemplo, só `hit`, o único consumido hoje),
     isso tipa — um subconjunto de `SimEvent[]` cabe no tipo —, mas é decisão de PRODUÇÃO com
     consequência para `e4.5`/`e4.6` e vai registrada no Dev Agent Record com a regra do filtro e os
     dois números (com e sem filtro). O teste do AC 5 (soma de `hit`) vale em qualquer dos casos.
10. **Setas** (§2.2): `net/` importa só tipos de `sim/` e `match/`; `chars/` entra em `projecao.ts` como
    **dado injetado** (`Record<string, CharDef>`), pela mesma porta que `createWorld` já usa — não por
    import de `chars/`.
11. **Extensão declarada da tabela de setas:** `tools/` passa a poder importar de `net/` para hospedar a
    guarda do AC 7. É uma seta nova, deliberada, e coerente com a tabela (`tools/` já está acima de
    todos os módulos puros) — mas é uma seta que **ninguém aprovou ainda**, e inventá-la em silêncio é
    exatamente o que a §2.2 existe para impedir.

    **Correção do @po na validação:** o @dev **não** edita `docs/architecture-e4.md` — documento de
    arquitetura é do @architect, e o precedente do projeto é commit próprio (`106de11`). O @dev
    **implementa** a seta e **registra o pedido de emenda** no Dev Agent Record; a linha no Anexo A é
    handoff para @architect. O @qa confere que o handoff existe, não que o documento foi editado.
12. **A prova de compatibilidade de TIPO fica para `e4.5`** — mas com o AC 6 corrigido ela muda de
    natureza e fica mais forte. Com `VisaoDoMundo` no cabeçalho de `desenhar`, a compatibilidade passa a
    ser verificada pelo `tsc` **em toda chamada existente**: se `projetar()` não produzir a forma certa,
    `e4.5` não compila. Aqui a garantia continua sendo por **enumeração de campos** (AC 7), e a asserção
    explícita nasce em `e4.5`, do lado certo da seta (`client/` importa `net/` legitimamente).
13. **QA-D8-01 — follow-up herdado, cobrado aqui porque esta é a primeira story da Fase 4 a tocar
    `determinism.ts`.** O gate de `debt.8` deixou registrado: *"a próxima story que tocar
    `determinism.ts` deve adicionar a asserção estrutural «BASELINE contém ≥1 `winner === -1`»"*.

    O motivo é o defeito que `debt.8` corrigiu: a cobertura do caminho de empate foi perdida em silêncio
    quando o baseline foi re-gravado em `e3.6`, e ninguém percebeu porque nenhum teste afirmava que o
    empate **estava** coberto — os hashes batiam, e um baseline sem empate bate consigo mesmo
    perfeitamente. A asserção é de uma linha e transforma "temos uma seed de empate" de fato histórico
    em invariante verificada a cada `sim:check`.

    Esta story adiciona a asserção. Custo: uma linha. É follow-up barato de fase anterior sendo pago no
    primeiro momento em que o arquivo estava aberto de qualquer jeito.
14. **Escopo de arquivos.** Criados: `src/net/snapshot.ts`, `src/net/projecao.ts`. Alterados:
    `src/tools/determinism.ts` (só a guarda nova do AC 7 **e a asserção QA-D8-01 do AC 13**) e
    `src/client/render.ts` (**só as anotações de tipo do AC 6**, nenhuma linha de corpo). Proibido:
    `src/sim/`, `src/match/`, `src/shop/`, `src/bot/`, `src/chars/`, o resto de `src/client/` (inclusive
    `main.ts`, que não precisa mudar: ver AC 6) e **`docs/`** (ver AC 11).

    **`src/net/protocolo.ts` — alterado SÓ com justificativa (E40-REQ-002).** O default é não tocar: a
    forma é de `e4.0` e já cobre tudo o que `render.ts` lê. Mas a versão anterior deste AC proibia o
    arquivo por omissão, e isso deixaria a story sem saída legítima se a produção revelasse um defeito
    de forma. Fica permitido **um ajuste de forma de `Snapshot`/`EstaticoDaRodada`** quando, e só quando:
    - houver motivo concreto e citável — um campo que `render.ts` lê e a forma não tem, um campo que a
      guarda do AC 7 prova desnecessário, ou a medição do AC 9 —, registrado no Dev Agent Record com o
      motivo e o diff do tipo;
    - o ajuste continuar por `Pick`/indexação de `sim/types.ts`, sem campo escrito à mão (AC 8 de `e4.0`);
    - nenhum campo de classe 3 entrar (AC 4) e a cor da bola continuar fora (AC 4, correção acima).

    É a "alteração deliberada e revisada" do AC 10 de `e4.0`, e o @qa confere a justificativa, não só o
    diff. `DoCliente`/`DoServidor` e as duas constantes continuam **proibidos** aqui: esta story não tem
    motivo para tocá-los.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Serialization / Contract
**Secondary Type(s)**: Test infrastructure (a guarda do AC 7)
**Complexity**: Medium — a lógica é simples, mas o erro caro é silencioso: um campo a mais no snapshot não
quebra nada hoje e custa banda, superfície e confusão de autoridade depois.

### Specialized Agent Assignment

**Primary Agents**:
- @dev
- @qa (quality gate — o AC 4 e o AC 9 são exatamente o tipo de invariante que passa despercebido em
  revisão manual e é trivial de conferir por teste)

**Supporting Agents**:
- (nenhum necessário)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Rodar antes de marcar a story como completa
- [ ] Pre-PR (@github-devops): Rodar antes de criar pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 20 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Campo da classe 3 (§5.1) aparecendo no snapshot — `memory`, `base`, `bonusItem`, `stat` inteiro (AC 4)
- Eventos de ticks intermediários sendo descartados (AC 5)
- `client/render.ts` com qualquer linha de corpo de função alterada: só import e anotações de tipo (AC 6)
- `src/net/protocolo.ts` alterado sem a justificativa exigida pelo AC 14

**Secondary Focus**:
- ε de quantização sem nome e sem comentário, ou confundido com o quantum do `hash()` (AC 8)
- `projecao.ts` importando `chars/` em vez de receber o roster injetado (AC 10)

## Tasks / Subtasks

- [x] Task 1 — `net/snapshot.ts` (AC: 3, 4, 5, 8)
  - [x] `estaticoDaRodada(world): EstaticoDaRodada` — classe 1
  - [x] Produzir `Snapshot` — classe 2, quantizado, com ε nomeado e comentado. **A assinatura é
        decisão do @dev.** `snapshot(world): Snapshot` sozinho não basta, porque a acumulação do AC 5
        exige estado entre ticks (os `world.events` dos ticks sem snapshot). Serve um acumulador (por
        exemplo, um objeto criado por fábrica que recebe cada tick e entrega o snapshot quando pedido)
        ou uma função `snapshot(world, acumulado)` com o acúmulo passado de fora. Vale qualquer desenho
        que mantenha `net/` puro (AC 3): estado explícito, sem relógio, sem I/O e sem estado de módulo
        mutável escondido. O desenho escolhido vai para o Dev Agent Record
  - [x] Acumulação de eventos entre snapshots (AC 5)
  - [x] Conferir por leitura que nenhum campo da classe 3 é lido

- [x] Task 2 — `net/projecao.ts` (AC: 3, 6, 10)
  - [x] Exportar `VisaoDoMundo` — a lista fechada de campos que `render.ts` lê
  - [x] `projetar(snap, estatico, chars): VisaoDoMundo`
  - [x] Exportar o tipo do elemento de `balls` (nome sugerido `BolaVisivel`)
  - [x] Em `client/render.ts`, trocar **só anotações** (tabela do AC 6): o import (linha 1),
        `minhasBolas` (21), o `world` de `desenhar` e dos 6 auxiliares (44, 63, 97, 131, 155, 261, 301) e o
        `b` de `desenharBola` (156). Nenhuma linha de corpo
  - [x] Confirmar que o modo local segue passando `World` e `Ball[]` sem cast (satisfação estrutural) e
        que `client/main.ts` não muda
  - [x] Registrar no Dev Agent Record o pedido de emenda de §5.1 ao @architect
  - [x] Roster injetado como `Record<string, CharDef>`, não importado

- [x] Task 3 — Guarda de ida-e-volta (AC: 4, 7, 11)
  - [x] Nova guarda em `src/tools/determinism.ts`, no padrão das existentes
  - [x] Cobrir todos os campos da lista fechada do Dev Notes
  - [x] Cobrir presença: contagem e `id` de bolas, projéteis e zonas
  - [x] **Tripwire de `stat.maxHp`/`stat.radius`:** comparar por tick contra o `EstaticoDaRodada` e
        falhar se divergirem (AC 4, correção do @po)
  - [x] Registrar no Dev Agent Record o pedido de emenda do Anexo A (seta `tools/ → net/`) — **não**
        editar `docs/`

- [x] Task 4 — Orçamento (AC: 9)
  - [x] Medir bytes/tick médios e de pico com a implementação real, por quadro na cadência de
        `SNAPSHOT_HZ`, com `events` acumulados de **todos** os tipos de `SimEvent`
  - [x] Registrar à parte a parcela de `events` (média e pico); se houver filtro de eventos, os dois
        números e a regra do filtro
  - [x] Registrar no Dev Agent Record; comparar com 263 B / 433 B de §1.2

- [x] Task 5 — QA-D8-01, follow-up herdado (AC: 13)
  - [x] Asserção estrutural em `determinism.ts`: `BASELINE` contém ≥1 `winner === -1`

- [x] Task 6 — Verificação (AC: 1, 2, 6, 14)
  - [x] `npm run check` — 0 erros
  - [x] `npm run sim:check` — golden hash idêntico, guarda nova verde, asserção de empate verde
  - [x] Grep de pureza na forma do AC 3 — exit 1 nos dois comandos
  - [x] `git diff src/client/render.ts` — só import e anotações de tipo, nenhuma linha de corpo
        (referência: `--numstat` = `10 10`); escopo respeitado;
        `protocolo.ts` intacto ou com a justificativa do AC 14 no Dev Agent Record

## Dev Notes

### A lista fechada de campos que `render.ts` lê (conferida no código nesta sessão)

De `world`: `arena` (`w`, `h`, `pad`), `balls`, `projectiles`, `zones`, `chars`, `time`, `over`, `winner`.

De cada `ball`: `id`, `charId`, `team`, `x`, `y`, `facing`, `hp`, `alive`, `ultCharge`, `ultThreshold`,
`effects` (só o `kind` — `render.ts:220-222` usa `.some()`), `stat.radius`, `stat.maxHp`,
`abilityReadyAt`, `abilityIndex`.

De cada elemento de `o.minhasBolas` (`desenharMiras`, `desenharHud`): `alive`, `charId`, `abilityIndex`,
`x`, `y`, `ultCharge`, `ultThreshold`, `abilityReadyAt`. É um subconjunto dos campos de bola acima, e por
isso `minhasBolas: BolaVisivel[]` fecha sem campo novo (AC 6).

De cada `projectile`: `x`, `y`, `vx`, `vy` (usados em `render.ts:134` para o rastro), `radius`, `color`.

De cada `zone`: `kind`, `x`, `y`, `angle`, `halfLen`, `radius`, `pull`, `ownerColor`.

De `events`: só `hit` é consumido hoje (`client/main.ts:407`), com `x`, `y`, `amount`, `crit`.

**`facing` merece nota:** ele é escrito por `sim/physics.ts:95` (`Math.atan2`) e lido **só** por
`render.ts:198`. Não está no hash e não alimenta física — conferido por grep. É dado de exibição puro, e
por isso pode ser quantizado agressivamente sem consequência.

### Por que o snapshot não é o mundo (fonte: §1.2, §5.1)

Medido: `World` serializável inteiro custa **5 049 B/tick**; o que o render lê custa **263 B**. Fator
**19×**. A diferença são campos que não mudam durante a rodada (`base`, `bonusItem`, `stat`) ou que render
nenhum consome (`memory`, `ax/ay`, `nextId`, `phase`). Mandar `World` inteiro a 60 Hz custaria
**2 423,7 kbit/s** por cliente; o snapshot comprimido a 30 Hz custa **8,2 kbit/s**.

### Por que a guarda mora no `sim:check` (fonte: §2.2, §10 passo 3)

`net/` é puro exatamente para que isto seja possível: provar a serialização inteira sem subir servidor e
sem abrir navegador, no mesmo comando que já protege o golden hash. É o mesmo dividendo que `match/` puro
deu em `e3.2` (partida inteira headless) e que `tools/harness.ts` deu em `e2.0` (uma definição, dois
consumidores).

### O ε de exibição não é o quantum do `hash()` (fonte: §8, §11.1)

`architecture-e4.md` §11.1 mostra que o `toFixed(4)` do `hash()` **não** é banda de tolerância — é
arredondamento, e portanto uma loteria com boas probabilidades. Nada disso vale para o ε desta story: aqui
a quantização é de **exibição**, o consumidor é um canvas de ~1000 px, e errar o último bit de uma posição
não tem consequência nenhuma. Os dois números são independentes e o comentário do AC 8 existe para que
ninguém os amarre por engano no futuro.

### O que esta story explicitamente NÃO faz

- **Não interpola.** O buffer e a interpolação entre snapshots são `e4.5` (§5.3).
- **Não decide `SNAPSHOT_HZ`.** A story produz um snapshot por tick quando solicitada; quem escolhe a
  cadência é o servidor (`e4.4`), e o número final sai de P4.4 (R-03).
- **Não abre socket nem cria sala.** Passos 4 e 3.
- **Não prova compatibilidade de tipo com `desenhar`** — AC 12 explica por quê e para onde vai.

### Testing

- `npm run check` — 0 erros.
- `npm run sim:check` — golden hash idêntico **e** guarda de ida-e-volta verde numa rodada completa.
- Teste de vazamento: serializar o snapshot e confirmar que nenhum dos nomes da classe 3 aparece.
- Teste de eventos: soma de `hit` nos snapshots == soma de `hit` produzidos pela rodada.
- Medição de orçamento: bytes/tick médio e de pico, com `events` de todos os tipos e a parcela de
  `events` registrada à parte (AC 9), no Dev Agent Record.
- Grep de pureza na forma do AC 3, exigindo exit 1 nos dois comandos.
- `git diff src/client/render.ts` prova que só mudaram o import e anotações de tipo, sem nenhuma linha de
  corpo (AC 6; referência `--numstat` = `10 10`), e que
  `src/net/protocolo.ts` está intacto — ou, se mudou, que a justificativa do AC 14 está no Dev Agent
  Record.

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (1M context), como @dev (Dex), modo YOLO.

### File List

| arquivo | status | o quê |
|---|---|---|
| `src/net/snapshot.ts` | **novo** | `estaticoDaRodada(world)`, `criarProdutorDeSnapshot(world)` (acumulador: `observar`/`snapshot`), `EPS_POSICAO_PX`, `EPS_ANGULO_RAD`. Puro, só tipos de `sim/` |
| `src/net/projecao.ts` | **novo** | `BolaVisivel`, `VisaoDoMundo`, `projetar(snap, estatico, chars)`. Puro, só tipos de `sim/`, roster injetado |
| `src/client/render.ts` | alterado | **só anotações** da tabela do AC 6: `git diff --numstat` = `10 10`, nenhuma linha de corpo |
| `src/tools/determinism.ts` | alterado | guarda de ida-e-volta do fio (AC 4/5/7/9) + asserção QA-D8-01 (AC 13); imports de `net/` (seta do AC 11) e de `step`/`TICK_HZ`/`hash`/`MAX_ROUND_TICKS`; nota de 2 linhas no comentário de import de `hash`, que deixou de ser verdade |

Intocados, conferido por `git status`: `src/net/protocolo.ts` (a forma de `e4.0` bastou, AC 14 sem uso),
`src/client/main.ts`, `src/sim/`, `src/match/`, `src/shop/`, `src/bot/`, `src/chars/`, `docs/` (fora esta story).

### Decisões de implementação

- **[AUTO-DECISION] Desenho do acumulador (Task 1)** → fábrica `criarProdutorDeSnapshot(world)` com
  `observar(world)` depois de CADA `step` e `snapshot(world)` quando a cadência pedir (devolve o estado
  corrente + todos os eventos desde o anterior, e esvazia). Estado explícito por instância, sem estado de
  módulo, sem relógio. **Falha alto em vez de perder eventos:** observar pulando tick, observar o mesmo
  tick duas vezes, ou fotografar tick não observado lançam exceção. (razão: `snapshot(world, acumulado)`
  com acúmulo de fora deixa o erro de contrato silencioso; o produtor tem como detectá-lo pelo `world.tick`.)
  **Consequência para `e4.4`:** o servidor precisa de um `snapshot` final no tick em que a rodada acaba,
  senão os eventos do último tick (golpe fatal, `roundEnd`) ficam no acúmulo — a guarda emula exatamente
  esse flush.
- **[AUTO-DECISION] O que quantizar (AC 8)** → só o que o AC 8 nomeia: posições (`x`/`y` de bolas,
  projéteis e zonas) a `EPS_POSICAO_PX = 0.01` e ângulos (`facing`, `angle`) a `EPS_ANGULO_RAD = 0.001`,
  erro máximo de meio passo. Exatos de propósito: `time`, `abilityReadyAt` e `ultCharge` (o render compara
  `time >= abilityReadyAt` e `ultCharge >= ultThreshold`; arredondar poderia acender PRONTO antes da hora),
  e `hp`, `vx`/`vy`, raios, `halfLen`, `pull`, `arena`. A implementação quantiza por `round(v·k)/k` com
  `k = 1/passo` inteiro, para o JSON sair curto (`round(v/passo)·passo` dá `0.30000000000000004`).
- **[AUTO-DECISION] Eventos sem filtro (AC 9)** → todos os 6 tipos de `SimEvent` vão no snapshot, copiados
  literalmente. Nenhum filtro, então não há "número com filtro" a registrar.
- **[AUTO-DECISION] Nada do snapshot aponta para o `World`** → `arena`, `effects`, projéteis, zonas e
  eventos são objetos novos, e todo campo é copiado por nome (nunca `{ ...b }`), que é como a classe 3
  vazaria sem o `tsc` reclamar.
- **[AUTO-DECISION] Onde a guarda roda** → 5 rodadas na convenção de `architecture-e4.md` §1 (`[golem,
  vex]` idx 0, heuristic dos dois lados, seeds 1/1001/2001/3001/379). Soma de ticks 23 166, igual à
  tabela de §1.1, o que confirma a mesma convenção. O laço é uma 2ª cópia do de `harness.ts` (`runRound`
  não expõe o mundo por tick), e **não pode divergir em silêncio**: o hash final de cada rodada observada
  tem que ser igual ao de `runRound` com a mesma seed e o mesmo driver, o que prova ao mesmo tempo que o
  laço é o mesmo e que produzir snapshot não escreve no `World`.
- **Além do que o AC pede, dentro da mesma guarda:** a sequência inteira de eventos (todos os tipos, na
  ordem) tem que ser idêntica, o que é mais forte que a soma de `hit`; um canário de cobertura exige ao
  menos um tick com projétil, Muralha, vórtice, efeito, morte e arena encolhida (sem ele a guarda
  compararia listas vazias); um canário do detector de classe 3 exige que ele acuse os 10 nomes
  serializáveis no `World` do motor.
- **Detector de classe 3** → confere CHAVES em qualquer nível do JSON parseado, não substring (substring de
  `ax` casaria com outros campos). `rng` é função e não serializa, então o canário cobre 10 dos 11 nomes.
  O estático também é varrido, e não pode ter chave `color`.

### IDS

| artefato | decisão | justificativa |
|---|---|---|
| `net/snapshot.ts`, `net/projecao.ts` | CREATE | exigidos pelo AC 14; `src/net/` só tinha `protocolo.ts`, e nada em `src/` produz ou projeta snapshot |
| `Snapshot`, `EstaticoDaRodada` | REUSE | de `net/protocolo.ts` (`e4.0`), sem redeclarar; `BolaVisivel`/`VisaoDoMundo` são derivados deles |
| laço da guarda | ADAPT | cópia de `runRound` com observação por tick, amarrada ao arnês pelo hash |
| `hash`, `MAX_ROUND_TICKS`, `heuristicDriver`, `setup`, `CHARS` | REUSE | de `harness.ts` e do próprio `determinism.ts` |

### Handoffs para @architect (o @dev não edita `docs/`)

1. **§5.1 de `architecture-e4.md`** afirma *"O renderizador não muda... Nenhuma linha de `render.ts` entra
   nesta fase"*. Na realidade entram 10 linhas de anotação de tipo e nenhuma de corpo: o comportamento do
   renderizador não muda, a assinatura muda (AC 6). Pedido de emenda nesses termos.
2. **Anexo A / tabela de setas de §2.2:** `tools/` passa a importar de `net/` (`determinism.ts` →
   `net/protocolo.ts`, `net/snapshot.ts`, `net/projecao.ts`), para hospedar a guarda do AC 7. A seta é
   nova e deliberada (AC 11); pedido de emenda da linha `tools/ → sim/, chars/, bot/, match/, shop/` para
   incluir `net/`. Não existe seta `net/ → tools/` (conferido por grep).
3. **§1.2 e o limiar de ~600 B do AC 9:** ver abaixo. O limiar pressupõe a codificação que §1.2 mediu, e
   essa não é a do fio de hoje.

### Orçamento de bytes medido (AC 9)

Implementação real, JSON UTF-8 do `Snapshot` exatamente como `snapshot()` o devolve, 5 rodadas (23 166
ticks), eventos acumulados de **todos** os tipos (2 049 eventos: `cast`, `death`, `hit`, `roundEnd`,
`suddenDeath`, `ult`). Sai no próprio `sim:check`, linha `orçamento`:

| medida | média | pico |
|---|---|---|
| **como produzido, 30 Hz** (por quadro, eventos de 2 ticks) | **879,0 B/quadro** = **439,5 B/tick** | **1 554 B** |
| — dos quais `events` (à parte) | **17,1 B** | **381 B** |
| 1 snapshot por tick, 60 Hz (a base de §1.2) | **872,7 B/tick** | **1 554 B** |
| estático da rodada (uma vez) | 425 B | — |
| §1.2, para comparar | 263 B | 433 B |

**Passou de ~600 B/tick. Investigado antes de seguir, como o AC manda, e o orçamento NÃO foi ajustado.**
Conclusão: **não é vazamento de classe 3.** O detector de chaves não acha nenhum nome da classe 3 em
nenhum snapshot, e a decomposição por campo (medida no scratchpad, fora do repo) põe 100% dos bytes em
campos permitidos: cabeçalho (`time`/`over`/`winner`/`arena`) 85,4 B · `balls` 530,7 B · `zones` 146,0 B
· `projectiles` 58,0 B · `events` 9,6 B (média a 60 Hz). O custo é de **codificação**: nomes de campo
repetidos por entidade (`"abilityReadyAt":`, `"ultCharge":`… 4× por quadro) e dígitos de float não
quantizados (`hp: 443.12000000000035`, `abilityReadyAt: 49916.66666666667`).

Os mesmos snapshots, em outras codificações (medição de diagnóstico, script descartado):

| codificação | média | pico |
|---|---|---|
| JSON com nomes de campo (o de hoje) | 872,7 B | 1 554 B |
| JSON com nomes de campo, todo número quantizado a 0,01 | 802,3 B | 1 383 B |
| tupla posicional sem nomes | 361,1 B | 774 B |
| **tupla posicional, todo número a 0,01** | **290,7 B** | **609 B** |
| deflate isolado do JSON de hoje | 387,3 B | 638 B |
| deflate com contexto (context takeover, flush por quadro) | **77,9 B** | 257 B |

A tupla quantizada chega a ~§1.2 (263/433 B; a diferença plausível é `events`, os `id` de projétil e zona
que `e4.0` acrescentou, e o grau de quantização). Ou seja, **§1.2 mediu uma codificação compacta, e o
limiar de 600 B só vale para ela.** Quantizar mais campos no JSON de hoje tira só ~8%, e não resolve.
Banda continua não sendo restrição (§1.2, "Leitura"): 77,9 B/tick comprimido a 30 Hz dá ~9,3 kbit/s.

**Não decidi a codificação**, porque o serializador e o parser do fio são de `e4.3`, e `DoServidor`/as
constantes estão proibidos aqui (AC 14). **Item em aberto para @architect/@po:** (a) ratificar que o
limiar de ~600 B se refere à codificação compacta; ou (b) mandar `e4.3` adotar codificação posicional
quantizada; ou (c) aceitar JSON com nomes mais `permessage-deflate` e re-baselinar o número de §1.2.

### Verificação

- `npm run check` → exit 0.
- `npm run sim:check` → exit 0. **Golden hash idêntico:** `golden hash    ✓ ok — 6 seeds batem o baseline`,
  e também build coverage, replay, bot e partida. `diff` da saída inteira antes e depois: **0 linhas
  removidas ou alteradas**, 8 linhas novas (`empate coberto` e o bloco `fio snapshot`, com 6 sub-linhas).
- `npm run build` → exit 0.
- Grep de pureza na forma do AC 3: `grep -rnwE "ws|document|window|localStorage|Math\.random" src/net/`
  → **exit 1**; `grep -rnE "Date\.now|performance\.now" src/net/` → **exit 1**.
- Setas: `net/` só tem `import type` de `sim/types.ts` e de `./protocolo.ts`; nenhum import de `tools/`,
  `client/`, `chars/`, `bot/` ou `server/` (grep, exit 1).
- `git diff --numstat src/client/render.ts` → `10 10`: o import (1), `minhasBolas` (21), `world` (44, 63,
  97, 131, 155, 261, 301) e `b` (156). Nenhuma linha de corpo. `main.ts` intocado, sem cast.
- **Bateria negativa** (mutações temporárias restauradas por `cmp`, fora do repo). A guarda tem que falhar,
  e falhou nas **9/9**, todas com exit 1: M1 `memory` no snapshot → classe 3; M2 descartar eventos de tick
  ímpar → 1752 ≠ 810 `hit`; M3 `facing` a 0,1 rad → fidelidade; M4 estático com `radius+1` → tripwire;
  M5 some um projétil → presença; M6 snapshot escreve em `b.x` → hash ≠ arnês; M7 projeção corrompe
  `abilityReadyAt` → fidelidade; M8 seed 379 sem `winner: -1` → `empate coberto ✗`; M9 detector cego →
  canário.

### Self-critique (Step 5.5 / 6.5), inline por causa do AC 14

- Classe 3 fora por construção (cópia por nome) e por teste (chaves + canário). ✓
- Nenhum tipo paralelo ao de `protocolo.ts`; `BolaVisivel`/`VisaoDoMundo` são derivados dele. ✓
- ε nomeados, comentados como precisão de exibição e desamarrados do `toFixed(4)` do `hash()`. ✓
- Risco: a guarda depende de o laço copiado continuar igual a `runRound`. Coberto pelo hash. ✓
- Risco aceito: `projetar` repassa `arena`/`effects`/projéteis/zonas do snapshot sem cópia (documentado
  como somente-leitura, o que o render já é).

### Não verificado / em aberto

- **Limiar de bytes do AC 9** (acima): acima de 600 B na codificação de hoje, pela causa medida. Precisa de
  ratificação ou de decisão de codificação em `e4.3`.
- **Render real a partir de `projetar()`** não foi exercitado em navegador: nenhum ponto de chamada usa
  `projetar` ainda (`e4.5`). A compatibilidade de tipo está garantida pelo `tsc` em `render.ts`, e a de
  valor pela guarda.
- **Tripwire contra passiva real:** nenhuma passiva de hoje muda `maxHp`/`radius`, então a tripwire foi
  provada por mutação no estático (M4), não por uma passiva de verdade (`sim/`/`chars/` estão fora do escopo).
- **CodeRabbit:** indisponível nesta máquina (sem WSL/CLI). Coberto por `check`, `sim:check`, `build`,
  bateria negativa e greps. A checkbox de Pre-Commit fica desmarcada.
- Durante a sessão apareceu `docs/stories/debt.10...story.md` modificado por outro agente. Não é desta
  story e ficou fora do commit.

## QA Results

_(a preencher pelo @qa)_

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-08-19 | 1.0.0 | Story criada a partir do passo 2 de `docs/architecture-e4.md` §10 | @sm |
| 2026-08-19 | 1.1.0 | **Validação @po: GO 10/10** (8/10 antes das correções — o achado (a) era um AC factualmente frágil, não só de forma). **Status: Draft → Ready.** Dois achados: **(a)** o AC 4 classificava `stat.maxHp` e `stat.radius` como estáticos da rodada *por natureza*. `sim/stats.ts:14-15` os chama **"estruturais — recomputados só em evento explícito"**, que não é o mesmo que constantes, e `chars/vex.ts:108` prova que `stat` muda no meio da rodada (`addBonus` condicional em `maxSpeed` abaixo de 40% de HP). Hoje nenhuma passiva toca `maxHp`/`radius` e `bonusItem` é congelado — a classificação está certa **hoje** —, mas a Fase 5 traz 6 personagens novos. Virou **tripwire** na guarda do AC 7: comparação por tick contra o `EstaticoDaRodada`, falhando se divergir. **(b)** o AC 11 mandava o @dev editar `docs/architecture-e4.md` — mesmo problema de autoridade de `e4.1`, virou handoff. Corrigida ainda a citação `client/main.ts:405` → `:407` (linha real do `ev.t === 'hit'`). | @po |
| 2026-08-19 | 1.2.0 | **Reabertura pelo @po durante a validação de `e4.5`** — a story continua **Ready**, mas o AC 6 estava factualmente impossível e teria reprovado no gate. `desenhar` recebe `world: World` **completo** (`client/render.ts:40-46`), incluindo `rng`, `phase`, `nextId` e os campos de classe 3 de cada bola; nenhuma projeção satisfaz esse tipo. As duas saídas ruins seriam fabricar um `World` falso no cliente (reintroduzindo a classe 3 que o AC 4 proíbe) ou reescrever o renderizador. A saída certa tem precedente no projeto — `WorldView = Omit<World,'rng'>` de `debt.7` estreitou um tipo sem tocar em ponto de chamada nenhum: `net/projecao.ts` passa a exportar `VisaoDoMundo`, e `render.ts` muda **exatamente uma linha** (a anotação do parâmetro). O modo local continua passando `World` sem cast, por satisfação estrutural. Efeito colateral bom: com o tipo estreito no cabeçalho, o `tsc` passa a verificar a compatibilidade em toda chamada, e o AC 12 fica mais forte do que era. **Handoff para @architect:** `architecture-e4.md` §5.1 afirma que "nenhuma linha de `render.ts` entra nesta fase" — é otimista por uma linha e merece emenda. | @po |
| 2026-09-21 | 1.3.0 | **Alinhamento ao shape de `e4.0` (E40-REQ-002 e E40-TST-003, gate `docs/qa/gates/e4.0-net-protocolo.yml`). Status permanece Ready**: nenhuma tarefa nova de implementação, só texto que teria tropeçado no gate. Conferido na fonte antes de emendar: `src/net/protocolo.ts:64-93` (forma completa por `Pick`), `render.ts:159/267/308/361` (`def = world.chars[charId]`) e `:166-386` (cor sempre de `def.color`), `sim/types.ts:119-125` (os 6 tipos de `SimEvent`). **(1)** "Depende de": a forma é dada por `e4.0`; "preenche" virou **produz**, e a story decide produção, não forma. **(2)** Tabela do AC 4: "cor" saiu da linha do estático — vem de `chars[charId].color` pelo `CHARS` injetado — e a tabela passou a listar os campos reais da forma (`id`, `abilityIndex`, `time`/`over`/`winner`/`arena`, `events`); nova linha "conteúdo local do bundle". **(3)** AC 9: a medição inclui `events: SimEvent[]` de **todos** os tipos, acumulados na cadência de `SNAPSHOT_HZ`, com a parcela de `events` registrada à parte; filtro de eventos é permitido, mas registrado. **(4)** AC 14: `src/net/protocolo.ts` pode ser alterado **só** para ajuste de forma de `Snapshot`/`EstaticoDaRodada` com motivo citável no Dev Agent Record, por `Pick`, sem classe 3 e sem cor — a porta que o AC 10 de `e4.0` exige. **(5)** AC 3: grep de pureza fixado na forma recursiva e com fronteira de palavra, exigindo exit 1 (sem `-r` o grep sai com 2 e passa em vazio; `ws` solto casa "draws"). Limpeza no mesmo passo: `quality_gate_tools`, Focus Areas e Testing ainda diziam "`render.ts` intacto", contradizendo o AC 6 corrigido em 1.2.0 — agora dizem "exatamente 1 linha". | @po |
| 2026-09-21 | 1.4.0 | **Terceira correção do AC 6, a pedido do @dev, que parou antes de escrever código (opção (a)). Status permanece Ready**: o conserto está inteiramente especificado e foi reproduzido. **Conferido na fonte:** `render.ts:1` (`import type { Ball, World }`), `:21` (`minhasBolas: Ball[]`), `:44` (`world: World` de `desenhar`) e os seis auxiliares anotados `world: World`: `:63`, `:97`, `:131`, `:155` (+ `b: Ball` em `:156`), `:261`, `:301`. `tsconfig.json` tem `noUnusedLocals: true`. **Reproduzido numa cópia de `src/` fora do repositório** (nenhum arquivo de aplicação tocado): trocar só a linha 44 dá exatamente **6 × TS2345** em `render.ts:52-58`; o conjunto de anotações compila sem erro em `client/` e `net/`, com `main.ts` intocado. **(1) AC 6** reescrito: `render.ts` muda só em anotações de tipo (import, `minhasBolas`, `world` em `desenhar` e nos 6 auxiliares, `b` em `desenharBola`). O critério passa a ser a **natureza** da linha (nenhuma linha de corpo), com `--numstat 10 10` como referência e não como regra. **(2) `minhasBolas` (`render.ts:21`) entra no conjunto por decisão do @po:** o campo exige `Ball` do motor, que o modo conectado não fornece. Estreitar aqui custa uma anotação, tira `Ball` do import (que vira uma troca de linha única para `net/projecao.ts`) e mantém verdadeiro o AC 12 de `e4.5` ("é lá que se corrige"). Registrado em `e4.5` v1.2.0. **(3) AC 14:** `determinism.ts` pode receber a guarda do AC 7 **e** a asserção QA-D8-01 do AC 13. A versão anterior listava só a guarda e contradizia o AC 13. **(4) Task 1:** a assinatura de produção é do @dev (acumulador ou `snapshot(world, acumulado)`), desde que `net/` continue puro; `snapshot(world)` sozinho não carrega o estado entre ticks que o AC 5 exige. As repetições de "exatamente 1 linha" foram alinhadas em `quality_gate_tools`, Focus Areas, Task 2, Task 6, Testing e no handoff de §5.1 ao @architect. Dev Notes ganha os campos lidos de `minhasBolas`. | @po |
| 2026-09-21 | 1.5.0 | **Implementação @dev. Status: Ready → InProgress → Ready for Review (InReview).** Criados `src/net/snapshot.ts` (estático + produtor acumulador `observar`/`snapshot`, ε `EPS_POSICAO_PX = 0.01` e `EPS_ANGULO_RAD = 0.001`) e `src/net/projecao.ts` (`BolaVisivel`, `VisaoDoMundo`, `projetar`). `render.ts` com só as anotações do AC 6 (numstat `10 10`). Guarda `fio snapshot` e asserção `empate coberto` (QA-D8-01) em `determinism.ts`. `protocolo.ts` intocado. `check`/`sim:check`/`build` verdes, golden hash idêntico, sim:check com 0 linhas alteradas e 8 novas, greps de pureza com exit 1, bateria negativa 9/9. **Em aberto:** o orçamento do AC 9 dá 872,7 B/tick (60 Hz) e 439,5 B/tick (30 Hz, 1 554 B de pico por quadro, `events` com 17,1 B de média e 381 B de pico). Investigado: não é classe 3, é a codificação JSON com nomes de campo (a tupla quantizada mede 290,7 B, perto de §1.2). Orçamento não ajustado; decisão de codificação encaminhada a @architect/@po (`e4.3`). Handoffs de §5.1 e do Anexo A registrados. | @dev |
